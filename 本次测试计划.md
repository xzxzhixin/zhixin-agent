# 本次测试计划

## 本轮范围

- 需求标题：`2026年6月4日15:30:20-已确认`、`2026年6月4日15:45:43-已确认`、`2026年6月4日15:55:03-已确认`。
- 测试入口：只执行浏览器端验证。
- 不执行范围：不执行桌面端测试项，不执行 IDEA 插件端测试项，不执行 Electron 壳交互测试。
- 启动依据：测试子代理读取 `项目启动.md`，需要浏览器端测试时只能使用并维护 `浏览器测试窗口.md` 中记录的测试专用页签。
- 前置状态：程序员完成 `本次计划.md` P01-P12 并更新启动记录后，才允许开始本测试计划。

## 测试项清单

- [x] T01 浏览器端统一对话组件与唯一 `id` 验证。
- [x] T02 浏览器端智能体对话弹窗完整对话能力验证。
- [x] T03 浏览器端任务状态同步与任务进度语义验证。
- [x] T04 浏览器端智能体状态同步验证。
- [x] T05 浏览器端排队、引导和确认状态展示验证。
- [x] T06 浏览器端输入框不被压缩验证。
- [x] T07 浏览器端过程卡片聚合、实时追加和 `20vh` 日志滚动验证。
- [x] T08 浏览器端 Markdown 渲染验证。
- [x] T09 浏览器端命令工具调用 Node.js 版本验证。
- [x] T10 浏览器端静态回归脚本与收口核对。

## 测试项详情

### T01 浏览器端统一对话组件与唯一 `id` 验证

- 验证端：浏览器端。
- 建议工具：Chrome DevTools 测试专用页签、中心服务会话详情接口、页面 DOM 检查。
- 是否必测：是。
- 前置启动条件：`项目启动.md` 标记浏览器端可测；P01 已完成。
- 当前状态：通过。
- 验收标准：普通对话、项目对话和智能体对话均能定位到唯一对话 `id`；切换不同对话不会串用消息、任务或输入草稿；发送能力来自同一完整对话链路。
- 测试并行组：无。
- 浏览器页签范围：复用 `浏览器测试窗口.md` 记录的测试专用页签。
- 宿主范围：只读使用既有浏览器端和中心服务，不执行桌面端测试。
- 最近一次测试结果：通过；项目对话草稿发送后创建真实项目会话 `f1bd7592-2504-4465-94c0-b1e2e9300399`，普通会话保持各自 `sessionId`，切换时消息、任务和草稿未串用。
- 失败原因：无。
- 复现步骤：打开浏览器端 `/chat`，分别进入普通对话、项目对话和智能体对话弹框，检查每场对话的消息列表、输入区和发送结果是否绑定各自唯一 `id`。
- 反馈对象：程序员子代理。
- 修复摘要：无。
- 复测结果：通过。

### T02 浏览器端智能体对话弹窗完整对话能力验证

- 验证端：浏览器端。
- 建议工具：Chrome DevTools、页面交互、网络请求观察。
- 是否必测：是。
- 前置启动条件：P02 已完成。
- 当前状态：通过。
- 验收标准：智能体状态弹框展示主智能体、长期智能体和子智能体两级树；点击智能体节点后出现完整对话能力，包括消息查看、输入、发送、任务状态和引导状态，不是只读简化展示。
- 测试并行组：无。
- 浏览器页签范围：同 T01。
- 宿主范围：浏览器端。
- 最近一次测试结果：通过；智能体状态弹框展示主智能体树、当前会话消息、任务状态和输入区，发送后追加 `@致心` 用户消息和助手回复，任务从 `2/2` 更新为 `3/3`。
- 失败原因：无。
- 复现步骤：打开“智能体状态”入口，选择主智能体和至少一个子智能体节点，发送一条短消息并观察消息、任务和状态是否更新。
- 反馈对象：程序员子代理。
- 修复摘要：无。
- 复测结果：通过。

### T03 浏览器端任务状态同步与任务进度语义验证

- 验证端：浏览器端。
- 建议工具：Chrome DevTools、中心服务 `/api/session/detail` 或事件列表接口。
- 是否必测：是。
- 前置启动条件：P03 已完成。
- 当前状态：通过。
- 验收标准：输入区或任务状态入口显示的“任务”表示当前轮次任务编排进度，不表示对话次数；输入区任务入口、任务详情弹框、右侧任务状态和过程卡片使用同一真实任务状态源，状态和进度一致。
- 测试并行组：无。
- 浏览器页签范围：同 T01。
- 宿主范围：浏览器端。
- 最近一次测试结果：通过；任务入口、任务详情弹框和右侧任务栏均显示三轮任务完成状态，并在详情中展示步骤、排查 ID 和“当前对话当前轮次”作用域。
- 失败原因：无。
- 复现步骤：发送一轮可触发多步骤的消息，观察任务入口进度、任务详情列表、右侧栏和事件接口中的 taskId、turnId、step 状态是否一致。
- 反馈对象：程序员子代理。
- 修复摘要：无。
- 复测结果：通过。

### T04 浏览器端智能体状态同步验证

- 验证端：浏览器端。
- 建议工具：Chrome DevTools、WebSocket 或事件接口观察。
- 是否必测：是。
- 前置启动条件：P04 已完成。
- 当前状态：通过。
- 验收标准：右侧智能体状态、输入区智能体状态入口、智能体状态弹框和智能体对话视图展示同一状态；运行中、排队、等待用户、失败和结束状态不互相矛盾。
- 测试并行组：无。
- 浏览器页签范围：同 T01。
- 宿主范围：浏览器端。
- 最近一次测试结果：通过；右侧栏、输入区入口和智能体弹框均显示主智能体“致心”空闲状态，未出现运行、排队、等待用户、失败或结束状态互相矛盾。
- 失败原因：无。
- 复现步骤：发送消息并刷新事件，分别检查多个智能体状态展示区域；必要时模拟或触发等待用户、失败状态，确认所有区域同步。
- 反馈对象：程序员子代理。
- 修复摘要：无。
- 复测结果：通过。

### T05 浏览器端排队、引导和确认状态展示验证

- 验证端：浏览器端。
- 建议工具：Chrome DevTools、页面交互。
- 是否必测：是。
- 前置启动条件：P05 已完成。
- 当前状态：通过。
- 验收标准：当前对话排队、等待引导或确认时，用户能在输入区附近、任务详情或智能体对话中看到明确状态；文案说明作用域是当前对话当前轮次，不暗示全局阻塞。
- 测试并行组：无。
- 浏览器页签范围：同 T01。
- 宿主范围：浏览器端。
- 最近一次测试结果：通过；输入区附近、右侧任务栏、任务详情和智能体弹框均展示当前对话当前轮次作用域，未暗示全局阻塞。
- 失败原因：无。
- 复现步骤：连续发送或触发需要审批/确认的操作，观察排队和等待用户状态是否可见且作用域准确。
- 反馈对象：程序员子代理。
- 修复摘要：无。
- 复测结果：通过。

### T06 浏览器端输入框不被压缩验证

- 验证端：浏览器端。
- 建议工具：Chrome DevTools，桌面浏览器视口和窄宽度浏览器视口。
- 是否必测：是。
- 前置启动条件：P06 已完成。
- 当前状态：通过。
- 验收标准：输入框在常规桌面宽度和窄宽度下保持稳定可用宽度、高度和胶囊形态；任务、智能体状态、编辑入口和上下文用量不挤压输入文字区域；无文字重叠。
- 测试并行组：无。
- 浏览器页签范围：同 T01。
- 宿主范围：浏览器端。
- 最近一次测试结果：通过；测试子代理在 Chrome DevTools `900x800x1` 视口复查，主输入框宽度为 `508px`、高度 `84px`，长文本和多行输入后仍无控件重叠，页面级 `scrollWidth` 与 `clientWidth` 均为 `900`，根高度无滚动。
- 失败原因：无。
- 复现步骤：打开 `/chat`，分别调整到常规桌面宽度和较窄宽度，输入长句、多行文本和带引用草稿，观察输入框布局。
- 反馈对象：程序员子代理。
- 修复摘要：`apps/frontend/src/views/Chat/style.css` 在 `1100px` 以下隐藏右侧状态栏、让主对话列优先占宽，底部工具栏允许换行并收紧选择器宽度；`scripts/check-dialog-agent-workflow-regression.mjs` 新增窄视口输入区防挤压静态断言。
- 复测结果：通过。

### T07 浏览器端过程卡片聚合、实时追加和 `20vh` 日志滚动验证

- 验证端：浏览器端。
- 建议工具：Chrome DevTools、中心服务事件接口、页面 DOM 检查。
- 是否必测：是。
- 前置启动条件：P07、P10 已完成。
- 当前状态：通过。
- 验收标准：同一流式输出、命令执行、MCP 调用或工具调用过程只展示为一个过程框；状态从执行中更新到完成或失败；日志或过程输出在框内追加，日志区域最大高度为 `20vh`，超出后框内滚动，不撑开主界面。
- 测试并行组：无。
- 浏览器页签范围：同 T01。
- 宿主范围：浏览器端。
- 最近一次测试结果：通过；同类过程聚合为单个过程框，命令、MCP、插件和 skill 过程均显示状态和摘要，`.process-log-list` 最大高度为约 `190.6px`，等于当前视口 `20vh`，且 `overflow-y: auto`。
- 失败原因：无。
- 复现步骤：发送能产生流式输出、命令工具和 MCP 不可用或失败事件的消息，检查页面过程框数量、状态变化和日志滚动区域样式。
- 反馈对象：程序员子代理。
- 修复摘要：无。
- 复测结果：通过。

### T08 浏览器端 Markdown 渲染验证

- 验证端：浏览器端。
- 建议工具：Chrome DevTools、页面 DOM 检查。
- 是否必测：是。
- 前置启动条件：P08 已完成。
- 当前状态：通过。
- 验收标准：模型返回的 Markdown 标题、列表、代码块、链接、引用和行内代码按 GitHub Markdown 样式正常渲染；暗黑和亮色主题下可读。
- 测试并行组：无。
- 浏览器页签范围：同 T01。
- 宿主范围：浏览器端。
- 最近一次测试结果：通过；消息区渲染出二级标题、列表、引用、行内代码、代码块和链接，暗色主题下可读。
- 失败原因：无。
- 复现步骤：发送要求返回包含标题、列表、代码块、链接和引用的 Markdown 内容，观察消息渲染结果和主题切换后的可读性。
- 反馈对象：程序员子代理。
- 修复摘要：无。
- 复测结果：通过。

### T09 浏览器端命令工具调用 Node.js 版本验证

- 验证端：浏览器端。
- 建议工具：Chrome DevTools、中心服务事件接口、页面过程卡片。
- 是否必测：是。
- 前置启动条件：P09 已完成；中心服务由桌面端或项目约定宿主提供，不直接运行 `services/center`。
- 当前状态：通过。
- 验收标准：在浏览器对话中发送“返回系统 Node.js 版本”或等价明确请求后，中心服务通过命令工具链路执行 Node.js 版本命令；页面出现同一命令过程卡片，展示命令、执行中状态、输出中的 Node.js 版本和完成状态；事件接口包含命令开始和完成或失败事件。
- 测试并行组：无。
- 浏览器页签范围：同 T01。
- 宿主范围：浏览器端。
- 最近一次测试结果：通过；测试子代理发送 `T09 测试子代理复测：请通过命令工具执行 node -v，并展示命令输出。`，页面命令过程卡片显示 `v20.9.0`、处理中和已完成状态；事件接口返回 `tool.command.started`、`tool.command.output`、`tool.command.completed`。
- 失败原因：无。
- 复现步骤：在测试专用对话发送“请通过命令工具返回系统 Node.js 版本”，观察页面过程卡片和事件接口。
- 反馈对象：程序员子代理刷新宿主中心服务。
- 修复摘要：已停止旧 `dev:desktop-shell` 宿主根进程树并确认 `5173`、`8866` 释放；随后通过 IDEA Run Configuration `dev:desktop-shell` 重新拉起宿主链路，未使用 `dev:center:new`，未直接运行 `services/center`。
- 复测结果：通过。

### T10 浏览器端静态回归脚本与收口核对

- 验证端：浏览器端相关静态检查。
- 建议工具：IDEA Terminal 或项目约定终端；只运行项目允许的 Node 脚本，不运行 `tsc --noEmit`、`vue-tsc` 或等价 TypeScript 编译器检查。
- 是否必测：是。
- 前置启动条件：P11 已完成。
- 当前状态：通过。
- 验收标准：本轮新增或扩展的静态回归脚本通过；`scripts/check-code-file-line-limit.mjs` 通过；脚本覆盖统一对话组件、任务状态同步、智能体状态同步、过程卡片聚合、Markdown 渲染、命令工具和 MCP 过程可见链路。
- 测试并行组：无。
- 浏览器页签范围：无；静态检查不维护浏览器页签。
- 宿主范围：无。
- 最近一次测试结果：通过；IDEA Terminal 串行执行 `node scripts/check-dialog-agent-workflow-regression.mjs`、`node scripts/check-chat-render-regression.mjs`、`node scripts/check-current-plan-regressions.mjs`、`node scripts/check-code-file-line-limit.mjs`，四个脚本退出码均为 `0`。
- 失败原因：无。
- 复现步骤：按程序员结果列出的命令逐项执行静态回归脚本，核对退出码和输出。
- 反馈对象：程序员子代理。
- 修复摘要：无。
- 复测结果：通过。

## 完成口径

- 只有 T01-T10 全部勾选 `[x]`，且每个详情“当前状态”为“通过”，才允许进入测试收口。
- 测试收口必须重新读取 `当前需要实现的需求.md` 中本轮 3 个标题正文，按公共模板处理完成标记和临时图片清理。
- 任一测试失败、阻塞或需复测时，不得把临时需求标题标记为 `-完成`。

## 风险与阻塞

- 当前阻塞：无；T01-T10 全部通过，已进入测试收口。
- 风险：`清空数据重测` 涉及数据删除或重置，测试子代理不得自行清空中心目录、数据库或业务数据；如确需清空，必须反馈主线程并等待用户明确授权。
- 风险：浏览器测试依赖 `项目启动.md` 中的实际健康环境；如果 `5173` 或 `8866` 过期，反馈程序员子代理刷新启动状态，不由测试子代理直接启动中心服务。
