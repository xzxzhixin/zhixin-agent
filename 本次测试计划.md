# 本次测试计划

## 本轮范围

- 本轮需求标题：`# 2026年6月2日13:37:41`。
- 本轮测试只执行浏览器端验证。
- 当前前置状态：`本次计划.md` 中 P01 至 P11 已全部完成，允许进入启动阶段；T02 启动阻塞已修复待复测，测试阶段仍等待启动子代理重新刷新可用 `项目启动.md`。
- 测试前必须确认 `本次计划.md` 摘要允许进入测试，且 `项目启动.md` 显示可进入测试。
- 需要真实对话时，使用项目根目录 `AI账号.md` 中账号配置；不得在测试结果中泄露账号、密钥或密码原文。

## 测试项清单

- [ ] T01 浏览器端验证真实对话能调用供应商并返回非占位回复。
- [x] T02 浏览器端验证 `http://127.0.0.1:8866` 前端资源刷新或开发期刷新策略有效。
- [ ] T03 浏览器端验证普通对话和项目对话删除功能有效。
- [ ] T04 浏览器端验证对话标题总结功能有效并刷新列表标题。
- [ ] T05 浏览器端验证用量统计在真实对话后产生并展示数据。
- [x] T06 浏览器端验证顶部菜单激活样式与当前路由一致。
- [x] T07 浏览器端验证供应商页可选择内置协议插件并维护模型能力字段。
- [x] T08 浏览器端验证网络代理页包含完整协议、账号密码和无认证代理处理。
- [ ] T09 浏览器端验证智能体管理页具备主智能体和长期智能体真实管理功能。
- [ ] T10 浏览器端验证代码文件行数治理结果在页面功能回归中未破坏对话、管理页和路由。

## 测试项详情

### T01 真实对话调用供应商

- 验证端：浏览器端。
- 建议工具：chrome-devtools。
- 是否必测：是。
- 前置启动条件：启动子代理通过桌面端拉起中心服务；供应商账号按 `AI账号.md` 配置完成。
- 当前状态：失败待修复。
- 验收标准：在 `/chat` 发送一条普通对话消息后，页面展示供应商返回内容；网络请求成功；任务状态完成；消息内容不是内部占位、静态模板或仅回显用户输入。
- 最近一次测试结果：失败。已按 `AI账号.md` 将现有供应商切换为 OpenAI 兼容协议、测试账号地址、启用状态和脱敏 API Key；在 `/chat` 发送短消息后，任务状态为失败。
- 失败原因：审计摘要出现 `model.failed`，错误为 `invokeProviderModelGateway is not defined`；未产生供应商返回内容。
- 复现步骤：进入 `/chat`，按账号配置选择可用模型，发送短消息，观察消息流、控制台、网络请求和任务状态。
- 反馈对象：程序员子代理。
- 修复摘要：需程序员修复模型网关调用缺失函数并确保真实供应商调用返回内容。
- 复测结果：未执行。

### T02 8866 前端资源刷新

- 验证端：浏览器端。
- 建议工具：chrome-devtools。
- 是否必测：是。
- 前置启动条件：启动子代理记录 8866 访问地址和 5173 复用或代理策略。
- 当前状态：通过。
- 验收标准：访问 `http://127.0.0.1:8866` 后能看到程序员最新前端改动；刷新页面不会长期停留旧资源；控制台无资源缓存导致的旧入口错误。
- 最近一次测试结果：通过。打开 `http://127.0.0.1:8866/#/providers` 后进入开发期 `http://127.0.0.1:5173/?port=8866#/providers`，刷新后仍显示最新供应商页，控制台无资源加载错误。
- 失败原因：无。
- 复现步骤：打开 8866 页面，刷新，切换几个已改页面，对比最新 UI 和控制台资源加载。
- 反馈对象：程序员子代理。
- 修复摘要：已将中心服务开发期重定向调用改为 URL 在前、302 状态码在后，避免 Fastify 把 Vite 地址误判为状态码。
- 复测结果：通过。

### T03 对话删除

- 验证端：浏览器端。
- 建议工具：chrome-devtools。
- 是否必测：是。
- 前置启动条件：浏览器端可访问 `/chat` 且中心服务可写会话数据。
- 当前状态：失败待修复。
- 验收标准：创建普通对话和项目对话后点击删除，目标对话从导航列表消失，详情不再选中已删除会话；刷新后仍不显示；失败时有可见错误。
- 最近一次测试结果：部分通过。普通对话 hover 后出现删除按钮，删除刚创建的失败测试对话后，该对话从列表消失，刷新后未再显示该测试消息。
- 失败原因：项目对话列表为空；点击“新增项目对话”无可见效果，无法创建项目对话，也无法验证项目对话删除功能。
- 复现步骤：分别创建普通对话和项目对话，执行删除，刷新页面并检查列表。
- 反馈对象：程序员子代理。
- 修复摘要：需程序员修复项目对话新增和项目对话删除链路。
- 复测结果：未执行。

### T04 标题总结

- 验证端：浏览器端。
- 建议工具：chrome-devtools。
- 是否必测：是。
- 前置启动条件：T01 真实对话可用。
- 当前状态：失败待修复。
- 验收标准：完成一轮真实对话后，会话列表标题从默认标题更新为与用户问题相关的摘要标题；刷新后仍保持；标题失败时页面有可排查错误事件但不阻断对话。
- 最近一次测试结果：失败。真实对话 T01 未完成，发送测试消息后列表标题仍为“新的对话”，未出现与问题相关的摘要标题。
- 失败原因：模型调用失败，且会话标题没有从默认标题更新为摘要标题。
- 复现步骤：发送一条主题明确的消息，等待对话完成，观察左侧会话标题和详情标题。
- 反馈对象：程序员子代理。
- 修复摘要：需程序员在真实对话完成后生成并固化标题摘要，失败时写入可排查事件。
- 复测结果：未执行。

### T05 用量统计

- 验证端：浏览器端。
- 建议工具：chrome-devtools。
- 是否必测：是。
- 前置启动条件：T01 至少完成一次真实供应商调用。
- 当前状态：失败待修复。
- 验收标准：进入 `/usage` 后能看到本轮真实调用产生的原始记录、总量概览、供应商维度和项目维度图形化内容；供应商未返回缓存字段时显示未提供而不是 0 冒充。
- 最近一次测试结果：失败。进入 `/usage` 后总量概览、供应商维度、项目维度均显示暂无真实模型调用用量。
- 失败原因：T01 真实供应商调用失败，未产生真实用量原始记录和聚合统计。
- 复现步骤：完成真实对话后打开 `/usage`，检查筛选、图表、记录和时间格式。
- 反馈对象：程序员子代理。
- 修复摘要：需程序员修复真实模型调用后用量原始记录和聚合统计落库展示。
- 复测结果：未执行。

### T06 顶部菜单激活样式

- 验证端：浏览器端。
- 建议工具：chrome-devtools。
- 是否必测：是。
- 前置启动条件：浏览器端可访问工作台路由。
- 当前状态：通过。
- 验收标准：点击顶部菜单或直接访问对应路由时，当前菜单具有明确激活样式；与 `img.png` 反馈位置一致；刷新后激活态仍正确。
- 最近一次测试结果：通过。直接访问 `/providers`、`/proxies`、`/agent-management`、`/usage` 时，对应顶部菜单分别出现 `active` 样式；供应商页还带 `aria-current="page"`。
- 失败原因：无。
- 复现步骤：依次访问 `/chat`、`/providers`、`/proxies`、`/agent-management`、`/usage` 等路由，检查顶部菜单状态。
- 反馈对象：程序员子代理。
- 修复摘要：已验证通过。
- 复测结果：通过。

### T07 供应商协议插件与模型能力

- 验证端：浏览器端。
- 建议工具：chrome-devtools。
- 是否必测：是。
- 前置启动条件：中心服务已注册内置模型协议插件。
- 当前状态：通过。
- 验收标准：`/providers` 可选择 OpenAI 兼容和 Anthropic Messages 内置协议插件；可保存协议模式、模型、上下文窗口、推理深度和能力字段；刷新后仍正确展示；API Key 不回显明文。
- 最近一次测试结果：通过。`/api/provider/protocol-plugin/list` 返回 OpenAI 兼容和 Anthropic Messages 两个内置协议插件；供应商页显示协议插件、协议模式、默认模型、手填模型与上下文、推理深度、能力声明和 API Key 新值字段；供应商列表仅显示 API Key 已保存，不回显明文。
- 失败原因：无。
- 复现步骤：进入供应商页，新增或编辑供应商，选择协议插件，保存并刷新。
- 反馈对象：程序员子代理。
- 修复摘要：已验证通过。
- 复测结果：通过。

### T08 网络代理协议和认证字段

- 验证端：浏览器端。
- 建议工具：chrome-devtools。
- 是否必测：是。
- 前置启动条件：中心服务代理配置接口可用。
- 当前状态：通过。
- 验收标准：`/proxies` 协议选项包含 HTTP、HTTPS、SOCKS4、SOCKS4a、SOCKS5；用户名和密码可填可留空；保存后列表显示认证状态不泄露密码；无认证代理能正常保存。
- 最近一次测试结果：通过。`/proxies` 协议下拉包含 HTTP、HTTPS、SOCKS4、SOCKS4a、SOCKS5；用户名和密码字段可见；创建无认证代理后列表显示“无认证”，未泄露密码。
- 失败原因：无。
- 复现步骤：进入网络代理页，分别创建无认证代理和带认证代理，刷新检查列表。
- 反馈对象：程序员子代理。
- 修复摘要：已验证通过。
- 复测结果：通过。

### T09 智能体管理

- 验证端：浏览器端。
- 建议工具：chrome-devtools。
- 是否必测：是。
- 前置启动条件：中心服务智能体接口可用。
- 当前状态：失败待修复。
- 验收标准：`/agent-management` 显示主智能体且不可删除；长期智能体可以创建、修改、停用和删除；删除前有影响提示；操作后列表刷新并持久化。
- 最近一次测试结果：失败。`/agent-management` 可显示长期智能体创建表单和删除影响提示，但主智能体区域显示“中心服务暂未返回主智能体定义”。
- 失败原因：验收标准要求显示主智能体且不可删除；当前中心服务或前端未返回主智能体真实定义，不能证明主智能体不可删除约束。
- 复现步骤：进入智能体管理页，创建测试智能体，修改、停用、删除并刷新验证。
- 反馈对象：程序员子代理。
- 修复摘要：需程序员修复主智能体定义返回和不可删除展示约束。
- 复测结果：未执行。

### T10 行数治理功能回归

- 验证端：浏览器端。
- 建议工具：chrome-devtools。
- 是否必测：是。
- 前置启动条件：P01 已完成且启动状态覆盖最新前端代码。
- 当前状态：失败待修复。
- 验收标准：拆分大文件后 `/chat`、`/providers`、`/proxies`、`/usage`、`/agent-management` 均非白屏，主要交互仍可用，控制台无拆分导致的模块导入错误。
- 最近一次测试结果：失败。`/providers`、`/proxies`、`/usage`、`/agent-management` 可渲染，但多次路由切换出现 Vue 异常 `Unhandled error during execution of component update`；`/chat` 的真实对话链路失败。
- 失败原因：拆分后的页面仍有运行时异常和真实对话功能回归，不能判定页面功能回归未破坏对话、管理页和路由。
- 复现步骤：依次打开相关路由，检查页面渲染、控制台错误和基础交互。
- 反馈对象：程序员子代理。
- 修复摘要：需程序员修复路由切换 Vue 异常和真实对话功能回归。
- 复测结果：未执行。

## 完成口径

- 只有 T01 至 T10 全部勾选为 `[x]`，且详情状态均为 `通过`，才允许测试子代理把 `当前需要实现的需求.md` 标题追加 `-完成`。
- 标记完成前必须复查 `当前需要实现的需求.md`、`本次计划.md`、`本次测试计划.md` 中的临时图片引用，并在主线程授权下删除 `img.png`、`img_1.png`、`img_2.png`。

## 风险与阻塞

- `AI账号.md` 账号不可用、网络不可达或额度不足时，T01、T04、T05 阻塞归属为用户。
- 中心服务无法由桌面端拉起或 8866 不可访问时，阻塞归属为启动子代理或程序员子代理，按 `项目启动.md` 判断。
- 浏览器端发现功能未落地或回归时，对应测试项保持 `[ ]`，状态写为 `失败待修复` 并反馈程序员子代理。
