# 本次需求分析

## 需求标题

- `当前修改的需求-2026年6月1日11:30:04`

## 章节范围

- 来源文件：`提示词模板.md`
- 处理顺序：文件从上到下第一个未标记 `-完成` 的 `## 当前修改的需求-*` 章节。
- 本轮范围：从 `## 当前修改的需求-2026年6月1日11:30:04` 开始，到下一个二级标题 `## 全量测试` 之前结束。
- 排除范围：`## 全量测试`、`## 只用浏览器测试`、`## 公共模板` 及后续子代理模板均为调度或模板说明，不作为本轮临时需求正文。

## 验收项

- Web 端构建产物不得包含中心服务入口、SQLite、本地数据库驱动、`services/center` 源码相对引用或桌面壳专属文件。
- 中心服务只随桌面端存在，并在桌面端开发或打包链路中动态装配和拉起。
- `dev:desktop-shell` 启动桌面端时应具备中心服务前置状态，方便浏览器端和桌面端联动测试。
- 顶部菜单切换必须通过 Vue Router 导航，URL 或 hash 必须同步变化，支持刷新、复制链接和直接进入对应页面。
- 不再使用 `<MainView initial-page="..."/>` 或同类传参方式伪装页面拆分。
- 每个顶部菜单页面需要独立路由入口、独立页面组件和清晰的数据加载边界。
- 公共页面头部应作为公共壳处理，二级菜单和页面主体通过 `router-view` 或等价嵌套路由承载。
- `apps/frontend/src/views/AgentManagement/RouterIndex.vue` 不能只是空壳转发，主要业务模板和状态不应继续集中堆在 `WorkspacePage.vue`。
- `apps/frontend/src/views/Chat/RouterIndex.vue` 等页面入口应提升可读性，能明确看出公共壳、路由出口和页面内容关系。
- 执行模式下拉面板必须展示“全自动、自动编辑、建议模式”的解释说明，让用户理解审批语义和影响范围。

## 是否建议同步正式需求

- 本轮不建议、也不允许由需求分析师修改 `需求.md`。
- 原因：用户明确要求“本轮不要修改业务代码，不要修改 `需求.md`，只输出结构化结论”。
- 评估结果：`需求.md` 和 `架构.md` 已包含本轮多数正式口径，包括 Web 端不内置中心服务、顶部菜单真实独立页面、执行模式下拉说明、URL 路由同步、桌面端管理中心服务生命周期等内容。
- 后续建议：如果进入实现阶段，可由架构师基于本分析更新 `本次计划.md`；只有用户明确要求同步正式需求时，才再评估并修改 `需求.md`。

## 阻塞项

- 需求分析阶段无阻塞项。
- 后续实现前需要架构师明确页面拆分层级、嵌套路由结构、`AgentManagement` 页面组件边界，以及 `dev:desktop-shell` 拉起中心服务的开发期边界。
- 后续验收依赖可用的桌面端启动链路、浏览器测试环境，以及能确认 Web 构建产物边界的静态或打包检查。
