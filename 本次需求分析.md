# 本次需求分析

## 本次需求分析.md 路径

- `本次需求分析.md`

## 涉及需求标题清单

- `当前修改的需求-2026年6月1日11:30:04-先不做`
- `当前修改的需求-2026年6月1日17:51:44`

## 需求章节顺序和范围

- 来源文件：`提示词模板.md`
- 处理顺序：按文件从上到下处理所有未标记 `-完成` 的 `## 当前修改的需求-*` 章节。
- 第一段范围：从 `## 当前修改的需求-2026年6月1日11:30:04-先不做` 开始，到 `## 当前修改的需求-2026年6月1日17:51:44` 之前结束。
- 第二段范围：从 `## 当前修改的需求-2026年6月1日17:51:44` 开始，到 `## 全量测试` 之前结束。
- 本轮用户额外场景：`只用浏览器测试` 中的“新窗口任务：只执行浏览器端验证”，属于本轮验证调度边界，不是正式产品需求章节。
- 排除范围：`## 全量测试`、`## 只用浏览器测试`、`## 公共模板` 及后续子代理模板不是 `当前修改的需求-*` 正文，不作为新增产品需求处理。

## 需求意图和边界

### `当前修改的需求-2026年6月1日11:30:04-先不做`

- 章节标题已写明 `先不做`，本轮只做需求识别和边界记录，不进入代码实现、正式计划同步或完成标记。
- 产品和架构意图包括：中心服务只随桌面端存在；Web 端构建产物不得包含中心服务入口、SQLite、本地数据库驱动或桌面壳专属文件。
- 前端结构意图包括：顶部菜单页面必须真实拆分为独立路由入口、独立页面组件和明确数据加载边界，不继续用 `<MainView initial-page="..."/>` 或大组件传参伪装页面拆分。
- 页面组织意图包括：公共头部应作为公共壳，二级菜单和页面主体通过 `router-view` 或等价嵌套路由承载。
- URL 意图包括：顶部菜单切换必须同步更新 hash URL 或等价路由地址，支持刷新、复制链接和直接进入页面。
- 交互说明意图包括：执行模式下拉面板需要解释“全自动、自动编辑、建议模式”的审批语义和影响范围。
- 启动链路意图包括：`dev:desktop-shell` 应能拉起中心服务，解决浏览器端和桌面端测试时中心服务未启动的问题。

### `当前修改的需求-2026年6月1日17:51:44`

- 明确要求开发环境启动桌面端时同步拉起中心服务，关闭桌面端时同步关闭中心服务，不允许把中心服务作为独立启动项绕开桌面端生命周期。
- 明确要求相关启动方式和限制写入规范，避免后续测试或开发绕过桌面端直接启动中心服务。
- 明确要求不修改用户电脑环境或版本；遇到环境问题应按项目规范检查并停止说明，不能擅自调整本机环境来规避。
- 明确要求“不测试，不构建”。本轮用户又要求“只执行浏览器端验证”，两者存在表面冲突；按当前用户最新指令，本次需求分析只生成分析文档，不实际启动、不测试、不构建。后续若进入新窗口验证任务，应只创建浏览器端验证子代理，不创建桌面测试子代理。

### 本轮验证任务边界

- 本轮用户只要求“新窗口任务：只执行浏览器端验证”，并明确不要同步正式需求。
- 本次需求分析只产出或更新 `本次需求分析.md`，不修改 `需求.md`，不修改业务代码，不修改 `计划.md`。
- 若后续调度进入验证阶段，只允许浏览器端验证；不创建桌面测试子代理，不使用桌面测试工具验证桌面客户端。
- 浏览器端验证仍需遵守公共模板：浏览器测试子代理只使用 chrome-devtools，先维护 `浏览器测试窗口.md`，只操作测试专用页签。

## 必须验收项

- `本次需求分析.md` 已列出所有未标记 `-完成` 的 `当前修改的需求-*` 标题，并说明章节顺序和范围。
- 本轮明确识别 `当前修改的需求-2026年6月1日11:30:04-先不做` 为暂不实现需求，只记录分析，不落代码，不标记完成。
- 本轮明确识别 `当前修改的需求-2026年6月1日17:51:44` 为开发启动生命周期和协作规范要求，只记录分析，不落代码。
- 明确本轮用户没有要求同步正式需求，因此不得修改 `需求.md`。
- 明确本轮只要求浏览器端验证场景；后续若执行测试，只创建浏览器测试子代理，不创建桌面测试子代理。
- 明确当前轮次不修改业务代码、不启动服务、不运行测试、不运行构建。
- 明确需要后续架构师或计划阶段决策的边界：页面拆分层级、嵌套路由结构、`AgentManagement` 页面组件边界、`dev:desktop-shell` 拉起中心服务的开发期生命周期规则。

## 非目标和禁止顺手做的事项

- 不修改 `需求.md`。
- 不修改 `计划.md`。
- 不修改 `架构.md`。
- 不修改业务代码、路由、Vue 页面、启动脚本、Electron 主进程或中心服务代码。
- 不把 `当前修改的需求-*` 标题追加 `-完成`。
- 不执行浏览器测试、桌面测试、构建或打包。
- 不创建桌面测试子代理。
- 不直接启动 `services/center`。
- 不修改用户电脑 Node.js、pnpm、nvm、Java、Gradle 或其他本机环境版本。
- 不删除、回滚、覆盖无关文件，不执行 `git add` 或提交。

## 需要架构师决策的问题

- `先不做` 标题是否代表该章节只作为后续暂存需求，还是后续需要拆成正式计划但保持未执行状态。
- 顶部菜单页面拆分的嵌套路由层级、公共工作台壳命名、二级菜单与 `router-view` 的目录边界。
- `AgentManagement` 页面中 `RouterIndex.vue`、`WorkspacePage.vue` 和子页面组件的职责切分方式。
- `dev:desktop-shell` 在开发期拉起中心服务的实现边界：由脚本编排、Electron 主进程生命周期管理，还是两者协同。
- “不允许独立启动中心服务”在开发、测试、打包、排障场景中的例外边界是否需要写入 `架构.md` 或 `AGENTS.md`。

## 是否同步 `需求.md`

- 未同步 `需求.md`。
- 原因：本轮用户明确说明“没有要求同步正式需求，所以不要修改 `需求.md`”。
- `需求.md` 中删除或替换的冲突旧内容摘要：无，未修改 `需求.md`。

## 阻塞项

- 需求分析阶段无阻塞项。
- 后续若进入浏览器端验证，必须先有可用的启动信息或由启动阶段产出 `项目启动.md`；否则浏览器测试子代理无法确认访问 URL 和服务状态。
- `当前修改的需求-2026年6月1日11:30:04-先不做` 的执行优先级需要用户或架构师在后续阶段确认，本轮不推进实现。
