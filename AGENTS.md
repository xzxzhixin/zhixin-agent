# 致心智能体项目协作规则（简化版）

> 新会话、新 agent 或新开发者进入项目后必须首先读取本文件。  
> 冲突处理顺序：用户当前明确指令 > 本文件 > 其他文档。

## 一、必读文件

开始任何工作前，必须先阅读：

- `总体计划.md` – 系统方向（仅用户可修改）
- `需求.md` – 产品需求、最新进度
- `设计.md` – 功能设计、验收设计
- `架构.md` – 技术栈、目录边界、工程约定
- `功能清单与关系.md` – 功能依赖、影响范围、回归关系
- `AGENTS.md` – 本协作规则

## 二、文档入口规则

### 清单图标
- 清单状态图标：📋 未开始、⏳ 进行中、✅ 已完成、🔄 持续性。

### 通用原则
- `需求.md`、`设计.md`、`架构.md`、`功能清单与关系.md` 的具体职责、同步规则和更新边界以各文件头部说明为准。
- 开始实现前必须先确认对应事实源文档已经覆盖当前任务；实现完成后必须同步回查相关文档是否需要更新。
- 最终回复前必须自查文档同步情况。

## 三、工程目录与构建

### 目录规则（详见 `架构.md`）
- 不同端（桌面端、Web端、插件）、中心服务、共享代码不得混放。
- `架构.md` 中的目录树、模块清单、接口清单必须带说明。

### 包管理与构建
- Node 版本使用用户系统环境，不符时提醒用户。
- TypeScript：不通过 `tsc` 或 `vue-tsc` 做项目级校验，仅用于 IDE 识别。禁止在 `package.json` 脚本中加入 `tsc --noEmit` 等类型检查命令。
- 构建产物命名、打包规则以 `架构.md` 为准。

## 四、中心服务原则

- 中心服务是唯一事实源，客户端所有能力访问必须通过中心服务。
- 中心服务**不允许独立启动**。必须通过桌面端或 `dev:desktop-shell` 拉起。
- `dev:frontend` – 独立启动前端 Vite 服务（热更新）。
- `dev:desktop-shell` – 启动桌面壳并拉起中心服务（不拉起前端 Vite）。重启前必须关闭旧进程。
- 浏览器验收必须用 Chrome DevTools 真实操作（点击、输入等），不能只静态检查或调接口。
- 项目对话测试固定使用 `项目对话测试` 文件夹。

## 五、文件与资源规则

- 应用图标：`图标.png`（用于 Electron 所有图标位置）。
- 项目身份文件：`致心项目ID.md`。
- 中文文件名必须与磁盘实际文件名完全一致。
- 文档、注释、回复使用中文，UTF-8 编码。

## 六、代码风格规则

### 注释
- 新增/修改代码必须补中文注释。
- 方法/函数、字段/接口/枚举、复杂逻辑、边界处理都要注释说明用途、参数、返回值、来源、约束等。

### 格式
- 多属性对象、数组、参数拆成多行。
- Vue 属性每个一行，CSS 声明一行一个。
- 三目运算符仅用于简单变量赋值，禁止嵌套。

### 组织
- 单个代码文件不超过 1500 行，超出则拆分模块。
- 前端页面入口：`apps/frontend/src/views/页面/RouterIndex.vue`，路由使用动态导入。
- 页面专属弹框放在 `dialogs/` 目录，全局复用弹框才放 `packages/ui`。
- 样式优先 `<style scoped>`，全局 `styles.css` 只放主题变量、基础覆盖等。
- 路径别名（@、~、@views、@components、@stores、@api）需同步维护。

### 布局
- 主工作区使用 flex 布局固定视口高度，禁止页面级滚动。只有对话消息列表可主滚动，其他局部滚动限制在自身容器内。

### 编码习惯
- 不要用多个候选字段兜底（如 `row.a || row.b`）。
- 默认值仅在需求或业务规则明确时使用，并写明原因。
- 删除无用导入和 URL（测试专用也要删除）。
- 复杂功能用 class 组织代码。
- 一个class就是一个文件，不允许多个class在一个文件里面，文件名和要class名字一致

## 七、开发流程规则

- 不测试或不构建时，最终说明必须明确写出“未测试”或“未构建”。
- 若用户明确要求“不测试、不构建”，只做静态自查。
- 不要擅自删除未跟踪文件或未提交改动。
- 需要删除文件、重置 Git、清理数据库等危险操作前，必须征得用户确认。
- 所有时间使用中心服务本机时间，格式 `YYYY-MM-DD HH:mm:ss`。
- 中心服务开发日志只记录命令启动和结束，中间不输出。
- 优先使用 ideaMCP 服务检索代码。
- 代码按功能模块化（工具放 `utils/`，智能体定义放 `agents/`，智能体工具放 `tools/`）。

## 八、测试规范

- 不编译 TS 源码再启动，直接用 `dev:desktop-shell` 等命令。
- 启动前先关闭之前进程，保证最新代码。
- 测试启动时必须记录 `启动进程.md`：一行一个 `{pid} = {port} = {启动命令}`。
- 打开的浏览器页面记录 `浏览器页面.md`：一行一个 `{pageId} = {pageUrl}`。
- 浏览器测试页面不要主动切换过去，避免打断用户使用。

## 九、提交规范

- 不校验 `提示词模板.md` 和本文件，直接推送。
- `需求.md`、`设计.md`、`架构.md`、`功能清单与关系.md` 发生文档改动时，不做额外校验，直接提交并推送。
- `.gitignore` 忽略的文件不要强制加入。
- 远端领先时先拉取。
- 工作区所有改动自动推送。
- 所有任务完成后必须执行 git 推送。

---

# Karpathy 准则（简化版）

> 用于减少 LLM 编码常见错误，谨慎优先。

## 1. 编码前先思考

- 明确说出假设，不确定就问。
- 存在多种理解时列出，不要暗自选择。
- 有更简单的方案就说明，必要时提出异议。
- 不清楚的地方停下来，询问清楚再继续。

## 2. 简单优先

- 用最少代码解决问题，不写臆测性实现。
- 不添加用户未要求的功能、抽象、灵活性或错误处理。
- 如果 50 行能解决却写了 200 行，就重写。
- 自问：“资深工程师会不会认为这太复杂？”是则简化。

## 3. 外科手术式修改

- 只改必须改的内容，不“顺手改进”旁边的代码、注释或格式。
- 不重构没有坏的代码，匹配现有风格。
- 如果发现无关死代码，只说明不删除。
- 改动造成的孤立内容（无用导入、变量）必须删除，但不删除原本就存在的死代码（除非用户要求）。
- 每一行改动都必须能直接追溯到用户请求。

## 4. 目标驱动执行

- 把任务转化为可验证目标：
    - “添加校验” → “先为无效输入编写测试，再让测试通过”
    - “修复 bug” → “先写能复现问题的测试，再让测试通过”
    - “重构 X” → “确保重构前后测试都通过”
- 多步骤任务先写简短计划，每步带验证标准。
- 强成功标准可独立循环推进；弱标准（如“让它能用”）需不断澄清。
