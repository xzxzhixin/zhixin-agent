# 致心智能体项目协作规则

本文件是新会话、新 agent 或新开发者进入项目后必须先读取的项目规则。出现冲突时，用户当前明确指令优先，其次遵守本文件。

## 必读文件

开始任何需求拆解、代码修改、审查或实现前，必须先读取：

- `需求.md`：产品需求源。
- `计划.md`：实施拆解和任务状态源。
- `AGENTS.md`：当前协作规则。

## 需求与计划同步规则

- `需求.md` 发生任何新增、删除、修改，都必须同步更新 `计划.md`。
- 完成 `计划.md` 中任意任务后，必须立即把对应任务从 `[ ]` 改为 `[x]`。
- `计划.md` 只维护计划阶段、任务状态、验收口径和必要的任务处理原因，不写流水记录。
- 如果实现过程中发现任务需要拆分、延期、替换或废弃，必须在 `计划.md` 保留原任务并说明处理原因。
- 不允许只改代码不改计划。
- 不允许只改需求不改计划。
- 如果用户临时增加需求，先更新 `需求.md`，再更新 `计划.md`，然后再落代码。
- 如果用户只要求更新计划或规则，不要顺手改业务代码。

## 工程目录规则

- 桌面端代码放在 `桌面端`。
- Web端代码放在 `Web端`。
- IDEA 插件 SDK 和 IDEA 插件接入代码放在 `IDEA_SDK`。
- 中心服务后端、数据读写、智能体调度、任务执行、记忆、会话和扩展能力管理放在 `中心服务`。
- 多端共用类型、协议、工具函数、接口定义和渲染组件放在 `共享`。
- 新增其他 UI 端或 SDK 时，必须在项目根目录新增独立目录，不能混入已有端目录。

## 包管理与构建工具

- Node.js 包管理统一使用 pnpm。
- 根目录必须保留 `pnpm-workspace.yaml`。
- 所有 `package.json` 依赖版本必须固定精确版本，不使用 `^` 或 `~`。
- 项目统一 `.npmrc` 必须包含镜像源配置和 `save-exact=true`。
- 遇到 Node.js 版本、命令或运行环境不符合项目要求时，必须先检查本机是否安装 nvm；如果有 nvm，优先使用 nvm 切换或安装所需 Node.js 版本后继续；如果没有 nvm，或者 nvm 无法解决当前版本问题，必须停止并等待用户处理，不能自行改用其他未确认的 Node.js 环境绕过。
- Java 依赖和构建统一使用 Maven。
- IDEA SDK 使用 `pom.xml` 管理。

## 中心服务原则

- 中心服务是唯一事实源。
- 桌面端、Web端、IDEA 插件都只能通过中心服务访问智能体、会话、项目、任务、记忆、供应商和扩展能力状态。
- API Key、账号密码等敏感信息只保存在中心电脑，由中心服务校验和使用。
- IDE 插件不得暴露 API Key 明文。
- Web端非本机访问必须走账号密码和登录态。
- IDEA 插件只连接 `127.0.0.1:端口`。

## 文件与资源规则

- 应用图标固定文件名为 `图标.png`。
- `图标.png` 用于 Electron 应用图标、系统托盘图标、任务栏图标、窗口图标和安装包图标。
- 项目身份文件固定为 `致心项目ID.md`。
- 中文文件名必须与磁盘真实文件名完全一致。
- 文档、注释和回复使用中文，文件使用 UTF-8 编码。

## 代码风格规则

- 新增或修改代码必须补中文注释。
- 方法或函数注释说明用途、关键逻辑、参数和返回值。
- 字段、接口字段、协议字段、状态码、枚举值必须单独注释来源、含义、格式、默认值或约束。
- 复杂逻辑和边界处理必须解释为什么这样处理。
- 多属性对象、数组、参数必须拆成多行。
- Vue 属性每个一行。
- CSS 声明一行一个。
- 不要用多个候选字段兜底猜测业务协议，例如 `row.a || row.b || row.c`。
- 默认值只能在需求、协议或业务规则明确时使用，并写清楚原因。

## 开发流程规则

- 默认不提交 Git，除非用户明确要求提交。
- 不运行测试或不构建时，最终说明必须明确写出未测试或未构建。
- 如果用户明确要求“不测试、不构建”，只能做静态自查，不运行测试和构建命令。
- 发现已有未跟踪文件或未提交改动时，不要擅自删除、回滚或覆盖无关内容。
- 需要删除文件、重置 Git、清理数据库等危险操作时，必须先征得用户明确确认。

## 计划执行规则

执行 `计划.md` 中任务时，按以下顺序处理：

1. 读取 `需求.md`、`计划.md` 和本文件。
2. 确认要执行的计划任务编号或阶段。
3. 按现有目录边界修改对应模块。
4. 完成后立即更新 `计划.md` 任务勾选状态。
5. 最终回复列出修改范围、计划同步情况，以及是否测试或构建。

## 当前项目事实

- 项目中文名：致心智能体。
- 项目英文名：zhixin-agent。
- 默认中心服务端口：8866。
- 默认 IDEA 插件连接地址格式：`http://127.0.0.1:端口`。
- 当前 Node.js 工作区目录：`桌面端`、`Web端`、`中心服务`、`共享`。
- 当前 Java/Maven 模块：`IDEA_SDK`。
