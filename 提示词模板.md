
# 使用

/using-superpowers 和我对齐需求所有细节再落实TDD执行。不使用视觉伴侣。
大任务执行规范：任务再大也必须在本轮持续推进到完成；任务明显过大或上下文复杂时，先创建或更新 `本次计划.md`，用清单固化目标、用户最新指令、硬性约束、任务拆分、子代理分工、验收方式、当前状态和校准记录。主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复；能隔离的执行工作全部交给子代理，存在强顺序依赖或共享文件风险的工作按阶段分批派发子代理处理。每完成一个阶段，主线程必须回看 `本次计划.md`、最新用户要求和当前文件状态，确认没有偏移后继续；上下文压缩、恢复或长时间执行后，必须先读取 `本次计划.md` 和最新状态继续执行，不重新理解成新任务。计划允许动态校准，但必须在 `本次计划.md` 写明调整原因。
本次测试规范：重启保证是最新代码生效；重启前查看是否有运行中的代码，有先关闭；浏览器使用chrome-devtools测试。
任务完成再处理：gitignore忽略的不要强制加入；发现有远端领先先拉取；工作区里面所有的改动全部自动推送；删除这段提示词里面引用的图片。
本次任务：1、我想了一下直接用https://github.com/langchain-ai/langgraphjs，把我们自己实现的全部替换到他这个来；然后去完整实现计划.md里面没有完成的；2、记忆如何实现，看看有没有好用的三方包集成；


# 整体模板

## 只测试浏览器端

/using-superpowers 和我对齐需求所有细节再落实TDD执行。不使用视觉伴侣。
大任务执行规范：任务再大也必须在本轮持续推进到完成；任务明显过大或上下文复杂时，先创建或更新 `本次计划.md`，用清单固化目标、用户最新指令、硬性约束、任务拆分、子代理分工、验收方式、当前状态和校准记录。主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复；能隔离的执行工作全部交给子代理，存在强顺序依赖或共享文件风险的工作按阶段分批派发子代理处理。每完成一个阶段，主线程必须回看 `本次计划.md`、最新用户要求和当前文件状态，确认没有偏移后继续；上下文压缩、恢复或长时间执行后，必须先读取 `本次计划.md` 和最新状态继续执行，不重新理解成新任务。计划允许动态校准，但必须在 `本次计划.md` 写明调整原因。
本次测试规范：重启保证是最新代码生效；重启前查看是否有运行中的代码，有先关闭；浏览器使用chrome-devtools测试。
任务完成再处理：gitignore忽略的不要强制加入；发现有远端领先先拉取；工作区里面所有的改动全部自动推送；删除这段提示词里面引用的图片。
本次任务：

## 浏览器和桌面端都测试

/using-superpowers 和我对齐需求所有细节再落实TDD执行。不使用视觉伴侣。
大任务执行规范：任务再大也必须在本轮持续推进到完成；任务明显过大或上下文复杂时，先创建或更新 `本次计划.md`，用清单固化目标、用户最新指令、硬性约束、任务拆分、子代理分工、验收方式、当前状态和校准记录。主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复；能隔离的执行工作全部交给子代理，存在强顺序依赖或共享文件风险的工作按阶段分批派发子代理处理。每完成一个阶段，主线程必须回看 `本次计划.md`、最新用户要求和当前文件状态，确认没有偏移后继续；上下文压缩、恢复或长时间执行后，必须先读取 `本次计划.md` 和最新状态继续执行，不重新理解成新任务。计划允许动态校准，但必须在 `本次计划.md` 写明调整原因。
本次测试规范：重启保证是最新代码生效；重启前查看是否有运行中的代码，有先关闭；浏览器使用chrome-devtools测试；桌面端用windows-mcp测试。
任务完成再处理：gitignore忽略的不要强制加入；发现有远端领先先拉取；工作区里面所有的改动全部自动推送；删除这段提示词里面引用的图片。
本次任务：
