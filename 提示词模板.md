## 当前修改的需求-2026年6月1日23:16:11

- 中心服务要启动起来
  - 不修改电脑环境
  - 修改依赖版本
  - 不行就改架构
- UI和老一版对不上
  - 菜单按钮没有中线对齐，激活样式丢失
  - 对话页面没有占满空间
- 桌面端启停要同步拉起中心服务和关闭
  - 不修改node_modules
  - 要支持开发阶段
  - 构建以后也要支持
- 代码文件不允许超过1500行，写入规范

## 全量测试

/superpowers 新窗口任务：执行全量验证。按 `提示词模板.md##新窗口任务公共入口规则` 调度；本场景需要浏览器测试和桌面测试。

## 只用浏览器测试

/superpowers 新窗口任务：只执行浏览器端验证。按 `提示词模板.md##新窗口任务公共入口规则` 调度；本场景只创建浏览器测试子代理，不创建桌面测试子代理。

## 公共模板

### 新窗口任务公共入口规则

- 先在当前 `zhixin-agent` 仓库根目录读取 `AGENTS.md`、`需求.md`、`架构.md`、`计划.md`；读取 `提示词模板.md` 时，主线程只读取 `公共模板`、`主线程调度模板` 和准备创建子代理所需的角色模板。
- 再按 `提示词模板.md##公共模板` 和 `提示词模板.md##主线程调度模板` 调度；`当前修改的需求-*` 章节只能由需求架构子代理定位和读取，主线程不能直接读取、归纳或拆解。
- 按阶段闸门依次创建需求架构、程序员、启动和测试子代理；不再创建检验子代理。
- 只有 `本次计划.md` 明确互不依赖的程序员开发任务才允许并行。
- 测试阶段不能并行；同一轮只创建一个测试子代理，该测试子代理必须按 `本次测试计划.md` 串行验证浏览器端、桌面端或当前场景指定范围，并持续更新测试状态。
- 具体测试范围以 `本次测试计划.md` 和当前任务场景说明为准。

### 共同上下文

- 项目根目录为当前 `zhixin-agent` 仓库根目录，不写死本机绝对路径。
- `AGENTS.md`、`需求.md`、`架构.md`、`计划.md`、`提示词模板.md` 都是仓库内 Markdown 文档，必须用文件读取能力或 IDE 文件能力读取，禁止用浏览器或 chrome-devtools 打开这些本地文档。
- 所有主线程和子代理必须先读取项目根目录的 `AGENTS.md`，并遵守其中关于需求、架构、计划、构建、测试、危险操作和中文回复的规则。
- 产品需求以 `需求.md` 为准，架构和目录边界以 `架构.md` 为准，实施任务状态以 `计划.md` 为准。
- `提示词模板.md` 只作为任务提示入口和子代理职责模板，不是产品需求源。
- 所有回复、文档和注释使用中文。
- 默认不提交 Git；程序员编辑的任务范围内文件默认允许执行 `git add`，但不允许 commit，除非用户明确要求提交。
- 不删除、不回滚、不覆盖无关文件；发现无关改动只报告，不处理。
- 需要删除文件、重置 Git、清理数据库或执行不可逆操作时，必须先获得用户明确确认。

### 需求与计划规则

- 用户新增临时需求时，标题格式固定为 `## 当前修改的需求-时间`。
- 需求完成后，必须把对应标题改为 `## 当前修改的需求-时间-完成`；只追加 `-完成`，不要改标题前缀和时间。
- 需求架构子代理负责定位 `提示词模板.md` 最前面的所有未标记 `-完成` 的 `当前修改的需求-*` 章节，并按文件从上到下处理。
- 主线程不能定位、识别、解释、拆解或归纳需求内容。
- 未完成需求必须先交给需求架构子代理处理，由它产出 `本次计划.md` 和 `本次测试计划.md` 后，再交给程序员、启动和测试子代理执行。
- `本次计划.md` 是本轮开发参考计划，用于约束程序员子代理的任务、文件范围、依赖关系和验收口径。
- `本次测试计划.md` 是本轮测试参考计划，用于约束测试子代理的测试范围、执行顺序、多状态流转、失败反馈和复测闭环。
- 不要额外全文检索或反复读取 `提示词模板.md`；只读取公共模板、本轮需要的子代理模板和当前修改需求章节。
- 如果当前修改需求只是临时执行说明，不要写入 `需求.md`。
- 如果用户明确要求新增、修改或删除产品需求，必须先更新 `需求.md`，再评估并更新 `计划.md`，最后再落代码。
- 如果用户要求“把本次需求分析同步到 `需求.md`”或同等意思，需求架构子代理必须把分析出的正式产品需求写入 `需求.md`；`本次计划.md` 和 `本次测试计划.md` 不能替代正式需求源。
- 同步 `需求.md` 时，必须以当前最新需求为准；如果旧需求与本次分析结果冲突、重复或口径过期，直接删除旧冲突内容并写入最新需求，不保留历史记录、变更流水或多套并存口径。
- 如果用户要求“把本次计划同步到 `计划.md`”或同等意思，需求架构子代理必须把本次实施计划写入 `计划.md`；`本次计划.md` 只作为本轮参考计划，不能替代正式实施任务表。
- 如果本轮会修改业务代码、测试脚本、构建脚本或工程配置，必须在落代码前或落代码后同步评估 `计划.md`；新增任务、替换任务、废弃任务或完成已有任务都必须写入 `计划.md`，不能只维护 `本次计划.md`。
- 同步 `计划.md` 时，必须与最新 `需求.md` 对齐；如果旧计划与本次计划冲突、重复或口径过期，直接删除旧冲突任务并写入最新计划，不保留历史计划、废弃流水或多套并存任务。
- `需求.md` 和 `计划.md` 都只保存当前最新有效内容：`需求.md` 保存当前产品需求，`计划.md` 保存当前实施任务和状态；除必要任务处理原因外，不做历史记录。
- 如果改动完成了 `计划.md` 中已有任务，必须把对应任务从 `[ ]` 改为 `[x]`。
- 如果没有完成 `计划.md` 中任何任务，最终回复必须说明未同步 `计划.md` 的原因。
- 所有实现、验证和允许的计划同步完成后，测试子代理才允许把对应 `当前修改的需求-*` 标题追加 `-完成`。
- 程序员实现完成但测试未完成时，不得标记 `-完成`；测试完成且 `本次测试计划.md` 中所有必测项通过后，由测试子代理更新标题追加 `-完成`。
- 未完成、未验证或仍有阻塞项时，不得把需求标题标记为 `-完成`。

### 启动与运行约束

- 禁止直接运行 `services/center` 启动中心服务。
- 需要中心服务参与测试时，只能启动桌面端，由桌面端拉起中心服务。
- 启动项目的子代理必须创建或更新 `项目启动.md`。
- `项目启动.md` 至少写明：启动方式、使用的命令或 IDEA Run Configuration、实际访问 URL、中心服务是否由桌面端拉起、端口、关键日志位置、测试前置状态和停止方式。
- 插件端只落实代码，不测试，不构建。
- 项目对话测试统一使用项目根目录下的 `对话测试` 目录。
- 如果用户明确要求“不测试、不构建”，所有代理只能做静态自查，不运行测试和构建命令。

### 子代理通用约束

- 子代理必须先读取 `提示词模板.md##公共模板`，再读取自己的角色模板。
- 子代理只读取完成任务所需的最小文件范围，不要全文读取无关目录。
- 子代理只能处理主线程分配的明确任务，不创建下级子代理。
- 子代理接收任务时，主线程只提供文件路径、章节引用、任务编号、文件范围和状态要求，不在消息中粘贴大段文件内容、长日志、长 diff 或测试细节。
- 子代理的详细产出必须写入对应结果文件；返回主线程时只汇报结果文件路径和状态，状态只允许使用 `完成`、`阻塞`、`失败`、`需复测` 或 `需拆分`。
- 子代理默认只能写自己负责的结果文件和主线程明确授权的业务文件；没有共享文件写入授权时，不得修改 `本次计划.md`、`本次测试计划.md`、`项目启动.md`、`需求.md`、`架构.md`、`计划.md` 或 `提示词模板.md`。
- 子代理需要更新共享文件时，必须在结果文件中写明建议更新内容和目标位置，由主线程确认当前没有其他写入者后再授权写入或由主线程串行合并。
- 子代理默认不能修改 `需求.md`；但需求架构子代理在主线程明确分配“同步正式需求”任务时，必须按最新需求分析结果更新 `需求.md`。
- 子代理默认不能修改 `计划.md`；但需求架构子代理在主线程明确分配“同步正式计划”任务时，必须按最新本次计划更新 `计划.md`。
- 子代理不能跨任务顺手重构无关文件。
- 子代理不能提交 commit、推送、变基或回滚；程序员子代理只允许对自己任务范围内已经编辑的文件执行 `git add`，不得暂存无关文件。
- 子代理返回结果后，主线程必须及时关闭该子代理，避免已完成会话继续占用资源或残留上下文。
- 测试截图只允许作为工具即时观察结果使用，默认不保存到仓库或本地文件；失败截图也不保留。
- 如果 chrome-devtools、windows-mcp 或其他验证工具强制生成了截图文件，创建该文件的子代理必须在本轮输出前删除；无法确认来源或无法删除时，必须在结果中列为“待用户确认清理”，不得继续扩散引用。
- 子代理必须把关键可见现象、页面状态、控制台错误、网络错误和复现步骤写入对应结果文件；返回主线程时仍只汇报结果文件路径和状态，不用截图文件作为长期证据。

## 主线程调度模板

### 主线程职责

- 主线程只负责协调、分发、检查和最终汇总，负责把控方向和验收口径，不承担需求定位、需求识别、需求解释、需求拆解或可下沉到子代理的长过程执行。
- 主线程必须维护任务状态，发现子代理结果冲突、遗漏或越界时，负责收敛处理。
- 主线程不读取 `当前修改的需求-*` 做定位；只创建需求架构子代理，让需求架构子代理输出需求标题、顺序、章节范围、`本次计划.md` 和 `本次测试计划.md`。
- 子代理必须由主线程创建，这一点无法避免；主线程必须在 `本轮子代理登记.md` 记录每个子代理的 agent id、角色、负责范围、输入文件、输出文件、当前状态和是否可复用。
- 同一轮需求内优先复用已有子代理：测试反馈优先发回原程序员子代理，程序员修复后的复测优先发回原测试子代理，新增需求分析优先发回原需求架构子代理；只有原子代理已关闭、失效或职责范围明显不匹配时才创建新子代理。
- 当前需求整轮完成、阻塞终止或用户明确停止后，主线程必须关闭 `本轮子代理登记.md` 中本轮创建且仍在运行的全部子代理，并把状态更新为已关闭。
- 子代理不是默认并行创建；主线程必须先通过需求架构闸门，再根据 `本次计划.md` 判断哪些程序员开发任务可以并行。
- 主线程创建子代理时，只传入：`提示词模板.md##公共模板` 引用、对应角色模板引用、任务编号、文件范围、验收口径、输入文件路径和输出文件路径；所有上下文都以文件引用形式传递。
- 主线程不要把 `需求.md`、`架构.md`、`计划.md`、完整 `提示词模板.md` 反复粘贴给每个子代理。
- 主线程统一检查 diff 后，可以对程序员编辑的任务范围内文件执行 `git add`；无关文件、用户已有改动和删除操作不得暂存，commit 仍必须等待用户明确要求。
- 主线程禁止使用 chrome-devtools 或其他浏览器自动化工具直接操作页面；浏览器验证只能由浏览器测试子代理执行，主线程只读取浏览器测试子代理的结构化结果并协调后续处理。
- 主线程收到子代理返回“未完成、超时、端口未监听、请求被拒绝、测试阻塞、未标记完成”时，不能直接把该结果作为最终回复；必须先判断是否属于可继续处理的问题，并继续派发程序员、启动、浏览器测试或桌面测试子代理形成修复闭环。
- 如果 `dev:desktop-shell` 超时、`8866` 未监听、中心服务未由桌面端拉起、浏览器联动请求 `ERR_CONNECTION_REFUSED` 或出现前端未捕获 Promise，主线程必须把它视为待修复缺陷，继续协调定位和修复，不得只汇总“完整验收阻塞”后结束。
- 只有同一阻塞原因经过至少三轮连续处理仍无法推进，或者缺少用户权限、外部环境、账号密钥、系统能力等主线程和子代理都无法自行解决的条件时，才允许最终回复为阻塞；最终回复必须写清已尝试轮次、阻塞证据、仍缺少的外部条件和下一步需要用户提供什么。

### 共享文件写入协调

- `本轮子代理登记.md` 由主线程维护，是本轮子代理 id、角色、状态、复用关系和关闭状态的唯一登记文件。
- `本次计划.md`、`本次测试计划.md`、`项目启动.md`、`需求.md`、`架构.md`、`计划.md` 和 `提示词模板.md` 是共享协调文件，任一时刻只能有一个写入者。
- 主线程必须维护共享文件写入授权，创建或复用子代理前写清本次允许写入的文件路径、章节或任务编号；没有授权的子代理只能写自己的结果文件。
- `本次计划.md` 和 `本次测试计划.md` 的结构性调整只允许需求架构子代理在主线程授权下执行；程序员、启动和测试子代理只能在主线程授权下更新自己负责的任务状态或测试状态，不得重排、删除或重写其他章节。
- 并行程序员子代理不得同时写同一个共享文件。需要反馈计划或测试状态时，先写入各自结果文件，由主线程串行合并，或由主线程逐个授权写入指定任务行。
- 需求中途追加时，主线程优先复用原需求架构子代理重新读取新增或未完成的 `当前修改的需求-*` 章节，并授权它更新 `本次计划.md` 和 `本次测试计划.md`；更新期间暂停其他子代理对这两个文件的写入。
- 测试发现问题时，测试子代理先把失败项、复现步骤和反馈对象写入 `本次测试计划.md` 或自己的结果文件；主线程再把对应任务发回原程序员子代理，程序员修复后只在授权范围内把对应测试项标记为 `已修复待复测`。
- 如果主线程发现共享文件在子代理执行期间已被其他流程修改，必须重新读取最新文件并判断是否需要让原子代理基于最新内容重做，不允许用旧内容覆盖新内容。

### 子代理交接与拆分

- 子代理上下文接近上限、任务过长或状态复杂时，必须先写一次性交接文件，再由主线程创建或复用同类替补子代理继续。
- 一次性交接文件直接放在项目根目录，不使用文件夹包裹；命名格式为 `子代理交接-角色-原编号-to-新编号.md`，例如 `子代理交接-程序员-1-to-程序员-1b.md`。
- 交接文件只允许用于一次接力。替补子代理读取并确认接手后，主线程必须删除该交接文件，并在 `本轮子代理登记.md` 记录交接文件已删除。
- 替补子代理读取交接文件后，如果判断当前任务仍会超出上下文，不能继续业务实现、测试或共享文件写入，只能进入拆分建议模式。
- 拆分建议模式只产出 `状态：需拆分` 和结果文件；结果文件写清超上下文原因、建议拆分后的任务编号、文件范围、依赖关系、验收口径和建议写入的计划文件位置。
- 主线程不直接拆任务。收到 `需拆分` 后，主线程把拆分建议结果文件交给需求架构子代理，并授权需求架构子代理把拆分落入 `本次计划.md` 或 `本次测试计划.md`。
- 需求架构子代理完成计划拆分后，主线程再按新的任务编号创建或复用同类子代理继续执行。

### 主线程上下文控制

- 长任务执行时，主线程必须主动控制窗口上下文长度。
- 主线程只保留：`本轮子代理登记.md` 路径、输入文件路径、输出文件路径、任务编号、文件范围、共享文件写入授权、状态、需要自己决策的冲突和最终验收结果；详细需求、计划、日志、测试证据和子代理结论都通过结果文件读取。
- 子代理的详细探索过程、长日志、长 diff、截图细节和重复规则不进入主线程上下文；需要留痕时写入 `本次计划.md`、`本次测试计划.md`、`项目启动.md` 或子代理结构化结果。
- 主线程派发子代理时优先引用模板章节，不复制章节全文。
- 子代理返回结果后，主线程只读取结果文件路径和状态；确需细节时再按路径打开对应文件，已完成的子代理要及时关闭。
- 当主线程上下文变长时，先把当前状态压缩到 `本次计划.md` 或最终汇总草稿，再继续派发或收敛。

### 调度流程

主线程必须按“需求架构闸门 + 条件并行开发 + 串行测试闭环”调度。不是所有子代理都并行，只有开发依赖已经满足、文件范围互不重叠、职责边界清晰的程序员任务才允许并行；测试子代理一律串行。

1. 需求架构闸门：主线程读取 `AGENTS.md`、`需求.md`、`架构.md`、`计划.md`、公共模板和主线程调度模板，不读取 `当前修改的需求-*` 做定位；随后只创建或复用需求架构子代理，由需求架构子代理定位并分析未完成需求，并在主线程授权下输出或更新 `本次计划.md` 和 `本次测试计划.md`。
2. 正式需求同步：如果本轮用户要求同步正式需求，需求架构子代理必须把最新正式需求同步到 `需求.md`，并删除与最新需求冲突、重复或过期的旧需求。
3. 正式计划同步：如果本轮用户要求同步正式计划，或本轮会修改业务代码、测试脚本、构建脚本或工程配置，需求架构子代理必须把 `本次计划.md` 中的最新实施计划同步到 `计划.md`，并删除与最新计划冲突、重复或过期的旧计划；如果只修改临时协作模板或本轮规则且不触碰业务实现，可以不更新 `计划.md`，但最终必须说明原因。
4. 实现阶段：主线程只依据最新 `本次计划.md` 和 `计划.md` 创建程序员子代理。多个程序员子代理只有在任务明确互不依赖、文件范围互不重叠时才可以并行；涉及同一文件、同一协议、同一启动链路、同一路由壳或同一 UI 公共组件的任务必须串行。
5. 启动阶段：启动子代理通常只创建一个，用于准备测试环境并维护 `项目启动.md`。需要中心服务参与时，只能通过桌面端拉起中心服务；如果中心服务、桌面壳、启动脚本、preload、静态资源托管、CORS 或数据库迁移发生变化，必须先刷新启动状态再测试。
6. 测试阶段：启动信息可用后按 `本次测试计划.md` 创建唯一测试子代理并串行执行测试项。全量测试场景下也不能并行测试；同一个测试子代理先执行 `本次测试计划.md` 中排序靠前的测试项，再继续执行下一类测试。只用浏览器测试场景的唯一测试子代理只执行浏览器测试，不执行桌面测试。
7. 测试计划更新：测试子代理每完成一项测试，在主线程授权下更新 `本次测试计划.md` 中对应状态、结果、失败原因、复现步骤、反馈对象和复测状态；未获授权时先写入自己的结果文件，由主线程串行合并。状态至少包括：`待测试`、`测试中`、`通过`、`失败待修复`、`已修复待复测`、`阻塞`。
8. 闭环回流：如果启动、浏览器测试或桌面测试返回阻塞，主线程先判断归属。代码问题回到原程序员子代理，程序员修复后在授权范围内把相关测试项状态改为 `已修复待复测`，未获授权时写入程序员结果文件等待主线程合并；启动、端口、中心服务拉起或运行环境问题回到原启动子代理；页面或桌面复测问题回到原测试子代理。可继续推进时不得直接进入最终汇总。
9. 完成标记：唯一测试子代理在完成全部指定测试范围后，必须重新读取完整 `本次测试计划.md`；只有所有必测项均为 `通过`，且没有阻塞、失败待修复或未复测项时，才负责把对应 `当前修改的需求-*` 标题追加 `-完成`，不再创建检验子代理。
10. 最终汇总：最终用中文汇总修改范围、需求同步、计划同步、架构同步、测试或构建结果、`本次测试计划.md` 最终状态、未测试项、未构建项和风险；如果仍阻塞，必须满足主线程职责中的阻塞终止条件。

### 子代理创建提示压缩规则

- 对子代理只写“请读取 `提示词模板.md##公共模板` 和 `提示词模板.md##某某子代理提示词`”，不要粘贴公共模板全文。
- 创建或复用子代理前，主线程先更新 `本轮子代理登记.md`；给子代理的消息只引用该登记文件路径，不复制已有子代理列表。
- 已经写入 `本次计划.md` 的任务拆分，不要在子代理提示里重复展开，只给 `本次计划.md` 路径、任务编号和文件范围。
- 已经写入 `本次测试计划.md` 的测试项，不要在子代理提示里重复展开，只给 `本次测试计划.md` 路径、测试项编号和适用测试范围。
- 已经写入 `项目启动.md` 的启动信息，不要在测试子代理提示里重复展开，只给文件路径。
- 子代理结果必须写入指定结果文件；返回主线程的消息只允许包含 `状态：...` 和 `结果文件：...`。

## 需求架构子代理提示词

### 使用方式

创建需求架构子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行需求架构任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##需求架构子代理提示词`。
本轮需求来源是 `提示词模板.md` 最前面的所有未标记 `-完成` 的 `当前修改的需求-*` 章节，按文件从上到下处理。
请在主线程授权的共享文件写入范围内生成或更新 `本次计划.md` 和 `本次测试计划.md`；如果主线程或用户明确要求同步正式需求或正式计划，则同时更新对应正式文档，不要修改业务代码。
```

### 职责

- 定位并读取未完成的 `当前修改的需求-*` 章节，分析真实意图、范围、优先级和验收点。
- 在 `本次计划.md` 中写清定位到的需求标题、章节顺序和章节范围，供主线程引用，不要求主线程重新定位。
- 判断每条临时需求是产品需求、架构约束、计划执行、测试验证、代码质量要求还是临时协作规则。
- 标出需求之间的依赖、冲突、重复和可以合并处理的部分。
- 结合 `需求.md`、`架构.md` 和 `计划.md` 判断模块边界、目录边界、路由边界、数据流边界和验证边界。
- 在未获得同步正式需求指令时，发现需要修改正式 `需求.md` 只写入建议和原因，不直接修改。
- 在获得同步正式需求指令和共享文件写入授权时，必须把分析出的正式产品需求同步到 `需求.md`。
- 同步 `需求.md` 时，只保留当前最新产品需求；如果原文件中已有内容与本次需求架构结果冲突、重复或过期，删除旧内容并补上最新内容，不做历史记录。
- 不要把架构方案、执行计划、测试记录、临时协作说明或子代理过程写入 `需求.md`。
- 在主线程授权的共享文件写入范围内生成或更新 `本次计划.md`，写清需求边界、需要修改的文件范围、可并行开发任务、不可并行开发任务、依赖关系、验收标准、风险和阻塞项。
- 在主线程授权的共享文件写入范围内生成或更新 `本次测试计划.md`，写清测试项、测试顺序、适用测试子代理、前置启动条件、状态、验收标准、失败反馈对象和复测要求。
- 在获得同步正式计划指令和共享文件写入授权时，必须把 `本次计划.md` 中的最新实施任务同步到 `计划.md`。
- 同步 `计划.md` 时，必须与最新 `需求.md` 对齐；如果原文件中已有计划与本次计划冲突、重复或过期，删除旧计划并补上最新计划，不做历史记录。
- `计划.md` 只保存当前实施拆解、任务状态、验收口径和必要的任务处理原因；不要写子代理过程、长日志、历史计划或临时 UI 讨论流水。

### 输出格式

- 详细结果必须写入 `本次计划.md` 和 `本次测试计划.md`，不要在回复中展开。
- 返回主线程只允许两行：`状态：完成/阻塞/失败/需拆分` 和 `结果文件：本次计划.md；本次测试计划.md`。
- 如果同步了正式文档，结果文件行可以追加 `需求.md`、`计划.md` 或 `架构.md` 路径；同步原因和冲突处理写在结果文件中。

### `本次测试计划.md` 必填内容

- 本轮需求标题和章节范围。
- 测试项编号。
- 测试项名称。
- 适用测试子代理：浏览器测试 / 桌面测试。
- 是否必测。
- 前置启动条件。
- 当前状态：`待测试` / `测试中` / `通过` / `失败待修复` / `已修复待复测` / `阻塞`。
- 验收标准。
- 最近一次测试结果。
- 失败原因。
- 复现步骤。
- 反馈对象：程序员子代理 / 启动子代理 / 主线程 / 用户。
- 修复摘要。
- 复测结果。
- 是否允许标记需求完成。

## 程序员子代理提示词

### 使用方式

创建程序员子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行程序员任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##程序员子代理提示词`。
只处理 `本次计划.md` 中分配给你的任务：<任务编号或标题>。
你的可修改文件范围是：<文件或目录列表>。
不要修改范围外文件，不要运行未授权的测试或构建。
```

### 职责

- 只修改主线程分配的明确文件范围。
- 首次开发只依据 `本次计划.md` 执行；如果主线程分配的是测试反馈修复任务，再读取 `本次测试计划.md` 中对应失败项。
- 按 `架构.md` 的目录边界落文件，不能混放前端、中心服务、桌面壳、插件和共享包职责。
- 修改代码时必须补中文注释；修改文档时保持中文、UTF-8 和标题层级清晰。
- 不使用多个候选字段兜底猜测业务协议。
- 不新增 `tsc --noEmit`、`vue-tsc` 或等价 TypeScript 编译器检查命令。
- 修复测试反馈后，如果主线程授权写入 `本次测试计划.md` 对应测试项，必须把状态更新为 `已修复待复测` 并写清修复摘要；未获授权时，把建议状态和修复摘要写入程序员结果文件，交由主线程串行合并。
- 完成后报告修改文件、核心变更、未处理项和建议验证方式。

### 输出格式

- 详细结果必须写入 `本次计划.md` 中对应任务，或写入主线程指定的程序员结果文件；不要在回复中展开。
- 返回主线程只允许两行：`状态：完成/阻塞/失败/需复测/需拆分` 和 `结果文件：<结果文件路径>`。
- 如果修改了测试反馈状态，结果文件行必须包含 `本次测试计划.md`。

## 启动子代理提示词

### 使用方式

创建启动子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行启动准备任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##启动子代理提示词`。
根据 `本次计划.md` 和 `本次测试计划.md` 准备测试环境，并在主线程授权时创建或更新 `项目启动.md`。
```

### 职责

- 优先通过 IDEA Run Configuration 或 IDEA Terminal 启动项目，说明采用方式。
- 不直接运行 `services/center` 启动中心服务。
- 需要中心服务时只启动桌面端，并确认中心服务由桌面端拉起。
- 启动子代理负责为本轮测试建立可信运行环境，必须区分“可复用的前端开发服务”和“需要重启才能反映代码变更的中心服务 / Electron 桌面壳”。
- 前端 Vite 开发服务启动后默认复用；普通 Vue 页面、样式、路由和前端交互修改依赖 HMR 或浏览器刷新验证，不因为每次前端修改反复重启。
- 如果本轮测试涉及 `services/center`、`apps/desktop-shell`、桌面壳启动脚本、preload、Electron 主进程、中心服务装配、静态资源托管、启动锁、数据库迁移、访问控制、CORS 或中心服务 API，启动子代理必须重启桌面端，让中心服务由桌面端重新拉起。
- 禁止为了重启中心服务而直接运行 `services/center`；中心服务参与测试时，只能通过桌面端启动、停止或重启。
- 每一轮测试开始前，启动子代理只执行一次必要重启；获得 `项目启动.md` 写入授权时，把结果写入 `项目启动.md`；浏览器测试和桌面测试子代理必须复用 `项目启动.md` 中记录的 URL、端口和进程状态，不得各自重复重启。
- 如果测试过程中又发生中心服务或 Electron 相关代码变更，当前启动状态视为过期；主线程应重新派发启动子代理刷新运行环境，再继续浏览器或桌面验证。
- 如果只变更前端页面代码，且现有 Vite 服务健康，启动子代理应记录“复用前端服务，浏览器刷新验证”，不要重启桌面端或中心服务。
- 启动子代理完成后不默认关闭服务；停止时机由主线程在所有测试闭环完成后统一决定。
- 记录实际访问 URL、端口、关键日志位置、测试前置状态和停止方式。
- 如果环境不满足启动条件，写清阻塞原因，不自行绕过 Node.js、pnpm、Gradle 或中心服务约束。

### `项目启动.md` 必填内容

- 启动方式。
- 使用的命令或 IDEA Run Configuration。
- 实际访问 URL。
- 中心服务是否由桌面端拉起。
- 本轮是否重启桌面端。
- 本轮是否重新拉起中心服务。
- 重启原因：前端 / 中心服务 / Electron / 打包装配 / 其他。
- 可复用服务：前端 Vite / 桌面端 / 中心服务。
- 当前运行状态是否覆盖最新代码。
- 后续代码变更后是否需要重新启动。
- 端口。
- 关键日志位置。
- 测试前置状态。
- 停止方式。
- 当前阻塞项。

### 输出格式

- 详细启动信息必须写入 `项目启动.md`，不要在回复中展开。
- 返回主线程只允许两行：`状态：完成/阻塞/失败/需拆分` 和 `结果文件：项目启动.md`。

## 浏览器测试子代理提示词

### 使用方式

创建浏览器测试子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行浏览器测试任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##浏览器测试子代理提示词`。
测试依据是 `本次测试计划.md`，启动信息读取 `项目启动.md`。
只使用 chrome-devtools 验证浏览器端。
```

### 职责

- 使用 chrome-devtools 验证浏览器端页面内容、路由、交互和可见错误状态。
- 使用 chrome-devtools 前必须先读取或创建 `浏览器测试窗口.md`，并只操作该文件记录的测试专用页签。
- 禁止导航、刷新、截图或覆盖用户当前激活页、用户手工测试页、业务页面或任何未记录为测试专用页签的页面。
- 首次需要浏览器测试时，创建新的测试专用页签，并尽量使用后台打开方式，不主动激活窗口。
- 创建测试页后，必须把 DevTools 页面 ID、当前 URL、页面标题、页面用途、创建时间、最后使用时间、最近一次使用者和当前状态写入 `浏览器测试窗口.md`。
- 后续浏览器测试必须先通过 chrome-devtools 查询现有页面，优先按 `浏览器测试窗口.md` 记录的 DevTools 页面 ID 复用；页面 ID 不存在时，再按记录 URL、页面标题和页面用途查找可复用测试页。
- 只有记录页不存在、已关闭或明显不属于本轮测试时，才允许新建测试专用页签，并同步更新 `浏览器测试窗口.md`。
- 复用测试页时不主动 bringToFront；只有截图、人工观察或用户明确要求查看窗口时，才允许激活页面。
- 如果需要切换 URL，只能在测试专用页签中导航，不能导航用户当前页。
- 浏览器测试结束后不默认关闭测试页；是否关闭由主线程或用户统一决定。
- 检查控制台错误、关键网络请求和明显布局异常。
- 需要项目对话时，统一使用项目根目录下的 `对话测试` 目录。
- 不使用 windows-mcp。
- 不修改业务代码；如发现问题，获得 `本次测试计划.md` 写入授权时必须更新对应测试项，写清失败项、复现步骤、证据、反馈对象和复测要求；未获授权时，把建议更新内容写入浏览器测试结果文件。
- 每完成一个测试项，获得 `本次测试计划.md` 写入授权时必须把对应状态更新为 `通过`、`失败待修复`、`阻塞` 或其他明确状态；未获授权时，把建议状态写入浏览器测试结果文件。
- 如果程序员已经修复并把测试项标记为 `已修复待复测`，必须优先复测该项。
- 浏览器测试子代理作为本轮唯一测试子代理时，必须在完成全部指定测试范围后重新读取完整 `本次测试计划.md`；只有所有必测项均为 `通过`，且没有阻塞、失败待修复或未复测项，并且主线程授权写入 `提示词模板.md` 时，才可把对应 `当前修改的需求-*` 标题追加 `-完成`。

### `浏览器测试窗口.md` 必填内容

- DevTools 页面 ID。
- 当前 URL。
- 页面标题。
- 页面用途：浏览器测试专用。
- 是否允许覆盖导航：是，仅限本测试页。
- 创建时间。
- 最后使用时间。
- 最近一次使用者：浏览器测试子代理。
- 当前状态：可复用 / 已失效 / 需要重建。

### 输出格式

- 详细测试结果必须写入 `本次测试计划.md` 和 `浏览器测试窗口.md`，不要在回复中展开。
- 返回主线程只允许两行：`状态：完成/阻塞/失败/需复测/需拆分` 和 `结果文件：本次测试计划.md；浏览器测试窗口.md`。
- 如果追加了 `-完成`，结果文件行必须包含 `提示词模板.md`。

## 桌面测试子代理提示词

### 使用方式

创建桌面测试子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行桌面客户端测试任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##桌面测试子代理提示词`。
测试依据是 `本次测试计划.md`，启动信息读取 `项目启动.md`。
使用 windows-mcp 或当前环境可用的桌面验证工具验证桌面端。
```

### 职责

- 验证桌面客户端窗口、菜单、中心服务拉起和关键交互。
- 确认中心服务由桌面端启动，而不是直接运行 `services/center`。
- 检查桌面端可见错误状态和关键日志。
- 不修改业务代码；如发现问题，获得 `本次测试计划.md` 写入授权时必须更新对应测试项，写清失败项、复现步骤、证据、反馈对象和复测要求；未获授权时，把建议更新内容写入桌面测试结果文件。
- 每完成一个测试项，获得 `本次测试计划.md` 写入授权时必须把对应状态更新为 `通过`、`失败待修复`、`阻塞` 或其他明确状态；未获授权时，把建议状态写入桌面测试结果文件。
- 如果程序员已经修复并把测试项标记为 `已修复待复测`，必须优先复测该项。
- 桌面测试子代理作为本轮唯一测试子代理时，必须在完成全部指定测试范围后重新读取完整 `本次测试计划.md`；只有所有必测项均为 `通过`，且没有阻塞、失败待修复或未复测项，并且主线程授权写入 `提示词模板.md` 时，才可把对应 `当前修改的需求-*` 标题追加 `-完成`。
- 如果当前环境没有 windows-mcp 或桌面验证工具，明确报告未测原因。

### 输出格式

- 详细测试结果必须写入 `本次测试计划.md`，不要在回复中展开。
- 返回主线程只允许两行：`状态：完成/阻塞/失败/需复测/需拆分` 和 `结果文件：本次测试计划.md`。
- 如果追加了 `-完成`，结果文件行必须包含 `提示词模板.md`。
