# idea里面使用
/using-superpowers:brainstorming 所有任务完成后再删除本段提示词里面引用的图片。启动服务前必须关闭老项目，必须使用idea启动run_configuration：`dev:desktop-shell`。用端口检测是否启动完成。
- 使用 chrome-devtools 测试
- 本次需求：

# 新需求

- 记忆召回缺失
- 输入框富文本化

# 明天优先处理：Agent 任务完成态与自动续跑

请完成“任务完成态与续跑”核心能力，不要把 Deep Agents 单次 graph 结束直接当成致心任务完成。

核心判断：
- Deep Agents 自己有底层停止标准：模型返回结构化 `tool_calls` 就继续工具循环；模型没有结构化 `tool_calls`，本次 graph 可以结束；超过框架预算也会停。
- 这个底层停止标准不是致心的业务任务完成标准。中心服务必须接管任务完成判断，Deep Agents 只负责一次次模型-工具循环。
- 中心服务要把 Deep Agents 单次运行结果先当作 `AgentRunCandidate` 候选终态，再由监督层判断 `completed`、`continue`、`retry`、`waiting_user` 或 `failed`。

必须实现或补强：
- 只有确认用户目标已经满足时，才创建最终助手消息、写长期记忆、完成任务和轮次。
- 模型只返回“我正在查看 / 我将继续 / 我来打开页面看看”等过程文本，但没有结构化工具调用时，不能固化成最终回复，应按预算续跑或重试。
- 模型把 `name`、`args`、工具名或伪工具调用字段夹在普通文本里时，只记录协议诊断并预算内重试，不能从文本恢复工具调用。
- 工具持续失败、模型多次无进展、续跑预算耗尽时，才进入 `waiting_user` 或结构化失败终态。
- 自动续跑必须仍在同一个 `turnId` / `taskId` 下推进，不能新建互不关联的任务。
- 所有过程文本、工具请求、工具执行、工具结果回填都要通过 `event.appended` 实时渲染，但过程文本不能等同最终回复。

重点检查文件：
- `services/center/src/deepagents-agent.ts`
- `services/center/src/agent-runtime/AgentCompletionGate.ts`
- `services/center/src/agent-runtime/DeepAgentTurnSupervisor.ts`
- `services/center/src/AgentMiddleware/CenterToolChoiceMiddleware.ts`
- `apps/frontend/src/stores/app-conversation-actions.ts`
- `apps/frontend/src/views/Chat/chat-view-helpers.ts`
- `需求.md`
- `设计.md`
- `架构.md`
- `功能清单与关系.md`

验收重点：
- 用“致心，用可控浏览器打开 github，看看今天 AI 圈出了什么好用的工具或者思想”这类长任务测试，不能只输出“正在读取 Trending”就结束。
- 至少用三种不同提示词验证同一类能力，不能反复使用同一句提示词。
- 页面应实时显示过程卡片和模型中途文本，最终只在任务真正完成时出现最终助手回复。
- 浏览器用 Chrome DevTools 真实点击、输入、观察；桌面端用 windows-mcp 验证。
- 必须用 IDEA Run Configuration `dev:desktop-shell` 启动，不允许独立启动中心服务。

# 明天优先处理：停止取消与多端终态收敛

请完成“停止取消与多端运行态收敛”能力，目标是浏览器端、桌面端和 IDE 插件端对同一轮次的完成、失败、取消、等待用户终态一致。

核心判断：
- 用户点击停止是业务取消终态 `cancelled`，不是普通失败 `failed`。
- LangGraph / Deep Agents 取消后可能把 `AbortSignal.reason` 包装成普通 `Error` 冒泡，这类异常如果发生在 `runtimeSignal.aborted === true` 后，应作为取消残留吞掉，不能写 `message.turn.failed`。
- 前端不能靠本地按钮状态猜测是否运行中，必须以后端 `turn.updated`、`turn.state.changed`、`task.updated` 和 `session.turn.state` 为事实源。

必须实现或补强：
- `session.turn.cancel` 成功后，中心服务写入 `cancelled` 终态并广播统一终态事件。
- 已取消轮次不能被迟到的工具结果、迟到的 completed、迟到的助手消息覆盖。
- 发送端点击停止后，按钮恢复“发送”，“当前轮次已耗时”消失，过程卡片展示已取消。
- 其他端没有发起停止、甚至没有 tracking 当前 turn，也要在收到终态事件后强制清理运行态并刷新当前会话快照。
- `TurnStateReconciler` 既要支持 tracking 中的终态收敛，也要支持被动端收到终态事件后的强制收敛。
- 控制台不应再把用户停止导致的 `Error: 用户点击停止当前执行。` 当成任务失败噪声。

重点检查文件：
- `services/center/src/api/sync-route.ts`
- `services/center/src/domain/session-domain.ts`
- `services/center/src/domain/turn-runtime-cancel-registry.ts`
- `services/center/src/deepagents-agent.ts`
- `apps/frontend/src/stores/TurnStateReconciler.ts`
- `apps/frontend/src/stores/app-conversation-actions.ts`
- `功能清单与关系.md`

验收重点：
- 浏览器端发起任务后点击停止，浏览器端必须立即收敛。
- 桌面端同时打开同一会话，收到取消终态后不能残留“停止”按钮或“当前轮次已耗时”。
- 取消后不能追加该轮次迟到工具结果为运行态，也不能生成最终助手消息。
- 控制台不应出现把用户停止当失败收尾的异常日志。
- 浏览器用 Chrome DevTools 真实操作，桌面端用 windows-mcp 观察。
- 必须用 IDEA Run Configuration `dev:desktop-shell` 启动，不允许独立启动中心服务。

# 其他

- 浏览器使用 chrome-devtools 测试。
- 桌面端用 windows-mcp 测试。

- 任务完成删除提示词里面引用的图片
- 浏览器使用 chrome-devtools 测试，测试必须按照点击，输入，视觉查看进行，尤其要对比对话轮次时间和最后回复时间，这个对不上就肯定有bug，需要修复。


