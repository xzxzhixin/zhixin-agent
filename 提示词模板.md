# 使用
/using-superpowers 不使用视觉伴侣，执行方式使用子代理驱动。
- 任务完成再做git处理，gitignore 忽略的不要强制加入。发现有远端领先先拉取。工作区里面所有的改动全部自动推送。不校验`提示词模板.md`直接提交
- 不测试，但要启动服务
- 使用用户环境里面的node启动
- 执行`dev:desktop-shell`前必须先关闭之前的进程，保证最新代码生效
- 检索代码使用MCP服务idea
- 本次需求：
- 删除代码里面无用的导入和url，如果删除文件为空了，文件一并删除
- [agents](services/center/src/agents)这个里面的agent没有接入系统代码里面，目前系统里面的智能体代码都比较分散全部按功能迁移进来，方便复用；
- 每个agent都可以调用外面的MCP工具，命令工具，内部工具，比如关键的工具权限是这样的，子智能体不允许继续创建其他智能体，长期智能体不能使用团队管理的工具
- 需求md文件不要分各个块，就是一个清单列表从头到尾，需要一个设计md

# 其他

- 浏览器使用 chrome-devtools 测试。
- 桌面端用 windows-mcp 测试。

- 任务完成删除提示词里面引用的图片，并清空`本次计划.md`文件的内容，注意不是删除
- 浏览器使用 chrome-devtools 测试，测试必须按照点击，输入，视觉查看进行，尤其要对比对话轮次时间和最后回复时间，这个对不上就肯定有bug，需要修复。

