# 使用
- 先读取项目根目录下面的`提示词模板.md`严格执行然后完成下面的任务
- 先读`公共`
- 测试只做`浏览器测试`
- 任务完成以后走`任务完成执行`
- ![img.png](img.png)窗口没有记忆
- 读取当前会话、任务状态、可用供应商和扩展能力后组织回复。每次思考都是这个不是模型真实的思考；

# 公共

- 必用`using-superpowers`和我对齐需求所有细节再落实`TDD`执行。
- 不使用视觉伴侣。
- 第一次回答里面必须明确使用了`using-superpowers`技能让我明确知道
- 任务再大也必须在本轮持续推进到完成。
- 任务明显过大或上下文复杂时，先创建或更新 `本次计划.md`。
- `本次计划.md` 用清单固化目标、用户最新指令、硬性约束、任务拆分、子代理分工、验收方式、当前状态和校准记录；普通说明使用 `-`，任务项使用 `- [ ]`，完成后改为 `- [x]`。
- 主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复。
- 能隔离的执行工作全部交给子代理。
- 存在强顺序依赖或共享文件风险的工作，按阶段分批派发子代理处理。
- 每完成一个阶段，主线程必须回看 `本次计划.md`、最新用户要求和当前文件状态，确认没有偏移后继续。
- 上下文压缩、恢复或长时间执行后，必须先读取 `本次计划.md` 和最新状态继续执行，不重新理解成新任务。
- 计划允许动态校准，但必须在 `本次计划.md` 写明调整原因。

# 浏览器测试

- 重启保证是最新代码生效。
- 重启前查看是否有运行中的代码，有先关闭。
- 浏览器使用 chrome-devtools 测试。

# 浏览器和桌面测试

- 走`浏览器测试`
- 桌面端用 windows-mcp 测试。

# git规则

- gitignore 忽略的不要强制加入。
- 发现有远端领先先拉取。
- 工作区里面所有的改动全部自动推送。
- 不校验本文件是否被修改，直接提交

# 任务完成执行

- 走`git规则`
- 删除提示词里面引用的图片。
