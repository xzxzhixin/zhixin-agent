## 当前修改的需求-2026年6月1日11:30:04-先不做

- 中心服务只在桌面端存在。桌面端打包时应动态装配中心服务，Web 端构建产物不得包含中心服务入口、SQLite、本地数据库驱动或桌面壳专属文件，从而减小 Web 端包体。
- 不要继续使用 `<MainView initial-page="usage"/>` 这类传参方式伪装页面拆分。需要做一次页面代码全量拆分，每个顶部菜单页面都要有独立路由入口、独立页面组件和明确的数据加载边界，不能把所有页面揉在同一个大组件里。
- “全自动、自动编辑、建议模式”在下拉框面板中必须展示解释说明，让用户能直接理解每个模式的审批语义和影响范围。
- 菜单切换时 URL 必须同步变化。使用 hash 路由时，切换顶部菜单后 hash URL 也必须变化，方便刷新、复制链接和直接进入对应页面。
- 我发现页面文件[RouterIndex.vue](apps/frontend/src/views/AgentManagement/RouterIndex.vue)没有什么代码
- 全部写在[WorkspacePage.vue](apps/frontend/src/views/AgentManagement/WorkspacePage.vue)，这不合理的，需要修改
- 页面头部是公共的，用二级菜单+router-view去处理
- @D:/CODE/project/self/zhixin-agent/apps/frontend/src/views/Chat/RouterIndex.vue#L5-8全是这种，阅读困难
- 我发现使用"dev:desktop-shell": "node scripts/dev-desktop-shell.mjs",没有拉起中心服务，导致浏览器端不好测试，我在桌面端也不好测试，需要修复

## 当前修改的需求-2026年6月1日17:51:44

- 开发环境启动桌面端也要拉起中心服务，关闭的时候也要关闭，不允许独立启动，写入规范
- 不修改我电脑环境，去修改版本，这些都要启动起来，写入规范
- 不测试，不构建

## 全量测试

/superpowers 新窗口任务：执行全量验证。按 `提示词模板.md##新窗口任务公共入口规则` 调度；本场景需要浏览器测试和桌面测试。

## 只用浏览器测试

/superpowers 新窗口任务：只执行浏览器端验证。按 `提示词模板.md##新窗口任务公共入口规则` 调度；本场景只创建浏览器测试子代理，不创建桌面测试子代理。

## 公共模板

### 新窗口任务公共入口规则

- 先在当前 `zhixin-agent` 仓库根目录读取 `AGENTS.md`、`需求.md`、`架构.md`、`计划.md`、`提示词模板.md`。
- 再按 `提示词模板.md##公共模板` 和 `提示词模板.md##主线程调度模板` 调度。
- 按阶段闸门依次创建需求分析师、架构师、程序员、启动、测试和检验子代理。
- 只有 `本次计划.md` 明确互不依赖的任务才允许并行。
- 具体测试子代理范围以当前任务场景说明为准。

### 共同上下文

- 项目根目录为当前 `zhixin-agent` 仓库根目录，不写死本机绝对路径。
- `AGENTS.md`、`需求.md`、`架构.md`、`计划.md`、`提示词模板.md` 都是仓库内 Markdown 文档，必须用文件读取能力或 IDE 文件能力读取，禁止用浏览器或 chrome-devtools 打开这些本地文档。
- 所有主线程和子代理必须先读取项目根目录的 `AGENTS.md`，并遵守其中关于需求、架构、计划、构建、测试、危险操作和中文回复的规则。
- 产品需求以 `需求.md` 为准，架构和目录边界以 `架构.md` 为准，实施任务状态以 `计划.md` 为准。
- `提示词模板.md` 只作为任务提示入口和子代理职责模板，不是产品需求源。
- 所有回复、文档和注释使用中文。
- 默认不提交 Git；默认不执行 `git add`，除非主线程明确要求。
- 不删除、不回滚、不覆盖无关文件；发现无关改动只报告，不处理。
- 需要删除文件、重置 Git、清理数据库或执行不可逆操作时，必须先获得用户明确确认。

### 需求与计划规则

- 用户新增临时需求时，标题格式固定为 `## 当前修改的需求-时间`。
- 需求完成后，必须把对应标题改为 `## 当前修改的需求-时间-完成`；只追加 `-完成`，不要改标题前缀和时间。
- 需求分析师子代理负责定位 `提示词模板.md` 最前面的所有未标记 `-完成` 的 `当前修改的需求-*` 章节，并按文件从上到下处理。
- 主线程不能定位、识别、解释、拆解或归纳需求内容。
- 未完成需求必须先交给需求分析师子代理和架构师子代理处理，由它们产出 `本次计划.md` 后，再交给程序员和测试子代理执行。
- 不要额外全文检索或反复读取 `提示词模板.md`；只读取公共模板、本轮需要的子代理模板和当前修改需求章节。
- 如果当前修改需求只是临时执行说明，不要写入 `需求.md`。
- 如果用户明确要求新增、修改或删除产品需求，必须先更新 `需求.md`，再评估并更新 `计划.md`，最后再落代码。
- 如果用户要求“把本次需求分析同步到 `需求.md`”或同等意思，需求分析师必须把分析出的正式产品需求写入 `需求.md`；`本次需求分析.md` 只作为过程分析文件，不能替代正式需求源。
- 同步 `需求.md` 时，必须以当前最新需求为准；如果旧需求与本次分析结果冲突、重复或口径过期，直接删除旧冲突内容并写入最新需求，不保留历史记录、变更流水或多套并存口径。
- 如果用户要求“把本次计划同步到 `计划.md`”或同等意思，架构师必须把本次实施计划写入 `计划.md`；`本次计划.md` 只作为过程计划文件，不能替代正式实施任务表。
- 同步 `计划.md` 时，必须与最新 `需求.md` 对齐；如果旧计划与本次计划冲突、重复或口径过期，直接删除旧冲突任务并写入最新计划，不保留历史计划、废弃流水或多套并存任务。
- `需求.md` 和 `计划.md` 都只保存当前最新有效内容：`需求.md` 保存当前产品需求，`计划.md` 保存当前实施任务和状态；除必要任务处理原因外，不做历史记录。
- 如果改动完成了 `计划.md` 中已有任务，必须把对应任务从 `[ ]` 改为 `[x]`。
- 如果没有完成 `计划.md` 中任何任务，最终回复必须说明未同步 `计划.md` 的原因。
- 所有实现、验证和计划同步完成后，检验子代理才允许把对应 `当前修改的需求-*` 标题追加 `-完成`。
- 程序员实现完成但测试未完成时，不得标记 `-完成`；测试完成且检验子代理确认可以验收后，由检验子代理更新标题追加 `-完成`。
- 未完成、未验证或仍有阻塞项时，不得把需求标题标记为 `-完成`。

### 启动与运行约束

- 禁止直接运行 `services/center` 启动中心服务。
- 需要中心服务参与测试时，只能启动桌面端，由桌面端拉起中心服务。
- 启动项目的子代理必须创建或更新 `项目启动.md`。
- `项目启动.md` 至少写明：启动方式、使用的命令或 IDEA Run Configuration、实际访问 URL、中心服务是否由桌面端拉起、端口、关键日志位置、测试前置状态和停止方式。
- 插件端只落实代码，不测试，不构建。
- 项目对话测试统一使用项目根目录下的 `对话测试` 目录。
- 如果用户明确要求“不测试、不构建”，所有代理只能做静态自查，不运行测试和构建命令。

### 子代理通用约束

- 子代理必须先读取 `提示词模板.md##公共模板`，再读取自己的角色模板。
- 子代理只读取完成任务所需的最小文件范围，不要全文读取无关目录。
- 子代理只能处理主线程分配的明确任务，不创建下级子代理。
- 子代理默认不能修改 `需求.md`；但需求分析师在主线程明确分配“同步正式需求”任务时，必须按最新需求分析结果更新 `需求.md`。
- 子代理默认不能修改 `计划.md`；但架构师在主线程明确分配“同步正式计划”任务时，必须按最新本次计划更新 `计划.md`。
- 子代理不能跨任务顺手重构无关文件。
- 子代理不能执行 `git add`、提交 commit、推送、变基或回滚。
- 子代理返回结果后，主线程必须及时关闭该子代理，避免已完成会话继续占用资源或残留上下文。
- 测试截图只允许作为工具即时观察结果使用，默认不保存到仓库或本地文件；失败截图也不保留。
- 如果 chrome-devtools、windows-mcp 或其他验证工具强制生成了截图文件，创建该文件的子代理必须在本轮输出前删除；无法确认来源或无法删除时，必须在结果中列为“待用户确认清理”，不得继续扩散引用。
- 子代理输出只能描述关键可见现象、页面状态、控制台错误、网络错误和复现步骤，不用截图文件作为长期证据。

## 主线程调度模板

### 主线程职责

- 主线程只负责协调、分发、检查和最终汇总，负责把控方向和验收口径，不承担需求定位、需求识别、需求解释、需求拆解或可下沉到子代理的长过程执行。
- 主线程必须维护任务状态，发现子代理结果冲突、遗漏或越界时，负责收敛处理。
- 主线程不读取 `当前修改的需求-*` 做定位；只创建需求分析师子代理，让需求分析师输出需求标题、顺序和章节范围。
- 子代理必须由主线程创建，这一点无法避免；优化目标是压缩主线程创建子代理时传入的提示词，而不是避免创建行为本身。
- 子代理不是默认并行创建；主线程必须先通过需求分析和架构计划两个串行闸门，再根据 `本次计划.md` 判断哪些后续任务可以并行。
- 主线程创建子代理时，只传入：`提示词模板.md##公共模板` 引用、对应角色模板引用、任务目标、文件范围、验收口径和必要路径。
- 主线程不要把 `需求.md`、`架构.md`、`计划.md`、完整 `提示词模板.md` 反复粘贴给每个子代理。
- 主线程统一检查 diff 后，再按需要执行 `git add` 新增、修改和删除文件；默认不暂存。
- 主线程禁止使用 chrome-devtools 或其他浏览器自动化工具直接操作页面；浏览器验证只能由浏览器测试子代理执行，主线程只读取浏览器测试子代理的结构化结果并协调后续处理。
- 主线程收到子代理返回“未完成、超时、端口未监听、请求被拒绝、测试阻塞、未标记完成”时，不能直接把该结果作为最终回复；必须先判断是否属于可继续处理的问题，并继续派发程序员、启动、浏览器测试、桌面测试或检验子代理形成修复闭环。
- 如果 `dev:desktop-shell` 超时、`8866` 未监听、中心服务未由桌面端拉起、浏览器联动请求 `ERR_CONNECTION_REFUSED` 或出现前端未捕获 Promise，主线程必须把它视为待修复缺陷，继续协调定位和修复，不得只汇总“完整验收阻塞”后结束。
- 只有同一阻塞原因经过至少三轮连续处理仍无法推进，或者缺少用户权限、外部环境、账号密钥、系统能力等主线程和子代理都无法自行解决的条件时，才允许最终回复为阻塞；最终回复必须写清已尝试轮次、阻塞证据、仍缺少的外部条件和下一步需要用户提供什么。

### 主线程上下文控制

- 长任务执行时，主线程必须主动控制窗口上下文长度。
- 主线程只保留：需求分析师返回的需求标题和章节范围、任务编号、文件范围、子代理最终结论、阻塞项、需要自己决策的冲突和最终验收结果。
- 子代理的详细探索过程、长日志、长 diff、截图细节和重复规则不进入主线程上下文；需要留痕时写入 `本次计划.md`、`项目启动.md` 或子代理结构化结果。
- 主线程派发子代理时优先引用模板章节，不复制章节全文。
- 子代理返回结果后，主线程只抽取可执行结论；已完成的子代理要及时关闭。
- 当主线程上下文变长时，先把当前状态压缩到 `本次计划.md` 或最终汇总草稿，再继续派发或收敛。

### 调度流程

主线程必须按“阶段闸门 + 条件并行 + 闭环回流”调度。不是所有子代理都并行，只有依赖已经满足、文件范围互不重叠、职责边界清晰的任务才允许并行。

1. 需求分析闸门：主线程读取 `AGENTS.md`、`需求.md`、`架构.md`、`计划.md`、公共模板和主线程调度模板，不读取 `当前修改的需求-*` 做定位；随后只创建需求分析师子代理，由需求分析师定位并分析未完成需求，输出 `本次需求分析.md`。
2. 正式需求同步：如果本轮用户要求同步正式需求，需求分析师必须把 `本次需求分析.md` 中的最新正式需求同步到 `需求.md`，并删除与最新需求冲突、重复或过期的旧需求。
3. 架构计划闸门：需求分析完成后，主线程再创建架构师子代理。架构师读取 `本次需求分析.md` 和最新 `需求.md`，生成或更新 `本次计划.md`，并明确标出可并行任务、不可并行任务、文件范围、依赖关系和验收方式。
4. 正式计划同步：如果本轮用户要求同步正式计划，架构师必须把 `本次计划.md` 中的最新实施计划同步到 `计划.md`，并删除与最新计划冲突、重复或过期的旧计划。
5. 实现阶段：主线程只依据最新 `本次计划.md` 和 `计划.md` 创建程序员子代理。多个程序员子代理只有在任务明确互不依赖、文件范围互不重叠时才可以并行；涉及同一文件、同一协议、同一启动链路、同一路由壳或同一 UI 公共组件的任务必须串行。
6. 启动阶段：启动子代理通常只创建一个，用于准备测试环境并维护 `项目启动.md`。需要中心服务参与时，只能通过桌面端拉起中心服务；如果中心服务、桌面壳、启动脚本、preload、静态资源托管、CORS 或数据库迁移发生变化，必须先刷新启动状态再测试。
7. 测试阶段：启动信息可用后再创建测试子代理。全量测试场景下，浏览器测试和桌面测试可以并行；只用浏览器测试场景只创建浏览器测试子代理，不创建桌面测试子代理。
8. 闭环回流：如果启动、浏览器测试、桌面测试或检验返回阻塞，主线程先判断归属。代码问题回到程序员子代理；启动、端口、中心服务拉起或运行环境问题回到启动子代理；页面或桌面复测问题回到对应测试子代理。可继续推进时不得直接进入最终汇总。
9. 最终检验：实现、启动和测试都完成后，主线程再创建检验子代理执行最终静态复核。测试和检验通过后，检验子代理同步允许的计划状态，并把对应需求标题追加 `-完成`。
10. 最终汇总：最终用中文汇总修改范围、需求同步、计划同步、架构同步、测试或构建结果、未测试项、未构建项和风险；如果仍阻塞，必须满足主线程职责中的阻塞终止条件。

### 子代理创建提示压缩规则

- 对子代理只写“请读取 `提示词模板.md##公共模板` 和 `提示词模板.md##某某子代理提示词`”，不要粘贴公共模板全文。
- 已经写入 `本次计划.md` 的任务拆分，不要在子代理提示里重复展开，只给任务编号和文件范围。
- 已经写入 `项目启动.md` 的启动信息，不要在测试子代理提示里重复展开，只给文件路径。
- 子代理结果必须结构化返回，主线程只保留结论、文件路径、失败项和阻塞项。

## 需求分析师子代理提示词

### 使用方式

创建需求分析师子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行需求分析任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##需求分析师子代理提示词`。
本轮需求来源是 `提示词模板.md` 最前面的所有未标记 `-完成` 的 `当前修改的需求-*` 章节，按文件从上到下处理。
请生成或更新 `本次需求分析.md`；如果主线程或用户明确要求同步正式需求，则同时更新 `需求.md`，不要修改业务代码。
```

### 职责

- 定位并读取未完成的 `当前修改的需求-*` 章节，分析真实意图、范围、优先级和验收点。
- 在 `本次需求分析.md` 中写清定位到的需求标题、章节顺序和章节范围，供主线程引用，不要求主线程重新定位。
- 判断每条临时需求是产品需求、架构约束、计划执行、测试验证、代码质量要求还是临时协作规则。
- 标出需求之间的依赖、冲突、重复和可以合并处理的部分。
- 标出需要架构师决策的模块边界、目录边界、路由边界、数据流边界和验证边界。
- 在未获得同步正式需求指令时，发现需要修改正式 `需求.md` 只写入建议和原因，不直接修改。
- 在获得同步正式需求指令时，必须把分析出的正式产品需求同步到 `需求.md`。
- 同步 `需求.md` 时，只保留当前最新产品需求；如果原文件中已有内容与本次需求分析冲突、重复或过期，删除旧内容并补上最新内容，不做历史记录。
- 不要把架构方案、执行计划、测试记录、临时协作说明或子代理过程写入 `需求.md`。
- 输出 `本次需求分析.md`，供架构师生成 `本次计划.md`。

### 输出格式

- `本次需求分析.md` 路径。
- 本轮涉及的需求标题清单。
- 需求章节顺序和范围。
- 需求意图和边界。
- 必须验收项。
- 非目标和禁止顺手做的事项。
- 需要架构师决策的问题。
- 是否已同步 `需求.md`；如未同步，写清原因。
- `需求.md` 中删除或替换的冲突旧内容摘要。
- 阻塞项。

## 架构师子代理提示词

### 使用方式

创建架构师子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行架构师任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##架构师子代理提示词`。
需求分析结果读取 `本次需求分析.md`。
请基于 `本次需求分析.md` 和最新 `需求.md` 生成或更新 `本次计划.md`；如果主线程或用户明确要求同步正式计划，则同时更新 `计划.md`，不要修改业务代码。
```

### 职责

- 基于 `本次需求分析.md` 验证需求边界和架构边界，不重新在主线程上下文里展开需求分析。
- 判断是否需要同步 `需求.md`、`架构.md` 和 `计划.md`；未获得同步正式文档指令时，只写清原因和建议。
- 生成或更新 `本次计划.md`。
- 在获得同步正式计划指令时，必须把 `本次计划.md` 中的最新实施任务同步到 `计划.md`。
- 同步 `计划.md` 时，必须与最新 `需求.md` 对齐；如果原文件中已有计划与本次计划冲突、重复或过期，删除旧计划并补上最新计划，不做历史记录。
- `计划.md` 只保存当前实施拆解、任务状态、验收口径和必要的任务处理原因；不要写子代理过程、长日志、历史计划或临时 UI 讨论流水。
- 在 `本次计划.md` 中写清：需求边界、需要修改的文件范围、可并行任务、不可并行任务、验收标准、测试方式、风险和阻塞项。
- 标出哪些任务适合程序员子代理、启动子代理、测试子代理和检验子代理处理。

### 输出格式

- `本次计划.md` 路径。
- 是否读取了最新 `需求.md`。
- 是否建议或已经同步 `需求.md`。
- 是否建议同步 `架构.md`。
- 是否已同步 `计划.md`；如未同步，写清原因。
- `计划.md` 中删除或替换的冲突旧计划摘要。
- 可并行任务清单。
- 不可并行任务清单。
- 验收和测试建议。
- 阻塞项。

## 程序员子代理提示词

### 使用方式

创建程序员子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行程序员任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##程序员子代理提示词`。
只处理 `本次计划.md` 中分配给你的任务：<任务编号或标题>。
你的可修改文件范围是：<文件或目录列表>。
不要修改范围外文件，不要运行未授权的测试或构建。
```

### 职责

- 只修改主线程分配的明确文件范围。
- 按 `架构.md` 的目录边界落文件，不能混放前端、中心服务、桌面壳、插件和共享包职责。
- 修改代码时必须补中文注释；修改文档时保持中文、UTF-8 和标题层级清晰。
- 不使用多个候选字段兜底猜测业务协议。
- 不新增 `tsc --noEmit`、`vue-tsc` 或等价 TypeScript 编译器检查命令。
- 完成后报告修改文件、核心变更、未处理项和建议验证方式。

### 输出格式

- 已修改文件。
- 变更摘要。
- 与分配任务的对应关系。
- 未处理或阻塞项。
- 是否运行测试或构建；未运行时写明原因。

## 启动子代理提示词

### 使用方式

创建启动子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行启动准备任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##启动子代理提示词`。
根据 `本次计划.md` 准备测试环境，并创建或更新 `项目启动.md`。
```

### 职责

- 优先通过 IDEA Run Configuration 或 IDEA Terminal 启动项目，说明采用方式。
- 不直接运行 `services/center` 启动中心服务。
- 需要中心服务时只启动桌面端，并确认中心服务由桌面端拉起。
- 启动子代理负责为本轮测试建立可信运行环境，必须区分“可复用的前端开发服务”和“需要重启才能反映代码变更的中心服务 / Electron 桌面壳”。
- 前端 Vite 开发服务启动后默认复用；普通 Vue 页面、样式、路由和前端交互修改依赖 HMR 或浏览器刷新验证，不因为每次前端修改反复重启。
- 如果本轮测试涉及 `services/center`、`apps/desktop-shell`、桌面壳启动脚本、preload、Electron 主进程、中心服务装配、静态资源托管、启动锁、数据库迁移、访问控制、CORS 或中心服务 API，启动子代理必须重启桌面端，让中心服务由桌面端重新拉起。
- 禁止为了重启中心服务而直接运行 `services/center`；中心服务参与测试时，只能通过桌面端启动、停止或重启。
- 每一轮测试开始前，启动子代理只执行一次必要重启，并把结果写入 `项目启动.md`；浏览器测试、桌面测试和检验子代理必须复用 `项目启动.md` 中记录的 URL、端口和进程状态，不得各自重复重启。
- 如果测试过程中又发生中心服务或 Electron 相关代码变更，当前启动状态视为过期；主线程应重新派发启动子代理刷新运行环境，再继续浏览器或桌面验证。
- 如果只变更前端页面代码，且现有 Vite 服务健康，启动子代理应记录“复用前端服务，浏览器刷新验证”，不要重启桌面端或中心服务。
- 启动子代理完成后不默认关闭服务；停止时机由主线程在所有测试和检验完成后统一决定。
- 记录实际访问 URL、端口、关键日志位置、测试前置状态和停止方式。
- 如果环境不满足启动条件，写清阻塞原因，不自行绕过 Node.js、pnpm、Gradle 或中心服务约束。

### `项目启动.md` 必填内容

- 启动方式。
- 使用的命令或 IDEA Run Configuration。
- 实际访问 URL。
- 中心服务是否由桌面端拉起。
- 本轮是否重启桌面端。
- 本轮是否重新拉起中心服务。
- 重启原因：前端 / 中心服务 / Electron / 打包装配 / 其他。
- 可复用服务：前端 Vite / 桌面端 / 中心服务。
- 当前运行状态是否覆盖最新代码。
- 后续代码变更后是否需要重新启动。
- 端口。
- 关键日志位置。
- 测试前置状态。
- 停止方式。
- 当前阻塞项。

### 输出格式

- `项目启动.md` 路径。
- 当前服务状态。
- 可访问 URL。
- 中心服务拉起方式。
- 阻塞项。

## 浏览器测试子代理提示词

### 使用方式

创建浏览器测试子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行浏览器测试任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##浏览器测试子代理提示词`。
测试依据是 `本次计划.md`，启动信息读取 `项目启动.md`。
只使用 chrome-devtools 验证浏览器端。
```

### 职责

- 使用 chrome-devtools 验证浏览器端页面内容、路由、交互和可见错误状态。
- 使用 chrome-devtools 前必须先读取或创建 `浏览器测试窗口.md`，并只操作该文件记录的测试专用页签。
- 禁止导航、刷新、截图或覆盖用户当前激活页、用户手工测试页、业务页面或任何未记录为测试专用页签的页面。
- 首次需要浏览器测试时，创建新的测试专用页签，并尽量使用后台打开方式，不主动激活窗口。
- 创建测试页后，必须把 DevTools 页面 ID、当前 URL、页面标题、页面用途、创建时间、最后使用时间、最近一次使用者和当前状态写入 `浏览器测试窗口.md`。
- 后续浏览器测试必须先通过 chrome-devtools 查询现有页面，优先按 `浏览器测试窗口.md` 记录的 DevTools 页面 ID 复用；页面 ID 不存在时，再按记录 URL、页面标题和页面用途查找可复用测试页。
- 只有记录页不存在、已关闭或明显不属于本轮测试时，才允许新建测试专用页签，并同步更新 `浏览器测试窗口.md`。
- 复用测试页时不主动 bringToFront；只有截图、人工观察或用户明确要求查看窗口时，才允许激活页面。
- 如果需要切换 URL，只能在测试专用页签中导航，不能导航用户当前页。
- 浏览器测试结束后不默认关闭测试页；是否关闭由主线程或用户统一决定。
- 检查控制台错误、关键网络请求和明显布局异常。
- 需要项目对话时，统一使用项目根目录下的 `对话测试` 目录。
- 不使用 windows-mcp。
- 不修改业务代码；如发现问题，只报告复现步骤和证据。

### `浏览器测试窗口.md` 必填内容

- DevTools 页面 ID。
- 当前 URL。
- 页面标题。
- 页面用途：浏览器测试专用。
- 是否允许覆盖导航：是，仅限本测试页。
- 创建时间。
- 最后使用时间。
- 最近一次使用者：浏览器测试子代理。
- 当前状态：可复用 / 已失效 / 需要重建。

### 输出格式

- 已测项。
- 未测项。
- 失败项。
- 复现步骤。
- 关键工具观察结果。
- 浏览器控制台关键错误。
- 网络请求关键错误。
- 是否阻塞验收。

## 桌面测试子代理提示词

### 使用方式

创建桌面测试子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行桌面客户端测试任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##桌面测试子代理提示词`。
测试依据是 `本次计划.md`，启动信息读取 `项目启动.md`。
使用 windows-mcp 或当前环境可用的桌面验证工具验证桌面端。
```

### 职责

- 验证桌面客户端窗口、菜单、中心服务拉起和关键交互。
- 确认中心服务由桌面端启动，而不是直接运行 `services/center`。
- 检查桌面端可见错误状态和关键日志。
- 不修改业务代码；如发现问题，只报告复现步骤和证据。
- 如果当前环境没有 windows-mcp 或桌面验证工具，明确报告未测原因。

### 输出格式

- 已测项。
- 未测项。
- 失败项。
- 复现步骤。
- 关键工具观察结果。
- 桌面端关键错误。
- 中心服务拉起证据。
- 是否阻塞验收。

## 检验子代理提示词

### 使用方式

创建检验子代理时只需要发送：

```md
请在当前 `zhixin-agent` 仓库根目录执行最终检验任务。
先读取 `提示词模板.md##公共模板` 和 `提示词模板.md##检验子代理提示词`。
检验依据是 `本次需求分析.md`、`本次计划.md`、`项目启动.md`、当前 diff 和各测试子代理结果。
测试和检验通过后，你负责把对应 `当前修改的需求-时间` 标题追加 `-完成`。不要修改业务代码。
```

### 职责

- 复核 `本次计划.md` 中任务是否完成。
- 复核 `本次需求分析.md` 中定位的需求标题和章节范围是否均已处理。
- 复核 `需求.md`、`架构.md` 和 `计划.md` 是否按规则同步。
- 复核是否存在未暂存文件、无关改动、不应提交文件或危险操作痕迹。
- 复核当前修改需求标题是否可以追加 `-完成`；可以验收时由检验子代理直接追加 `-完成`。
- 复核是否存在未测试、未构建或环境阻塞项。
- 不修改业务代码；确需修正文档状态时先报告主线程。

### 输出格式

- 计划任务完成情况。
- 需求同步情况。
- 架构同步情况。
- `计划.md` 同步情况。
- 当前修改需求是否可标记完成。
- 已追加 `-完成` 的需求标题。
- 未暂存文件或无关改动。
- 不应提交文件。
- 未测试项。
- 未构建项。
- 阻塞项和建议处理方式。
