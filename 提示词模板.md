# 使用
/using-superpowers 和我对齐需求所有细节再落实TDD执行
- 不使用视觉伴侣。
- 任务明显过大或上下文复杂时，先创建或更新 `本次计划.md`。
- `本次计划.md` 用清单固化目标、用户最新指令、硬性约束、任务拆分、子代理分工、验收方式、当前状态和校准记录；普通说明使用 `-`，任务项使用 `- [ ]`，完成后改为 `- [x]`。
- 主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复。
- 能隔离的执行工作全部交给子代理。
- 存在强顺序依赖或共享文件风险的工作，按阶段分批派发子代理处理。
- 每完成一个阶段，主线程必须回看 `本次计划.md`、最新用户要求和当前文件状态，确认没有偏移后继续。
- 上下文压缩、恢复或长时间执行后，必须先读取 `本次计划.md` 和最新状态继续执行，不重新理解成新任务。
- 计划允许动态校准，但必须在 `本次计划.md` 写明调整原因。
- 浏览器使用 chrome-devtools 测试，必须按照人的方式，点击，输入等进行测试。
- 任务完成再做git处理
- gitignore 忽略的不要强制加入。
- 发现有远端领先先拉取。
- 工作区里面所有的改动全部自动推送。
- 不校验`提示词模板.md`是否被修改，直接提交
- 任务完成再删除提示词里面引用的图片。
- 执行`dev:desktop-shell`前必须先关闭之前的进程，保证最新代码生效
- 服务启用使用用户环境的node启动
- 本次需求：
- ![img.png](img.png)![img_1.png](img_1.png)，这是deepseek网页端的思考，本项目里面的思考卡片可以参考，内容一定要是大模型真实的思考过程，代码里面不要有任何的修改
- ![img_2.png](img_2.png)这是deepseek网页的标题总结，这个标题总结只在第一次对话完成总结，本项目也要实现
- ![img_3.png](img_3.png)供应商删除列表没有更新，列表的更新按钮放在列表上面，弹窗的面板看不到报错按钮，原因是无法滚动![img_4.png](img_4.png)

# 测试

- 浏览器使用 chrome-devtools 测试。
- 桌面端用 windows-mcp 测试。
