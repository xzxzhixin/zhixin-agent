# 致心智能体多代理工作流提示词

本文件是当前项目使用多代理工作流的项目提示词，负责声明当前项目配置、入口命令、项目特定规则，并引用 `多代理工作流/` 中的公共工作流提示词和角色提示词。

`多代理工作流/` 目录只保存可复用的公共流程和角色模板，不保存本项目的事实源文件名、端口、启动方式或宿主规则。本项目不同于其他项目的内容必须留在本文件。

## 入口命令

### 全量测试

```md
/superpowers 新窗口任务：执行全量验证。先读取 `提示词模板.md`，再按 `多代理工作流/00-公共模板.md`、`多代理工作流/01-主线程调度模板.md` 调度；需求核对、用户确认、临时需求回写和需求变化重核对按公共模板执行；需要创建子代理时读取 superpowers 子代理创建 skill 和对应角色模板。最终测试范围为浏览器端和桌面端。
```

### 只用浏览器测试

```md
/superpowers 新窗口任务：只执行浏览器端验证。先读取 `提示词模板.md`，再按 `多代理工作流/00-公共模板.md`、`多代理工作流/01-主线程调度模板.md` 调度；需求核对、用户确认、临时需求回写和需求变化重核对按公共模板执行；需要创建子代理时读取 superpowers 子代理创建 skill 和对应角色模板。最终测试范围只包含浏览器端，不执行桌面端测试项。
```

## 公共工作流文件

- 公共模板：`多代理工作流/00-公共模板.md`。
- 主线程调度模板：`多代理工作流/01-主线程调度模板.md`。
- 需求架构子代理模板：`多代理工作流/10-需求架构子代理提示词.md`。
- 程序员子代理模板：`多代理工作流/20-程序员子代理提示词.md`。
- 启动子代理模板：`多代理工作流/30-启动子代理提示词.md`。
- 测试子代理模板：`多代理工作流/40-测试子代理提示词.md`。

主线程和子代理只引用这些文件，不把公共模板全文粘贴到派发消息中。

## 当前项目配置

- 项目中文名：致心智能体。
- 项目英文名：zhixin-agent。
- 项目根目录：当前 `zhixin-agent` 仓库根目录。
- 项目提示词文件：`提示词模板.md`。
- 当前项目规则文件：`AGENTS.md`。
- 正式需求源：`需求.md`。
- 正式架构源：`架构.md`。
- 正式计划源：`计划.md`。
- 临时需求文件：`当前需要实现的需求.md`。
- 需求核对文件：`需求核对.md`。
- 本轮计划：`本次计划.md`。
- 本轮测试计划：`本次测试计划.md`。
- 本轮子代理登记：`本轮子代理登记.md`。
- 程序员结果：`程序员结果.md`。
- 项目启动记录：`项目启动.md`。
- 测试结果：`测试结果.md`。
- 浏览器测试窗口记录：`浏览器测试窗口.md`。
- 项目对话测试目录：`对话测试`。

## 当前项目事实源规则

- `AGENTS.md` 是当前协作规则源；所有子代理开始任务前必须先读取并遵守。
- `需求.md` 是产品需求源，只保存产品需求，不保存架构、执行计划、开发记录或临时 UI 调整说明。
- `架构.md` 是架构、技术栈、目录边界、布局边界和工程约定源。
- `计划.md` 是实施拆解和任务状态源。
- 只有用户明确要求新增、修改、删除正式需求，或明确点名更新 `需求.md` 时，才允许修改 `需求.md`。
- `需求.md` 发生用户明确要求的新增、删除或修改后，必须同步评估并更新 `计划.md`。
- 完成 `计划.md` 中任意任务后，必须立即把对应任务从 `[ ]` 改为 `[x]`。
- 如果只修改本文件或 `多代理工作流/` 中的工作流模板，且不触碰业务代码、构建脚本、测试脚本、工程配置或正式需求，不需要同步 `计划.md`；最终回复必须说明原因。
- 不允许未获得明确需求修改指令时改 `需求.md`。
- 不允许只改业务代码不评估 `计划.md`。
- 不运行测试或不构建时，最终回复必须明确说明。

## 当前项目启动与测试约束

- 前端默认开发端口为 `5173`。
- 中心服务默认端口为 `8866`。
- 健康的 `5173` 前端开发服务应复用，不应另启 `5174`、`5175` 或其他备用端口。
- 禁止直接运行 `services/center` 启动中心服务来绕过桌面端宿主。
- 需要中心服务参与测试时，只能启动桌面端或当前项目约定宿主，由桌面端或宿主拉起中心服务。
- 涉及中心服务、桌面壳、preload、Electron 主进程、静态资源托管、CORS、数据库迁移、中心服务装配或启动锁时，必须派发启动子代理重启桌面端或宿主刷新环境。
- 只涉及普通 Vue 页面、样式、路由或前端交互时，优先复用健康的 `5173` 服务，通过 HMR 或浏览器刷新验证。
- Electron、桌面壳以及由桌面壳管理的中心服务不允许直接叠加启动；重启前必须确认旧实例已退出或阻塞等待处理。
- 桌面端测试必须确认中心服务由桌面端或宿主拉起，而不是直接运行中心服务入口。
- 插件端只落实代码，不测试、不构建，除非用户另行明确要求。
- 项目对话测试默认使用项目根目录下的 `对话测试` 目录。
- 如果用户明确要求“不测试、不构建”，所有代理只能做静态自查，不运行测试和构建命令。
- `当前需要实现的需求.md` 中引用的图片默认属于本轮临时输入资源；测试子代理在全部必测项通过并收尾时可直接删除这些图片，不再向用户确认，并把删除结果写入 `测试结果.md`。

## 子代理创建提示

### 需求架构子代理

```md
请在当前项目根目录执行需求架构任务。
先读取 `提示词模板.md`、`多代理工作流/00-公共模板.md` 和 `多代理工作流/10-需求架构子代理提示词.md`。
本轮需求来源是 `当前需要实现的需求.md` 中所有未标记 `-完成` 的一级标题需求，按文件从上到下连续处理全部标题。
先进入需求核对闸门：读取 `当前需要实现的需求.md` 和必要上下文后，先写入 `需求核对.md`，返回 `状态：需用户确认`，等待用户确认；用户确认后，先把确认后的明确需求回写 `当前需要实现的需求.md`，再读取上一轮固化协作文件路径：`本次计划.md`、`本次测试计划.md`、`程序员结果.md`、`项目启动.md` 和 `测试结果.md`，把它们与当前未完成临时需求整合后，在主线程授权的共享文件写入范围内覆盖生成新的 `本次计划.md` 和新的 `本次测试计划.md`；如果执行过程中发现 `当前需要实现的需求.md` 又发生变化，必须重新写入 `需求核对.md` 并返回 `状态：需用户确认`，不要继续生成计划；`浏览器测试窗口.md` 只由测试子代理维护，需求架构子代理不要读取、重置或更新；如果主线程或用户明确要求同步正式需求或正式计划，则同时更新 `需求.md` 或 `计划.md`，不要修改业务代码。
```

### 程序员子代理

```md
请在当前项目根目录执行程序员任务。
先读取 `提示词模板.md`、`多代理工作流/00-公共模板.md` 和 `多代理工作流/20-程序员子代理提示词.md`。
连续处理 `本次计划.md` 中分配给程序员的全部未完成任务：<任务编号或标题列表>。
你的可修改文件范围是：<文件或目录列表>。
你的固定结果文件是 `程序员结果.md`。
不要修改范围外文件，不要运行未授权的测试或构建。
```

### 启动子代理

```md
请在当前项目根目录执行启动准备任务。
先读取 `提示词模板.md`、`多代理工作流/00-公共模板.md` 和 `多代理工作流/30-启动子代理提示词.md`。
根据 `本次计划.md` 和 `本次测试计划.md` 准备测试环境，并在主线程授权时写入或更新 `项目启动.md`；如果该固化记录文件缺失，先反馈 `固化协作文件缺失：项目启动记录`，等待确认。
连续完成本轮全部启动准备、端口检查、健康检查和启动记录更新；不要按单个检查项返回主线程。
```

### 测试子代理

```md
请在当前项目根目录执行测试任务。
先读取 `提示词模板.md`、`多代理工作流/00-公共模板.md` 和 `多代理工作流/40-测试子代理提示词.md`。
测试依据是 `本次测试计划.md`，启动信息读取 `项目启动.md`。
按本轮指定范围串行执行测试项；验证端可能是浏览器端、桌面端或两者。
需要浏览器端测试时，只能使用并维护 `浏览器测试窗口.md` 中记录的测试专用页签。
```

## 主线程执行口径

### 调度边界

- 归属：主线程只调度。
- 读取：先读取本文件，再读取公共模板和主线程调度模板。
- 禁止：不直接执行启动、端口检查、浏览器测试或桌面端测试。
- 流程：按“需求架构闸门、程序员连续执行、启动闸门、串行测试闭环”推进。

### 正文禁区

- 禁止：不读取 `AGENTS.md`、`需求.md`、`架构.md` 或 `计划.md` 正文做判断。
- 禁止：不读取 `当前需要实现的需求.md`、`本次计划.md` 或 `本次测试计划.md` 正文做判断。
- 禁止：不读取 `程序员结果.md`、`项目启动.md`、`测试结果.md` 或 `浏览器测试窗口.md` 正文做判断。
- 归属：这些文件正文由对应子代理按职责读取。

### 需求架构派发

- 载荷：创建需求架构子代理时，只传文件路径和写入授权。
- 归属：需求、架构、计划和上一轮固化协作内容由需求架构子代理读取并整合。
- 禁止：主线程不自行整合固化协作文件正文。

### 固化文件边界

- 禁止：主线程不得删除、重建、静默创建、清空或覆盖固化协作文件。
- 范围：`本次计划.md`、`本次测试计划.md`、`程序员结果.md`。
- 范围：`需求核对.md`、`项目启动.md`、`测试结果.md`、`浏览器测试窗口.md`。
- 阻塞口径：固化协作文件缺失时，只记录缺失并派给对应子代理或等待用户确认。

### 问题回流

- 触发：用户指出问题、流程缺陷、子代理遗漏、测试未收尾、计划未完成或测试计划未完成。
- 禁止：主线程不得读取上述文件正文自行修复。
- 载荷：文件路径、问题描述、写入授权和期望状态要求。
- 派回：需求或计划缺陷给需求架构子代理。
- 派回：代码和实现遗漏给程序员子代理。
- 派回：启动或环境状态缺陷给启动子代理。
- 派回：测试遗漏、复测、完成标记和临时图片清理给测试子代理。

### 子代理复用

- 复用：每轮同角色默认只创建或复用一个子代理。
- 程序员：拿到本次计划后按清单顺序连续完成全部分配任务。
- 禁止：不按单个任务反复新建程序员。
- 派回：测试反馈优先回到原程序员子代理。
- 派回：修复后优先回到原测试子代理复测。
- 例外：原子代理上下文不足、阻塞、失败、需拆分、已关闭、已失效或职责不匹配时才接力。

### 最终回复

- 写明：修改范围。
- 写明：正式需求同步情况。
- 写明：正式计划同步情况。
- 写明：是否测试或构建。
- 写明：未测试或未构建原因。
