# 使用
/using-superpowers和我对齐需求所有细节再落实`TDD`执行
- 不使用视觉伴侣。
- 任务再大也必须在本轮持续推进到完成`大任务执行规范`。
- 先读取项目根目录下面的`提示词模板.md`严格执行然后完成下面的任务
- 先读`公共`
- 测试只做`浏览器测试`
- 任务完成以后走`任务完成执行`
- 执行`dev:desktop-shell`前必须先关闭之前的进程，保证最新代码生效
- 实现MCP使用这个MCP配置测试
```json
{
  "mcpServers": {
    "idea": {
      "type": "http",
      "url": "http://127.0.0.1:64342/stream"
    }
  }
}
```
```json
{
  "mcpServers": {
    "chrome-devtools": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "chrome-devtools-mcp@latest",
        "--browserUrl",
        "http://127.0.0.1:9222",
        "--no-usage-statistics"
      ]
    }
  }
}
```
- 测试内容为获取我现在打开项目的路径和项目下面提示词模板的内容
- ![img_1.png](img_1.png)我的要求是输出提示词模板完整内容并不对；
- 思考过程里面只保留模型真的思考的内容，不要有其他任何多余的内容；
- 工具卡片没有显示
- https://github.com/zhukunpenglinyutong/idea-claude-code-gui
- 这个CCGUI就可以一直不断的执行
- ![img.png](img.png)点击MCP菜单我想看一下加入的MCP配置结果报错；
- ![img_2.png](img_2.png)CCGUI里面配置了MCP服务以后就可以看到MCP的工具；

# 大任务执行规范

- 任务明显过大或上下文复杂时，先创建或更新 `本次计划.md`。
- `本次计划.md` 用清单固化目标、用户最新指令、硬性约束、任务拆分、子代理分工、验收方式、当前状态和校准记录；普通说明使用 `-`，任务项使用 `- [ ]`，完成后改为 `- [x]`。
- 主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复。
- 能隔离的执行工作全部交给子代理。
- 存在强顺序依赖或共享文件风险的工作，按阶段分批派发子代理处理。
- 每完成一个阶段，主线程必须回看 `本次计划.md`、最新用户要求和当前文件状态，确认没有偏移后继续。
- 上下文压缩、恢复或长时间执行后，必须先读取 `本次计划.md` 和最新状态继续执行，不重新理解成新任务。
- 计划允许动态校准，但必须在 `本次计划.md` 写明调整原因。

# 浏览器测试

- 浏览器使用 chrome-devtools 测试。

# 浏览器和桌面测试

- 走`浏览器测试`
- 桌面端用 windows-mcp 测试。

# git规则

- gitignore 忽略的不要强制加入。
- 发现有远端领先先拉取。
- 工作区里面所有的改动全部自动推送。
- 不校验本文件是否被修改，直接提交

# 任务完成执行

- 走`git规则`
- 删除提示词里面引用的图片。
