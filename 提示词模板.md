# 使用
/using-superpowers 和我对齐需求所有细节后落实TDD执行，不使用视觉伴侣。
- 任务很大主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复。
- 确认过程中要不断更新 `本次计划.md`，固化目标、用户最新指令、硬性约束、任务拆分、子代理分工、验收方式、当前状态和校准记录；普通说明使用 `-`，任务项使用 `- [ ]`，完成后改为 `- [x]`。
- 每完成一个阶段，主线程必须回看 `本次计划.md`，确认没有偏移后继续。
- 浏览器使用 chrome-devtools 测试，必须按照人的方式，点击，输入等进行测试。
- 任务完成再做git处理
- gitignore 忽略的不要强制加入。
- 发现有远端领先先拉取。
- 工作区里面所有的改动全部自动推送。
- 不校验`提示词模板.md`是否被修改，直接提交
- 任务完成再删除提示词里面引用的图片，并清空`本次计划.md`文件的内容，注意不是删除
- 使用用户系统环境里面的node启动，不是你自己环境里面的node
- 执行`dev:desktop-shell`前必须先关闭之前的进程，保证最新代码生效
- 检索代码使用MCP服务idea
- 本次需求：
- 无法停止
- 已在对话中我回车发送不是排队而是直接停止
- 每个agent要维护一个自己的上下文
- 创建子智能体规则：父级用的什么供应商就用什么供应商，推理深度让父级决定；
- 除了子智能体不能创建todoList，其他智能体可以维护一个自己的todoList，而这就是长任务拆解出来的，

# 测试

- 浏览器使用 chrome-devtools 测试。
- 桌面端用 windows-mcp 测试。


