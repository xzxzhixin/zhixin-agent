# 致心智能体多代理工作流提示词

本文件是当前项目使用多代理工作流的项目提示词，负责声明当前项目配置、入口命令、项目特定规则，并引用 `多代理工作流/` 中的公共工作流提示词和角色提示词。

`多代理工作流/` 目录只保存可复用的公共流程和角色模板，不保存本项目的事实源文件名、端口、启动方式或宿主规则。本项目不同于其他项目的内容必须留在本文件。

## 入口命令

### 全量测试

```md
/superpowers 新窗口任务：执行全量验证。先读取 `提示词模板.md`，再按 `多代理工作流/00-公共模板.md`、`多代理工作流/01-主线程调度模板.md` 调度；主线程先检查 `当前需要实现的需求.md` 标题状态，优先处理状态闸门：无状态或 `-编写中` 表示用户仍在编写，先等待用户继续编写，不创建子代理；只有显式 `-待确认` 标题才在当前对话框使用superpowers技能实时确认，确认后把标题标为 `-已确认`；全部待处理标题均为 `-已确认` 且未 `-完成` 后，才创建子代理；需要创建子代理时读取 superpowers 子代理创建 skill 和对应角色模板。最终测试范围为浏览器端和桌面端。
```

### 只用浏览器测试

```md
/superpowers 新窗口任务：只执行浏览器端验证。先读取 `提示词模板.md`，再按 `多代理工作流/00-公共模板.md`、`多代理工作流/01-主线程调度模板.md` 调度；主线程先检查 `当前需要实现的需求.md` 标题状态，优先处理状态闸门：无状态或 `-编写中` 表示用户仍在编写，先等待用户继续编写，不创建子代理；只有显式 `-待确认` 标题才在当前对话框使用superpowers技能实时确认，确认后把标题标为 `-已确认`；全部待处理标题均为 `-已确认` 且未 `-完成` 后，才创建子代理；需要创建子代理时读取 superpowers 子代理创建 skill 和对应角色模板。最终测试范围只包含浏览器端，不执行桌面端测试项。
```

## 公共工作流文件

- 公共模板：`多代理工作流/00-公共模板.md`。
- 主线程调度模板：`多代理工作流/01-主线程调度模板.md`。
- 计划编排子代理模板：`多代理工作流/10-计划编排子代理提示词.md`。
- 程序员子代理模板：`多代理工作流/20-程序员子代理提示词.md`。
- 测试子代理模板：`多代理工作流/40-测试子代理提示词.md`。

主线程和子代理只引用这些文件，不把公共模板全文粘贴到派发消息中。

## 当前项目配置

- 项目中文名：致心智能体。
- 项目英文名：zhixin-agent。
- 项目根目录：当前 `zhixin-agent` 仓库根目录。
- 项目提示词文件：`提示词模板.md`。
- 当前项目规则文件：`AGENTS.md`。
- 正式需求源：`需求.md`。
- 正式架构源：`架构.md`。
- 正式计划源：`计划.md`。
- 临时需求文件：`当前需要实现的需求.md`。
- 本轮计划：`本次计划.md`。
- 本轮测试计划：`本次测试计划.md`。
- 本轮子代理登记：`本轮子代理登记.md`。
- 程序员结果：`程序员结果.md`。
- 项目启动记录：`项目启动.md`。
- 测试结果：`测试结果.md`。
- 浏览器测试窗口记录：`浏览器测试窗口.md`。
- 项目对话测试目录：`对话测试`。

## 当前项目事实源规则

- `AGENTS.md` 是当前协作规则源；所有子代理开始任务前必须先读取并遵守。
- `需求.md` 是产品需求源，只保存产品需求，不保存架构、执行计划、开发记录或临时 UI 调整说明。
- `架构.md` 是架构、技术栈、目录边界、布局边界和工程约定源。
- `计划.md` 是实施拆解和任务状态源。
- 只有用户明确要求新增、修改、删除正式需求，或明确点名更新 `需求.md` 时，才允许修改 `需求.md`。
- `需求.md` 发生用户明确要求的新增、删除或修改后，必须同步评估并更新 `计划.md`。
- 完成 `计划.md` 中任意任务后，必须立即把对应任务从 `[ ]` 改为 `[x]`。
- 如果只修改本文件或 `多代理工作流/` 中的工作流模板，且不触碰业务代码、构建脚本、测试脚本、工程配置或正式需求，不需要同步 `计划.md`；最终回复必须说明原因。
- 不允许未获得明确需求修改指令时改 `需求.md`。
- 不允许只改业务代码不评估 `计划.md`。
- 不运行测试或不构建时，最终回复必须明确说明。

## 当前项目启动与测试约束

- 前端默认开发端口为 `5173`。
- 中心服务默认端口为 `8866`。
- 健康的 `5173` 前端开发服务应复用，不应另启 `5174`、`5175` 或其他备用端口。
- 禁止直接运行 `services/center` 启动中心服务来绕过桌面端宿主。
- 需要中心服务参与测试时，只能启动桌面端或当前项目约定宿主，由桌面端或宿主拉起中心服务。
- 涉及中心服务、桌面壳、preload、Electron 主进程、静态资源托管、CORS、数据库迁移、中心服务装配或启动锁时，必须由程序员子代理重启桌面端或宿主刷新环境，并更新 `项目启动.md`。
- 只涉及普通 Vue 页面、样式、路由或前端交互时，优先复用健康的 `5173` 服务，通过 HMR 或浏览器刷新验证。
- Electron、桌面壳以及由桌面壳管理的中心服务不允许直接叠加启动；重启前必须确认旧实例已退出或阻塞等待处理。
- 桌面端测试必须确认中心服务由桌面端或宿主拉起，而不是直接运行中心服务入口。
- 插件端只落实代码，不测试、不构建，除非用户另行明确要求。
- 项目对话测试默认使用项目根目录下的 `对话测试` 目录。
- 如果用户明确要求“不测试、不构建”，所有代理只能做静态自查，不运行测试和构建命令。
- 主线程和用户实时确认或头脑风暴 `当前需要实现的需求.md` 中的 `-待确认` 需求后，可以回写该需求正文并把标题标为 `-已确认`；回写前必须先保全原需求正文和本次确认对话中出现的图片引用。
- 如果本轮确认内容包含图片引用，主线程回写后的同一一级标题下必须保留固定小节 `## 本轮临时图片引用`，逐条写入原始图片引用和解析路径；该小节属于临时协作元数据，不写入正式 `需求.md`。
- 计划编排子代理不得修改 `当前需要实现的需求.md`；只能读取 `-已确认` 且未 `-完成` 的需求正文和 `## 本轮临时图片引用`，并把图片引用复制到 `本次测试计划.md` 的 `## 本轮临时图片清理清单`。
- 测试子代理最终只按 `本次测试计划.md` 的 `## 本轮临时图片清理清单` 删除本轮临时图片；全部必测项通过并收尾时可直接删除清单中标为本轮临时输入资源的图片，不再向用户确认，并把删除结果写入 `测试结果.md`。

## 子代理创建提示

### 计划编排子代理

```md
请在当前项目根目录执行计划编排任务。
先读取 `提示词模板.md`、`多代理工作流/00-公共模板.md` 和 `多代理工作流/10-计划编排子代理提示词.md`。
本轮需求来源是 `当前需要实现的需求.md` 中所有 `-已确认` 且未 `-完成` 的一级标题需求，按文件从上到下连续处理全部标题。
`-编写中`、无状态或 `-待确认` 标题不得进入计划；如果发现无状态或 `-编写中` 标题，返回阻塞并要求主线程等待用户继续编写；如果发现 `-待确认` 标题，返回阻塞并要求主线程先完成实时确认。读取上一轮固化协作文件路径：`本次计划.md`、`本次测试计划.md`、`程序员结果.md`、`项目启动.md` 和 `测试结果.md`，把它们与当前已确认需求整合后，在主线程授权的共享文件写入范围内覆盖生成新的 `本次计划.md` 和新的 `本次测试计划.md`；如果已确认需求中存在 `## 本轮临时图片引用`，必须把图片引用复制到 `本次测试计划.md` 的 `## 本轮临时图片清理清单`；`浏览器测试窗口.md` 只由测试子代理维护，计划编排子代理不要读取、重置或更新；计划编排子代理不得修改 `当前需要实现的需求.md`；如果主线程或用户明确要求同步正式需求或正式计划，则同时更新 `需求.md` 或 `计划.md`，不要修改业务代码。
```

### 程序员子代理

```md
请在当前项目根目录执行程序员任务。
先读取 `提示词模板.md`、`多代理工作流/00-公共模板.md` 和 `多代理工作流/20-程序员子代理提示词.md`。
连续处理 `本次计划.md` 中分配给程序员的全部未完成任务：<任务编号或标题列表>。
你的可修改文件范围是：<文件或目录列表>。
你的固定结果文件是 `程序员结果.md`。
如果本轮计划明确给出程序员并行组，只处理分配给你的任务编号和文件范围；`程序员结果.md` 仍只使用这一个文件，并按任务编号章节写入；写入 `程序员结果.md`、`本次计划.md` 或 `项目启动.md` 必须等待主线程串行授权。
如果本轮需要运行环境刷新、端口检查、健康检查、桌面端或宿主重启，按授权写入或更新 `项目启动.md`；如果该固化记录文件缺失，先反馈 `固化协作文件缺失：项目启动记录`，等待确认。
不要修改范围外文件，不要运行未授权的测试或构建。
```

### 测试子代理

```md
请在当前项目根目录执行测试任务。
先读取 `提示词模板.md`、`多代理工作流/00-公共模板.md` 和 `多代理工作流/40-测试子代理提示词.md`。
测试依据是 `本次测试计划.md`，启动信息读取 `项目启动.md`。
默认按本轮指定范围串行执行测试项；验证端可能是浏览器端、桌面端或两者。
如果本轮测试计划明确给出测试并行组，只处理分配给你的测试项编号、验证端、浏览器页签范围和宿主范围；`测试结果.md` 仍只使用这一个文件，并按测试项编号章节写入；写入 `测试结果.md`、`本次测试计划.md` 或 `浏览器测试窗口.md` 必须等待主线程串行授权。
需要浏览器端测试时，只能使用并维护 `浏览器测试窗口.md` 中记录的测试专用页签。
```

## 主线程执行口径

### 调度边界

- 归属：主线程只调度。
- 读取：先读取本文件，再读取公共模板和主线程调度模板。
- 读取：主线程可以读取 `当前需要实现的需求.md`，只用于处理无状态、`-编写中`、`-待确认`、`-已确认`、`-已确认-完成` 标题状态和与用户实时确认需求。
- 写入：主线程和用户实时确认或头脑风暴后回写 `当前需要实现的需求.md` 时，必须保全原需求和确认对话里的图片引用；存在图片引用时，回写后的同一标题下必须保留 `## 本轮临时图片引用`。
- 禁止：不直接执行启动、端口检查、浏览器测试或桌面端测试。
- 流程：按“计划编排闸门、程序员连续执行、串行测试闭环”推进。
- 流程：测试默认串行；只有 `本次测试计划.md` 明确测试并行组时才允许条件并行，最终完成标记和临时图片清理仍由唯一测试收口流程执行。

### 正文禁区

- 禁止：不读取 `AGENTS.md`、`需求.md`、`架构.md` 或 `计划.md` 正文做判断。
- 禁止：除需求确认状态流程外，不读取 `本次计划.md` 或 `本次测试计划.md` 正文做判断。
- 禁止：不读取 `程序员结果.md`、`项目启动.md`、`测试结果.md` 或 `浏览器测试窗口.md` 正文做判断。
- 归属：这些文件正文由对应子代理按职责读取。

### 计划编排派发

- 载荷：创建计划编排子代理时，只传文件路径和写入授权。
- 归属：已确认需求、架构、计划和上一轮固化协作内容由计划编排子代理读取并整合。
- 禁止：主线程不自行整合固化协作文件正文。

### 固化文件边界

- 禁止：主线程不得删除、重建、静默创建、清空或覆盖固化协作文件。
- 范围：`本次计划.md`、`本次测试计划.md`、`程序员结果.md`。
- 范围：`项目启动.md`、`测试结果.md`、`浏览器测试窗口.md`。
- 阻塞口径：固化协作文件缺失时，只记录缺失并派给对应子代理或等待用户确认。

### 问题回流

- 触发：用户指出问题、流程缺陷、子代理遗漏、测试未收尾、计划未完成或测试计划未完成。
- 禁止：主线程不得读取上述文件正文自行修复。
- 载荷：文件路径、问题描述、写入授权和期望状态要求。
- 派回：需求或计划缺陷给计划编排子代理。
- 派回：代码和实现遗漏给程序员子代理。
- 派回：启动或环境状态缺陷给程序员子代理。
- 派回：测试遗漏、复测、完成标记和临时图片清理给测试子代理。

### 计划实时同步

- 程序员：获得 `本次计划.md` 写入授权时，完成一个计划项就立即更新一个计划项，不等全部任务结束后批量勾选。
- 程序员：未获授权时，完成一个计划项就立即把建议勾选行和完成摘要写入 `程序员结果.md`。
- 程序员并行：只有 `本次计划.md` 明确标记程序员并行组时才允许条件并行；代码实现可并行，`本次计划.md`、`程序员结果.md` 和 `项目启动.md` 写入必须串行授权。
- 程序员并行：`程序员结果.md` 保持单文件，并行子代理只写自己的任务编号章节，不覆盖其他章节或主线程摘要。
- 测试：获得 `本次测试计划.md` 写入授权时，完成一个测试项就立即更新一个测试项，不等全部测试结束后批量勾选。
- 测试：未获授权时，完成一个测试项就立即把建议状态和原因写入 `测试结果.md`。
- 测试并行：只有 `本次测试计划.md` 明确标记测试并行组时才允许条件并行；测试执行可并行，`本次测试计划.md`、`测试结果.md` 和 `浏览器测试窗口.md` 写入必须串行授权。
- 测试并行：`测试结果.md` 保持单文件，并行子代理只写自己的测试项编号章节，不覆盖其他章节或主线程摘要。
- 测试收口：并行测试子代理不得追加 `-完成` 或清理临时图片；并行组收敛后由唯一测试收口子代理处理完成标记和临时图片清理。

### 中途新增需求

- 写入：用户中途新增需求时，只能新增 `当前需要实现的需求.md` 的一级标题，不修改已确认或已完成标题正文。
- 写入：新增需求经主线程确认并回写时，图片引用保全规则同样适用。
- 阻塞：新增标题无状态或为 `-编写中` 时，主线程等待用户继续编写，不创建子代理读取该需求。
- 确认：新增标题显式为 `-待确认` 时，主线程在当前对话框实时确认，并把确认后的标题标为 `-已确认`。
- 编排：新增标题标为 `-已确认` 后，主线程复用计划编排子代理判断是否并入当前轮，并更新 `本次计划.md` 和 `本次测试计划.md`。
- 执行：程序员阶段中途新增需求时，程序员完成当前最小计划项后停止并写入结果；测试阶段中途新增需求时，测试子代理完成当前测试项后停止，再由主线程回到计划编排。

### 子代理复用

- 复用：每轮默认按职责复用同角色子代理；计划编排保持唯一，程序员只有在 `本次计划.md` 明确并行组时允许条件并行，测试只有在 `本次测试计划.md` 明确并行组时允许条件并行。
- 程序员：拿到本次计划后按清单顺序连续完成全部分配任务。
- 禁止：不按单个任务反复新建程序员。
- 派回：测试反馈优先回到原程序员子代理。
- 派回：修复后优先回到原测试子代理复测。
- 例外：原子代理上下文不足、阻塞、失败、需拆分、已关闭、已失效或职责不匹配时才接力。

### 最终回复

- 写明：修改范围。
- 写明：正式需求同步情况。
- 写明：正式计划同步情况。
- 写明：是否测试或构建。
- 写明：未测试或未构建原因。
