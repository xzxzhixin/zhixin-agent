
# 使用

/using-superpowers 确认需求所有细节以后落实TDD再执行
- 不使用视觉伴侣
-
- 读取并加入：提示词模板.md##测试规则，只测试浏览器端
- 读取并加入：提示词模板.md##完成执行

# 整体模板

/using-superpowers 确认需求所有细节以后落实TDD再执行
- 不使用视觉伴侣
- 
- 读取并加入：提示词模板.md##测试规则，只测试浏览器端
- 读取并加入：提示词模板.md##完成执行

## 测试规则
- 重启保证是最新代码生效
- 重启前查看是否有运行中的代码，有先关闭
- 浏览器使用chrome-devtools测试
- 桌面端用windows-mcp测试

## git规则
- gitignore忽略的不要强制加入
- 发现有远端领先先拉取
- 工作区里面所有的改动全部自动推送

## 完成执行
- 读取并加入：提示词模板.md##git规则
- 删除这段提示词里面引用的图片