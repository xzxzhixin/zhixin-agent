# idea里面使用
/using-superpowers:brainstorming 不使用视觉伴侣，执行方式使用子代理驱动，不新建worktrees，直接在仓库代码里面修改。所有任务完成后再删除本段提示词里面引用的图片。启动服务前必须关闭老项目，必须使用idea启动：`dev:desktop-shell`。用端口检测是否启动完成。
- 使用 chrome-devtools 测试
- 本次需求：

# 新需求

- 先问 deepagent 是如何动态注入MCP工具的，在去实现
- MCP动态工具调用失败没过，需要修正，内部工具不允许排除，
- 中间件不允许使用 `createMiddleware` 而要使用 `AgentMiddleware`基类，放在 `AgentMiddleware`文件夹下
- tools改名 `StructuredTool`

# 其他

- 浏览器使用 chrome-devtools 测试。
- 桌面端用 windows-mcp 测试。

- 任务完成删除提示词里面引用的图片，并清空`本次计划.md`文件的内容，注意不是删除
- 浏览器使用 chrome-devtools 测试，测试必须按照点击，输入，视觉查看进行，尤其要对比对话轮次时间和最后回复时间，这个对不上就肯定有bug，需要修复。


