# 需求核对

## 主线程摘要

- 状态：需用户确认。
- 等待用户确认：是。
- 是否需要回写临时需求文件：是，用户确认后先把“建议确认口径”回写到 `当前需要实现的需求.md` 对应未完成一级标题下。
- 是否建议同步正式需求或正式计划：建议用户确认后再由本轮计划阶段评估是否同步 `需求.md`、`架构.md` 和 `计划.md`；当前用户只授权重新进入需求核对闸门，未授权修改正式需求、正式架构或正式计划。
- 确认后下一步：先回写 `当前需要实现的需求.md`，再读取上一轮固化协作文件 `本次计划.md`、`本次测试计划.md`、`程序员结果.md`、`项目启动.md` 和 `测试结果.md`，整合生成新的 `本次计划.md` 与新的 `本次测试计划.md`；最终测试范围只包含浏览器端，不执行桌面端测试项。

## 需求理解

### 2026年6月4日08:44:47

- 工具调用与命令执行：移除固定的“Node 版本”命令工具入口和 `/api/tool/command/node-version` 这类写死接口，不再为单一命令提供专门 API。用户期望 Agent 结合大模型理解自然语言后，通过通用命令行执行能力完成问题，例如询问本机 Python 环境时，也应由 Agent 规划并调用通用命令，而不是继续新增 `python-version` 之类固定接口。
- 主对话引导：取消独立的“主对话引导”入口、默认文案和提交模式。引导能力只出现在排队对话项后方：当某条对话消息处于当前对话内排队状态时，在该排队对话后显示“引导”按钮；点击后把这条排队对话作为本轮引导发送或提交，不再从输入区主动创建“主对话引导”。
- 审计摘要：对话页右侧或相关区域不再展示“审计摘要”模块。工具、命令、插件、MCP、skill、文件操作等过程记录仍应保留在消息流、任务详情或可追踪事件中，但不以“审计摘要”作为右侧常驻模块呈现。
- 任务状态：输入区“任务”和右上角任务区域都应展示本轮对话拆解出来的任务。用户发送一轮新对话后，大模型拆解出的一个个小任务显示在这里；新一轮对话开始时，任务列表重置为新一轮任务；输入区任务入口与右上角任务状态需要实时同步。
- 智能体状态：智能体状态弹框需要使用可折叠树形结构。长期智能体下面展示其子智能体；示例中的“测试子智能体”应位于“测试长期智能体”的折叠节点内并默认展开。智能体详情或对话区域需要具备外部完整输入框能力，不能只是简化输入。
- 上下文统计与 tokenizer：对话输入区或当前窗口需要真实展示上下文统计，不能只有静态文案，不能只用字符数、估算值或简单比例近似。token 统计方式直接参考 Codex 的 token 统计方式设计口径：围绕实际会送入模型的上下文包统计，包括当前消息、历史消息、系统或开发提示、工具或能力描述、结构化引用、附件摘要和必要的运行上下文。内部需要自研 tokenizer 能力，并且可以对接外部 tokenizer；整体形态参考模型协议插件或协议插件一样的内部协议化、插件化边界。tokenizer 层需要按模型或供应商选择内部实现或外部适配器，返回可审计的 token 统计来源、模型标识、输入范围和窗口上限。
- 管理页列表展示：上一轮“截图所指三个管理区域不应继续使用普通表格形态展示，需要改成非表格布局或卡片/列表式组织”的口径是错误的。本轮明确修正为：管理页信息展示需要继续使用 Element Plus 的 `el-table` 这类表格展示信息，不能为了本轮需求把相关管理页改成非表格卡片布局。
- ORM 与持久层：用户已确认 ORM 使用 Drizzle。中心服务需要在保持 Node.js、TypeScript、Fastify、SQLite 和中心服务唯一数据库连接边界的前提下接入 Drizzle，并把现有 SQL 全量迁移到独立持久层。SQL、表定义、查询、Mapper/Repository 或持久化访问逻辑不得继续散落在路由、业务服务、Worker 编排或 UI 相关代码中。路由和业务服务只调用明确的数据访问层接口，事务边界、表 schema、查询条件、分页和聚合逻辑集中维护。
- 测试范围：本轮入口为只执行浏览器端验证，最终测试范围只包含浏览器端，不执行桌面端测试项；但这不代表跳过需求核对、计划、程序员执行和必要的浏览器端前置准备。

## 待确认问题

无，按建议确认口径继续。用户已经明确修正本轮关键口径：tokenizer 采用内部协议化/插件化能力并可对接外部 tokenizer；ORM 使用 Drizzle；现有 SQL 全量迁移到独立持久层；token 统计方式直接参考 Codex 的 token 统计方式设计。

## 建议确认口径

```md
# 2026年6月4日08:44:47

- 移除写死的 Node 版本命令工具入口和 `/api/tool/command/node-version` 专用接口，不再为单一命令新增固定 API。命令类需求统一由 Agent 理解用户自然语言后，通过通用命令行执行能力完成；询问 Node.js、Python 或其他本机环境时，都走同一套 Agent 命令执行链路。
- 取消独立“主对话引导”入口、默认引导文案和提交模式。只有当对话消息进入当前对话内排队状态时，才在该排队对话后显示“引导”按钮；点击按钮后，把对应排队对话作为本轮引导提交。
- 对话页不再展示“审计摘要”常驻模块。工具、命令、插件、MCP、skill、文件操作和错误排查信息保留在消息流、任务详情或事件详情中展示。
- 输入区“任务”和右上角任务区域展示同一轮对话的大模型拆解任务，并实时同步。用户发送新一轮对话后，任务列表重置为新一轮任务，不沿用上一轮任务。
- 智能体状态弹框使用可折叠树形结构展示长期智能体和子智能体。子智能体挂在创建它的长期智能体下面；“测试子智能体”默认展示在“测试长期智能体”折叠节点内并默认展开。智能体对话区域需要具备与外部一致的完整输入框能力。
- 当前窗口需要展示上下文统计，显示已用 token 和模型窗口上限。token 统计方式直接参考 Codex 的 token 统计方式设计：按实际将送入模型的上下文包统计，覆盖当前消息、历史消息、系统或开发提示、工具或能力描述、结构化引用、附件摘要和必要运行上下文；统计结果需要记录 tokenizer 来源、模型标识、输入范围、已用 token 和窗口上限。
- 系统内部需要自研 tokenizer 能力，同时能够对接外部 tokenizer。tokenizer 能力采用类似模型协议插件或协议插件的内部协议化、插件化边界：内部定义统一 tokenizer 请求、响应、适配器接口、模型匹配规则和统计来源；不同供应商或模型可以选择内置 tokenizer 或外部 tokenizer 适配器；不允许继续使用字符数、字符串长度或简单估算冒充真实 token 统计。
- 管理页信息展示继续使用 Element Plus 的 `el-table` 这类表格展示信息。上一轮“截图所指三个管理区域不应继续使用普通表格，需要改成非表格卡片/列表布局”的口径废弃，后续实现不得按该错误口径改造管理页。
- 中心服务 ORM 明确使用 Drizzle。需要在保持 Node.js、TypeScript、Fastify、SQLite 和中心服务唯一数据库连接边界的前提下接入 Drizzle，并把现有 SQL 全量迁移到独立持久层。
- 中心服务中现有 SQL、表定义、查询、Mapper/Repository 或持久化访问逻辑不得继续散落在路由、业务服务、Worker 编排或 UI 相关代码中。路由和业务服务只调用明确的数据访问层接口；事务边界、Drizzle schema、查询条件、分页、聚合和用量统计查询集中维护在独立持久层。
- 本轮最终测试范围只包含浏览器端，不执行桌面端测试项。
```

## 变化检测

- 本次核对基于 `当前需要实现的需求.md` 中未标记 `-完成` 的一级标题：`2026年6月4日08:44:47`。
- 检测到上一轮需求核对草案存在已被用户修正的错误口径：ORM 方案不再等待 MyBatis Plus 替代方案确认，已明确为 Drizzle；SQL 分离粒度不再等待确认，已明确为现有 SQL 全量迁移到独立持久层；真实 tokenizer 覆盖范围不再继续追问，已按 Codex token 统计口径和内部/外部 tokenizer 插件化边界收敛。
- 检测到上一轮需求核对草案存在已被用户修正的 UI 口径：管理页三个区域仍应使用 Element Plus `el-table` 这类表格展示信息，不按非表格卡片布局改造。
- 未生成或覆盖 `本次计划.md`、`本次测试计划.md`。
- 未读取、重置或更新 `浏览器测试窗口.md`。
- 未修改业务代码。

## 下一步

用户确认后，先把确认后的明确需求回写 `当前需要实现的需求.md`，再读取上一轮固化协作文件 `本次计划.md`、`本次测试计划.md`、`程序员结果.md`、`项目启动.md` 和 `测试结果.md`，整合生成新的 `本次计划.md` 与新的 `本次测试计划.md`。生成计划前如果发现临时需求再次变化，需要重新覆盖写入 `需求核对.md` 并返回 `状态：需用户确认`。
