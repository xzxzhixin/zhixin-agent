---
description: 本文件是项目功能设计、交互设计与验收设计的事实源，用于明确“准备怎么做、模块如何协作、完成后如何验收”，避免设计规则散落在需求、架构或临时沟通中。每个设计必须使用独立一级标题，并包含目标、范围、设计要点、验收口径和清单状态。实现前若设计缺失或与当前实现目标冲突，必须先更新本文件；实现后若设计发生变化，也必须同步回写本文。文件关系固定为：`需求.md` 决定设计目标、业务边界与验收底线，`设计.md` 负责把需求翻译成可实现方案，`架构.md` 负责承接设计中的技术落点、模块边界与工程约束，`功能清单与关系.md` 负责把已确认设计拆成可追踪的功能项、依赖项与回归检查项。若 `设计.md` 与 `需求.md` 冲突，以 `需求.md` 为准；若 `设计.md` 更新影响技术边界或功能依赖，必须同步回写 `架构.md` 与 `功能清单与关系.md`。文档改动完成后不做额外校验，直接提交并推送。
---

# 记忆召回与富文本输入框关联设计

## 目标

- ⏳ 让富文本输入框产出的正式消息事实成为长期记忆写入来源。
- ⏳ 让跨窗口对话能通过 Mem0、SQLite 和 Markdown 召回文本事实、附件来源和附件摘要。

## 范围

- ⏳ 覆盖结构化草稿、正式消息事实、归档附件、附件记忆写入和跨窗口召回。
- ⏳ 不覆盖附件解析供应商选择和附件清理 UI。

## 设计要点

- ⏳ 富文本输入框只维护发送前草稿，草稿中的文本、附件、文件引用、代码位置和 IDE 选区不能直接写入长期记忆。
- ⏳ 用户发送消息后，中心服务把草稿固化为正式消息事实，正式事实包含文本、归档附件引用、项目引用、代码引用和 IDE 选区来源。
- ⏳ 正式附件只保存一份，归档到 `center-data/memory/attachments/{year}/{month}/{day}/{attachmentId}/原文件名`，会话消息和长期记忆通过附件 ID 引用同一份文件。
- ⏳ 删除会话只删除会话、消息和附件引用关系，不删除已经归档且可能被长期记忆引用的附件文件。
- ⏳ 轮次完成后，记忆写入先保存可复用文本事实和正式附件来源引用；可解析附件通过异步任务追加内容摘要，摘要失败只记录失败信息，不影响主轮次终态。
- ⏳ 跨窗口对话前，中心服务使用当前用户问题构造通用召回查询，并联合 Mem0、SQLite `memory_index` 和 Markdown 来源召回长期记忆。
- ⏳ 召回链路不能依赖固定身份问题、固定业务词或用户提示词硬编码，身份、偏好、当天事实和附件内容都走同一套来源可追溯召回策略。

## 验收口径

- ⏳ 发送前草稿中的文本、附件、项目引用、代码引用和 IDE 选区不出现在长期记忆、SQLite `memory_index` 或 Mem0 中。
- ⏳ 发送后正式消息事实能追溯文本、归档附件、项目引用、代码引用和 IDE 选区来源。
- ⏳ 附件从临时目录转入 `center-data/memory/attachments/{year}/{month}/{day}/{attachmentId}/原文件名` 后，会话消息和长期记忆引用同一份归档附件。
- ⏳ 删除包含附件的会话后，归档附件文件仍存在，长期记忆仍能追溯附件 ID 和来源。
- ⏳ 可解析附件摘要成功后追加到同一附件来源链路，摘要失败不改变主轮次完成状态。
- ⏳ 窗口 A 说“我今天吃了龙虾”并完成轮次后，窗口 B 问“我今天吃了什么”能通过长期记忆召回回答“龙虾”，源码不保留固定身份或业务词硬编码召回。

## 清单状态

- ⏳ 结构化草稿边界
- ⏳ 正式消息事实固化
- ⏳ 单份归档附件引用
- ⏳ 附件来源记忆写入
- ⏳ 附件摘要异步追加
- ⏳ 跨窗口长期记忆召回

# 智能体执行边界迁移与工具权限设计

## 目标

- ⏳ 把中心服务智能体执行边界接入 `services/center/src/agents`，让主智能体、长期智能体和子智能体通过同一套类层级声明身份、工具权限、外部 MCP、命令工具和内部工具可用范围。

## 范围

- ⏳ 本设计只覆盖智能体类型裁决、工具能力过滤和创建类工具权限。
- ⏳ 本设计不重写 LangGraph 执行图、不改变供应商模型调用协议、不新增外置插件系统。

## 设计要点

- ⏳ `services/center/src/agents` 承载 `BaseAgent`、`MainAgent`、`LongTermAgent`、`SubAgent` 以及工厂和工具策略。
- ⏳ `domain` 继续保留数据写入、会话、记忆和协作领域函数。
- ⏳ `tools` 继续保留具体工具执行器。
- ⏳ 所有智能体都可以按会话、项目、MCP、skill、内联工具、用户授权和执行模式裁决后请求外部 MCP、命令工具、todoList 和内部工具。
- ⏳ 创建智能体和 team 管理工具必须再叠加智能体类型边界，todoList 不归入创建类工具。
- ⏳ `MainAgent` 可以注入创建长期智能体、创建子智能体和四个 team 管理工具，并可使用普通命令、MCP、skill 和内部工具。
- ⏳ `LongTermAgent` 可以注入创建子智能体工具，并可使用普通命令、MCP、skill 和内部工具；长期智能体不能使用 team 创建、解散、添加成员和移除成员工具。
- ⏳ `SubAgent` 可以使用普通命令、MCP、skill 和内部工具，但不能注入创建长期智能体、创建子智能体或 team 管理工具。
- ✅ 模型可见工具列表必须先从统一工具注册表和官方 MCP adapter 发现工具生成，再通过当前智能体实例过滤。
- ✅ 统一工具 ID 通过表驱动映射为智能体权限名，官方 MCP adapter 工具由中心服务包装后继承 MCP 权限边界。
- ✅ 拒绝规则写入中文注释，避免调用点用字段兜底猜测权限。
- ✅ 长期智能体创建/更新、子智能体运行记录、team 创建/成员变更、智能体运行状态、智能体域记忆索引和事件审计时间统一使用中心服务本机时间字符串，格式为 `YYYY-MM-DD HH:mm:ss`。
- ⏳ 主对话内默认展示主智能体 `agentId = main` 的当前轮次拆解步骤；智能体弹框默认展示当前弹框 `agentId` 的 todoList，不混入主智能体或其他智能体任务。

## 验收口径

- ✅ 已有 `BaseAgent`、`MainAgent`、`LongTermAgent`、`SubAgent` 类层级可作为智能体权限裁决入口。
- ✅ 工具过滤使用统一工具注册表和智能体实例过滤，不在调用点用零散条件猜测权限。
- ✅ 智能体域时间不保存 UTC ISO 字符串，统一保存中心服务本机时间字符串。
- ⏳ 主智能体、长期智能体和子智能体的创建类工具、team 工具、普通工具权限符合各自边界。
- ⏳ 主对话和智能体弹框的 todoList 展示按 `agentId` 隔离。

## 清单状态

- ⏳ 智能体目录边界
- ⏳ 权限规则
- ⏳ 主智能体权限
- ⏳ 长期智能体权限
- ⏳ 子智能体权限
- ✅ 工具过滤
- ✅ 智能体时间
- ⏳ 主对话任务范围

# 文档事实源与规则整理设计

## 目标

- ⏳ 让 `需求.md`、`设计.md`、`架构.md` 和 `AGENTS.md` 的职责边界清晰，避免产品需求、设计口径、架构约定和协作规则互相混放。

## 范围

- ⏳ 本设计只整理文档结构、状态维护和事实源同步口径。
- ⏳ 本设计不改变具体产品能力、不改代码实现、不新增运行入口。

## 设计要点

- ⏳ `需求.md` 只保留一个从头到尾的清单列表，不再按多个二级或三级块组织。
- ⏳ 原有产品需求语义保留，新增智能体迁移和权限边界要求进入同一清单。
- ⏳ `设计.md` 按每一次设计拆分一级标题，每个标题下维护目标、范围、设计要点、验收口径和清单状态。
- ⏳ 当前设计与已有设计或用户之前明确提出的设计冲突时，先告知冲突点并等待用户决定口径，再修订 `设计.md`、`需求.md` 或 `架构.md`。
- ⏳ 静态检查能确认无引用的导入、无用 URL 和空文件需要删除。
- ⏳ 仅用于真实开发入口或架构说明的 URL 保留。

## 验收口径

- ⏳ `需求.md` 保持单层清单列表。
- ⏳ `设计.md` 不再把多个功能、多个端或多个验收口径揉在同一个一级标题下。
- ⏳ 设计冲突处理流程和需求冲突处理流程一致，都必须等待用户判断。
- ⏳ 无用导入、无用 URL 和空文件不继续累积。

## 清单状态

- ⏳ 需求文档
- ⏳ 设计文档结构
- ⏳ 设计冲突处理
- ⏳ 无用导入和 URL

# 命令工具安全名与流式工具解析设计

## 目标

- ⏳ 修正命令工具暴露名、流式工具调用解析和空参数失败收尾，避免模型工具调用进入错误循环或使用供应商不接受的工具名。

## 范围

- ⏳ 本设计覆盖命令工具模型可见名称、内部工具 ID、流式工具调用参数解析和命令空参数失败处理。
- ⏳ 本设计不改变命令工具本身的执行权限模型，不新增命令类型。

## 设计要点

- ⏳ 模型可见工具名使用 `builtin_command_run`。
- ⏳ 内部执行和权限继续使用 `builtin.command.run`。
- ✅ 中心服务不再设置 `tool_choice` 强制工具；工具调用提示、工具定义和供应商交互只能使用模型安全名。
- ✅ 供应商返回空工具名时按协议错误失败收尾，只记录 `model.tool_call.name_missing` 诊断事件；诊断事件记录最后一条模型输出摘要和 `rawModelMessage` 原始模型返回摘要；不再按结构化参数、用户提示词关键词或固定命令场景恢复工具名。
- ✅ OpenAI 兼容供应商使用 `OpenAiCompatibleChatCompletionsModel` 承载 Chat Completions；流式工具调用按同一 `index` 或 `id` 缓存首段非空工具名，后续空名称参数片段只保留该同一调用的已返回名称。
- ⏳ LangChain 标准 `tool_call_chunks` 优先。
- ⏳ 供应商原始 `additional_kwargs.tool_calls` 只在标准片段缺失时作为流式片段累积。
- ⏳ 缺少参数的片段不能被默认转换成 `{}`。
- ⏳ 当命令工具返回 `COMMAND_INPUT_EMPTY` 时，本轮对话固化可展示失败回复并结束。
- ⏳ 命令工具空参数失败后，不再把空参数工具结果回填给模型继续循环。

## 验收口径

- ⏳ 模型请求命令工具时只看到 `builtin_command_run`。
- ⏳ 中心服务内部权限和执行链路仍使用 `builtin.command.run`。
- ✅ 流式工具调用首段返回 `builtin_command_run` 且后续参数分片返回空名称时，最终进入 Deep Agents 工具链路的工具名仍为首段真实名称。
- ✅ 空工具名缺失事件使用 `model.tool_call.name_missing` 记录，payload 保存 `toolCallId`、参数键、错误码、模型最后一条输出诊断摘要和 `rawModelMessage` 原始模型返回摘要；不能写入用户原文，也不能恢复为任何工具名。
- ⏳ 流式工具调用只有完整参数对象才会转换为中心服务工具调用。
- ⏳ 空参数命令工具失败固化为可展示失败回复，整轮对话不会反复回填失败结果。

## 清单状态

- ⏳ 命令工具安全名
- ⏳ 流式工具解析
- ⏳ 命令工具失败收尾

# 多端同步与运行中轮次恢复设计

## 目标

- ⏳ 让桌面端、Web端和 IDE 插件端在会话更新、任务状态、标题变化和运行中轮次恢复上保持一致，避免其他端看不到后台会话变化或过早停止恢复。

## 范围

- ⏳ 本设计覆盖发送消息、标题更新、任务状态、失败收尾、多端事件推送和前端运行中轮次恢复。
- ⏳ 本设计不改变中心服务事实表结构之外的业务能力。

## 设计要点

- ⏳ 发送消息、标题更新、任务状态和失败收尾都通过 `event.appended` 与 `session.updated` 推送。
- ⏳ 其他端即使没有打开当前会话，也要刷新会话列表时间、标题和最新状态。
- ⏳ 如果更新命中当前活动会话，前端必须拉取完整会话快照补齐消息、任务和事件终态。
- ⏳ WebSocket 异常断线后必须持续自动重连，不能因为固定重试次数用尽而永久停在“已停止”。
- ⏳ 前端兜底恢复按活动续租设计，不按固定总时长结束。
- ⏳ 每次快照中的事件 `occurredAt`、任务 `updatedAt`、步骤 `startedAt/endedAt` 或轮次 `startedAt/endedAt` 推进时刷新本地最近活动时间。
- ⏳ `TurnStateReconciler` 每次读取 `session.turn.state` 后比较中心服务 `lastSequence` 与前端本地最大事件序号；如果中心服务更大，立即拉取完整会话快照补齐流式片段、工具事件和终态事件。
- ⏳ 只有在长时间无任何活动且仍处于运行态时，才停止本地短间隔恢复并输出控制台日志。
- ⏳ 模型流式输出、命令 stdout/stderr、工具开始/输出/完成、MCP 输出、todoList 步骤更新、步骤切换和后续中心服务心跳都算有效活动。
- ⏳ 心跳只表示执行器存活，不能冒充真实进度或助手回复。
- ⏳ 中心服务新增 `session.turn.state` 轻量轮次状态查询，返回当前会话运行中轮次、终态、最近事件序号、最近活动时间和最近助手消息时间；该查询只用于运行态事实对账，不拉取完整 `messages/events/tasks`。
- ⏳ 轮次状态写入终态时，在既有 `turn.updated` 后追加统一 `turn.state.changed` 事件，payload 直接携带 `sessionId`、`turnId`、`status`、`endedAt`、`durationMs`、`taskId` 和 `lastSequence`，让所有客户端不依赖完整快照也能收敛终态。
- ⏳ 前端新增 `TurnStateReconciler` 管理单会话单轮次状态机，状态流转为 `idle -> optimisticRunning -> confirmedRunning -> suspectedStale -> terminal`；确认运行态使用低频轻量对账，疑似卡住阶段以 `20ms` 间隔请求 `session.turn.state`。
- ⏳ `TurnStateReconciler` 在收到 `message.created` 助手消息、`turn.updated`、`turn.state.changed`、`task.updated` 终态、等待用户状态或完整快照终态后停止恢复；完整快照只负责补齐消息、事件和任务，运行态是否继续展示由轻量状态事实决定。
- ⏳ 中心服务 `close` 和下次 `initialize` 都要执行运行中轮次补偿收尾：把上一进程遗留的 `queued`、`running`、`waiting_user` 轮次、任务和步骤统一改成 `cancelled`，补齐结束时间、耗时、摘要和终态事件，避免数据库长期保存“僵尸运行中轮次”。
- ⏳ 前端只把当前进程之后仍有活动推进的轮次视为可恢复运行轮次；如果轮次开始时间明显早于中心服务当前进程启动时间，且快照里没有任何新活动推进，则判定为陈旧轮次并停止本地恢复，不再展示“当前轮次已耗时”和“停止”按钮。

## 验收口径

- ⏳ 任一端发送消息后，其他端的会话列表能同步更新时间、标题和最新状态。
- ⏳ 当前活动会话收到更新后能补齐完整会话快照。
- ⏳ 中心服务重启或网络短暂断开后，页面状态从重连中恢复到已连接，不停留在已停止。
- ⏳ 运行中轮次恢复不会因为固定轮询次数到达而停止。
- ⏳ 当前会话未收到助手消息且轮次仍非终态时，前端进入 `20ms` 疑似卡住快轮询 `session.turn.state`，收到中心服务终态或等待用户状态后“当前轮次已耗时”和“停止”按钮立即消失。
- ⏳ 当前会话漏收 `event.appended` 时，`session.turn.state.lastSequence` 大于本地事件序号会触发完整快照补齐，页面能显示已经落库的流式文本和过程事件。
- ⏳ 多端收到 `turn.state.changed` 后，即使未拉取完整快照，也能把对应轮次收敛到完成、失败、取消或等待用户状态。
- ⏳ 完整会话快照缺少新消息时不会让前端永久保持运行态；运行态展示必须跟随 `session.turn.state` 和终态事件收敛。
- ⏳ 心跳不会被展示成真实任务进度或助手回复。
- ⏳ 中心服务退出后再次启动，历史会话中不会继续保留上一进程遗留的运行中轮次、任务和步骤。
- ⏳ 已有脏数据会在中心服务启动补偿或前端陈旧轮次熔断后消失，不会继续占据输入区上方的运行中提示。

## 清单状态

- ⏳ 多端同步
- ⏳ WebSocket 持续重连
- ⏳ 运行中轮次恢复
- ⏳ 轻量轮次状态事实源
- ⏳ `TurnStateReconciler` 状态收敛
- ⏳ 超长期任务活动信号
- ✅ 运行中轮次关闭收尾
- ✅ 陈旧轮次前端熔断

# 对话过程展示边界设计

## 目标

- ⏳ 让对话页面只展示模型真实内容和真实工具过程，不再把中心服务写死的执行阶段文案、内部占位信息或无模型语义来源的摘要渲染到消息流里。

## 范围

- ⏳ 本设计覆盖服务端过程事件产出边界、前端过程卡片过滤和 Chrome DevTools 真实复现验收。
- ⏳ 本设计不改变输入区上方“当前轮次已耗时”的前端状态展示，也不改变真实工具结果的展示能力。

## 设计要点

- ⏳ 对话消息流只允许展示四类内容：模型自然语言说明、模型真实请求的工具调用、工具真实结果、基于这些结果生成的最终回复。
- ⏳ 服务端不能把 `Deep Agents 原生执行已开始，等待响应`、内部图执行开始、内部占位等待说明等写死文案作为用户可见过程卡片输出。
- ⏳ 前端过程卡片标题允许做中文化，但只能基于真实工具调用名称转换，例如“执行命令”“读取文件”“编辑文件”，不能展示中心服务自造阶段名。
- ⏳ 前端对历史或兼容事件增加兜底过滤：没有模型语义来源、没有真实工具调用上下文、只有内部开始/等待占位文本的过程卡片不渲染。
- ⏳ 输入区上方“当前轮次已耗时”保留，因为它属于前端轮次状态展示，不属于对话消息流内容。
- ⏳ 浏览器验收必须使用 Chrome DevTools 真实发送消息，确认页面无响应问题和错误过程卡片问题同时消失。

## 验收口径

- ⏳ 页面不再出现“Deep Agents 原生执行已开始，等待响应”或同类中心服务写死文案。
- ⏳ 页面仍能展示模型真实说明文字、真实工具调用和真实工具结果。
- ⏳ 输入区上方“当前轮次已耗时”继续保留。
- ⏳ 用 Chrome DevTools 真实发送消息时，对话不会只停留在写死过程卡片而没有后续响应。

## 清单状态

- ✅ 过程事件边界收口
- ✅ 前端过程卡片过滤
- ✅ Chrome DevTools 真实验收

# Agent 任务监督与实时过程渲染设计

## 目标

- ⏳ 把 Deep Agents 单次 graph 停止和致心任务完成标准拆开，避免模型只返回过程文本或非法工具形态时被中心服务误判为完成。
- ⏳ 让模型中途文本、工具请求、工具执行和工具结果事件在运行中实时进入前端消息流，不等最终助手消息或轮次终态才展示。

## 范围

- ⏳ 本设计覆盖 Deep Agents 执行收尾、候选终态判断、监督续跑预算、失败或等待用户收尾、前端事件合并和快照覆盖保护。
- ⏳ 本设计不覆盖供应商协议重写，不从普通文本恢复工具调用，不按具体 MCP 工具名或用户提示词硬编码调度。

## 设计要点

- ⏳ Deep Agents 自身按 LangGraph/ReAct 语义运行：模型返回结构化 `tool_calls` 时继续工具循环，模型没有返回结构化 `tool_calls` 时本次 graph 结束。
- ⏳ 中心服务新增任务监督层，把本次 graph 结束视为候选结果，再根据模型文本、模型诊断、工具事件、任务状态和预算判断完成、续跑、重试、等待用户或失败。
- ⏳ 任务监督层默认预算 `maxSupervisorAttempts`、`continuationRetryBudget` 和 `toolFailureRetryBudget` 全部固定为 `6`。
- ⏳ 监督层记录最近一次消耗预算的失败原因；下一次候选如果已经脱离该失败原因，则重置当前预算窗口和分类预算计数，相当于把三个预算恢复为 `6`。
- ⏳ 预算重置只依据候选诊断和真实工具失败事件，不依据用户提示词、自然语言关键词或具体工具名称。
- ⏳ 完成判定遵循 LangChain ReAct 语义：有结构化 `tool_calls` 时工具循环继续；没有结构化 `tool_calls` 且 Deep Agents 最终状态形成 AIMessage 时，`AgentCompletionGate` 返回 `REACT_FINAL_MESSAGE_READY` 并允许固化最终助手消息。
- ⏳ 工具调用质量问题不由完成网关解析自然语言解决；如果模型只说“我准备调用工具”但没有 `tool_calls`，中心服务把它视为模型真实最终回复，同时通过提示、工具定义和协议诊断改进后续工具调用行为。
- ⏳ 系统提示前置约束模型：需要继续使用工具时，可以在同一条 assistant 消息中输出简短过程文本，但必须同时返回结构化 `tool_calls`；禁止用普通文本、Markdown、JSON 文本或 text content block 里的 `id`、`name`、`args` 代替工具调用。
- ⏳ 系统提示前置约束模型：用户明确要求浏览器、命令、文件或 MCP 能力时，优先调用对应外部工具推进任务；todo 或计划工具只能辅助记录，不得替代真实浏览、读取、点击或执行。
- ⏳ 模型中途过程文本正常写入 `model.stream.delta` 并展示为过程 Markdown，但不能创建最终助手消息、不能触发记忆写入、不能完成任务。
- ⏳ 模型把 `id`、`name`、`args` 等工具字段夹带在 `type = text` 的 content block 中且没有结构化 `tool_calls` 时，`CenterToolChoiceMiddleware` 标记 `hasMalformedTextToolCallBlock`，`AgentCompletionGate` 返回 `MALFORMED_TEXT_TOOL_CALL_BLOCK` 并按续跑预算要求模型改用结构化 `tool_calls`；中心服务不执行文本字段里的工具名或参数。
- ⏳ 前端收到当前会话 `event.appended` 后立即合并事件并重算过程卡片；完整快照与实时事件按 `eventId` 和 `sequence` 合并，不能用旧快照覆盖新事件。
- ⏳ 前端实时事件合并后刷新 `events` 新数组引用并递增 `eventsRevision`，避免计算链路等到最终快照才显示过程事件。

## 验收口径

- ⏳ Deep Agents 工具结果回填后，如果模型没有继续返回结构化 `tool_calls` 而是返回最终 AIMessage，中心服务按 ReAct 语义固化该 AIMessage；若该文本质量不符合预期，通过工具协议诊断和提示约束修正，不在完成网关里解析文本。
- ⏳ 普通自然语言不会被按关键词解析；只有原始模型消息结构中出现 `text content block + id/name/args + 无 tool_calls` 的协议错误形态时，完成网关才按 `MALFORMED_TEXT_TOOL_CALL_BLOCK` 续跑。
- ⏳ 畸形 text 工具块不会被恢复为工具调用，不会执行其中的工具名和参数，续跑耗尽后进入等待用户或失败。
- ⏳ 监督预算耗尽后，当前轮次收敛为等待用户或失败，不会长期停留在运行中。
- ⏳ 同一失败原因连续出现时预算持续消耗；候选结果脱离上一类失败后，三个监督预算窗口都恢复为 `6`。
- ⏳ 模型已经给出满足用户目标的最终总结时，中心服务只创建一次助手消息、写一次记忆、完成一次任务和轮次。
- ⏳ 真实工具请求、工具执行、工具结果回填、中途模型 Markdown 都在运行中通过 `event.appended` 出现在前端消息流。
- ⏳ 实时事件进入前端后刷新全局事件数组新引用并递增事件版本号，过程卡片不需要等待最终助手消息或轮次终态快照。

## 清单状态

- ⏳ Deep Agents 候选终态
- ⏳ AgentCompletionGate
- ⏳ DeepAgentTurnSupervisor
- ⏳ 续跑与重试预算
- ⏳ 前端实时事件合并
- ⏳ 浏览器真实验收

# 记忆过滤与轮次失败收尾设计

## 目标

- ⏳ 修复主智能体长期记忆污染、`mem0` 读写未真正启用、轮次缺少一头一尾日志以及工具或 Deep Agents 异常后整轮对话长期卡住的问题；其中记忆过滤、轮次失败收尾、日志边界和页面真实内容展示已完成，`mem0` 仍受当前 embedding 通道可用性影响。

## 范围

- ⏳ 本设计覆盖 `mem0 + memory_index` 双读召回、主智能体长期记忆写入筛选、长期记忆提示读取过滤、Deep Agents 异常失败收尾和控制台轮次边界日志。
- ⏳ 本设计不重做前端消息流结构，不删除用户已有记忆文件，也不扩展新的工具类型。

## 设计要点

- ⏳ 删除“我是谁 / 你叫什么 / 徐志翔 / 致心”这类身份专项提示词硬编码，身份与偏好只能来自真实召回记忆。
- ⏳ `mem0` 读写链路改为显式使用中心服务当前启用供应商配置，不再依赖环境变量默认值，也不再默认关闭。
- ⏳ 主智能体长期记忆读取层同时读取 `mem0` 和 `memory_index` 候选，统一去重、过滤、排序后再注入模型。
- ⏳ 主智能体长期记忆写入前增加通用低质量筛选，只固化稳定用户事实、用户偏好和可信上下文；明显错误回复、回归验收口水和空洞失败回复直接跳过写入。
- ⏳ `runDeepAgentsAgentTurn` 增加统一异常收尾；只要 Deep Agents、StructuredTool、MCP 或 skill 运行时抛错，就立即写入失败事件并把当前轮次与任务收尾为失败。
- ⏳ 控制台事件输出继续压制中间流式噪声，但必须放行 `turn.started` 和 `turn.updated`，保证每轮对话至少有清晰开始和终态日志。

## 验收口径

- ⏳ 用户询问身份、自称或偏好时，模型答案必须来自 `mem0 + memory_index` 真实召回，而不是来自写死提示词。
- ✅ 明显错误的身份类回复不会再进入 Markdown 记忆、SQLite 索引或 Mem0 同步。
- ✅ 工具或 Deep Agents 链路抛错后，前端不会只停留在“当前轮次已耗时”，轮次会进入失败终态。
- ✅ 开发控制台能看到每轮对话清晰的开始和终态日志，仍不会持续刷命令输出块或流式片段。

## 清单状态

- ✅ 移除身份硬编码
- ✅ `mem0 + memory_index` 双读召回
- ✅ 主智能体记忆写入筛选
- ✅ 长期记忆读取过滤
- ✅ Deep Agents 失败收尾
- ✅ 控制台轮次边界日志

# IDEA 插件同步与展示设计

## 目标

- ⏳ 保证 IDEA 插件端的项目授权、实时连接、项目上下文登记和展示口径稳定一致。

## 范围

- ⏳ 本设计覆盖 IDEA 插件端实时连接顺序、本机授权项目 ID、工具窗口与菜单展示文案和图标。
- ⏳ 本设计不扩展新的 IDE 平台，不新增插件设置页。

## 设计要点

- ⏳ 插件端先建立实时连接，再登记或更新项目上下文。
- ⏳ 本机授权携带当前项目 ID，避免项目会话与普通 Web 端同步状态混用。
- ✅ 插件管理器展示名、右侧工具窗口名称、工具窗口内容页签和右键菜单入口统一展示“致心智能体”。
- ✅ 右侧工具窗口和右键菜单 Action 统一引用插件资源内的 16×16 小图标。
- ✅ 图标源来自 `assets/app-icon/图标.png`，避免 IDEA 菜单按原始大图尺寸撑开布局。

## 验收口径

- ⏳ IDEA 插件实时连接建立不被项目登记阻塞。
- ⏳ 本机授权能绑定当前项目 ID。
- ✅ 插件展示名、工具窗口、页签和右键菜单都使用“致心智能体”。
- ✅ IDEA 菜单图标使用 16×16 资源，不撑开菜单布局。

## 清单状态

- ⏳ IDEA 插件同步
- ✅ IDEA 插件展示

# 任务拆解展示与重规划设计

## 目标

- ⏳ 让任务入口、任务面板、任务行和运行中追加需求的重规划展示稳定、紧凑、可追溯。

## 范围

- ⏳ 本设计覆盖底部“任务”入口、任务计划版本展示、工具步骤联动、任务行样式、过程详情位置和运行中追加消息的重规划交互。
- ⏳ 本设计不改变任务事实表以外的 Agent 执行策略。

## 设计要点

- ⏳ 底部“任务”入口只展示当前轮次当前智能体已经拆解出的多步骤任务。
- ⏳ 只有一个默认任务或可见步骤不超过 1 个时，不显示任务数量和默认任务行。
- ⏳ 任务面板按 `taskId` 取最新 `planVersion` 的非 `graph`、非 `superseded` 步骤展示。
- ⏳ 旧计划版本只保留历史事实，不参与当前任务数量、序号和状态计算。
- ⏳ 历史数据中任务主状态已经终态但步骤仍停留在运行中或排队中时，展示层按任务终态修正步骤进度，避免“任务已完成”但入口仍显示 `0/N`。
- ⏳ 命令、MCP 或其他工具过程卡片默认不直接改写可见 todoList 步骤。
- ⏳ 只有中心服务事件或工具参数明确携带目标 `stepId` 时，才把工具结果回写到对应可见步骤状态。
- ⏳ 每个拆解步骤只占一行，左侧显示状态和步骤标题，右侧不显示时间、耗时、`序号/总数` 或任何附加信息。
- ⏳ 任务行右侧不重复状态，不显示第二行摘要、当前动作、责任智能体、失败原因或替换原因。
- ⏳ 任务主状态、过程事件或 `task.updated` 已进入完成、失败或取消终态后，任务步骤卡片、过程卡片和任务浮窗行都必须展示对应终态，不能继续显示“执行中”。
- ⏳ 当前动作、工具调用、命令输出、MCP、skill、子智能体协作、失败原因和替换原因继续在对话消息流或点击详情中体现，不放进任务行。
- ⏳ 运行中用户追加消息必须作为当前轮次引导提交到 `session.guidance.submit`。
- ⏳ 中心服务记录 `task.plan.revised`，并在同一 `taskId` 下保留旧步骤历史和新增步骤。

## 验收口径

- ⏳ 单个默认任务不显示任务数量和默认任务行。
- ⏳ 当前任务面板只展示最新有效计划版本。
- ⏳ 工具结果不会在没有明确 `stepId` 时猜测更新任务步骤。
- ⏳ 任务行保持单行紧凑展示。
- ⏳ 任务浮窗右侧不显示时间、耗时、序号或状态。
- ⏳ 任务和过程完成后展示已完成、失败或已取消终态，不保留执行中文案。
- ⏳ 运行中追加消息产生同一 `taskId` 下的重规划记录，旧步骤保留历史。

## 清单状态

- ⏳ 长任务拆解展示
- ⏳ 任务计划版本展示
- ⏳ 工具步骤联动
- ✅ 任务行样式
- ⏳ 过程详情位置
- ⏳ 重规划交互

# 执行图过程与用户可见任务拆解分离设计

## 目标

- ✅ 修正任务状态“开始、执行、终态”混用问题，让 Deep Agents 执行图节点只表达过程进展，让 `task_steps` 只表达用户可见拆解步骤。
- ✅ 完成后任务浮窗和任务步骤卡片只有对应用户可见步骤真实运行时才显示“执行中”，完成、失败或取消后展示终态。

## 范围

- ✅ 本设计覆盖中心服务图节点事件、todoList 步骤创建、用户引导重规划、任务状态副作用和前端过程聚合。
- ⏳ 本设计不重写 Deep Agents 执行内核，不改变供应商模型协议，不新增独立任务队列表。

## 设计要点

- ✅ `task_steps` 只保存用户可见拆解步骤，来源限定为 todoList、用户引导、模型计划或系统续跑中明确要展示给用户的计划项。
- ✅ Deep Agents 的 thinking、model、tool plan 等图节点不再调用 `createTaskStep` 或 `updateTaskStep`，改写 `graph.node.started`、`graph.node.completed` 或 `graph.node.failed` 内部审计事件，并在 `payload.graph` 中保留恢复检查点；这些固定节点事件不进入用户可见过程卡片。
- ✅ todoList 新建拆解步骤时按模型声明状态直接创建，默认是 `queued`，不能先创建成 `running` 再改回 `queued`。
- ✅ 领域层新增计划步骤创建入口；该入口不隐式把任务主状态改成 `running`，任务主状态只由 worker/task 生命周期入口更新。
- ✅ 前端过程卡片只消费模型返回、模型 tool_calls、工具执行输出、MCP、skill、命令和子智能体协作等真实过程事件；`graph.node.*` 只用于审计和恢复，不作为 UI 可见文字来源。
- ✅ 任务浮窗行右侧不显示时间、耗时、序号、状态或其他过程详情。

## 验收口径

- ✅ 一轮对话产生的 graph 节点事件只在审计事件中保留开始与终态，不生成 `source = graph` 的 `task_steps`，也不在用户可见过程卡片中展示固定节点文案。
- ✅ todoList 生成的新步骤如果状态为 `queued`，数据库和事件中都不会先出现同一步骤的 `running` 启动事件。
- ✅ 用户补充引导仍能在同一 `taskId` 下重规划并追加用户可见步骤，旧步骤按需进入 `superseded`。
- ✅ 任务卡片、过程卡片和任务浮窗只在真实运行态显示“执行中”；开始事件只负责打开卡片，执行中内容来自模型或工具流式数据，终态事件到达后展示完成、失败或取消。

## 清单状态

- ✅ 需求事实源
- ✅ 图节点过程事件
- ✅ 用户可见步骤创建
- ✅ 前端过程聚合
- ✅ Chrome DevTools 验收

# 对话过程卡片与模型中途文字设计

## 目标

- ⏳ 让命令、工具、MCP、skill、智能体协作和模型中途文字在对话消息流中按调用维度清晰展示，避免过程信息被拆散或错误合并。
- ⏳ 修复同一 MCP 工具调用中完成输出与模型回填结果重复展示的问题，确保同一 `toolCallId` 只形成一张正文去重后的过程卡片。
- ⏳ 修复模型只返回继续执行意图但没有结构化工具调用时被错误固化为完成回复的问题。

## 范围

- ⏳ 本设计覆盖命令过程卡片、主对话过程提示和非工具过程的模型中途文字展示。
- ⏳ 本设计不改变供应商模型输出内容本身。

## 设计要点

- ⏳ 前端按同一 `toolCallId` 聚合 `model.tool.requested`、`tool.plan.created`、`tool.command.started`、`tool.command.output`、`tool.command.completed`、失败事件和 `model.tool.result.appended`。
- ⏳ 同一张卡片承载开始、执行中、完成和失败状态。
- ⏳ 同一工具调用中，真实工具完成输出优先作为卡片正文，模型回填结果只表示回填状态，不重复展示同一结果正文。
- ⏳ 工具计划事件、工具执行事件和模型回填事件必须共享同一工具调用 ID；只有运行时没有模型调用 ID 的兼容路径才允许生成本地 ID。
- ⏳ 命令卡片内容区按 bash 形态展示，第一行是真实命令，后续换行直接追加流式执行过程。
- ⏳ 命令卡片不展示“原始命令”“执行结果”或内部过程时间线小标题。
- ⏳ 前端按会话轮次聚合工具、命令、MCP、skill 和智能体协作事件。
- ⏳ 同一 `toolCallId` 的请求、计划、执行、输出、完成、失败和结果回填展示在同一张过程卡片内部。
- ⏳ 所有非工具过程的模型中途文字独立渲染为无标题 Markdown 卡片，流式追加。
- ⏳ 模型中途文字不并入工具卡片或最终回复。
- ⏳ 模型只返回“我先看 / 我将继续 / 我改用工具”等继续执行意图但没有结构化工具调用时，中心服务写入 `message.turn.incomplete` 并失败收尾，不固化助手最终回复，不写入长期记忆。

## 验收口径

- ⏳ 同一工具调用全过程只形成一张过程卡片。
- ⏳ MCP 卡片不会把 `tool.mcp.completed.outputSummary` 和 `model.tool.result.appended.resultSummary` 重复拼接为两份相同正文。
- ⏳ 工具计划、工具执行和结果回填使用同一聚合 ID。
- ⏳ 命令卡片首行展示真实命令，后续直接展示流式输出。
- ⏳ 非工具过程的模型中途文字独立展示，不和工具卡片或最终回复混在一起。
- ⏳ 半截工具意图不会进入 `completed` 轮次，不会生成助手最终消息。

## 清单状态

- ⏳ 命令过程卡片
- ⏳ 主对话过程提示
- ⏳ MCP 卡片去重
- ⏳ 半截工具意图收尾

# 中心服务日志设计

## 目标

- ✅ 统一中心服务控制台日志和文件日志口径，方便人和 AI 做 SOP 审查，同时避免控制台输出持续刷屏。

## 范围

- ✅ 本设计覆盖中心服务文件日志命名、单文件大小切换、开发控制台输出范围。
- ✅ 本设计不改变业务事件事实源结构。

## 设计要点

- ✅ 文件日志使用中心服务本机时间生成 `center_YYYY_MM_DD_HH_mm_ss.log` 命名；同一天内优先复用最后一个未达到 `1MB` 的日志文件，只有达到 `1MB` 才轮转；跨天后即使旧文件未满也必须创建新的当天日志文件。
- ✅ 单文件超过 `1MB` 前切换新文件。
- ✅ 开发控制台保留命令启动和结束这类一头一尾日志。
- ✅ 开发控制台不输出命令输出块、运行中轮次等持续中间态。
- ✅ 开发控制台保留失败、终态和关键审计节点。
- ✅ Windows 开发启动链路不在用户启动命令前拼接 `chcp 65001`；开发编排脚本接管 Electron 和中心服务输出，按 UTF-8 解码后交给当前 Node 控制台输出，并清理 `ELECTRON_RUN_AS_NODE`，确保控制台中文按原文显示。

## 验收口径

- ✅ 日志文件名使用中心服务本机时间。
- ✅ 单个日志文件不超过 `1MB` 后继续追加。
- ✅ 控制台只展示一头一尾、失败、终态和关键审计节点。
- ✅ 控制台中文按 UTF-8 原文显示，不显示 mojibake 或 `\uXXXX` 编码。

## 清单状态

- ✅ 中心服务日志

# Deep Agents 执行内核迁移设计

## 目标

- ⏳ 把中心服务当前自研模型循环、工具调度、todoList 规划和子智能体委派迁移到 Deep Agents 执行内核；迁移后 Deep Agents 已提供的相同功能不再保留中心服务自研主路径或内联工具包装。

## 范围

- ⏳ 本设计包含 Deep Agents 依赖接入、中心服务执行 runner 适配、模型与工具桥接、todoList 包装移除、子智能体委派映射、事件适配和未使用旧文件清理。
- ⏳ 本设计不改变中心服务唯一事实源、安全边界、多端同步协议、供应商配置管理、MCP/skill 配置管理、Markdown 记忆事实源和 SQLite 事件事实源。

## 设计要点

- ✅ `services/center` 安装精确版本 `deepagents`，并继续通过 pnpm workspace 管理依赖。
- ✅ 新增 Deep Agents 执行 runner，使用 Deep Agents 创建当前轮次 agent graph，并先复用中心服务现有节点执行器承接事实源、工具闭环、消息固化、记忆和用量收尾。
- ✅ 中心服务把供应商配置解析出的 LangChain ChatModel 传入 Deep Agents，不让 Deep Agents 直接读取供应商密钥或客户端配置。
- ✅ 中心服务统一工具注册表继续生成模型可见工具；Deep Agents 工具只调用中心服务包装后的命令、MCP、skill、创建长期智能体、创建子智能体和 team 工具。
- ✅ Deep Agents 的原生 todoList 能力由 Deep Agents 自身维护；中心服务已删除 `builtin.deepagents.write_todos` 包装工具、旧 `builtin.todo.list` 兼容入口和 `deepagents-todo-tool.ts` 同步执行器。
- ✅ Deep Agents runner 不再保留 `thinking.context` 节点；中心服务不再用固定上下文整理事件冒充模型思考，真实思考只来自供应商公开 thinking/reasoning 摘要。
- ✅ Deep Agents `tool.execute` 节点直接承载工具执行和工具结果回填；旧独立 `tool.result` 节点、命令工具按用户文本规划入口和统一工具按用户文本规划入口不再保留。
- ⏳ Deep Agents 的 subagents 能力适配到当前 `SubAgent` 运行记录，子智能体创建、父级模型配置继承、状态事件和子对话边界仍由中心服务落库。
- ⏳ Deep Agents 文件系统能力默认使用受控虚拟工作区；真实文件读写只能通过中心服务文件/命令/编辑工具走权限、审批和审计。
- ⏳ Deep Agents 运行事件必须适配为现有 `model.stream.*`、`model.tool.*`、`tool.*`、`task.step.*`、`agent.*`、`message.*` 和失败事件，前端过程卡片协议不因内核迁移而失效。
- ✅ 已确认旧 `langgraph-runner.ts` 不再作为真实调用方依赖，并删除旧 runner 文件；保留的检查脚本只允许断言旧入口不得回归。

## 验收口径

- ✅ `services/center/package.json` 包含精确版本 `deepagents` 依赖。
- ✅ 中心服务主执行路径通过 Deep Agents runner 发起当前轮次执行，不再以旧手写 `runLangGraphTurn` 作为主路径。
- ✅ Deep Agents todoList 不再通过中心服务内联工具同步为 `task_steps`；用户可见任务步骤只由明确的模型计划、用户引导或系统续跑入口写入。
- ✅ 自研思考上下文节点已移除，非供应商公开思考不会生成 thinking.* 正文事件。
- ✅ 工具执行和工具结果回填已归入 Deep Agents 工具节点，旧 `tool.result` 节点和用户文本工具规划入口不会继续作为运行入口。
- ⏳ Deep Agents subagents 委派能生成中心服务子智能体运行记录、状态事件和过程卡片。
- ⏳ 命令、MCP、skill 和文件副作用仍经过中心服务权限审批、审计事件、过程卡片和 WebSocket 同步。
- ✅ 未使用的旧 `langgraph-runner.ts` 已删除；保留执行文件均有中心服务真实调用方或回归检查职责。

## 清单状态

- ✅ Deep Agents 依赖接入
- ✅ Deep Agents runner
- ✅ 主路径切换
- ⏳ 模型桥接
- ⏳ 工具桥接
- ✅ todoList 包装移除
- ✅ 思考节点移除
- ✅ 工具节点回填
- ⏳ 子智能体映射
- ⏳ 事件适配
- ✅ 旧文件清理

# Deep Agents 原生执行替代中心服务执行图设计

## 目标

- ✅ 删除中心服务 `deepagents-runner.ts` 和自建 `StateGraph` 会话执行图，让当前轮次直接由 Deep Agents 原生 agent 执行。
- ⏳ 把中心服务现有工具统一迁为 Deep Agents middleware 注入或 `StructuredTool` 类工具实现，不再保留图节点式工具编排壳。

## 范围

- ⏳ 本设计覆盖中心服务执行入口、工具实现形态、工具注入方式和旧执行图清理。
- ⏳ 本设计不改变中心服务唯一事实源、安全边界、事件表结构、消息表结构和多端同步协议。

## 设计要点

- ✅ 删除 `services/center/src/deepagents-runner.ts`，不再保留中心服务自建 `StateGraph`、`tool.execute`、`message.persist`、`memory.commit`、`tool.plan`、`usage.record`、`failure.close` 等执行图节点壳。
- ✅ `session-domain` 不再组装图节点执行器，也不再调用 `runDeepAgentsTurn`；当前轮次直接进入 Deep Agents 原生 agent 调用入口。
- ✅ 工具能力统一改成两类实现：一类通过 `services/center/src/AgentMiddleware` 下继承 `CenterAgentMiddleware` 抽象基类并返回 LangChain 官方 `AgentMiddleware` 的 Deep Agents middleware 注入，一类通过 `StructuredTool` 类工具暴露给模型。
- ✅ `command`、`mcp` 和 agent/team 领域能力继续由中心服务领域函数执行；Deep Agents 工具只负责参数收敛、权限校验请求和结果回填，不直接写核心事实源。
- ✅ 旧 `tool-openai-adapter`、统一工具意图转换、图节点回填辅助和只服务旧执行图的事件桥要逐步删除或收缩为工具元数据层。
- ✅ 中心服务保留消息持久化、记忆写入、用量记录、失败收尾、审计事件和 WebSocket 广播，但这些能力改为由 Deep Agents 工具、中间件或回调驱动，而不是由中心服务执行图节点驱动。

## 验收口径

- ✅ `services/center/src/deepagents-runner.ts` 已删除，中心服务源码中不再存在会话主执行 `StateGraph`。
- ✅ `session-domain.ts` 不再依赖 `runDeepAgentsTurn` 或图节点执行器类型。
- ✅ 中心服务工具以 middleware 或 `StructuredTool` 形式对 Deep Agents 暴露，不再通过旧图节点工具循环适配。
- ✅ 命令、MCP、agent/team 领域能力仍保留中心服务权限、安全、审计和持久化边界。
- ✅ 文档、代码和真实执行入口一致，不再出现“中心服务执行图壳仍存在”的旧口径。

## 清单状态

- ✅ 执行图删除
- ✅ 执行入口切换
- ✅ middleware 工具迁移
- ✅ StructuredTool 工具迁移
- ✅ 旧适配层清理
- ✅ 边界能力保留

# Deep Agents 工具三层重构设计

## 目标

- ✅ 把 `services/center/src/StructuredTool` 当前核心工具重构为明确三层结构：`StructuredTool` 类、纯执行器 helper 和工具注入工厂。
- ✅ 删除旧 `command-tool.ts`、`mcp-tool.ts` 一类“执行函数即工具本体”的旧壳，同时清理 `UnifiedToolCallIntent` 转换口。

## 范围

- ✅ 本设计覆盖 `command`、`mcp`、长期智能体、子智能体、team 相关工具，以及对应模型工具定义、管理页读取口、middleware 注册与统一出口。
- ⏳ 本设计不改中心服务事件表结构、SQLite 表结构、WebSocket 协议和前端 UI 协议。

## 设计要点

- ✅ 每个核心工具拆成三层：`*-structured-tool.ts` 只放 `class extends StructuredTool`；`*-tool-executor.ts` 只放执行逻辑、事件写入和结果整理；`DeepAgentsToolFactory.ts` 只负责当前轮次工具实例装配。
- ✅ `command`、`mcp`、长期智能体、子智能体和 team 工具都不再把执行函数文件当作主出口，工具主出口统一为 `StructuredTool` 类文件。
- ✅ `mcp` 管理页读取保留只读工具查看能力；Deep Agents MCP 调用主路径改为官方 `@langchain/mcp-adapters`，不再由中心服务维护动态工具短名注册表和模型可见 MCP specs。
- ✅ MCP adapter 工具模型可见名由官方 adapter 负责生成；中心服务通过 wrapper 统一记录权限、审计事件、结果摘要和失败原因。
- ✅ Deep Agents MCP 对话主链路拆为 `McpToolProvider`、`McpToolWrapperStructuredTool` 和 `McpToolResultNormalizer`：Provider 只负责配置读取、官方 `MultiServerMCPClient.getTools()` 和 cleanup 注册；StructuredTool 只负责中心服务 `StructuredTool` 权限、审计、IDEA 项目路径补参和失败收尾；ResultNormalizer 只负责模型文本、UI 摘要和审计 artifact 规范化。
- ✅ `DeepAgentsToolFactory.ts` 只承担 Deep Agents 当前轮次工具工厂职责，不使用 middleware 命名，也不内嵌 MCP client 创建、`getTools()`、IDEA 参数补全或结果规范化细节。
- ✅ MCP adapter 返回的 `structuredContent` 和 `artifact` 只进入审计 payload，不再要求与 MCP Server 声明输出 schema 匹配后才允许轮次继续。
- ✅ `tools/index.ts` 只保留统一出口职责，不再继续暴露旧壳式工具文件。
- ✅ `commandRequestFromUnifiedToolIntent`、`mcpRequestFromUnifiedToolIntent` 和同类旧统一工具意图转换口直接删除，不保留兼容转发文件。
- ✅ 所有新的 `StructuredTool` 类继续通过中心服务事件、权限和持久化边界执行业务，不允许绕过中心服务事实源。

## 验收口径

- ✅ `services/center/src/StructuredTool` 当前核心工具文件已经按三层结构拆分完成，不再由 `command-tool.ts`、`mcp-tool.ts` 这类旧壳承担工具本体职责。
- ✅ Deep Agents middleware 只依赖 `StructuredTool` 类实例注入当前轮次工具。
- ✅ 管理页和模型工具定义读取不再耦合在工具执行文件中。
- ✅ `UnifiedToolCallIntent` 的工具级转换口已从 `services/center/src/StructuredTool` 删除。
- ✅ 文档、静态检查脚本和真实代码结构一致。

## 清单状态

- ✅ StructuredTool 类分层
- ✅ 执行器 helper 分层
- ✅ middleware 注册分层
- ✅ MCP specs/view 分层
- ✅ 旧壳删除
- ✅ 旧转换口删除
