# 智能体迁移与工具权限设计

- ⏳ 目标：把中心服务智能体执行边界接入 `services/center/src/agents`，让主智能体、长期智能体和子智能体通过同一套类层级声明身份、工具权限、外部 MCP、命令工具和内部工具可用范围。
- ⏳ 范围：本轮只迁移智能体类型裁决、工具能力过滤和创建类工具权限，不重写 LangGraph 执行图、不改变供应商模型调用协议、不新增外置插件系统。
- ⏳ 智能体目录边界：`services/center/src/agents` 承载 `BaseAgent`、`MainAgent`、`LongTermAgent`、`SubAgent` 以及工厂和工具策略；`domain` 继续保留数据写入、会话、记忆和协作领域函数；`tools` 继续保留具体工具执行器。
- ⏳ 权限规则：所有智能体都可以按会话、项目、MCP、skill、内联工具、用户授权和执行模式裁决后请求外部 MCP、命令工具、todoList 和内部工具；创建智能体和 team 管理工具必须再叠加智能体类型边界，todoList 不归入创建类工具。
- ⏳ 主智能体权限：`MainAgent` 可以注入创建长期智能体、创建子智能体和四个 team 管理工具，并可使用普通命令、MCP、skill 和内部工具。
- ⏳ 长期智能体权限：`LongTermAgent` 可以注入创建子智能体工具，并可使用普通命令、MCP、skill 和内部工具；长期智能体不能使用 team 创建、解散、添加成员和移除成员工具。
- ⏳ 子智能体权限：`SubAgent` 可以使用普通命令、MCP、skill 和内部工具，但不能注入创建长期智能体、创建子智能体或 team 管理工具。
- ✅ 工具过滤：模型可见工具列表必须先从统一工具注册表和项目 MCP 动态工具生成，再通过当前智能体实例过滤；统一工具 ID 通过表驱动映射为智能体权限名，MCP 动态工具继承 MCP 权限边界，拒绝规则写入中文注释，避免调用点用字段兜底猜测权限。
- ✅ 智能体时间：长期智能体创建/更新、子智能体运行记录、team 创建/成员变更、智能体运行状态、智能体域记忆索引和事件审计时间统一使用中心服务本机时间字符串，格式为 `YYYY-MM-DD HH:mm:ss`。
- ⏳ 需求文档：`需求.md` 只保留一个从头到尾的清单列表，不再按多个二级或三级块组织；原有产品需求语义保留，新增智能体迁移和权限边界要求进入同一清单。
- ⏳ 无用导入和 URL：删除静态检查能确认无引用的导入、无用 URL 和空文件；仅用于真实开发入口或架构说明的 URL 保留。
- ⏳ 命令工具安全名：模型可见工具名使用 `builtin_command_run`，内部执行和权限继续使用 `builtin.command.run`；`tool_choice`、工具调用提示和供应商交互只能使用模型安全名。
- ⏳ 流式工具解析：LangChain 标准 `tool_call_chunks` 优先，供应商原始 `additional_kwargs.tool_calls` 只在标准片段缺失时作为流式片段累积；缺少参数的片段不能被默认转换成 `{}`。
- ⏳ 命令工具失败收尾：当命令工具返回 `COMMAND_INPUT_EMPTY` 时，本轮对话固化可展示失败回复并结束，不再把空参数工具结果回填给模型继续循环。
- ⏳ 多端同步：发送消息、标题更新、任务状态和失败收尾都通过 `event.appended` 与 `session.updated` 推送；其他端即使没有打开当前会话，也要刷新会话列表时间、标题和最新状态；如果更新命中当前活动会话，前端必须拉取完整会话快照补齐消息、任务和事件终态。
- ⏳ 运行中轮次恢复：前端兜底恢复按活动续租设计，不按固定总时长结束；每次快照中的事件 `occurredAt`、任务 `updatedAt`、步骤 `startedAt/endedAt` 或轮次 `startedAt/endedAt` 推进时刷新本地最近活动时间，只有在长时间无任何活动且仍处于运行态时才停止本地短间隔恢复并输出控制台日志。
- ⏳ 超长期任务活动信号：模型流式输出、命令 stdout/stderr、工具开始/输出/完成、MCP 输出、todoList 步骤更新、步骤切换和后续中心服务心跳都算有效活动；心跳只表示执行器存活，不能冒充真实进度或助手回复。
- ⏳ IDEA 插件同步：插件端先建立实时连接，再登记或更新项目上下文；本机授权携带当前项目 ID，避免项目会话与普通 Web 端同步状态混用。
- ⏳ 验收：本轮按用户要求不运行测试和构建；完成后启动 `dev:desktop-shell` 前先关闭旧桌面壳和中心服务进程，使用用户环境中的 Node 与 pnpm 启动服务。
- ⏳ 长任务拆解展示：底部“任务”入口只展示当前轮次当前智能体已经拆解出的多步骤任务；只有一个默认任务或可见步骤不超过 1 个时，不显示任务数量和默认任务行。
- ⏳ 任务计划版本展示：任务面板按 `taskId` 取最新 `planVersion` 的非 `graph`、非 `superseded` 步骤展示；旧计划版本只保留历史事实，不参与当前任务数量、序号和状态计算。
- ⏳ 工具步骤联动：命令、MCP 或其他工具过程卡片默认不直接改写可见 todoList 步骤；只有中心服务事件或工具参数明确携带目标 `stepId` 时，才把工具结果回写到对应可见步骤状态。
- ⏳ 主对话任务范围：主对话内默认展示主智能体 `agentId = main` 的当前轮次拆解步骤；智能体弹框默认展示当前弹框 `agentId` 的 todoList，不混入主智能体或其他智能体任务。
- ⏳ 任务行样式：每个拆解步骤只占一行，左侧显示状态和步骤标题，右侧只显示 `序号/总数` 与可选耗时；右侧不重复状态，不显示第二行摘要、当前动作、责任智能体、失败原因或替换原因。
- ⏳ 过程详情位置：当前动作、工具调用、命令输出、MCP、skill、子智能体协作、失败原因和替换原因继续在对话消息流或点击详情中体现，不放进任务行。
- ⏳ 重规划交互：运行中用户追加消息必须作为当前轮次引导提交到 `session.guidance.submit`，中心服务记录 `task.plan.revised`，并在同一 `taskId` 下保留旧步骤历史和新增步骤。
