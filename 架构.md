# 架构

electron + vite + vue3 + vue-router + pinia + elementplus

系统不可能只有一个页面，桌面端、Web端和后续页面型插件都必须纳入路由模块管理，统一通过路由组织页面入口、页面跳转和页面级权限边界。

路由页面组件必须使用`() => import(...)`懒加载方式导入，不能在路由表顶部直接静态引入页面组件，避免首屏一次性加载所有页面。

系统页面的公共状态必须使用pinia保存，例如当前主题、中心服务连接状态、当前客户端类型、当前会话、当前项目、登录态、通知状态、执行模式和供应商公共选择等。

桌面端和桌面浏览器页面主要使用elementplus保持统一体验；Web端在手机浏览器访问时，移动端交互和组件使用vant适配，不能把桌面端复杂表格和大屏布局直接塞进手机页面。

桌面端、Web端桌面浏览器和后续插件页面的主工作区 UI 参考 Codex App 的工作台形态：整体窗口、侧边栏、顶部栏、右侧状态栏、对话区和输入区必须使用 flex 布局组织，不能依赖页面级滚动撑开高度。

窗口级布局必须动态适配窗口大小变化。根容器固定为当前视口高度，页面主体禁止出现全局滚动条；侧边栏、顶部栏、右侧状态栏和输入框区域保持在可视区域内，只有对话消息列表区域允许纵向滚动。

需要展示较多状态、任务、供应商或智能体信息时，优先通过 flex 子容器压缩、换行或内部固定区域展示，不允许让整个页面随内容滚动。确实需要局部列表滚动时，必须把滚动限制在该列表自身，并保证对话消息区仍是主滚动区域。

桌面端窗口不展示 Electron 原生菜单栏。桌面端应用内头部菜单用于切换“对话、供应商、网络代理、运行环境、用量统计、中心服务”六个主页面，连接状态放在头部区域。

中心服务启动、停止和重启属于桌面端本机进程管理能力，只在桌面端“中心服务”页面展示和操作；Web端和插件页面不提供中心服务启停入口。

桌面端对话输入框参考 Codex App 的圆角输入面板形态，权限下拉框、执行模式下拉框、推理程度下拉框、发送和停止按钮放在输入框底部操作栏中；输入框占位符单行展示“/ 搜索 skill  @ 引用项目文件”；执行模式使用“建议模式、自动编辑、全自动”中文选项，不占用顶部栏空间。

桌面端自定义交互按钮统一使用 Element Plus 的 `el-button` 默认样式，不在按钮上配置图标；需要表达选中状态时优先使用 Element Plus 按钮类型。

桌面端应用内头部不展示额外品牌块，左侧直接从主页面菜单开始，避免头部出现重复应用身份信息。

桌面端对话页面左侧只展示对话导航，不再混入供应商、网络代理和运行环境入口。对话导航分为“项目对话”和“普通对话”：项目对话按项目分组展示并支持收起/展开，普通对话以列表形式展示。

桌面端对话页面右侧状态栏只展示任务状态和智能体状态，不展示中心服务配置摘要；中心服务配置和进程状态统一进入“中心服务”页面。

桌面端和 Web端的错误、警告和临时通知统一使用 Element Plus 的 `ElMessage` 触发式提示，不使用 `el-alert` 占用页面布局。

桌面端和 Web端必须同时引入 Element Plus 基础样式和官方暗黑主题变量样式，主题切换时通过 `html.dark` 让 `el-table`、`el-select`、`el-button`、`el-empty` 和弹层组件使用 Element Plus 官方暗黑配色。

大模型回答返回的Markdown内容使用GitHub Markdown样式渲染，需要兼容暗黑和亮色主题。

版本在package.json中要固化

系统需要有自己的应用图标，文件名固定为`图标.png`，图标风格需要贴合“致心智能体”的名称和智能体应用定位。

`图标.png`用于Electron应用图标、系统托盘图标、任务栏图标、窗口图标和安装包图标。

对话框中的工具调用、文件读取、文件写入、文件删除等消息过程图标独立存放在项目根目录`界面图标`文件夹中，避免和应用主图标混用。

`界面图标/mcp调用.svg`用于表示MCP工具调用或MCP能力执行过程。

`界面图标/读取文件.svg`用于表示读取项目文件、加载文件内容或文件上下文引用过程。

`界面图标/写入文件.svg`用于表示写入、创建或修改项目文件过程。

`界面图标/删除文件.svg`用于表示删除项目文件过程。

界面图标使用SVG和`currentColor`，由桌面端、Web端和插件页面根据当前主题统一控制颜色。

## 代码目录约定

代码层面需要按客户端和SDK拆分顶层目录，桌面端以及其他UI端都必须放在各自独立文件夹中，不能把不同端代码混在同一个根目录下。

目录示例：

```md
项目根目录
├─ 桌面端
├─ Web端
├─ IDEA_SDK
├─ 中心服务
└─ 共享
```

目录说明：

- 桌面端：保存Electron桌面端代码、桌面端UI、系统托盘、任务栏、窗口和安装包相关逻辑。
- Web端：保存浏览器Web端代码，需要同时兼容桌面浏览器和手机浏览器；UI体验和桌面端保持一致，但不能包含Electron专属能力。
- IDEA_SDK：保存最终打包安装到IDEA中的插件工程代码。IDEA插件负责连接IDE能力、本机中心服务和插件WebView，WebView统一加载中心服务提供的
  `plugin.html`作为插件页面入口，并向页面提供读取项目路径、项目名称、项目ID和发送上下文等能力。IDEA插件构建使用Gradle
  IntelliJ Platform Plugin，不使用手工`systemPath`引用IDEA安装目录Jar。后续其他IDE插件同理，在各自SDK目录中独立打包安装到对应IDE。
- 中心服务：保存中心服务后端、数据读写、智能体调度、任务执行、记忆、会话和扩展能力管理逻辑。
- 共享：保存各端共用的类型、协议、工具函数、接口定义和渲染组件。

后续如果增加其他UI端或SDK，也必须在项目根目录下新增独立目录，例如独立App形态的`移动端`、`VSCode_SDK`
等。手机浏览器访问属于Web端适配范围，不需要单独拆成移动端目录。

凡是包含`package.json`的目录，都必须使用`pnpm`作为Node.js包管理器，并使用项目统一的pnpm配置。优先使用项目根目录下的`.npmrc`
，让子目录通过向上查找继承配置；如果某个工具链或执行方式无法继承根目录`.npmrc`，则需要在该`package.json`同级目录放置一份内容一致的
`.npmrc`。

项目统一`.npmrc`至少需要包含npm镜像源配置和`save-exact=true`，确保依赖安装来源一致，并且写入`package.json`
时固定精确版本。项目根目录必须提供`pnpm-workspace.yaml`管理Node.js工作区。

TypeScript 配置只服务源码组织和 IDE 识别，不把 TypeScript 单独配置成质量校验门槛，也不在 `package.json` 脚本中配置 `tsc` 或
`vue-tsc`。TypeScript 类型提示、常规诊断和编辑期问题发现交给 IDE 完成；开发运行和打包分别由 `tsx`、Vite 等运行时或打包工具直接处理源码。

## 构建打包约定

中心服务源码保留在项目根目录的`中心服务`目录中，作为独立模块开发和维护。

桌面端构建时，必须把中心服务一起构建并打包进桌面端产物中。用户安装桌面端后，不需要再单独安装或手动启动中心服务。

桌面端负责启动、停止、重启和监控随包携带的中心服务，并继续负责中心服务端口、Web端账号密码和系统通知权限等本机配置。

中心服务默认中心目录不使用系统用户主目录。开发期由桌面端传入工程根目录下的 `中心`；桌面端构建安装后默认使用应用启动位置下的 `中心`。中心目录、Web端访问账号和 Web端访问密码都由桌面端中心服务页面配置，修改后重启中心服务生效。

开发期中心目录虽然位于工程根目录下，但属于运行期数据目录，必须从 Vite 文件监听、TypeScript 项目识别和 IDE 索引范围中排除，避免会话、记忆、附件和日志变化触发开发服务或语言服务反复扫描。

Web端和IDE插件不内置中心服务，只连接桌面端启动并管理的中心服务。

IDEA_SDK构建必须优先接入本地Maven仓库，Gradle仓库配置中`mavenLocal()`必须排在远程仓库之前。

IDEA_SDK远程Maven仓库必须接入阿里云公共镜像`https://maven.aliyun.com/repository/public`，并放在Maven Central等默认远程仓库之前。

IDEA_SDK的Gradle下载缓存由 IDEA Gradle 集成和本机 IDE 配置管理，项目提交文件不能写死个人机器绝对缓存路径。

各 IDE 插件平台的工程名和安装包名必须使用 `zhixin-agent-平台名` 格式，平台名使用小写英文短名，例如 IDEA 平台使用 `zhixin-agent-idea`；后续 VSCode 平台使用 `zhixin-agent-vscode`，其他 IDE 平台按同一规则扩展。

IDEA_SDK 在 IDEA 项目视图、Gradle 工具窗口、普通归档产物和插件安装包 ZIP 中统一使用 `zhixin-agent-idea`，插件安装包 ZIP 按 Gradle 工程名输出为 `zhixin-agent-idea-版本.zip`。

IDEA_SDK 的 Java 编译必须使用 IDEA Gradle JVM 执行，并通过 Gradle `options.release = 21` 固定输出 Java 21 字节码；项目不启用 Gradle toolchain 自动查找或下载 JDK，避免构建脱离 IDEA 集成环境后要求本机额外安装 JDK 21。

IDEA_SDK 当前没有 Settings 配置页时，必须禁用 `buildSearchableOptions`，避免 `buildPlugin` 打包阶段额外启动沙箱 IDE 生成可搜索配置索引并导致构建长时间挂起；后续新增 Settings 配置页时再按实际需求恢复。

## 中心服务连接与访问控制

中心服务需要监听本机服务端口，默认端口为8866。

中心服务端口可以由用户配置。端口配置属于中心服务配置，修改后需要重启或重新启动中心服务后生效。

桌面端负责启动和管理中心服务，也负责配置中心服务端口。

IDE插件连接中心服务时，IP固定为127.0.0.1，只允许连接本机中心服务。IDE插件中可以配置端口，默认端口为8866。

IDE插件最终连接地址格式为：

```md
http://127.0.0.1:端口
```

Web端访问中心服务时，如果访问来源是本机，可以直接进入已授权的本机使用流程。本机访问和非本机访问的判断必须由中心服务根据请求来源、监听地址和连接信息完成，前端不能只依赖
`window.location.hostname`自行判断，否则容易被伪造或绕过。

Web端访问中心服务时，如果不是本机访问，第一次打开页面必须要求输入账号和密码。账号密码验证通过后，中心服务给Web端发放登录态，后续请求使用登录态访问。

Web端登录态必须使用Cookie保存和传递，不能使用`sessionStorage`保存登录 token。Cookie由中心服务设置，并按访问方式配置合适的
`HttpOnly`、`SameSite`、`Secure`和过期时间策略。

Web端账号密码只能在桌面端配置和修改，不能在Web端自己配置账号密码。

账号密码等访问控制信息只保存在中心电脑，由中心服务校验。IDE插件不使用账号密码登录，只使用本机127.0.0.1加端口连接。
