# 致心智能体架构

本文是致心智能体当前工程架构、技术栈、模块边界、数据边界、接口规范、构建交付和实现约束的事实源。产品需求以 `需求.md` 为准，工程边界以本文为准。

## 总体架构

致心智能体采用“个人中心服务内核 + Deep Agents 执行内核 + Worker 执行进程 + 统一前端 + 多宿主壳”的架构。

中心服务运行在用户中心电脑上，是唯一事实源。桌面壳、Web 浏览器、手机浏览器和 IDE 插件都是中心服务客户端，不直接持有智能体、会话、项目、任务、记忆、供应商、内联工具、MCP、skill、用量统计、通知或扩展能力状态。

中心服务主进程负责：

- 中心目录初始化和迁移。
- 配置、会话、项目、任务、记忆、供应商、内联工具、MCP、skill、个人事务领域、用量统计、通知、审计事件和访问控制。
- REST API、WebSocket 实时同步和统一错误格式。
- Worker 子进程生命周期、Deep Agents 任务调度、权限审批和事件落库。
- 内联工具、MCP、skill、LangChain 模型运行时、Deep Agents middleware、StructuredTool 和内置能力包的统一管理。

Agent 执行不放在中心服务主进程。每轮对话、模型调用、工具调用、命令执行、MCP 调用、skill 注入、多智能体协作和记忆归纳由 Worker 子进程内的 Deep Agents 原生 agent 执行。Deep Agents 已提供的 planning/todo、虚拟文件上下文、subagents、上下文管理、工具调度、middleware 和 StructuredTool 能力作为主执行内核；Worker 和 Deep Agents 都不能直接写核心数据文件或 SQLite，必须通过中心服务 IPC 请求能力。本阶段原插件能力都作为中心服务内联工具或一等领域模块参与执行。

桌面壳负责启动、停止、重启和监控中心服务，提供托盘、窗口、系统通知权限检测、本机配置入口和绿色版 zip 打包。Web 和 IDE 插件不提供中心服务生命周期管理能力。

中心服务运行代码只在桌面壳绿色版或安装包构建阶段动态装配到桌面产物中。`apps/frontend` 的 Web 端构建产物只输出 `index.html`、`plugin.html` 和浏览器静态资源，不能包含中心服务入口、better-sqlite3、SQLite 原生驱动、`services/center` 源码相对引用或 Electron 壳专属文件。

桌面壳生产期不直接通过 `file://` 加载前端构建产物。桌面壳启动中心服务后加载中心服务提供的 HTTP 页面，由中心服务托管 `index.html`、`plugin.html` 和 Vite 拆包后的 `assets` 资源，避免 ES module 分包在 file 协议下被浏览器安全策略拦截。

前端开发服务器和桌面壳开发进程分开管理。`dev:frontend` 是前端 Vite 开发服务器的独立启动入口，固定提供 `http://127.0.0.1:5173` 热更新页面；`dev:desktop-shell` 只启动 Electron 桌面壳，并由桌面壳拉起或复用 `http://127.0.0.1:8866` 中心服务，不再启动前端 Vite 服务。执行 `dev:desktop-shell` 前必须先关闭之前的桌面壳和中心服务进程，确保本轮启动加载最新中心服务源码、桌面壳源码和内联能力代码。`dev:desktop-shell` 启动前只检查 `http://127.0.0.1:5173` 是否已经由 `dev:frontend` 提供；未启动前端时应给出明确提示。Windows 开发启动链路不能把 `chcp 65001` 拼到用户启动命令上，开发编排脚本必须接管 Electron 和中心服务子进程输出，按 UTF-8 解码后交给当前 Node 控制台输出，必要时兼容 Windows 本地代码页输出，并在启动 Electron 前清理 `ELECTRON_RUN_AS_NODE`，避免中文日志乱码或 Electron 被当成 Node 运行。Electron 启动后继续传入 `ZHIXIN_FRONTEND_DEV_URL`，让中心服务开发期页面清晰指向 `5173` HMR 入口。桌面壳创建主窗口后，必须先轮询中心服务 `GET /api/health` 并在健康检查通过后再加载 `http://127.0.0.1:端口`，避免中心服务子进程尚未监听时窗口抢先加载导致 `ERR_CONNECTION_REFUSED`。

中心服务生命周期只能由桌面壳管理。开发、测试、验收和演示禁止使用 `dev:center:new`、直接执行 `services/center/src/index.ts` 或其他独立中心服务启动方式；需要最新中心服务代码生效时，必须先停止旧桌面壳管理进程和旧中心服务进程，再通过 `dev:desktop-shell` 重新拉起。该规则用于保证中心目录始终由桌面壳传入，避免在 `services/center/center-data` 等错误位置生成数据目录。

浏览器端验收必须使用 Chrome DevTools 按真实用户操作路径执行。测试时需要通过页面点击、输入、选择、展开、提交、等待反馈和观察界面变化完成验证；静态源码检查、直接调用接口、直接改 DOM 或只读取内部状态不能替代浏览器端人的操作验收。

前端只有一个业务应用，构建多个入口：

```text
index.html：桌面壳、本机 Web、远程 Web、手机 Web
plugin.html：IDE 插件 WebView
```

`plugin.html` 进入 IDE 插件紧凑项目对话模式，只展示当前项目对话、输入框、文件/选区引用、任务状态、智能体状态和必要连接设置。

## 工程目录约定

新版工程使用 pnpm monorepo 和英文路径。文档、注释、UI 文案继续使用中文。

```text
zhixin-agent
├─ apps
│  ├─ frontend
│  │  └─ 唯一业务前端，使用 Vue 3、Vite、Vue Router、Pinia、Element Plus 和 Vant，构建 index.html 和 plugin.html 两个入口。
│  └─ desktop-shell
│     └─ Electron 壳，只负责窗口、托盘、系统通知权限检测、中心服务生命周期、本机配置入口、绿色版 zip 打包和前端资源承载，不保存供应商、会话、记忆、任务等核心事实。
├─ services
│  └─ center
│     └─ 中心服务，使用 Node.js、TypeScript、Fastify、better-sqlite3、pino、moment 和 Deep Agents，包含领域模块、对外 API 文件夹、WebSocket、数据仓储、事件日志、Worker 管理、访问控制、Deep Agents 原生执行入口和内联工具运行管理。
├─ packages
│  ├─ shared
│  │  └─ 跨端共享常量、枚举、协议类型和纯工具。
│  ├─ api-client
│  │  └─ 中心服务 REST API 客户端和 WebSocket 客户端。
│  ├─ ui
│  │  └─ 跨入口复用的消息渲染、输入框、状态卡片、Markdown 渲染和文件引用组件。
│  └─ plugin-sdk
│     └─ 插件开发后续目标的历史 SDK 类型，本阶段不作为运行时依赖新增能力。
├─ plugins
│  ├─ idea
│  │  └─ IDEA 插件工程，使用 Gradle IntelliJ Platform Plugin，产物名为 zhixin-agent-idea-版本.zip。
│  └─ builtin-*
│     └─ 历史插件目录只作为迁移参考；当前阶段能力必须内联到中心服务工具、MCP、skill 或领域模块，插件开发后续再恢复规划。
├─ docs
│  └─ 项目文档、迁移说明和验收材料。
├─ assets
│  └─ 应用图标和界面过程图标资源。
└─ scripts
   └─ 构建、打包、迁移和回归检查脚本。
```

旧 `桌面端`、`Web端`、`中心服务`、`共享`、`IDEA_SDK` 目录只作为历史实现参考。新增开发必须进入新版职责目录。

本阶段不开发插件，`plugins/builtin-*` 不再作为当前架构事实源。模型协议直接使用 LangChain 的 OpenAI 和 Anthropic ChatModel 能力；自动化、浏览器采集、办公集成和资料整理必须内联到中心服务工具、MCP、skill 或一等领域模块。插件开发只保留为后续计划目标。

中心服务源码目录边界：

```text
services/center/src/api：所有对外暴露 API 和 WebSocket 请求处理入口；按 /api/{资源} 拆分文件，例如 auth.ts、project.ts、session.ts、agent.ts，统一由聚合入口注册。
services/center/src/domain：中心服务领域编排模块；按业务域拆分文件并由 domain/index.ts 聚合，不把多个大领域物理合并成单个超大文件。
services/center/src/tools：所有内联工具、中间件注入层和 StructuredTool 工具实现；每个工具单独一个文件，例如 create-long-term-agent-tool.ts、create-sub-agent-tool.ts、create-agent-team-tool.ts，旧图节点适配层完成迁移后必须删除。
services/center/src/agents：智能体基类、主智能体、长期智能体、子智能体派生类、执行工厂和工具权限过滤入口；模型工具可见性、Deep Agents 工具注入和工具执行前的智能体类型边界必须先经过该目录裁决。
services/center/src/data-access：SQLite 和文件仓储访问层；不承载 HTTP 路由或模型工具执行逻辑。
```

## 包管理与运行环境

Node.js 版本要求为 Node.js 20 或更高。pnpm 版本要求为 9 或更高。

凡是包含 `package.json` 的 Node.js 包，都必须使用 pnpm，并纳入根工作区管理。`plugins/idea` 使用 Gradle IntelliJ Platform Plugin，不纳入 pnpm workspace。

根目录 `.npmrc` 必须包含 npm 镜像源配置和 `save-exact=true`。所有 `package.json` 依赖版本必须使用精确版本，不使用 `^` 或 `~`。

遇到 Node.js 版本不符合要求时，必须先检查本机是否安装 nvm。有 nvm 时优先切换或安装所需 Node.js 版本；没有 nvm 或 nvm 无法解决时停止等待用户处理，不能自行改用未确认的 Node.js 环境绕过。

TypeScript 配置服务源码组织和 IDE 识别。项目不把 `tsc` 或 `vue-tsc` 作为默认质量门槛；开发运行和打包由 Vite、tsx、Electron、Fastify 和相关构建工具处理。

中心服务开发控制台和文件日志统一使用 `pino` 输出结构化 JSON；中心服务本机时间统一通过 `moment` 按 `YYYY-MM-DD HH:mm:ss` 格式化。执行链路新增日志必须接入 `services/center/src/logger.ts`，不能直接散落 `console.info`、`console.error` 或手写 JSON 日志。中心服务文件日志写入中心目录 `logs` 子目录，单个文件最大 `1MB`，按 `center_YYYY_MM_DD_HH_mm_ss.log` 命名。开发控制台保留命令启动和结束这类一头一尾日志，不输出命令输出块、运行中轮次等持续中间态，同时保留失败、终态和关键审计节点；控制台中文必须按 UTF-8 原文显示，不能输出 mojibake 或 `\uXXXX` 编码。

## 中心目录与数据存储

中心目录是完整迁移单元。复制整个中心目录到另一台电脑后，应能恢复记忆、会话、项目、插件配置、供应商配置、审计记录和统计数据。

默认中心目录名为 `center-data`。

开发期默认中心目录：

```text
项目根目录/center-data
```

绿色版桌面产物默认中心目录：

```text
解压目录/center-data
```

用户可以在桌面壳配置外部中心目录。中心目录改到外部路径后，删除程序目录不会删除外部中心数据，UI 必须明确提示。

中心目录结构：

```text
center-data
├─ db：SQLite 数据库和迁移文件目录。
│  ├─ zhixin.sqlite：中心服务主数据库，保存会话、消息、轮次、任务、事件、用量、通知、项目索引和个人事务索引。
│  └─ migrations：SQLite 迁移记录和迁移脚本目录，中心服务启动时按需执行。
├─ config：中心服务本机配置、访问控制配置和通知配置目录。
│  ├─ center.json：中心服务端口、中心目录、静态资源路径和启动参数等本机配置。
│  ├─ access.json：远程 Web 访问账号密码摘要、Cookie 策略和本机访问控制摘要。
│  └─ notification.json：桌面端、Web 端和 IDE 插件端通知开关、通知范围和权限状态。
├─ memory：用户记忆和智能体长期记忆目录。
│  ├─ user.md：用户长期记忆 Markdown 文件。
│  ├─ agents：各智能体按 agentId、年月日分层保存的长期记忆 Markdown。
│  └─ mem0：Mem0 OSS 语义记忆索引目录，保存本地 SQLite 向量索引、history 和可重建索引文件。
├─ agents：长期智能体定义 Markdown 目录，主智能体缺失时由中心服务恢复。
├─ providers：供应商配置、模型列表缓存和模型能力声明目录。
├─ plugins：中心级插件清单、配置、启用状态和用户安装插件内容目录。
├─ mcp：中心级 MCP Server 配置、运行参数、启用状态和调用记录目录。
├─ skills：中心级 skill 内容、说明、启用状态和调用记录目录。
├─ runtimes：Node.js、Python、Java、Maven、Git 等运行环境配置目录。
├─ sessions：会话附件和会话相关可迁移文件目录。
│  └─ attachments：已发送正式消息附件目录，SQLite 保存附件元数据和消息关联。
├─ personal：个人事务领域可迁移数据目录。
│  ├─ todos：待办事项领域文件和补充数据目录。
│  ├─ calendar：日程领域文件和补充数据目录。
│  └─ knowledge：个人知识库领域文件和补充数据目录。
├─ temp：未发送输入框附件临时目录，中心服务启动和停止时清理未绑定正式消息的文件。
└─ logs：中心服务、客户端连接、任务执行、扩展能力调用和异常排查日志目录。
```

存储分工：

- Markdown 保存用户记忆、智能体记忆、智能体定义、skill 内容和可读说明。
- JSON 保存中心服务本机配置、访问控制摘要、供应商配置、代理配置、运行环境配置、插件清单和 MCP 配置等低频配置。
- SQLite 保存会话、消息、轮次、任务、智能体运行状态、事件日志、流式片段索引、调用记录、用量统计、通知事件、项目索引、待办、日程和知识库索引。
- Mem0 OSS 只作为语义记忆抽取、语义索引和召回引擎，默认索引文件固定保存在 `memory/mem0` 下；Markdown 记忆和 SQLite `memory_index` 仍是可读、可迁移、可重建的长期记忆事实源。
- 附件文件保存在正式会话附件目录，SQLite 保存附件元数据和消息关联。
- `temp` 只保存未发送输入框附件，中心服务启动和停止时清理未绑定正式消息的临时文件。

better-sqlite3 只允许中心服务主进程打开数据库连接。Worker 和 Deep Agents 原生 agent 不能直接写 SQLite，必须通过 IPC 把状态变更和事件交回中心服务，由中心服务统一事务写入。

中心服务启动时执行 SQLite 迁移。迁移记录写入 SQLite 内部表。

## SQLite 状态表与事件日志

SQLite 按“当前状态表 + 追加事件表”组织。

核心状态表：

```text
projects
sessions
messages
conversation_turns
tasks
task_steps
agents_index
agent_runtime_states
agent_sub_conversation_messages
pending_edit_records
memory_index
attachments
notifications
usage_records
usage_daily_stats
todos
calendar_events
knowledge_items
plugin_installs
extension_call_records
sync_clients
```

`projects` 保存项目 ID、项目文件夹名、项目别名或备注、最近路径、首次登记时间和最后访问时间。项目 ID 来自项目根目录 `致心项目ID.md`。项目路径只作为当前位置记录，不作为身份。前端主名称只能展示项目文件夹名，别名或备注只能作为次要说明展示，不能替代文件夹名。项目删除通过中心服务事务删除 `projects` 索引和该项目下的 `sessions`、`messages`、`conversation_turns`、`tasks`、`task_steps`、`pending_messages`、`attachments` 等事实表记录；不删除项目根目录、项目文件或 `致心项目ID.md`。

`sessions` 保存普通会话和项目会话。项目会话关联 `project_id`，普通会话不绑定项目。

`messages` 保存用户消息、Agent 回复、系统过程消息、错误消息和工具过程消息等最终可展示消息。

`conversation_turns` 保存每轮用户输入到 Agent 完成、失败、取消或等待用户之间的边界。

`tasks` 保存当前轮次任务主生命周期，`task_steps` 只保存用户可见拆解步骤。一次用户轮次可以触发多个任务。Deep Agents 图节点、模型流、工具计划和内部执行边界不写入 `task_steps`，只通过 `graph.node.started`、`graph.node.completed`、`graph.node.failed` 或其他过程事件写入 `payload.graph`；图节点元数据包含 `graphRunId`、`threadId`、`nodeId`、`nodeKind`、`superstep`、`checkpointId`、`parentCheckpointId`、`attempt`、`idempotencyKey`、`resumable`、`nextNodeIds` 和 `stateSummary`，用于恢复对话内复杂任务编排。todoList、用户引导、模型计划或系统续跑中明确要展示给用户的计划项才允许写入 `task_steps`。

`agents_index` 保存主智能体和长期智能体索引。主智能体 `main` 是内置入口，不允许停用或删除；长期智能体 Markdown 定义必须保存可用插件、MCP 和 skill 范围字段，字段用于描述权限边界和默认启用范围。

`agent_teams` 保存会话级 team 当前状态。team 绑定 `session_id`，由主智能体创建和协调；会话删除时物理删除该会话下的 team 记录。解散 team 时也物理删除 team 记录，但历史事件和消息保留。

`agent_team_members` 保存会话级 team 与长期智能体成员关系。成员只能引用启用状态的长期智能体，不能引用主智能体或子智能体。移除成员只删除成员关系，不删除长期智能体和记忆。

`agent_sub_conversation_messages` 保存当前主会话内按 `parent_session_id + agent_id` 隔离的智能体子对话消息。智能体弹框打开完整对话组件时只读取该智能体自己的子对话内容，不能混入主对话消息。

`pending_edit_records` 保存已经真实写入磁盘但仍可由用户“保存/撤回”的文件编辑记录。记录包含会话 ID、智能体 ID、文件路径、编辑前内容、编辑后内容、增删行统计和状态；保存只把记录标为 `accepted`，撤回会在当前文件内容仍等于编辑后内容时写回编辑前内容，文件缺失或已再次变更时标记 `conflicted`，避免盲目覆盖。

`memory_index` 保存智能体长期记忆索引。正常会话轮次完成并固化助手消息后，中心服务把本轮用户输入、助手回复、关键词和摘要追加到主智能体记忆文件，并把 `source_session_id`、`source_turn_id` 和附件引用写入索引；后续 Worker 或 Deep Agents 原生 agent 上下文请求从该索引读取主智能体及相关智能体记忆，普通模型请求也会把主智能体最近长期记忆摘要作为 system 消息注入，不能只依赖当前会话消息临时上下文。

当前会话发送接口把一轮对话拆成可展示、可恢复的过程事件和用户可见任务步骤，主执行内核使用 Deep Agents。Deep Agents 负责组织 planning/todo、受控虚拟文件上下文、供应商真实公开思考摘要、模型流式输出、结构化工具请求解析、工具调度、工具结果回填、subagents 委派、上下文管理和失败收尾；中心服务继续负责事件事实源、任务步骤事实源、SQLite 状态表、Markdown 记忆、权限审批、供应商网关、MCP/skill 配置、用量记录和多端实时同步。Deep Agents 底层 LangGraph 运行状态可以作为运行中检查点，但长期迁移事实仍以中心目录中的 Markdown、JSON、SQLite 和附件文件为准，不能只依赖 Deep Agents 或 LangGraph checkpoint 作为 UI、审计或迁移事实源。每个 Deep Agents 关键执行边界必须写入带 `payload.graph` 的过程事件；只有 todoList、用户引导、模型计划或系统续跑明确形成用户可见计划项时才写入 `task_steps`。项目内部 `threadId` 映射为 `sessionId`，`graphRunId` 映射为 `turnId`。恢复只从已完成的可恢复边界继续，不尝试从模型 SSE token 或命令 stdout 中间恢复；命令、文件写入、插件/MCP 调用、消息固化、记忆写入和用量记录等副作用使用 `idempotencyKey` 防止恢复时重复执行已经完成的副作用。工具调用由 Deep Agents 基于中心服务提供的结构化工具定义自主请求，中心服务不再通过用户文本中的 `node`、`python` 或 `版本` 等硬编码关键词直接触发命令，也不再兼容历史文本 JSON 工具调用。模型请求命令工具时先写入 `model.tool.requested` 和携带同一 `toolCallId` 的 `tool.plan.created`，再由中心服务校验并执行命令工具，写入 `tool.command.started`、逐块 `tool.command.output`、`tool.command.completed` 或失败事件，随后按每个 `toolCallId` 分别写入 `model.tool.result.appended` 并把工具结果交回 Deep Agents 继续生成最终回复。命令工具使用异步进程执行，标准输出和标准错误的数据块一到达就追加事件，前端按 `toolCallId` 把模型请求、工具计划、命令或 MCP 执行、输出、完成、失败和结果回填渲染到同一张独立过程卡片内部；开始、执行中、完成和失败都更新同一张卡片，不按状态拆成多张卡片。命令卡片正文以类 bash 输出展示，第一行保存真实执行命令，后续直接追加 stdout、stderr、失败原因或完成摘要对应的流式过程文本，不再生成“原始命令”“执行结果”“时间线”等分区标题。命令工具模型参数必须包含 `shellCommand` 和 `inputSummary`，运行时继续兼容 `executablePath` 和 `args` 以承接后续非 shell 形态；Windows 上由 PowerShell 执行，macOS/Linux 上由 `sh -lc` 执行；模型误把可执行名重复放进 `args` 或在 Windows 上请求 `which`、`bash -lc` 这类 POSIX 命令时，中心服务按平台做受控兼容转换并保留展示命令。MCP 配置读取中心目录 `mcp/global.json` 和项目级 MCP 配置，支持 `type: "http"` 的 Streamable HTTP endpoint，也支持 `type: "stdio"` 的本机子进程 JSON-RPC transport；stdio MCP 子进程由中心服务启动、复用、超时和关闭，stdout 只用于 MCP 协议消息，调试日志必须进入 stderr 或中心服务日志。中心服务在 Deep Agents 工具注入前通过 `tools/list` 发现 HTTP 和 stdio MCP 工具并生成动态工具定义，模型请求动态 MCP 工具后写入 `tool.mcp.started`、`tool.mcp.completed` 或 `tool.mcp.failed`，事件载荷必须包含 `toolCallId`、`serverId`、`toolName`、`inputSummary`、`outputSummary`、`failureReason` 和 `traceId`，随后写入 `model.tool.result.appended` 并把结果交回 Deep Agents 继续生成最终回复。MCP 管理接口按全局 Server 粒度返回列表行：`POST /api/mcp/list` 只返回全局配置中的 Server ID、协议类型、单服务配置和更新时间等轻量信息，前端不展示配置文件列和作用域列；编辑弹框的 JSON 文本仍使用完整 MCP 配置形态 `{"mcpServers":{"服务 ID":{...}}}`，但前端保存前只允许其中包含一个 Server，并抽取该 Server 调用单服务保存；`POST /api/mcp/tools` 按 `relativePath + serverId` 异步执行单个 Server 的 `tools/list`，避免管理页首屏同时连接所有 Server 并铺开全部工具；`POST /api/mcp/save` 支持 `serverId + serverConfig` 的单服务保存，中心服务读取既有 `mcp/global.json` 后只更新 `mcpServers[serverId]` 并保留其他 Server，同时继续兼容旧的完整 `mcpServers` 保存入参。插件、MCP 和 skill 在尚未解析到具体执行器时写入 `tool.plugin.unavailable`、`tool.mcp.unavailable`、`tool.skill.unavailable`，并保留不可用原因，不伪造成功调用，也不进入对话消息流。模型如果输出“准备继续执行、改用命令、重新查询、调用工具”等半截工具意图但没有结构化工具调用，中心服务写入 `message.turn.incomplete`、标记轮次失败并禁止固化助手消息。

前端对话区按事件聚合后渲染过程卡片。`thinking.*` 事件只来自供应商明确返回的可公开 `thinking`、`reasoning` 或公开摘要内容块，按 `payload.thinkingId`、`payload.phaseId` 或明确阶段拆成独立思考卡片，生成中默认展开，收到完成事件后默认折叠；思考过程状态只显示在思考卡片标题后方，正文片段只显示 `thinkingText` 代表的真实公开思考内容，不在正文前重复渲染“生成中、已完成、失败”等状态标签。所有非工具过程的 `model.stream.delta` 中途文字都渲染为独立无标题 Markdown 卡片，按流式片段追加，不并入工具卡片或最终回复；没有后续工具过程的模型流式片段仍按 `turnId` 作为临时最终助手消息展示，固化助手消息到达后由真实 `messages` 表记录替代，`model.stream.completed` 只表示流结束，不能重复追加正文。`model.tool.requested`、`tool.plan.created`、`tool.command.*`、`tool.mcp.*`、`tool.call.*` 和 `model.tool.result.appended` 优先按同一 `toolCallId` 聚合为独立卡片，任务步骤、`agent.loop.batch_limit_reached` 和其他工具事件继续按步骤 ID、命令标识或 `serverId + toolName` 聚合。工具开始事件、MCP 开始事件、任务步骤事件或工具计划事件一到达就创建卡片，不能等完成事件或响应内容返回后才显示。工具卡片标题使用核心执行对象，例如 `执行：node -v` 或 `MCP：idea · get_file_text_by_path`，标题行只允许单行展示并在溢出时显示省略号；同一张卡片承载请求、计划、开始、输出、完成、失败和回填状态，但正文不展示时间线小标题。命令过程卡片必须带有命令专属类型标识或样式，正文内容区参考 bash：第一行是真实命令，后续换行直接追加执行过程；普通工具、MCP、插件和 skill 卡片正文只展示输出、失败原因或完成摘要。正文内容区最大高度固定为 `200px`，超出部分在卡片正文内部滚动。中心服务命令运行器需要按平台把 stdout 和 stderr 解码为可读 UTF-8 文本后写入 `tool.command.output`，Windows 上不能把 PowerShell、Python traceback 或中文错误输出直接按错误编码显示为替换字符乱码。同一轮内思考卡片、工具卡片、模型中途 Markdown 卡片和临时最终助手消息按首次事件 `sequence` 交错排序，保证过程显示在用户问题下方且不改变真实执行顺序。

事件日志表为 `events`：

```text
events
- id
- event_type
- scope_type
- scope_id
- session_id
- turn_id
- task_id
- step_id
- agent_id
- project_id
- client_id
- sequence
- status
- occurred_at
- title
- summary
- payload_json
- error_code
- trace_id
```

`event_type` 使用固定枚举，至少包括轮次开始、轮次结束、消息创建、会话删除、流式片段、思考片段、模型调用开始、模型调用结束、工具调用开始、工具调用结束、命令执行开始、命令执行结束、文件读取、文件写入、文件删除、插件调用、MCP 调用、skill 使用、子智能体创建、智能体状态变更、协作消息、记忆写入、通知创建和错误。

`sequence` 在同一个 `turn_id` 内递增，用于断线后继续拉取事件。WebSocket 实时推送事件，客户端断线重连后使用 REST 查询缺失事件，再恢复订阅。会话发送接口创建用户消息和轮次后先返回轮次身份，后台执行链路使用带追加监听的事件仓储；每次 `events.append` 落库后立即通过 WebSocket 推送当前会话当前轮次事件，异步执行结束时只按已推送最大 `sequence` 补发遗漏事件，不能再从 `afterSequence: 0` 整轮重播。可恢复任务编排依赖事件 `payload.graph` 中的 checkpoint，不把 Pinia 或客户端本地状态作为恢复事实源。

会话删除必须先写入 `session.deleted` 事件，再把同一条事件通过实时通道广播给其他端。客户端收到删除包后刷新导航；如果被删除的是当前会话，必须清空当前详情、过程事件和待确认编辑状态，并进入可继续输入的草稿。客户端重新打开历史会话时必须同时加载会话详情和事件快照，确保思考、工具、模型流式片段和过程卡片可恢复。

`usage_records` 只追加模型调用用量原始记录。聚合表 `usage_daily_stats` 可以由中心服务按需刷新或增量维护，但不能替代原始记录。

## 中心服务模块边界

中心服务使用 Fastify 模块化组织。每个领域模块包含路由、仓储、服务、事件写入和权限检查。

模块边界：

```text
bootstrap
config
auth
client-access
websocket
projects
sessions
turns
tasks
agents
memory
providers
model-gateway
plugins
mcp
skills
personal-todos
personal-calendar
personal-knowledge
runtimes
usage
notifications
files
worker-manager
audit
```

模型网关边界：

- 中心服务内部模型请求、消息、工具定义、工具调用、工具结果、流式片段和用量归一化统一使用 OpenAI Chat Completions 规范，不再维护 `packages/model-protocol` 内部协议包。
- OpenAI 兼容供应商直接调用 `/v1/chat/completions`，中心服务不再把 OpenAI Responses 作为内部协议模式。
- Anthropic、OpenAI Responses 或后续其他供应商协议只能作为适配层存在。适配层负责把中心服务 OpenAI Chat Completions 请求转换为目标供应商请求，并把目标供应商响应、SSE 事件、工具调用和用量转换回 OpenAI Chat Completions 形态。
- 工具调用闭环只使用 OpenAI 标准 `tools`、`tool_calls`、`tool_call_id` 和 `role: "tool"`。历史文本 JSON 工具调用解析不属于中心服务协议，必须删除。
- 确需新增第三方包时，优先选择 GitHub star 数最多且近期仍维护的包；OpenAI 协议优先选官方 OpenAI SDK。若现有 `fetch` 网关能清晰满足需求，则不为追求封装而新增依赖。

供应商配置边界：

- 供应商允许保存未完整配置。只填写 `baseUrl` 和 API Key 时可以保存，便于用户先保存密钥再获取模型列表和补全配置。
- 供应商启用必须校验完整配置。缺少默认模型、模型列表、协议必需字段或其他启用条件时，中心服务拒绝启用并返回明确原因。
- 前端管理页的保存、获取模型、启用、停用、删除、失败和警告提示统一使用 Element Plus `ElMessage`，不能把提示渲染到弹框背后。

`bootstrap` 负责中心目录初始化、SQLite 迁移、启动锁、内置主智能体恢复和内置插件注册。

`auth` 和 `client-access` 负责本机访问识别、远程 Web 登录、Cookie 登录态和 IDE 插件本机连接限制。

`worker-manager` 只在中心服务主进程管理 Worker 生命周期，不对客户端直接暴露内部执行细节。

## 对外 API 与 WebSocket 规范

中心服务所有对外暴露 API 必须集中放在 `services/center/src/api` 文件夹。非对话管理能力仍可使用 REST API；对话页面的会话详情、历史消息、事件补齐、发送消息、附件动作、任务步骤、流式 token、等待用户、错误状态和多端同步全部改为 WebSocket 请求、响应、初始快照或增量事件，不再通过 REST 处理。

REST API 只允许 `GET` 和 `POST`，不使用路径参数。路径必须稳定，不把实体 ID 拼进路径。业务实体不存在返回统一业务错误，不通过 404 表达。

路径示例：

```text
GET  /api/health
GET  /api/bootstrap/state
GET  /api/session/list
POST /api/session/detail
POST /api/session/create
POST /api/session/message/send
POST /api/session/event/list
POST /api/project/register
POST /api/project/detail
POST /api/project/delete
POST /api/agent-sub-conversation/detail
POST /api/agent-sub-conversation/message/send
POST /api/edit-pending/list
POST /api/edit-pending/save
POST /api/edit-pending/save-all
POST /api/edit-pending/revert
POST /api/edit-pending/revert-all
POST /api/edit-pending/diff
POST /api/provider/create
POST /api/provider/update
POST /api/provider/delete
POST /api/task/cancel
POST /api/plugin/install
POST /api/plugin/enable
POST /api/plugin/disable
```

`GET` 只用于简单无副作用查询，参数放 query。

`POST` 用于创建、修改、删除、命令触发和复杂查询，参数尽量使用 JSON body。

删除、停用、取消等操作使用 `POST /api/xxx/delete`、`POST /api/xxx/disable`、`POST /api/xxx/cancel`。

统一响应包：

```text
{
  success: boolean;
  data: unknown | null;
  error: {
    code: string;
    message: string;
    displayMessage: string;
    traceId: string;
    details?: unknown;
  } | null;
}
```

所有 API 错误写入审计日志和事件日志，并返回 `traceId`。HTTP 状态码只表达协议层状态，业务失败通过响应包表达。

WebSocket 用于实时同步、流式消息、通知、任务状态和客户端在线状态。

客户端连接前必须先取得中心服务授权。IDE 插件授权时必须携带当前项目 ID，后续 `client.hello` 中的项目 ID 必须与授权记录一致；插件端必须先建立 WebSocket 连接，再通过 `project.register` 登记或更新项目上下文，避免项目登记反向依赖尚未建立的实时通道。

客户端连接后先发送 `client.hello`，携带客户端类型、项目 ID、会话订阅和最后收到的事件序号。中心服务返回 `server.ready`，随后推送 `event.appended`、`task.updated`、`agent.state.changed`、`session.updated`、`notification.created` 和 `connection.state` 等事件。

对话页已实现的 WebSocket 请求类型包括：

```text
navigation.snapshot：读取对话导航所需项目和会话列表。
session.snapshot：读取当前会话详情、消息、轮次、任务、任务步骤和事件。
chat.bootstrap.snapshot：读取对话页输入区所需供应商、内联协议能力、供应商模型列表和智能体列表。
tokenizer.count：统计当前对话窗口上下文 token 用量。
session.create：发送第一条消息前把本地草稿创建为中心服务会话。
session.message.send：发送用户消息并启动当前轮次。
session.turn.cancel：取消当前会话当前运行轮次，保留本地排队消息并广播轮次、任务、步骤和智能体收尾事件。
session.event.replay：断线或漏事件后补齐当前会话事件。
session.guidance.submit：把运行中补充引导合并进当前轮次任务。
project.register / project.delete：登记或删除项目事实。
attachment.temporary.create / attachment.commit：创建临时附件并绑定正式消息附件。
edit.pending.*：读取、保存、撤回和 diff 当前会话待确认编辑。
agent.sub_conversation.*：读取和发送智能体子对话消息。
```

对话页面客户端断线重连后必须通过 WebSocket 补齐缺失事件，再继续订阅 WebSocket。对话页首屏快照、历史事件补齐、发送、停止、引导、当前窗口 token 统计和对话内编辑状态读取都必须走 WebSocket；非对话管理页面可以继续按管理 API 语义使用 REST。上下文 token 总览不因用户输入变化触发 tokenizer 请求。客户端收到 `event.appended`、历史事件补齐或当前会话详情刷新时，必须用新的事件数组引用写入状态，并按 `sequence` 排序，保证 Vue/Pinia 计算链路立即更新消息列表、流式助手消息、过程卡片和失败状态；不能只依赖数组原地 `push`、原地 `sort` 或窗口重新绘制触发 UI 显示。任一端发送消息后，中心服务必须推送 `session.updated` 刷新会话列表时间、标题和当前详情；即使另一个端没有打开该会话，也不能丢失导航列表同步。前端收到 `session.updated` 且命中当前活动会话时，必须拉取完整会话快照，补齐可能因 WebSocket 时序或漏包造成的助手消息、任务状态和事件终态差异。

## Agent 执行引擎

Agent 执行引擎由中心服务调度、Worker 子进程中的 Deep Agents 原生 agent 执行。中心服务负责事实源、权限、状态、事件和资源管控。Deep Agents agent 负责一次任务的推理流程、planning/todo、受控虚拟文件上下文、工具编排、上下文管理和 subagents 委派，但不能直接写核心数据文件或 SQLite。

模型可见工具声明由 `services/center/src/tools/tool-capability-registry.ts` 统一维护，并通过 LangChain `bindTools` 注入模型。工具参数 JSON Schema 必须保持供应商结构化工具接口兼容，顶层只暴露 `type: "object"`、`required` 和 `properties` 等通用字段；跨字段约束由字段描述和中心服务运行时校验兜底，不能用会被供应商拒绝的顶层组合关键字。

一轮对话流程：

```text
用户发送消息
-> 中心服务创建 message、conversation_turn、task
-> 中心服务启动或复用 Worker
-> Worker 内 Deep Agents agent 请求上下文包
-> 中心服务返回会话、项目、智能体、记忆索引、可用工具和权限
-> Deep Agents agent 组装 middleware、StructuredTool 和子智能体能力
-> Deep Agents agent 通过中心服务模型网关使用 LangChain ChatModel
-> Deep Agents agent 产生流式事件、工具请求、todo 更新和 subagents 委派
-> 中心服务审批、执行或转发
-> 中心服务写状态和事件并广播
-> Deep Agents agent 输出最终结果
-> 中心服务固化消息、轮次、任务、用量、记忆索引
```

Worker 与中心服务使用 JSON-RPC 风格 IPC。核心消息包括：

```text
worker.ready
task.start
task.cancel
context.request
model.call.request
tool.call.request
file.read.request
file.write.request
command.run.request
plugin.call.request
mcp.call.request
skill.resolve.request
memory.write.request
event.emit
task.complete
task.failed
heartbeat
```

Worker 和 Deep Agents agent 所有有副作用操作都必须请求中心服务代理执行或批准，包括文件写入、文件删除、命令执行、插件调用、MCP 调用、记忆写入和通知触发。

中心服务根据当前客户端类型的执行模式决定是否需要用户审批。审批结果进入事件日志。

模型工具调用协议由中心服务统一生成和校验。中心服务把当前会话可用工具转换为 OpenAI Chat Completions `tools` 函数定义，随模型请求传入模型网关；`tools[].function.name` 是满足供应商命名约束的模型安全名称，例如把内部工具 ID `builtin.command.run` 转为 `builtin_command_run`，`sourceToolId` 只在中心服务内部保留用于回查。模型回复中的结构化工具请求必须来自 OpenAI `message.tool_calls`、流式 `delta.tool_calls` 或 LangChain 透传的等价工具调用字段，中心服务先按模型安全名称映射回内部工具 ID，再转换为中心服务 `UnifiedToolCallIntent` 后才允许执行。用户明确要求使用命令工具、执行命令或查看本机环境版本时，中心服务在 LangChain 调用选项中使用模型可见安全名 `builtin_command_run` 作为 `tool_choice`，并在工具调用规则提示中同样使用该安全名；内部工具 ID 不能直接暴露给供应商作为函数名。LangChain 流式工具片段只有在拿到完整参数对象后才能进入执行队列，不能把缺参片段默认成 `{}`；同一轮流式最终工具调用需要按 `toolCallId` 去重并保留参数更完整的一条。工具结果回填给模型后不再强制 `tool_choice`，避免模型在已有工具结果后重复调用同一个工具。命令工具空参数或缺少 `shellCommand` 时，中心服务必须固化可展示失败回复并结束本轮，不能把空命令传入进程启动，也不能循环回填导致整轮卡死。多端实时同步以中心服务数据库为事实源，发送端和其他端在收到 `session.updated`、`message.created`、`model.stream.completed`、`turn.updated` 或 `task.updated` 后都要能够拉取当前会话完整快照；如果其他端只收到本轮起始会话更新，则需要启动运行中轮次活动续租式恢复，按事件 `occurredAt`、任务 `updatedAt`、步骤开始或结束时间、轮次时间以及后续中心服务心跳推导最近活动时间，只要最近活动仍在推进就继续恢复助手消息、过程输出、任务终态和轮次结束时间，不能因为固定轮询次数到达而停止；长时间无任何活动时只能停止当前前端本地观察并输出诊断日志，中心服务事实源仍以轮次和任务状态为准。

Agent 执行引擎分层：

```text
deepagents-agent
middleware-stack
structured-tools
context-bridge
agent-router
model-bridge
event-bridge
memory-committer
usage-collector
```

`deepagents-agent` 是 Worker 内部的 Deep Agents 原生执行入口。它基于 `deepagents` 创建当前轮次 agent，把中心服务 LangChain ChatModel、middleware、StructuredTool、todoList、subagents、事件桥和受控虚拟文件上下文注入 Deep Agents。Deep Agents 运行过程只能通过中心服务已有领域函数读写会话、事件、工具、模型、记忆和用量，不能绕过中心服务直接写 SQLite、Markdown、供应商密钥或中心目录核心文件。旧 `langgraph-runner` 和 `deepagents-runner.ts` 在迁移完成并确认无真实调用方后都必须删除。

子智能体只存在于当前任务上下文中，不能再创建子智能体。长期智能体创建必须通过中心服务写入智能体定义 Markdown 并更新索引，不能由 Worker 自行落盘。

每个智能体都有独立运行上下文。上下文至少包含 `agentId`、可见消息范围、子对话消息范围、记忆召回范围、任务拆解状态、todoList 状态、模型发送配置和 token 统计键。中心服务、Worker 和 Deep Agents agent 在构建模型上下文、写入事件、读取子对话、统计 token 或维护 todoList 时必须显式传递 `agentId`，不能只按 `sessionId` 混用所有智能体上下文。

创建子智能体工具必须接收并保存父级当前实际模型调用配置，包括父级 `agentId`、供应商 ID、模型名称和由父级决定的推理深度。子智能体运行时只使用该父级配置，不读取全局默认供应商来覆盖父级决策。主智能体、长期智能体和子智能体都可以在具备对应工具权限时创建或维护自己的 todoList；todoList 必须按 `agentId` 隔离，简单任务不创建 todoList。子智能体可以维护自身执行清单，但不能继续创建下级智能体或把任务派生成新的下级智能体树。

Worker 崩溃、Deep Agents 原生 agent 异常、超时或心跳丢失时，中心服务将相关任务标记为失败，写入 `task.failed` 事件，保留 `traceId`，并通知客户端。已写入事件不回滚。用户在对话窗口点击停止时，中心服务只向当前会话当前运行轮次发送 `task.cancel`，并停止该轮次后续模型流、工具续跑和自动续跑；同一窗口的本地排队消息继续留在前端待用户处理。取消完成后，中心服务必须写入轮次、任务、任务步骤和智能体状态收尾事件，避免出现助手消息已固化但任务仍显示执行中的实时状态。

## 模型网关与 LangChain 运行时

中心服务提供 `model-gateway`。Worker 和 Deep Agents 原生 agent 不能直接访问供应商 API 密钥，只能提交中心服务统一模型调用请求，由中心服务基于供应商配置初始化 LangChain ChatModel，并把该 ChatModel 或受控调用包装传入 Deep Agents，以中心事件流返回模型片段、工具调用、工具结果和用量。

OpenAI 和 Anthropic 协议直接使用 LangChain 已支持的对应 ChatModel 能力。仓库不再为 OpenAI 或 Anthropic 维护自研协议适配器，也不再把模型协议适配器作为插件动态加载。供应商配置负责提供 `baseUrl`、API Key 密钥引用、默认模型、推理深度、代理策略和能力声明等 LangChain 初始化参数。

工具运行时按目录拆分到 `services/center/src/tools`。每个工具一个文件，并通过统一工具注册入口对 Agent 暴露，例如命令工具、MCP 工具、创建长期智能体工具和创建子智能体工具都必须单独维护。

供应商配置引用协议适配器：

```text
providerId
providerName
providerProtocol
protocolMode
baseUrl
apiKeySecretRef
modelListStrategy
modelContextWindows
reasoningEffortStrategy
proxyPolicy
enabled
```

`providerProtocol` 使用固定枚举，例如 `openai` 或 `anthropic`，用于选择 LangChain ChatModel。`protocolMode` 可保留为供应商模式字段，但不能再指向插件适配器。

API Key 使用中心服务敏感信息存储，客户端只显示是否已配置，不返回明文。

模型网关职责：

- 根据供应商配置选择 LangChain ChatModel。
- 应用网络代理策略。
- 拉取模型列表和推理深度列表。
- 把中心服务模型请求、工具定义和历史消息映射为 LangChain 调用参数。
- 把供应商流式响应转换为中心服务事件，至少包括 `model.stream.delta`、`model.stream.completed`、`model.tool.requested` 和 `model.tool.result.appended`。
- 归集 LangChain 或供应商返回的用量，写入 `usage_records`。
- 区分代理失败、认证失败、供应商连接失败、供应商接口失败和协议解析失败。
- 为每次调用生成 `traceId`，写入事件日志。

供应商能力声明是否支持图片、工具调用、JSON 输出、推理深度、缓存用量字段、模型列表接口和流式输出。中心服务基于模型能力判断附件和工具是否可用，前端不能自行猜测。供应商模型列表保存为中心目录 `providers/{providerId}.models.json`，其中模型条目包含模型名称和上下文窗口上限，窗口上限按 `contextWindowTokens` 数值保存；前端表单统一以 `K` 为输入单位，保存前转换成 token 数值，展示时可格式化为 `1M`、`200K`。

中心服务提供 `POST /api/provider/model-fetch` 作为供应商真实模型列表获取入口。浏览器端、桌面端和插件端都只能调用中心服务该接口，不能直接请求供应商上游或读取 API Key。OpenAI 协议供应商通过 `baseUrl` 拼接 `/v1/models` 获取模型 ID，并避免重复拼接 `/v1`；Anthropic 协议供应商按 LangChain 或供应商模型列表能力处理。中心服务解析 OpenAI 风格 `data[].id` 或明确的 `models[]` 响应后，按模型名中出现的数字段降序排序，数字段不要求在末尾；排序后写入 `providers/{providerId}.models.json`，前端把排序首项写入当前供应商默认模型。`/models` 不返回上下文窗口时，中心服务保留已有模型的 `contextWindowTokens`，仅对新增模型写入 `200K` 作为可继续编辑的默认窗口。

供应商删除接口必须执行删除语义，不能只把供应商停用后继续留在 `POST /api/provider/list` 返回结果中。当前实现采用删除中心目录 `providers/{providerId}.json` 并同步清理 `providers/{providerId}.models.json` 的方式；历史会话、历史用量和事件日志保留原始 `providerId`。停用供应商仍通过更新 `enabled=false` 实现，并继续保留在列表中，避免停用和删除语义混淆。

供应商、网络代理、运行环境和模型网关准备/错误分类接口集中注册在 `services/center/src/provider-routes.ts`。`services/center/src/api-routes.ts` 只负责调用该路由注册函数，避免中心服务主路由文件继续膨胀并超过单文件行数限制。

## 内联工具、MCP、skill 与内置个人事务

扩展系统当前阶段采用“中心服务内联工具 + MCP + skill”的模型。插件开发只作为后续目标，本阶段不实现插件安装、插件市场、动态插件扫描、插件 SDK 运行时或项目级插件能力。

内联工具全部放在 `services/center/src/tools` 文件夹。每个工具单独一个文件，统一通过工具注册入口暴露给 Agent 执行图。工具文件需要声明工具 ID、名称、输入 schema、权限、风险等级、适用智能体类型和执行函数。

MCP 分为全局 MCP 配置和项目级 MCP 配置。

skill 分为全局 skill 和项目级 skill。

同名能力优先级：

```text
当前项目级能力
> 用户启用全局能力
> 中心服务内联能力
```

个人事务能力采用混合架构。待办、日程和个人知识库是一等领域模块，由中心服务提供稳定 API、SQLite 表和可迁移数据目录。

资料整理、浏览器信息收集、办公集成和自动化流程作为中心服务内联工具或一等领域模块交付，不作为插件包交付。插件开发后续目标需要重新规划插件清单、权限、安装、启停和 SDK 边界。

## 前端入口、布局与客户端能力

前端只有一个业务应用：`apps/frontend`。

运行时根据 `clientType` 和 `entryMode` 选择能力和路由：

```text
desktop-shell
web-local
web-remote
web-mobile
ide-plugin
```

客户端能力通过适配层暴露，不允许组件直接判断 Electron、浏览器或 IDE API。

主题由统一前端本地状态管理，属于客户端 UI 状态，不进入中心服务事实源。运行时识别层输出 `preferredTheme`，桌面壳、Web 本机、远程 Web 和手机 Web 使用 `prefers-color-scheme` 作为默认值，IDE 插件优先读取宿主传入的 `theme=light|dark` 查询参数。用户手动切换后按客户端类型和入口模式写入浏览器本地存储，后续启动优先使用该客户端保存值。

能力字段：

```text
canManageCenterService
canUseSystemNotification
canOpenLocalFile
canResolveProjectContext
canSendIdeContext
canUseRemoteLogin
canManageAccessAccount
```

主题协议：

```text
light：亮色主题
dark：暗黑主题
```

布局模式：

```text
workspace：桌面壳和桌面浏览器主工作台
mobile：手机浏览器
plugin-compact：IDE 右侧工具窗口紧凑项目对话
```

`workspace` 使用 flex 固定视口高度，包含顶部主导航、左侧对话导航、中间对话区和右侧任务/智能体状态栏。页面级禁止滚动，只有消息列表作为主滚动区域。

对话页面所有会话数据和动作都通过 WebSocket 进入中心服务，包括首屏会话详情、历史消息、事件补齐、发送消息、附件动作、任务步骤、流式 token、等待用户、错误状态和多端同步。对话页面不允许再通过 REST 请求拉取或提交这些对话数据，避免同步请求堆积导致页面卡顿。

桌面壳主窗口固定从中心服务 HTTP 入口加载页面，并通过查询参数传递中心服务端口；开发期是否复用 Vite HMR 由中心服务重定向决定。中心服务开发期重定向只把 Vite 作为前端入口和 assets 承载方，`/chat`、`/agent-management` 等业务路径必须规范到 hash 路由，避免出现中心服务路径、查询参数和 hash 混杂的非规范 URL；`/plugin.html` 保持独立插件入口。

桌面端和桌面浏览器 Web 端顶部主导航的每个菜单都必须对应独立页面或独立路由视图，不能把“智能体管理、供应商、网络代理、运行环境、用量统计、插件、MCP、skill、中心服务”等页面内容继续混写在登录页、对话页或 `loginViewRoute` 一类过渡路由配置中。没有独立页面的菜单不允许进入顶部主导航；中心服务页面只允许桌面壳显示。

页面级路由必须全部使用 Vue Router 懒加载。每个页面必须在 `apps/frontend/src/views/页面/RouterIndex.vue` 建立对应入口文件，并在 `apps/frontend/src/router.ts` 使用 `component: () => import("@views/页面/RouterIndex.vue")` 或等价别名动态 `import` 注册对应 URL。禁止在路由文件顶部静态导入 `views` 页面组件后再挂到 `component`。路径别名必须在 Vite 和 IDE 可识别配置中同步维护，常用别名至少包括 `@`、`~`、`@views`、`@components`、`@stores` 和 `@api`，避免页面路由继续依赖 `./views` 这类相对路径。

管理类页面不能继续通过 `<MainView initial-page="..."/>` 传参进入同一个大组件。公共工作台壳可以复用，但每个顶部菜单页面必须有自己的页面组件、数据加载边界和模板文件；`MainView.vue` 只保留公共布局、顶部导航、对话主页面承载和插槽出口，不再集中维护供应商、网络代理、运行环境、用量统计、插件、MCP、skill、中心服务等页面模板。

执行模式下拉项必须使用带说明的选项结构。`suggest` 对应建议模式，说明每一步副作用操作都需要用户确认；`auto_edit` 对应自动编辑，说明低风险读取或编辑流程可自动执行、高风险操作仍需确认；`full_auto` 对应全自动，说明在权限和沙箱范围内自动执行。

`workspace` 对话页的项目级插件、MCP 和 skill 信息不作为常驻主内容块渲染。中间对话区只保留一个图标按钮或简短摘要入口，点击后打开“项目能力详情”弹框。弹框只展示当前项目上下文扫描得到的能力详情，包括项目 ID、能力名称、来源、全局或项目级范围、启用状态和不可用原因；顶部全局插件、MCP、skill 管理页不承载项目级能力管理。

`workspace` 对话页输入区保存当前会话和当前智能体的模型发送配置 UI 状态，字段包括模型名称、推理深度、执行模式、当前 `agentId` 和当前窗口上下文用量。供应商 ID 可以作为模型来源和中心服务发送设置保存在状态容器中，但输入框内部不展示供应商选择控件。默认值从中心服务返回的启用供应商、供应商默认模型、已保存模型列表和推理深度配置中取值；供应商没有模型列表或刷新失败时，模型名称允许由用户手动输入或创建。输入区不再提供“文件上下文”按钮，项目文件引用仍通过 `@` 检索进入草稿结构化引用；底部上下文外层只展示进度圈和已用百分比，比例允许超过 100%；hover 提示只使用 `K` 或 `M` 短单位展示 `用量 / 上限` 和百分比。上下文 token 总览统计对象是当前会话窗口当前智能体已经进入上下文的所有对话消息、子对话消息、记忆摘要和中心服务实际注入片段，不把正在编辑的未发送草稿计入当前窗口总量。前端在模型开始响应、模型流式片段到达、工具结果回填后继续响应或模型响应完成时，基于中心服务 tokenizer 统计刷新总览，并使用包含 `sessionId`、`turnId`、`agentId` 或草稿标识的请求签名去重和请求序号防止旧响应覆盖较新的响应期统计。中心服务必须把最新上下文 token 总览按 `sessionId + agentId` 写入 SQLite，并记录关联 `turnId`、已用 token、窗口上限、占用比例和统计时间；会话快照、智能体弹框快照和 WebSocket 初始快照必须返回该持久化结果，保证重新打开会话或切换窗口后可以恢复。该状态不能只绑定到 hover 弹层、窗口焦点或临时 DOM 生命周期；窗口失焦、切换到其他应用、返回当前 Web 端或桌面端窗口后，已经取得的 token 用量和窗口上限仍然保留并继续刷新。该选择只影响后续发送和后续任务，不回改历史消息、历史任务、历史用量或记忆；在中心服务执行链路真正消费该配置前，前端不得破坏现有发送协议。

桌面壳加载中心服务页面后，需要拦截 WebContents 导航和新窗口请求。指向中心服务本机页面、hash 路由和前端静态资源的导航允许在主窗口内继续；HTTP、HTTPS、mailto 或其他外部链接必须交给系统默认浏览器或系统默认处理器打开，桌面端主窗口不能自身跳转到外部链接页面，避免用户点击模型回复里的链接后丢失当前工作台。

运行中轮次存在时，输入区按 Enter 发送的新草稿进入当前会话本地排队消息列表，不立即调用中心服务发送接口。本地排队消息只保存在前端当前对话窗口状态中，点击“引导”后立即移除该排队项。移除后再把排队正文作为当前运行轮次补充引导提交。发送按钮由当前会话轮次状态推导，只有“发送”和“停止”两个展示状态；停止操作通过中心服务取消任务接口接入前，前端必须给出可见待接入提示。

`workspace`、`plugin-compact` 和后续复用对话输入区的页面必须统一保留输入区三入口：“任务”、“智能体”、“编辑”。输入区容器使用中间对话区的完整可用宽度，不保留桌面宽屏固定窄宽度上限。“任务”入口名称不改，入口外部显示当前轮次任务进度 `已完成序号/总数`，例如 `1/7` 表示当前轮次已完成到第 1 个编排任务、总任务数为 7；没有运行轮次时只展示最新一轮任务，不把历史对话次数或历史轮次任务累计到入口数字中。详情从输入框内部入口向上打开浮层，不参与输入框高度计算，不占用主对话区常驻空间，浮层最大高度为 `40vh`，超过后在浮层内部滚动。“智能体”入口外部显示 `运行中长期智能体数/当前窗口长期智能体总数`，例如 `1/7` 表示当前窗口中 1 个长期智能体正在运行、共 7 个长期智能体可见；分母包含主智能体，不包含子智能体。详情同样向上打开浮层。智能体状态浮层只展示智能体名称和状态，名称与状态必须放在同一行；不展示任务摘要或提示文案，不使用 Element Plus 树节点高亮，也不在鼠标悬停时显示边框激活态。每个智能体节点都可以点击查看，并进入具备完整对话能力的智能体对话视图或弹框。编辑入口用于查看本轮新增、修改和 diff 摘要，详情同样使用向上浮层。

`apps/frontend/src/views/Chat/components/ChatConversationPanel.vue` 是主对话和智能体子对话共用的完整对话组件，包含消息内容区、内容区左侧时间线、输入区、任务浮层、智能体浮层和编辑浮层。智能体对话弹框只包装该组件并传入 `variant="agent"` 和当前智能体节点，不复制主对话输入区或消息区；弹框内消息内容区域最小高度为 `40vh`，空消息时也必须展示稳定内容区和空态，不能被输入区压缩到不可见。点击主智能体“致心”时，弹框读取当前主会话消息、事件、任务、编辑和 token 状态并以主智能体视角展示，避免主智能体没有独立子对话记录时出现空弹窗；长期智能体和子智能体继续通过中心服务 `parentSessionId + agentId` 读取独立消息，不能复用主会话消息数组。弹框内左侧时间线必须使用弹框自身消息滚动容器和消息锚点，点击时间线节点后滚动到对应用户消息并短暂高亮，不能误用主页面滚动容器。外部对话框“任务”入口展示当前主对话拆解出的任务；智能体弹框“任务”入口展示当前智能体自己的 todoList，右上角进度同步对应任务范围，不能把主对话任务和其他智能体 todoList 混在一起。

任务入口只展示当前轮次当前智能体已经拆解出的多步骤任务。只有一个默认任务或可见拆解步骤不超过 1 个时，不显示任务数量或默认任务行，避免把普通对话轮次冒充为任务拆解。任务入口外部显示当前可见拆解步骤进度 `已完成序号/总数`，例如 `1/7` 表示当前轮次当前智能体的拆解步骤总数为 7 且已完成到第 1 个。

拆解任务浮层中每个步骤只占一行，左侧显示状态和步骤标题，右侧不显示时间、耗时、`序号/总数`、重复状态、第二行摘要、当前动作、责任智能体、失败原因或替换原因。右侧任务状态栏只展示当前对话运行概览，不承载完整拆解列表。外部对话框“任务”入口展示当前主对话主智能体的任务拆解视图；智能体弹框“任务”入口展示当前智能体自己的 todoList。

编辑浮层只展示中心服务 `pending_edit_records` 返回的真实待确认编辑。保存全部/单文件保存调用中心服务把记录标为已接受；撤回全部/单文件撤回调用中心服务写回编辑前内容。对比按钮优先调用 IDE 宿主原生 diff 能力，Web 端没有宿主桥接时展示前后内容 diff 弹框。

`workspace` 的项目对话分组新增入口必须先调用浏览器目录选择或桌面端目录桥接选择文件夹，再通过 `/api/project/register` 登记项目，最后进入该项目对话草稿。已有项目行上的新增项目对话入口只在该项目文件夹下创建新的本地草稿。浏览器端项目对话验收固定选择项目根目录 `项目对话测试` 文件夹。

前端弹框必须按业务语义拆成独立组件。项目能力详情、任务详情、智能体状态、智能体对话、编辑详情、删除确认、供应商配置、插件配置等弹框都不能把模板、状态和交互长期堆在页面主组件中；页面只负责触发弹框和传入必要 ID，弹框组件内部负责加载、展示、提交和关闭。页面专属弹框放在对应 `views/页面/dialogs` 目录，跨页面复用弹框应放在对应领域组件目录或 `packages/ui`，不允许复制粘贴多份近似弹框。

管理类配置弹框使用 `apps/frontend/src/components/ManagementDialogShell.vue` 作为共享外壳。该外壳只负责 Element Plus 弹框的固定高度、内容区滚动和底部操作区固定，不承载具体业务字段。供应商、网络代理、运行环境、插件、MCP、skill、智能体管理等管理页弹框需要把长表单放入默认内容槽，把保存、取消、刷新等操作放入 `footer` 槽，保证内容超出视口时只滚动内容区，底部按钮始终可见。

Vue 单文件组件的页面专属样式优先写在当前 `.vue` 的 `<style scoped>` 中，尤其是 `views/页面` 下的页面、弹框和局部组件。`apps/frontend/src/styles.css` 只保留主题变量、第三方库基础覆盖、全局滚动条和少量跨入口根布局样式，不继续承载某个页面或某个弹框的全部样式。

`workspace` 和 `plugin-compact` 的新增普通对话、项目对话或项目页签入口只更新前端本地待发送草稿，不立即调用 `/api/session/create`。发送草稿时如果当前没有真实 `sessionId`，前端先按草稿类型创建会话，再发送消息并刷新导航；会话只有在真实发送后才进入历史列表。

左侧对话导航行右侧使用 `updatedAt` 的 `YYYY-MM-DD HH:mm:ss` 展示时间节点，时间只出现在右侧区域。hover tooltip 展示会话标题、用户最近发送内容摘要和同一格式时间。轮次时间展示属于消息内容区域：已结束轮次在本轮内容末尾展示开始、结束和耗时；当前运行轮次在输入框上方固定展示已耗时，结束后移入内容末尾。

会话标题总结只在 `conversation_turns.turn_number = 1` 的第一次对话完成后由中心服务自动写入。后续轮次完成时不再自动覆盖 `sessions.title`，但仍通过任务、消息、轮次和事件同步刷新会话详情与列表时间。标题更新事件继续使用 `session.updated`，仅首轮标题总结成功时发送。

思考卡片标题由前端根据同一思考过程首尾事件时间和状态计算。运行中的思考过程显示“正在思考”并默认展开；完成的思考过程显示“已思考（用时 X 秒）”并默认折叠。思考正文只读取中心服务事件 `payload.thinkingText`，该字段只能由供应商返回的可公开 `thinking`、`reasoning` 或公开摘要内容块写入；中心服务上下文整理、项目、记忆和可用工具编排阶段不能写成思考正文，也不能冒充模型私有思考；没有真实可展示内容时不渲染固定假内容。

消息列表滚动采用“贴底跟随 + 用户接管”模式。用户当前位于底部时，token 级流式输出继续自动贴底；用户主动滚动后停止自动贴底。滚动距离超过 `100px` 时，在消息区右下角绝对定位显示快捷箭头，按最近滚动方向决定动作：最近向上滚动显示回顶部，最近向下滚动显示回底部。贴顶或贴底时不显示箭头。

`plugin-compact` 只保留当前项目对话页签、消息列表、输入框、文件/选区引用、任务状态、智能体状态和连接设置。它不展示供应商、代理、运行环境、用量统计和中心服务管理页面。

`mobile` 使用 Vant 和移动路由结构，优先保证对话、消息查看、输入、通知、待办、日程和常用设置可用。

Vite 构建必须通过 `manualChunks` 把 Vue 运行时、UI 框架、Markdown 渲染库和其他三方依赖拆出主入口文件，避免 `index.html`、`plugin.html` 共用入口的首屏 JS 过大。

前端统一滚动条视觉必须参考 Element Plus 轻量滚动条：轨道弱化或透明，thumb 使用窄轨道、圆角、半透明颜色，hover 或拖拽时增强可见性。消息列表、对话导航列表、右侧任务/智能体状态栏、任务详情小弹框、智能体状态小弹框、智能体对话弹框、编辑详情弹框、项目能力详情弹框、diff 区域、管理页内部列表和插件紧凑模式的滚动容器都必须应用统一滚动条样式；页面根容器仍禁止出现全局滚动条。

Pinia 只保存客户端 UI 状态和当前订阅状态，不作为核心事实源。

大模型回答返回的 Markdown 内容使用 GitHub Markdown 样式渲染，需要兼容暗黑和亮色主题。

## IDE 插件架构

IDE 插件是宿主能力层，不重新实现完整业务 UI。每个平台单独维护插件工程，先实现 IDEA。

IDEA 插件使用 Gradle IntelliJ Platform Plugin 构建，工程名和安装包名为 `zhixin-agent-idea`，插件管理器展示名为“致心智能体”，包名和插件 ID 命名空间使用 `top.xzxsrq`。

插件工具窗口名称为“致心智能体”，默认放在 IDEA 右侧。工具窗口图标和右键菜单图标统一使用插件资源内的 `16×16` 小图标，避免 IDEA 菜单按原始大图尺寸撑开布局；小图标来源为 `assets/app-icon/图标.png`。工具窗口打开后加载中心服务提供的：

```text
http://127.0.0.1:端口/plugin.html
```

插件只连接本机中心服务，IP 固定为 `127.0.0.1`，端口默认 `8866`，允许用户在插件本地配置端口。

插件启动或打开项目时处理项目身份：

```text
1. 检查项目根目录是否存在 致心项目ID.md
2. 存在则读取项目 UUID
3. 不存在则创建 UUID 并写入 致心项目ID.md
4. 将项目 ID、项目路径、项目名称发送给 plugin.html
5. 中心服务登记或更新项目索引
```

IDE 插件提供宿主桥接能力：

```text
getProjectContext
insertContextReference
openInternalFileLink
readSelectionReference
readCurrentFileReference
readFolderReference
updateCenterPort
openEditDiff
```

右键菜单交互模仿常见 AI 插件。编辑器选区、编辑器标签页、项目文件树文件、项目文件树文件夹都提供“发送到致心智能体对话框”。点击后只把文件、文件夹、行号或选区引用插入 `plugin.html` 的输入框，不直接发送问题。

内部文件定位链接使用结构化协议。UI 可显示为 `文件名#L行号`，底层保留项目 ID、绝对路径、相对路径、开始行、结束行和选区。

`openEditDiff` 用于编辑详情中的“对比”操作。IDEA 插件通过宿主 `DiffManager` 打开编辑前内容与编辑后内容的原生对比视图；其他 IDE 后续按同一桥接语义实现，Web 端无宿主能力时退回前端 diff 弹框。

IDE 插件入口只展示当前项目相关数据，不展示其他项目的数据，也不展示普通非项目会话。

IDE 插件支持多页签。每个页签对应一个独立项目会话。同一项目允许多个页签并行执行。页签标题显示会话名和任务状态标志。状态标志至少包括执行中、等待用户、失败和未读。

## 安全、权限与敏感信息

中心服务是唯一安全边界。所有客户端、Worker、插件、MCP 和 skill 都不能绕过中心服务直接访问核心数据、敏感信息或执行副作用操作。

敏感信息只保存在中心电脑，包括供应商 API Key、代理用户名和密码、Web 远程访问账号密码摘要、插件私密配置、办公集成授权凭据、Cookie 签名密钥和本机访问令牌。

客户端永远不返回已保存密码或 API Key 明文，只返回 `hasSecret`、更新时间、使用状态和必要的脱敏摘要。

访问控制按客户端类型处理：

```text
desktop-shell：本机桌面壳，允许管理中心服务生命周期和本机配置
web-local：本机 Web，可直接进入授权流程，但不能管理中心服务生命周期
web-remote：远程 Web，必须登录，登录态使用中心服务 Cookie
ide-plugin：固定连接 127.0.0.1，只能访问当前项目范围
worker：无直接外部权限，只能通过中心服务 IPC 请求能力
plugin：按权限声明和启用范围调用中心服务插件 API
```

远程 Web 登录态必须使用 Cookie，不使用 `sessionStorage` 保存 token。Cookie 由中心服务设置。

本机访问和远程访问判断由中心服务基于请求来源、监听地址和连接信息完成，前端不能自行判断。

权限模型采用“声明 + 授权 + 审计”。

权限枚举：

```text
file.read
file.write
file.delete
command.run
network.request
memory.read
memory.write
project.read
project.write
provider.call
plugin.call
mcp.call
skill.use
personal.todo
personal.calendar
personal.knowledge
notification.send
```

执行模式影响审批策略。建议模式每个副作用步骤都需用户确认。自动编辑允许低风险读操作和部分编辑流程自动执行，高风险操作仍审批。全自动在沙箱和权限范围内自动执行。

无论哪种模式，所有副作用操作都写入事件日志和调用记录。

## 会话、轮次、任务与同步

会话分为普通会话和项目会话。普通会话不绑定项目，项目会话必须绑定 `project_id`。

IDE 插件中的每个页签对应一个独立项目会话。每个页签可独立发送消息、执行任务和显示状态。

一轮对话以用户成功发送一条输入为起点，以 Agent 完成本轮回应、失败、取消或等待用户为终点。

每个对话框、项目会话或 IDE 页签都有独立的对话图执行线程。当前实现把 `sessionId` 作为 thread，把 `turnId` 作为 graph run，把思考、模型、工具、扩展状态和消息固化拆成节点；节点完成时写入 checkpoint，后续恢复只影响当前会话当前轮次，不阻塞其他对话框并发执行。

会话、轮次、任务关系：

```text
session
  -> message[]
  -> conversation_turn[]
       -> task[]
            -> task_step[]
            -> event[]
```

长任务拆解出的子任务全部归属同一个当前轮次任务。自动续跑、工具批次切换、skill 工作流对齐和用户中途修改需求时，只能追加或更新同一 `taskId` 下的 `task_steps`，不能新建多个互不关联的任务块。`task_steps` 字段至少包含 `id`、`task_id`、`status`、`title`、`started_at`、`ended_at`、`summary`、`plan_version`、`step_order`、`source`、`depends_on`、`acceptance`、`superseded_by` 和 `superseded_reason`；`depends_on` 保存依赖步骤 ID JSON 数组，`source` 只记录模型计划、todoList 工具、用户引导或系统续跑等用户可见计划来源，不记录 Deep Agents 内部图节点。

任务步骤需要支持稳定步骤 ID、标题、状态、来源、依赖、验收口径、替换原因和计划版本。用户中途修改需求后，中心服务写入 `task.plan.revised` 事件并生成新的计划版本：仍有效的已完成步骤保留；被新需求否定的步骤保留历史并标记 `superseded`；未开始且不再相关的步骤标记 `superseded`；新增需求追加新步骤；仍相关但目标变化的步骤更新目标和验收口径。Deep Agents 自带 todoList 由执行内核自身维护，中心服务不再接受 `builtin.deepagents.write_todos` 或旧 `builtin.todo.list` 作为内联工具；用户可见 `task_steps` 只由明确的模型计划、用户引导或系统续跑入口写入。

Agent 工具循环分为单批工具循环和任务级自动续跑。单批工具循环达到内部上限时写入 `agent.loop.batch_limit_reached` 事件，保存已完成工具结果、待完成目标和下一步计划，然后在同一轮次同一任务内自动进入下一批工具循环。只有任务级总预算、无进展保护、缺少用户输入、缺少审批或外部工具持续失败时，才允许停止并进入 `waiting_user` 或结构化失败状态。

任务状态至少包括：

```text
queued
running
waiting_user
completed
failed
cancelled
```

任务状态语义：

- `queued`：仅表示同一个对话窗口、项目会话或 IDE 页签内部等待上一项任务处理完成，不表示全局任务队列，也不表示其他对话不能并发执行。
- `running`：表示该对话窗口、项目会话或 IDE 页签中的当前任务正在执行；多个会话可以同时处于 `running`。
- `waiting_user`：表示当前对话当前轮次需要用户引导、审批或确认，作用域仍是当前对话和当前轮次。
- `completed`：表示当前任务已完成。
- `failed`：表示当前任务失败，错误详情需要通过事件或任务步骤查看。
- `cancelled`：表示当前任务被取消。
- `superseded`：表示任务步骤被用户新需求或重规划替换，只用于任务步骤，不用于任务主状态。

智能体运行状态至少包括：

```text
idle
working
queued
waiting_user
ended
failed
```

智能体运行状态语义：

- `idle`：智能体当前没有执行任务。
- `working`：智能体正在当前会话或当前轮次内执行任务；不同会话中的智能体状态可以并发为 `working`。
- `queued`：智能体在当前对话内部等待上一项任务或步骤完成，不代表全局智能体只能串行工作。
- `waiting_user`：智能体在当前对话当前轮次内等待用户引导、审批或确认。
- `ended`：智能体本次任务或子任务已结束。
- `failed`：智能体本次任务或子任务失败。

所有状态变更都写入 `events`，并同步到订阅客户端。UI 不能只依赖本地计时或本地推断状态。

WebSocket 订阅范围按客户端类型限制：

```text
desktop-shell / web-local / web-remote：可订阅有权限访问的普通会话和项目会话
ide-plugin：只能订阅当前项目 ID 下的会话、任务和智能体状态
web-mobile：同 Web 权限，但前端使用移动展示模式
```

断线重连流程：

```text
1. 客户端记录每个订阅范围最后收到的 event sequence
2. WebSocket 异常断开后持续自动重连，固定重试次数只作为快速重试窗口，不作为停止条件
3. 重连成功后通过 WebSocket 请求补齐缺失事件
4. 补齐完成后恢复实时订阅
5. 尚未成功发送到中心服务的本地排队消息必须停留在待确认状态
```

主动关闭连接只发生在页面销毁、登录态切换或上层显式替换 WebSocket 客户端时；只有主动关闭才允许 UI 进入 `stopped`。服务重启、网络波动或中心服务短暂不可用时，UI 必须停留在 `retrying` 并继续恢复连接。

## 记忆系统与智能体定义

记忆系统使用 Markdown 作为可读、可迁移的长期存储，SQLite 保存索引和来源关系，Mem0 OSS 保存可随中心目录迁移或从 Markdown 重建的语义索引。

用户记忆固定为：

```text
memory/user.md
```

每个智能体有独立记忆目录：

```text
memory/agents/{agentId}/{year}/{month}/{day}.md
```

每段记忆格式：

```md
# HH:mm:ss

## 关键词

## 总结

## 使用的电脑

## 用户说的

## 回答的
```

记忆标题 `# HH:mm:ss` 和 `memory/agents/{agentId}/{year}/{month}/{day}.md` 日期目录都使用当前中心电脑的本机时间，不能使用 UTC 时间或模型返回时间。`## 使用的电脑` 写入当前运行中心服务进程所在操作系统的可读身份，格式为 `os.type() / os.platform() / os.release() / os.arch()`。该字段用于迁移整个 `center-data` 后识别记忆写入发生在哪类操作系统环境，不能写成固定 `center`、中心目录名或中心服务角色名。

记忆写入边界是一轮完整对话。只有这一轮结束后，才允许写入长期记忆。记忆写入只能追加，不能覆盖或插入已有内容。

每个智能体的记忆写入使用中心服务内的单写队列。不同智能体之间允许并行写入。

Mem0 OSS 通过中心服务 `memory-engine` 适配层接入。正常轮次完成后，中心服务先追加 Markdown 记忆并写入 `memory_index`，再把同一来源内容同步到 Mem0。Mem0 配置必须显式指定 `historyDbPath`、`vectorStore.config.dbPath` 和智能体/项目作用域，路径位于 `center-data/memory/mem0` 下。Mem0 的 LLM 和 embedding 默认不能直接读取客户端密钥或绕过供应商网关；需要使用中心服务供应商网关、中心服务配置的本地 embedding 提供方，或明确标记为未启用。模型上下文构建时可以先用 Mem0 做语义召回，再通过 `memory_index` 回查 Markdown 来源，保证召回结果可追溯。

智能体定义保存为 Markdown：

```text
agents/{agentId}.md
```

定义文件包含结构化 frontmatter 和可读正文。frontmatter 保存 ID、名称、角色、能力边界、可用内联工具范围、可用 MCP 范围、可用 skill 范围、默认供应商、默认模型、推理深度、记忆索引、创建来源和启用状态。

智能体定义保存的可用内联工具、MCP 和 skill 范围是该智能体的权限边界和默认启用范围。智能体执行时最终可用能力仍由当前会话窗口动态裁决：

```text
当前会话类型
当前项目 ID
项目级 MCP/skill
全局启用 MCP/skill
中心服务内联工具
用户权限和执行模式
扩展能力声明的适用范围
```

当前会话窗口里可用的能力，智能体默认都可以请求使用。是否真正执行由中心服务权限、适用范围和审批策略决定。统一工具 ID 必须先映射为智能体权限名再由智能体实例裁决，映射关系集中维护在 `services/center/src/agents/base-agent.ts`；MCP 动态工具统一通过 `mcp_` 前缀继承 `builtin.mcp.call` 权限边界。todoList 是任务执行状态工具，不属于创建类工具。

智能体执行代码位于 `services/center/src/agents`，必须抽象为类层级，并由该目录统一输出执行工厂和工具权限过滤函数：

```text
BaseAgent：共享上下文、工具权限、任务执行、事件写入和记忆边界。
MainAgent：主智能体，可注入创建长期智能体工具、创建子智能体工具和 team 管理工具。
LongTermAgent：长期智能体，可注入创建子智能体工具，不能注入 team 管理工具。
SubAgent：子智能体，禁止注入任何创建智能体工具和 team 管理工具。
createAgentForTask：按任务记录解析当前执行智能体类型，供模型网关和工具执行链路复用。
filterToolIdsForAgent：按智能体类型过滤模型可见工具，避免调用点用字段兜底猜测权限。
```

智能体域时间统一使用 `services/center/src/time.ts` 的中心服务本机时间格式化函数。长期智能体创建/更新、子智能体运行记录、team 创建/成员变更、智能体运行状态和智能体域记忆索引都保存 `YYYY-MM-DD HH:mm:ss`，不能保存 UTC ISO 字符串。

主智能体“致心”是系统内置智能体，不可删除。其定义缺失或损坏时，中心服务按内置模板恢复，并写入事件日志。

长期智能体可以由用户手动创建，也可以由主智能体通过创建长期智能体工具在用户授权后创建。删除长期智能体必须提示影响。

子智能体可以由主智能体或长期智能体通过创建子智能体工具创建。子智能体不落为长期定义，只存在于任务执行上下文和事件日志中，任务结束后关闭。子智能体不能再创建任何子智能体或长期智能体。

主智能体额外注入四个会话级 team 工具：`create-agent-team`、`disband-agent-team`、`add-agent-team-member` 和 `remove-agent-team-member`。team 成员只允许启用状态的长期智能体；主智能体作为 team 队长和协调者，不写入普通成员；子智能体不能加入或管理 team。创建 team 优先复用已有启用长期智能体；没有合适长期智能体时，主智能体可按执行模式和审批策略先创建长期智能体，再加入 team。解散 team 物理删除 team 记录和成员关系，历史消息和事件保留。

## 构建、打包与交付

构建产物：

```text
apps/frontend -> dist，包含 index.html 和 plugin.html
services/center -> 可由 Node 运行的中心服务产物
apps/desktop-shell -> Electron 绿色版 zip，内含中心服务产物、前端资源、内联能力代码和图标
plugins/idea -> zhixin-agent-idea-版本.zip
```

桌面壳打包脚本负责把中心服务产物装配到 `resources/center`。前端构建脚本不负责复制中心服务文件，Web 端资源目录也不允许出现 `resources/center`、`services/center` 源码路径、SQLite 原生依赖或 Electron 主进程文件。

桌面端主要交付物支持绿色版 zip，解压即可运行。默认把中心目录放在解压目录下的 `center-data`，便于删除整个目录时清理程序本体和默认数据。

桌面壳启动时读取本机配置，拉起随包中心服务，并传入端口、中心目录和前端资源路径。

随包中心服务必须接收前端资源目录并提供 `index.html`、`plugin.html` 和 `/assets/*` 静态资源。桌面壳窗口加载 `http://127.0.0.1:端口`，IDE 插件加载 `http://127.0.0.1:端口/plugin.html`。

IDEA 插件构建继续使用 IDEA Gradle JVM，Java 编译输出固定 Java 21 字节码。无 Settings 页时禁用 `buildSearchableOptions`。Gradle 仓库优先 `mavenLocal()`，再阿里云公共镜像，再 Maven Central。

## 资源文件

应用图标固定文件名为 `图标.png`。

资源目录：

```text
assets/app-icon/图标.png
plugins/idea/src/main/resources/icons/zhixin.png
plugins/idea/src/main/resources/icons/zhixin-16.png
plugins/idea/src/main/resources/icons/zhixin-32.png
assets/ui-icons/mcp-call.svg
assets/ui-icons/file-read.svg
assets/ui-icons/file-write.svg
assets/ui-icons/file-delete.svg
```

`图标.png` 用于 Electron 应用图标、系统托盘图标、任务栏图标、窗口图标和绿色版资源。IDEA 插件在 `plugins/idea/src/main/resources/icons/zhixin.png` 保存同源大图标副本，在 `plugins/idea/src/main/resources/icons/zhixin-16.png` 和 `plugins/idea/src/main/resources/icons/zhixin-32.png` 保存菜单和工具窗口使用的小尺寸图标；`plugin.xml` 中的工具窗口和右键菜单 Action 必须引用 `zhixin-16.png`。

界面过程图标使用 SVG 和 `currentColor`，由前端根据当前主题统一控制颜色。

## 验收与迁移原则

静态协议验收：

- `packages/shared` 类型可被前端、中心服务和插件 SDK 使用。
- REST API 不出现路径参数。
- REST API 只使用 GET 和 POST。
- 响应包格式统一。
- 事件类型使用固定枚举。
- 插件开发当前只作为后续目标，不纳入本阶段验收。
- 模型调用必须直接使用 LangChain 的 OpenAI 和 Anthropic 能力，不再通过 `plugins/builtin-model-*` 动态适配器进入。

中心服务验收：

- 首次启动创建 `center-data` 目录。
- SQLite 迁移可重复执行。
- 中心服务启动锁防止同目录多实例。
- 本机访问和远程访问判定由服务端完成。
- 远程 Web Cookie 登录态有效。
- Worker 崩溃后任务标记失败并保留 `traceId`。
- 事件日志可用于断线补齐。
- Markdown 记忆只追加不覆盖。
- 敏感信息不返回明文。

前端验收：

- `index.html` 支持 workspace 和 mobile 模式。
- `plugin.html` 支持 plugin-compact 模式。
- 桌面端和 Web 端首次打开默认跟随系统主题，IDE 插件首次打开默认跟随 IDE 宿主主题，用户手动切换后当前客户端优先使用本地保存主题。
- 页面路由全部懒加载，第三方包独立拆成 vendor chunk，主入口文件不能混入页面组件和大体积三方依赖。
- 中心服务能通过 HTTP 托管前端入口和拆包 assets，桌面壳生产期不依赖 file 协议加载分包产物。
- 页面根容器固定视口高度。
- 消息列表是主滚动区域。
- 桌面壳才显示中心服务管理入口。
- 远程 Web 必须登录。
- IDE 插件只显示当前项目会话。
- IDE 多页签可并行执行并显示状态标志。

扩展能力验收：

- OpenAI 和 Anthropic 供应商通过 LangChain ChatModel 接入。
- 内联工具权限声明缺失时禁止执行敏感操作。
- 项目级能力优先于全局能力。
- 内联工具、MCP、skill 调用全部进入审计记录。
- 待办、日程、知识库作为中心服务一等领域模块可迁移。

迁移策略：

- 复制 `center-data` 即迁移用户数据。
- 项目身份跟随项目根目录的 `致心项目ID.md`。
- 项目路径变化后通过项目 ID 继续识别。
- SQLite 保存索引和事件，Markdown/JSON 保持可读。
- 附件保存在 `center-data/sessions/attachments`。
- `temp` 目录不作为迁移事实源，启动和停止时清理。

