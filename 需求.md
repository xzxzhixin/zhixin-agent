---
description: 本文件是项目产品需求、业务规则、交互口径、数据口径与验收口径的唯一事实源，用于约束“要做什么、为什么做、做到什么算完成”，避免需求散落在架构、设计、开发记录或临时说明中。修改任何产品能力、业务规则、交互口径或验收标准前，必须先更新本文件；如果与本文件现有内容冲突，必须先覆盖旧口径再落代码。本文只记录当前最新需求，不记录架构设计、执行计划、开发过程或临时 UI 调整。文件关系固定为：`需求.md` 定义目标与边界，`设计.md` 依据本文件细化实现方案与验收设计，`架构.md` 依据本文件和 `设计.md` 约束技术落点与工程边界，`功能清单与关系.md` 依据前三者整理功能拆分、依赖关系、影响范围与回归测试范围。若四者不一致，先修正 `需求.md`，再同步 `设计.md`、`架构.md` 与 `功能清单与关系.md`。文档改动完成后不做额外校验，直接提交并推送。用户后续提出的需求如果本文件没有明确覆盖，必须作为新增需求写入本文件；如果与本文件已有内容或用户之前明确提出的需求冲突，必须先告知冲突点并等待用户确认口径，确认后再按用户最终决定修订旧口径。
---

# ⏳ 个人中心服务 Agent 需求

- ✅ 产品名称固定为中文“致心智能体”、英文 `zhixin-agent`。
- ✅ 致心智能体定位为运行在用户中心电脑上的个人中心服务 Agent 平台。
- ✅ 中心服务是唯一事实源，统一维护用户记忆、智能体、会话、项目、任务、供应商、内联工具、MCP、skill、运行环境、通知、用量统计和扩展能力状态。
- ✅ 桌面客户端、Web端、手机浏览器和 IDE 插件端都是连接中心服务的客户端，不直接持有核心事实源。
- ✅ 平台服务一台中心电脑上的个人智能体工作流，覆盖个人事务和项目开发场景。
- ✅ 当前阶段个人事务和项目开发能力以中心服务内联工具、MCP 和 skill 接入；插件开发作为后续目标规划，本阶段不实现外置插件安装、动态插件扫描或插件市场能力。
- 🔄 `需求.md` 必须只保存当前最新产品需求、交互口径、数据口径、验收口径和业务规则，不保存架构、执行计划、开发记录或临时 UI 调整说明。
- 🔄 `需求.md` 采用从头到尾的单层清单列表维护需求状态，不再按端、模块或功能块展开分组说明。
- 🔄 `设计.md` 必须保存当前功能设计和验收设计；需求、设计、架构和真实代码实现必须持续保持一致。
- 🔄 需求状态图标固定使用 📋 未开始、⏳ 进行中、✅ 已完成、🔄 持续性。
- ✅ 所有敏感信息只保存在中心电脑，供应商 API Key、代理用户名密码、Web端访问账号密码等敏感信息由中心服务使用，客户端不能明文保存或展示已经保存的密码。
- ✅ 数据必须可迁移，中心目录保存所有固化数据；迁移中心目录后，应能继续使用已有记忆、智能体、会话、项目关联和扩展能力配置。
- ✅ 多端只同步业务数据，不强制同步本地 UI 状态；不同客户端可以看到同一套会话、消息、任务和智能体状态，但不强制同步当前打开窗口、当前页签、滚动位置和本地布局。
- ✅ 扩展能力不能绕过中心服务直接操作核心数据，内联工具、MCP 和 skill 可以参与任务执行，但不能绕过中心服务读写核心记忆、项目数据和会话状态。
- ✅ Agent 执行内核主路径已迁移为 Deep Agents，当前轮次直接进入 Deep Agents 原生 agent；Deep Agents 已提供的 planning/todo、虚拟文件上下文、subagents、上下文管理、工具调度和运行时中间件能力必须优先承载对应执行功能。
- ✅ 中心服务已删除 `deepagents-runner.ts`、自建 `StateGraph` 会话主执行图和旧工具执行图壳；当前轮次执行必须直接进入 Deep Agents 原生 agent，由 middleware 和 StructuredTool 组合承载工具、上下文和子智能体能力。
- ⏳ 中心服务继续作为唯一事实源和安全边界，Deep Agents middleware、StructuredTool 和子智能体都不能绕过中心服务直接读写核心 SQLite、Markdown 记忆、中心目录、供应商密钥、命令、MCP、skill、文件副作用或多端同步状态。
- ✅ 模型调用必须直接使用 LangChain 已支持的 OpenAI 和 Anthropic 协议能力，不再为 OpenAI 或 Anthropic 维护自研协议适配器。
- ⏳ 模型调用完成后必须由 Deep Agents middleware 写入模型调用日志，日志必须包含供应商、模型、协议适配器、协议模式、调用 URL 和模型返回的原始响应字段；OpenAI 兼容供应商、Anthropic 供应商和 OpenAI Responses 必须按协议精准区分，不能用供应商名称或工具名猜测协议。
- ✅ 模型没有返回结构化工具调用时，不能继续按文本猜测工具调用；历史文本 JSON 工具调用兼容逻辑必须删除。
- ⏳ 需要引入第三方包时，优先选择 GitHub star 数最多且近期仍维护的包；OpenAI 协议相关能力优先选择官方维护的 OpenAI SDK。
- ⏳ 桌面客户端使用 Electron 实现，是中心服务的本机管理入口，负责中心服务启动、停止、重启、健康检查、本机配置、完整工作台 UI 和桌面端交付。
- ✅ 桌面壳已实现中心服务启动、停止、重启、健康检查后加载页面、本机配置保存和中心服务配置页。
- ✅ 桌面壳开发期和发布后都必须是单实例；第二次启动只能唤醒已有窗口，不能重复创建桌面壳进程或重复拉起中心服务。
- ⏳ Web端连接中心服务使用，兼容桌面浏览器和手机浏览器，不负责中心服务生命周期管理。
- ⏳ 手机浏览器访问属于 Web端能力，需要响应式适配对话、消息查看、输入、通知和常用配置。
- ⏳ IDE 插件端以 WebView 承载插件页面，提供项目身份、项目上下文发送和内部文件定位宿主能力。
- ✅ IDEA 插件基础骨架已具备 Gradle 工程、`plugin.xml`、工具窗口、右键上下文入口、项目身份文件处理、`plugin.html` 加载和本机中心服务地址拼接。
- ✅ IDEA 插件工具窗口名称、工具窗口内容页签和右键菜单入口必须使用“致心智能体”展示口径，并在工具窗口和右键菜单中使用项目应用图标。
- ⏳ 中心服务监听本机服务端口，默认端口为 `8866`，并提供健康检查、目录映射、仓储、日志、访问控制、实时同步和统一错误格式。
- ✅ 中心服务开发控制台日志必须使用第三方结构化日志包统一输出，不能在执行链路中散落手写 `console.info` 或 `console.error`；日志时间必须使用中心服务本机时间格式；控制台中文必须按 UTF-8 原文显示，不能显示 mojibake 或 `\uXXXX` 编码；Windows 开发启动命令不能直接拼接 `chcp 65001`，编码转换必须由开发编排脚本内部跨平台处理；控制台保留命令启动和结束这类一头一尾日志，不输出命令输出块、运行中轮次等持续中间态，文件日志保留可审计终态、失败和关键节点。
- ✅ 每一轮对话在开发控制台都必须保留清晰的“轮次开始”和“轮次终态”日志；中间流式片段、命令输出块和无终态意义的运行中噪声仍不得持续刷屏。
- ⏳ 中心服务日志文件单个最大 `1MB`，命名使用 `center_YYYY_MM_DD_HH_mm_ss.log`，时间来源为中心服务本机时间；同一天内上一个日志文件未达到 `1MB` 时继续追加，不创建新文件；跨天后即使上一个日志文件未达到 `1MB` 也必须自动创建新的当天日志文件。
- ⏳ 中心服务生命周期只能由桌面端管理；开发、测试、验收和演示时必须通过桌面端或 `dev:desktop-shell` 拉起和停止。
- ⏳ 中心目录统一保存记忆、供应商、智能体、MCP、skill、会话、附件、日志和可迁移数据。
- ⏳ 项目身份文件固定为 `致心项目ID.md`，用于跟随项目迁移并帮助中心服务识别项目身份。
- ⏳ 智能体执行代码必须接入 `services/center/src/agents` 类层级，分散在 API、domain、tools 或模型运行时中的智能体类型裁决和工具权限过滤需要按功能迁移或收束到该目录，便于复用。
- ✅ 中心服务已具备 `BaseAgent`、`MainAgent`、`LongTermAgent` 和 `SubAgent` 类层级。
- ⏳ 系统内置主智能体“致心”，主智能体直接和用户对话，可配置供应商、模型和推理深度，但不能删除。
- ⏳ 用户可以手动创建多个长期智能体，主智能体和其他长期智能体也可以在权限允许时按任务需要创建新的长期智能体。
- ⏳ 每个智能体都必须维护自己的上下文，包括可见对话消息、子对话消息、记忆读取范围、任务拆解状态、todoList 状态、模型发送配置和 token 统计对象。
- ⏳ 每个智能体在中心服务权限、执行模式和会话上下文裁决后都可以请求外部 MCP 工具、命令工具、skill 和中心服务内部工具。
- ⏳ 智能体工具权限必须由中心服务统一工具 ID 映射到智能体权限名后裁决；官方 MCP adapter 注入的工具统一由中心服务包装为 MCP 权限边界，不能在调用点用零散条件分支猜测权限。
- ⏳ 主智能体可以使用创建长期智能体、创建子智能体和 team 管理工具。
- ⏳ 长期智能体可以使用创建子智能体工具，但不能使用 team 管理工具。
- ⏳ 子智能体不能继续创建任何智能体，也不能使用 team 管理工具。
- ⏳ 创建子智能体时，子智能体必须继承父级智能体当前实际使用的供应商、模型和推理深度配置。
- ⏳ 所有智能体都可以在长任务拆解或任务执行需要时维护自己的 todoList，todoList 必须按 `agentId` 隔离保存和展示。
- ⏳ todoList 属于智能体任务执行状态工具，不属于创建类工具；创建类工具只包含创建长期智能体、创建子智能体和 team 管理工具。
- ⏳ 任务入口只展示已经拆解出的多步骤任务；当前轮次只有一个默认任务且没有拆解步骤时，不显示任务入口数量或默认任务行。
- ⏳ 主智能体可以创建、解散 team，添加或移除长期智能体成员；team 跟随会话生命周期保存，会话删除时随会话事实清理。
- ⏳ 子智能体不能加入 team，不能创建 team，不能解散 team，也不能添加或移除 team 成员。
- ⏳ UI 需要同步展示主智能体、长期智能体和子智能体的空闲、工作中、排队中、等待用户、已结束和失败状态。
- ⏳ 记忆系统使用 Markdown、SQLite 索引和 Mem0 OSS 语义索引维护用户与智能体长期记忆。
- ⏳ 记忆写入以用户和 agent 之间的一轮完整对话为单位，每一轮完成后才允许写入记忆。
- ⏳ 主智能体长期记忆召回必须由 `mem0` 与现有 Markdown/SQLite 事实源联合完成；不得为“我是谁”“你叫什么”之类问题写死专项提示词或业务常量答案。
- ✅ 主智能体长期记忆只允许固化稳定用户事实、用户偏好和可信上下文；明显错误回复、回归验收口水或空洞失败回复不得写入 Markdown 记忆、SQLite 索引或 Mem0 同步。
- ⏳ 记忆写入按智能体隔离，每个智能体自己的记忆写入必须单线程执行，不同智能体之间可以并行写入各自记忆。
- ⏳ 永久记忆的段落标题时间和按年/月/日组织的记忆文件目录必须使用当前中心电脑的本机时间，不能使用 UTC 时间或模型返回时间。
- ⏳ 智能体创建、子智能体运行记录、team 创建或成员变更、智能体运行状态和智能体域记忆索引时间必须使用中心服务本机时间，格式固定为 `YYYY-MM-DD HH:mm:ss`。
- ⏳ Mem0 不能替代 Markdown 记忆文件，也不能成为唯一事实源；Mem0 生成的语义记忆必须保留来源会话、轮次、智能体、项目、Markdown 记忆路径和附件引用。
- ⏳ 中心服务维护普通会话和项目会话；普通会话不绑定具体项目，项目会话绑定某一个项目。
- ⏳ 新增项目对话必须先选择项目文件夹，浏览器端项目对话测试固定使用项目根目录 `项目对话测试` 文件夹。
- ⏳ 一轮对话从用户向 agent 发起一次输入开始，到 agent 完成本次回应、引导、排队、子智能体协作和相关处理并等待用户下一次输入为止。
- ⏳ 每一轮对话需要保存轮次编号、会话 ID、用户消息 ID、开始时间、结束时间、持续时长、当前状态、思考过程、工具调用、命令执行、文件操作、MCP、skill、子智能体协作和流式输出片段记录。
- ⏳ 长任务必须拆解为同一个当前轮次任务下的连续子任务步骤，自动续跑、工具批次切换和模型继续执行只能追加或更新同一 `taskId` 下的子步骤。
- ⏳ 超长期任务不能依赖固定总时长判断前端恢复结束；模型流式输出、命令输出、工具事件、任务步骤更新、步骤切换和中心服务心跳都必须作为运行活动信号刷新最近活动时间。
- ⏳ 中心服务必须提供 `session.turn.state` 轻量轮次状态事实源，用于返回当前会话运行中轮次、终态、最近事件序号、最近活动时间和最近助手消息时间；前端判断运行态是否继续展示时必须以该轻量事实为准，不能只依赖完整会话快照轮询次数或本地计时器。
- ⏳ 轮次进入完成、失败、取消或等待用户等终态时，中心服务必须追加统一 `turn.state.changed` 事件，事件载荷至少包含会话 ID、轮次 ID、状态、结束时间、持续时长、任务 ID 和最新事件序号，供多端即时收敛“当前轮次已耗时”和“停止”按钮。
- ⏳ 前端必须使用 `TurnStateReconciler` 收敛单会话单轮次运行态；当轮次未终态且完整快照没有返回助手消息或终态时，进入 `20ms` 疑似卡住快轮询 `session.turn.state`，收到终态、助手消息或完整快照终态后立即停止本地恢复并隐藏运行中展示。
- ⏳ 长任务拆解步骤必须支持计划版本、步骤顺序、来源、依赖、验收口径、替换关系和替换原因；用户中途补充或修改需求时，旧步骤保留历史并按实际情况标记为 `superseded`。
- ⏳ 执行图过程和用户可见任务拆解必须分离；Deep Agents 图节点、模型流、工具计划和内部执行边界只能形成过程卡片或审计事件，不能写入 `task_steps` 伪装成用户可见拆解步骤。
- ✅ 中心服务不得再保留自研 `thinking.context` 思考上下文节点；思考过程只允许来自供应商明确返回的可公开思考正文或公开思考摘要。
- ⏳ 任务面板只展示当前轮次当前任务的最新有效计划版本；旧计划版本和已替换步骤保留在事实源中，但不能和新计划一起叠加成重复任务行。
- ⏳ 工具执行、命令执行或 MCP 调用只有明确绑定到某个可见拆解步骤时，才允许联动更新该步骤状态；没有绑定关系时只能作为过程卡片展示，不能猜测某个任务已经完成。
- ✅ Deep Agents 自带 todoList 必须作为模型侧任务执行状态能力使用；中心服务不得再包装 `write_todos` 为 `builtin.deepagents.write_todos` 或旧 `builtin.todo.list` 内联工具。
- ⏳ Agent 工具循环上限只能作为安全预算边界，不能作为用户可见的普通失败终点；达到单批上限时应保存进度并在同一轮次内自动续跑下一批工具循环。
- ⏳ Deep Agents 单次 graph 结束只能作为中心服务候选终态，不能直接等同于任务完成；中心服务必须通过任务监督层判断最终回复、续跑、重试、等待用户或失败。
- ⏳ 任务监督层必须遵循 LangChain ReAct 语义：模型返回结构化 `tool_calls` 时由 Deep Agents/LangGraph 执行工具并继续循环；模型没有结构化 `tool_calls` 且 Deep Agents 结束后形成最终 AIMessage 时，该 AIMessage 就是最终回复，中心服务允许创建最终助手消息、写长期记忆、完成任务和轮次。
- ⏳ 任务监督层默认预算 `maxSupervisorAttempts`、`continuationRetryBudget` 和 `toolFailureRetryBudget` 必须全部为 `6`；同一失败原因连续出现时消耗对应预算，续跑后候选结果脱离上一类预算失败原因时必须把当前预算窗口和所有分类预算计数重置回 `6`。
- ⏳ 模型在需要工具推进时，可以同时返回简短过程文本和结构化 `tool_calls`；过程文本用于界面先展示，工具执行只能来自结构化 `tool_calls`。
- ⏳ 用户明确要求使用浏览器、命令、文件或 MCP 能力时，模型必须优先调用对应外部结构化工具推进任务；todo 或计划工具只能作为辅助记录，不能替代真实浏览、读取、点击或执行。
- ⏳ 模型在需要工具推进时只返回自然语言计划而没有结构化 `tool_calls`，属于模型没有遵守 ReAct 工具协议；中心服务不得从普通文本恢复工具调用，也不得按提示词内容硬编码调度工具，只能通过系统提示、工具定义、供应商工具调用兼容性和 `model.tool_calls.received` 等协议诊断改进工具调用质量。
- ⏳ 供应商返回的 `text` 内容块如果夹带 `id`、`name`、`args` 等工具调用字段但没有结构化 `tool_calls`，中心服务必须判定为工具协议形态错误，按监督预算要求模型改用结构化 `tool_calls` 重试；重试耗尽后进入等待用户或失败，不能执行文本里的工具名和参数，也不能把该畸形内容固化为最终回复。
- ⏳ 对话执行过程中，桌面端、Web端和 IDE 插件需要实时显示本轮开始时间、当前更新时间和已持续时间；本轮结束后固定展示开始时间、结束时间和总持续时长。
- ⏳ agent 回复、公开思考正文、模型工具请求、工具计划、任务步骤、工具调用进度、命令输出、工具批次自动续跑和子智能体协作过程都需要支持流式传输显示。
- ⏳ 前端收到当前会话 `event.appended` 后必须立即合并到本地事件流并渲染过程卡片；完整快照只能补齐事实，不能覆盖已经实时收到的更新事件。
- ⏳ 前端合并当前会话实时事件时必须刷新全局事件数组新引用并递增事件版本号，保证过程卡片、模型中途 Markdown 和快照兜底都能实时重算。
- ⏳ 前端运行态恢复轮询 `session.turn.state` 时，如果中心服务返回的 `lastSequence` 大于本地已合并事件序号，必须立即拉取完整会话快照补齐缺失事件，不能只刷新最近活动时间。
- ⏳ 对话页面首屏会话详情、历史消息、事件补齐、发送消息、附件动作、任务步骤、流式 token、等待用户、错误状态和多端同步都必须通过 WebSocket 完成。
- ⏳ 对话内容显示区域的滚动规则必须支持用户接管，用户主动滚动后停止自动贴底，并按最近滚动方向显示回顶或回底箭头。
- ⏳ 中心服务需要通过 Deep Agents 原生 agent 承载上下文整理、模型流式输出、工具计划、工具执行、工具结果回填、todoList、子智能体委派、消息固化前执行过程和失败收尾；中心服务只保留领域服务、权限校验、事件适配、消息持久化、记忆写入、用量记录和 WebSocket 同步。
- ⏳ 真实 Agent 工具调用必须形成多步闭环：模型请求工具，中心服务执行或发起审批，工具结果回填给模型，模型基于工具结果继续生成最终回复。
- ✅ Deep Agents、StructuredTool、MCP 或 skill 运行时一旦抛出异常，当前轮次必须立即收尾为失败或等待用户，不得长时间停留在运行中，也不得只留下过程事件而没有轮次终态。
- ⏳ 工具执行和工具结果回填必须收敛在 Deep Agents 原生工具链路内；中心服务不得再保留独立 `tool.result` 节点、`tool.execute` 图壳实现或按用户文本猜测工具调用的旧入口。
- ⏳ 中心服务需要把当前会话、当前项目、当前智能体可用的工具能力整理为 Deep Agents 可注入的结构化工具定义；工具实现统一迁为 middleware 注入或 StructuredTool 类工具。
- ⏳ 工具定义至少包含工具名称、能力说明、输入参数 schema、权限要求、风险等级、适用范围、是否需要审批、可见展示文案和不可用原因。
- ⏳ 硬编码规则只允许作为临时降级兜底、明确快捷入口或安全拦截策略，不能作为 Agent 判断工具调用的主要机制。
- ⏳ 执行模式需要作用到每一次工具调用；建议模式、自动编辑和全自动都必须受中心服务权限、沙箱和审计约束。
- ✅ 工具调用过程需要完整展示在 UI 中，用户应能看到 Agent 为什么请求工具、工具参数摘要、审批状态、执行时间、输出摘要、失败原因、排查 ID 和工具结果如何影响最终回复。
- ⏳ 命令工具对模型暴露的结构化参数必须要求 `shellCommand` 和 `inputSummary`。
- ⏳ 中心服务运行时可继续兼容 `executablePath`，但模型缺少命令字段时只能生成可展示的工具失败事件，不能把空字符串传给进程启动函数导致整轮对话崩溃。
- ⏳ 模型可见工具 JSON Schema 必须兼容当前供应商结构化工具接口，不能使用会被供应商拒绝的顶层 `oneOf`、`anyOf`、`allOf` 或同类组合关键字。
- ⏳ 用户明确要求使用命令工具、执行命令或查看本机环境版本时，模型必须通过供应商结构化 `tool_calls` 调用命令工具，不能只返回命令文本、代码块或自然语言说明。
- ✅ 中心服务不再按用户文本固定解析并强制工具选择；工具提示和模型工具定义必须使用模型可见工具安全名，不得把内部工具 ID 当作供应商工具名。
- ✅ OpenAI 兼容供应商返回空工具名时，中心服务必须按协议错误处理并失败收尾，只记录 `model.tool_call.name_missing` 诊断事件；事件日志必须带上最后一条模型输出诊断和原始模型返回摘要；不得按结构化参数、用户提示词、关键词、固定场景或具体 MCP 工具名恢复工具名。
- ✅ OpenAI 兼容供应商流式 `tool_calls` 需要按同一工具调用的 `index` 或 `id` 合并；首段返回非空 `function.name` 后，后续参数分片里的空 `function.name` 不能覆盖首段真实工具名。
- ✅ 中心服务逻辑代码不得硬编码具体 MCP 工具名处理模型回调；模型回调进入工具链路时必须已经携带供应商返回的真实工具名。
- ✅ MCP 工具调用参数必须按官方 adapter tool 原样执行，中心服务不得为具体 MCP 工具做描述增强、参数补全或按用户提示词关键词推断路径。
- ✅ MCP adapter 工具结果回填模型时优先使用官方 adapter 的 content 文本，artifact 只作为非模型上下文保留，避免同一 MCP 结果在过程卡片和模型上下文中重复出现。
- ⏳ LangChain 流式工具调用解析必须只把完整参数对象转换为中心服务工具调用；流式 `delta` 片段不能因为缺少参数被默认解析为 `{}`。
- ⏳ 命令工具空参数失败只能固化为可展示失败回复，不能反复回填模型造成循环失败。
- ✅ 工具结果回填给模型后不再由中心服务强制同一工具选择，避免模型在已有工具结果后重复调用同一个工具。
- ✅ 对话页面只允许展示模型真实生成的说明文字、模型真实发起的工具调用、工具真实执行结果和基于这些结果生成的最终回复；中心服务写死的阶段文案、内部执行占位文案和没有模型语义来源的过程摘要都不能显示在页面消息流中。
- ⏳ 中心服务 API 必须有真实业务调用方；仅被检查脚本、临时验收脚本或旧兼容脚本引用的 URL 不算实际使用，确认无前端、API 客户端、中心执行链路或外部协议调用后必须删除。
- ⏳ 项目使用 UUID 作为唯一 ID，不直接使用项目路径作为 ID；项目文件夹迁移后只要项目 ID 文件还在就应继续识别为同一个项目。
- ⏳ IDE 插件打开项目时，先读取或创建 `致心项目ID.md`，再连接中心服务。
- ⏳ 项目显示名称必须使用项目文件夹名，项目别名或备注只能作为备注信息展示。
- ⏳ 项目聊天执行时，优先使用当前项目配置的 MCP 和 skill；只有当前项目没有同名能力时，才使用全局 MCP 和 skill。
- ⏳ IDE 插件中可以为同一个项目打开多个聊天页签，每个页签对应一个项目聊天会话。
- ⏳ 桌面端和 Web端中，项目聊天显示为项目下的子聊天；项目聊天内容、智能体状态、消息状态在 IDE 插件、桌面端和 Web端之间实时同步。
- ⏳ 多端实时同步必须覆盖当前会话详情、后台会话导航列表和 IDE 插件项目会话。
- ⏳ 任一端发送消息后，其他端即使停留在其他会话，也需要通过 WebSocket 收到会话更新时间、标题和最新状态刷新。
- ⏳ 其他端如果只收到本轮起始 `session.updated`，也必须启动运行中轮次活动续租式恢复，从中心服务快照恢复助手消息、过程输出、任务终态和轮次结束时间；只要最近仍有输出、步骤更新或心跳，就不能因为固定轮询次数到达而停止恢复。
- ⏳ 中心服务关闭、桌面壳退出或服务异常重启后，数据库中上一进程遗留的 `queued`、`running`、`waiting_user` 轮次、任务和步骤必须被收尾为可展示终态，不能在下次启动后继续作为运行中轮次恢复。
- ⏳ 前端恢复运行中轮次时，如果中心服务快照中的轮次长时间没有任何活动推进且明显早于当前进程重启时间，必须停止本地恢复并隐藏该轮次的运行中展示，避免历史脏数据长期占据“当前轮次已耗时”与“停止”按钮。
- ⏳ 客户端和中心服务断开连接后需要持续自动重连并展示连接状态；异常断线不能永久停在已停止，只有用户或上层主动关闭连接才允许进入停止状态；实时连接未恢复时用户主动发送、引导或停止不能进入中心服务，不能创建会话、不能清空草稿或移除本地排队消息；本地未发送排队消息不能在恢复连接后自动发送。
- ⏳ IDE 插件本机授权必须绑定当前项目 ID，WebSocket 握手和项目登记顺序不能互相阻塞；插件端必须先建立实时连接，再通过中心服务登记或更新当前项目上下文。
- ⏳ 对话输入框需要支持文本、附件和上下文引用的混合输入。
- ⏳ 富文本输入框发送前的文本、附件、文件引用、代码位置和 IDE 选区只属于草稿，不能写入长期记忆。
- ⏳ 用户发送消息后，中心服务必须把草稿固化为正式消息事实，包含文本、归档附件引用、项目引用和代码引用来源。
- ⏳ 正式附件文件只保存一份，归档到 `center-data/memory/attachments/{year}/{month}/{day}/{attachmentId}/原文件名`，会话消息和长期记忆都引用该归档附件。
- ⏳ 删除会话只删除会话和附件引用关系，不删除已经归档并可被长期记忆引用的附件文件。
- ⏳ 轮次完成后，长期记忆必须写入可复用文本事实和正式附件来源引用；可解析附件需要异步写入内容摘要，摘要失败不得影响主轮次终态。
- ⏳ 跨窗口对话前必须通过 Mem0、SQLite `memory_index` 和 Markdown 来源联合召回长期记忆，不能依赖固定身份问题、固定业务词或用户提示词硬编码。
- ⏳ 桌面端、桌面浏览器 Web端和 IDE 插件工作区的对话输入区必须使用更宽、更高的稳定胶囊式输入框，并固定在当前对话区底部。
- ⏳ 对话输入区或对话顶部需要提供当前会话的模型和推理深度选择，供应商只能作为模型来源或会话设置说明展示。
- ⏳ 输入框内部三段入口固定为“任务 / 智能体 / 编辑”，下方主体包含附件入口、文本输入区、当前窗口上下文用量、底部控制条和发送按钮。
- ⏳ 当前窗口上下文用量展示格式为 `占用比例 / 窗口上限`，统计对象是当前会话窗口和当前智能体已经形成的对话上下文总量。
- ⏳ 用户输入过程中不进行 token 统计；token 只在本轮执行中根据中心服务或模型事件更新，并在本轮完成后显示最终总览。
- ⏳ 运行中轮次存在时，用户按 Enter 发送的新消息不能直接打断或替换当前轮次，必须进入当前对话的本地排队消息区。
- ⏳ 点击排队消息右侧的“引导”后，该排队消息必须立即从排队区移除。
- ⏳ 发送按钮空闲时显示“发送”，当前对话存在运行中或排队轮次时显示“停止”；轮次进入等待用户后必须隐藏“当前轮次已耗时”和“停止”按钮，输入区用于补充引导、审批或确认。
- ⏳ 点击“停止”只取消当前会话当前正在执行的轮次，不取消同一会话输入框中的本地排队消息，也不影响其他会话或 IDE 页签。
- ⏳ 附件类型不限制为多媒体文件，必须支持图片、音频、视频、PDF、Word、Excel、压缩包、文本文件和其他普通文件。
- ⏳ 用户可以直接把剪贴板中的图片、文件或只有内存数据的附件内容粘贴到对话输入框中。
- ⏳ 临时附件在发送前只属于输入框草稿，不进入正式会话消息，也不能进入永久记忆；用户真正发送消息后，中心服务需要把临时附件转为正式归档附件引用。
- ⏳ 输入框以 `/` 开头时进入命令和 skill 检索模式。
- ⏳ 项目级别会话中，输入框需要支持 `@` 检索项目文件信息并插入文件、文件夹或代码位置引用。
- ⏳ 插件页面、聊天消息和 Markdown 渲染内容中，需要支持内部文件定位链接，点击后由已安装的 IDE 插件打开对应文件并跳转到指定行号或选区。
- ⏳ 内联工具、MCP 和 skill 扩展能力由中心服务统一管理，供主智能体、长期智能体和子智能体按权限使用。
- ⏳ MCP Server 配置必须同时支持 HTTP 和 stdio 两种协议，MCP 工具发现、工具调用、输出摘要和失败原因都必须进入同一套工具调用闭环和审计事件。
- ✅ MCP 工具暴露给 Deep Agents 的主路径必须使用官方 `@langchain/mcp-adapters` 的 `MultiServerMCPClient.getTools()`，中心服务只负责配置来源、权限过滤、审计事件、结果包装和失败收尾，不再维护自建 MCP 短名注册表或自写 `tools/call` 主路径。
- ✅ MCP adapter 工具结果优先以文本结果回填模型，避免部分 MCP Server 实际输出与声明 structured output schema 不一致时导致工具调用失败。
- ⏳ Deep Agents 当前轮次注入的 MCP 工具必须保留模型安全工具名和工具描述，供模型自主选择；中心服务不得按用户提示词把 IDEA 状态查询硬编码到某个 MCP 工具或命令工具。
- ✅ 全局 MCP 配置管理已具备全局 MCP Server 保存、列表、单服务编辑和工具查看入口。
- ⏳ skill 系统用于给 agent 提供可复用的工作流程、专业知识和行为约束，支持全局安装和项目安装。
- ✅ 全局 skill 安装与列表已具备中心服务和前端的全局 skill 安装、保存和列表入口。
- ⏳ 供应商用于配置模型或能力提供方，由中心服务统一管理，支持添加、修改、删除、启用、停用。
- ✅ 供应商保存与启用允许先保存未完整供应商，启用时校验完整性并统一提示。
- ⏳ 供应商配置包括名称、类型、接口地址、API Key、可用模型列表、默认模型、推理深度、代理策略、启用状态和能力声明。
- ⏳ 网络代理由中心服务统一管理，至少支持 HTTP、HTTPS、SOCKS4、SOCKS4a 和 SOCKS5。
- ⏳ 供应商的代理策略只影响后续供应商访问、模型列表刷新、推理深度刷新和模型调用，不回改历史对话、任务记录和调用记录。
- ⏳ 供应商配置弹框的“手填模型与上下文”区域需要提供“获取”按钮，由中心服务使用已保存供应商配置和 API Key 调用供应商模型列表接口。
- ⏳ 模型列表获取成功后需要按模型名称中匹配到的数字段降序排序，并自动写入默认模型和选项。
- ⏳ 供应商页面需要支持维护模型上下文窗口，表单统一使用 `K` 作为输入单位。
- ⏳ 运行环境管理 Node.js、Python、Java、Maven、Git 等可执行程序和工具链目录。
- ⏳ pnpm、npm、yarn、npx、uv、pip 等包管理器或命令入口不作为独立运行环境类型内置。
- ⏳ 执行插件、MCP Server、skill 脚本或命令任务时，可以指定使用某个运行环境；未指定时使用该类型的默认启用环境。
- ⏳ 用量统计需要记录模型调用、token、费用估算、供应商维度、项目维度和每日聚合。
- ⏳ 用量统计界面必须图形化展示统计结果，至少包括总量概览、各供应商维度和项目维度。
- ⏳ 通知配置需要支持对话完成通知、窗口不活跃时通知、普通对话、项目对话、智能体协作对话、失败或需要用户处理的对话。
- ⏳ 通知配置按客户端类型分别保存，所有通知都由中心服务统一判断、生成和发送。
- ⏳ 对话执行模式支持建议、自动编辑和全自动，并作用到每一次工具调用。
- ⏳ 执行模式不在桌面端、Web端、IDE 插件之间互相同步，而是按客户端类型分别保存和同步。
- ⏳ Web端如果从非本机访问中心服务，首次打开页面必须进入登录页，输入桌面端配置的账号和密码后才能访问。
- ⏳ Web端登录态必须使用 Cookie 保存和传递，不能使用 `sessionStorage` 保存登录 token。
- ⏳ IDE 插件最终需要打包为可安装到对应 IDE 中的插件，各 IDE 平台分别维护 SDK 工程，但插件页面统一通过 WebView 加载中心服务提供的 `plugin.html`。
- ⏳ IDEA 插件工程名和安装包名使用 `zhixin-agent-idea`，插件管理器展示名、工具窗口名和右键菜单展示口径使用“致心智能体”，包名和插件 ID 命名空间统一使用 `top.xzxsrq`。
- ⏳ 安全与访问控制要求 Web端访问来源判断必须由中心服务完成，前端不能只依赖 `window.location.hostname` 自行判断。
- ⏳ 账号密码等访问控制信息只保存在中心电脑，由中心服务校验；IDE 插件不使用账号密码登录，只使用本机 `127.0.0.1` 加端口连接。
- ⏳ UI 与主题支持暗黑和亮色，各客户端首次打开默认主题必须匹配当前客户端环境，用户手动切换后本地优先。
- ✅ 前端路由与工作台页面已按独立路由和 `RouterIndex.vue` 拆分对话、智能体管理、供应商、代理、运行环境、用量、插件、MCP、skill 和中心服务页面。
- ✅ 主题模式已支持暗黑和亮色主题、首次跟随系统或 IDE 宿主主题、用户手动切换后本地优先。
- ✅ 应用图标固定文件名为 `图标.png`，用于 Electron 应用图标、系统托盘图标、任务栏图标、窗口图标和安装包图标；IDEA 插件入口图标使用由该文件派生的小尺寸资源，避免菜单图标撑开布局。
- ⏳ 桌面端、Web端桌面浏览器和后续插件页面的主工作区 UI 参考 Codex App 的工作台形态，整体窗口、侧边栏、顶部栏、右侧状态栏、对话区和输入区必须使用 flex 布局组织。
- ⏳ 对话导航分为“项目对话”和“普通对话”，项目对话按项目分组展示并支持收起或展开。
- ⏳ 项目对话行和普通对话行右侧需要常态展示对话时间节点，时间只允许显示在行右侧。
- ⏳ 桌面端对话页面右侧状态栏只展示任务状态和智能体状态，不展示中心服务配置摘要。
- ⏳ 右侧任务状态栏只展示当前对话运行概览，不展示完整拆解列表；完整拆解步骤只通过底部输入区“任务”入口查看。
- ⏳ 拆解任务行必须单行紧凑展示：左侧显示状态和步骤标题，右侧不显示任何时间、耗时、序号、总数、状态、第二行摘要、当前动作、责任智能体、失败原因或替换原因。
- ⏳ 对话过程卡片、任务步骤卡片和任务浮窗行只有对应任务或过程真实处于运行态时才显示“执行中”；任务或过程完成、失败、取消后必须展示对应终态，不能因为历史步骤或过程事件残留继续显示执行中。
- ✅ 对话消息列表需要展示一轮对话中的完整执行过程，包括回复、思考折叠块、模型工具请求卡片、工具计划卡片、任务步骤卡片、工具调用卡片、命令执行卡片、文件操作卡片、MCP 调用卡片、skill 使用记录、智能体协作记录和错误详情。
- ⏳ 工具、命令、MCP、skill 等过程按一次调用一个过程卡片展示，同一张卡片承载开始、执行中、完成和失败全部状态；同一轮中连续调用多个命令、多个 MCP 工具或多个 skill 时，仍按调用维度分别形成独立过程卡片，不能合并到同一个框里。
- ⏳ 命令执行卡片内容区参考 bash 展示：第一行展示真实命令，换行后直接流式追加执行过程；不再拆分“原始命令”“执行结果”或“时间线”等小标题。
- ⏳ 所有非工具过程的模型中途文字都作为独立无标题 Markdown 卡片展示，按流式片段追加；不能与工具卡片或最终回复合并。
- ✅ 工具过程卡片标题只占用一行，超出可用宽度时用省略号展示；正文内容区最大高度为 `200px`，超出后在卡片正文内部滚动。
- ⏳ 所有 UI 展示时间必须统一使用 `YYYY-MM-DD HH:mm:ss` 格式。
- ✅ 思考内容必须来自供应商返回的真实可公开自然语言思考内容或供应商明确返回的可公开思考摘要字段，不能用固定阶段摘要冒充模型真实思考。
