# 个人中心服务 Agent 需求

## 产品定位

中文名字：致心智能体

英文名字：zhixin-agent

致心智能体是一个运行在用户中心电脑上的个人中心服务 Agent 平台。

系统以中心服务作为唯一事实源，统一维护用户记忆、智能体、会话、项目、任务、供应商、插件、MCP、skill、运行环境、通知、用量统计和扩展能力状态。桌面客户端、Web端、手机浏览器和各 IDE 插件端都是连接中心服务的客户端，不直接持有核心事实源。

平台服务一台中心电脑上的个人智能体工作流，既支持个人事务类场景，也支持项目开发类场景。个人事务能力通过插件、MCP 和 skill 接入，但这些接入能力属于本需求完整实现范围，例如日程、待办、资料管理、个人知识库和自动化流程。

## 核心原则

中心服务是唯一事实源。智能体、会话、项目、任务、记忆、供应商、插件、MCP、skill 和扩展能力状态都由中心服务统一保存和同步。

所有敏感信息只保存在中心电脑。供应商 API Key、代理用户名密码、Web端访问账号密码等敏感信息由中心服务使用，客户端不能明文保存或展示已经保存的密码。

数据必须可迁移。中心目录保存所有固化数据；迁移中心目录后，应能在另一台电脑上继续使用已有记忆、智能体、会话、项目关联和扩展能力配置。

多端只同步业务数据，不强制同步本地 UI 状态。不同客户端可以看到同一套会话、消息、任务和智能体状态，但不会强制同步当前打开窗口、当前页签、滚动位置和本地布局。

扩展能力不能绕过中心服务直接操作核心数据。插件、MCP 和 skill 可以参与任务执行，但不能直接绕过中心服务读写核心记忆、项目数据和会话状态。

## 端形态

### 桌面客户端

桌面客户端使用 Electron 实现，是中心服务的本机管理入口。

桌面客户端负责：

- 启动、停止、重启和监控中心服务。
- 打开桌面客户端时自动启动中心服务，关闭桌面客户端时停止由桌面端管理的中心服务。
- 配置中心服务端口，默认端口为 `8866`。
- 配置中心目录位置。
- 配置 Web端非本机访问账号和密码。
- 检测系统通知权限并同步给中心服务。
- 提供完整桌面端工作台 UI，包括对话、供应商、网络代理、运行环境、用量统计和中心服务管理页面。

桌面客户端构建安装后必须携带中心服务，用户不需要再单独安装或手动启动中心服务。

项目主要构建交付物是 Electron 桌面端安装包。这个安装包包含桌面客户端、中心服务和可由中心服务提供的 Web端页面资源，但不包含各 IDE 插件安装包。IDE 插件按各自平台单独构建和安装。

中心服务只在桌面端安装包或绿色版桌面产物中动态装配。Web端页面构建产物只包含浏览器运行所需的前端资源和客户端代码，不能把中心服务入口、SQLite、本地数据库驱动或桌面壳专属文件打入 Web端资源包。

### Web端

Web端连接中心服务使用，兼容桌面浏览器和手机浏览器。

桌面客户端启动中心服务后，本机用户可以通过 `http://127.0.0.1:端口` 打开 Web端页面；默认情况下端口为 `8866`，也就是 `http://127.0.0.1:8866`。

Web端可以查看和使用普通对话、项目对话、智能体、供应商、网络代理、运行环境、用量统计、插件、MCP 和 skill 等能力。

手机浏览器访问属于 Web端能力，不单独拆分为移动端。手机浏览器需要根据小屏幕做响应式适配，不能把桌面端复杂表格和大屏布局直接塞进手机页面。

Web端不负责启动、停止或重启中心服务。非本机访问 Web端时，必须先通过中心服务校验桌面端配置的账号和密码。

### IDE 插件端

各 IDE 插件端使用 WebView 作为基座，不重新实现完整业务 UI。

IDE 插件端默认加载中心服务提供的 `plugin.html`，并由 WebView 承载插件页面。IDE 插件负责提供宿主能力，例如：

- 读取或创建当前项目的 `致心项目ID.md`。
- 获取当前项目路径和项目名称。
- 配置连接中心服务的端口。
- 默认连接 `http://127.0.0.1:端口`。
- 在编辑器、文件树、标签页等位置提供右键发送上下文入口。
- 把文件、文件夹、代码行或选区引用发送到 WebView 中的对话输入区。
- 点击内部文件定位链接时，调用 IDE 能力打开文件并跳转到指定位置。

IDE 插件不直接展示其他项目的数据，不使用 Web端账号密码登录，不暴露供应商 API Key 明文。

## 中心服务

中心服务监听本机服务端口，默认端口为 `8866`。

中心服务需要支持通过桌面客户端配置端口和中心目录。端口、中心目录、Web端访问账号密码、通知权限状态等本机配置保存到中心服务配置文件中。

中心服务启动时必须创建中心目录及必要子目录。中心服务未收到中心目录配置时，开发期默认使用当前代码工程根目录下的 `中心` 目录；桌面端构建安装后默认使用桌面端启动位置下的 `中心` 目录。

中心服务需要提供基础能力：

- 健康检查接口。
- 中心目录映射接口。
- 统一 JSON 和 Markdown 文件仓储层。
- 日志写入能力。
- API 错误格式。
- 中心服务启动锁，避免多个桌面端重复启动同一个中心目录服务。
- Web端访问控制和登录态管理。
- 实时同步通道。

中心服务 API 错误格式需要统一返回错误码、错误消息、可展示原因和排查 ID。

## 中心目录与数据固化

所有固化信息都存放在中心服务的中心目录下，方便统一管理、迁移和删除。

如果中心服务中心目录是：

```md
D:/中心
```

那么中心级目录树为：

```md
D:/中心
├─ 记忆
├─ 供应商
├─ 智能体
├─ 插件
├─ MCP
├─ skills
├─ 运行环境
├─ 会话
├─ 临时
└─ 日志
```

目录说明：

- 记忆：保存用户记忆、主智能体记忆、其他智能体记忆、子智能体任务沉淀出的可保存记忆。
- 供应商：保存模型或能力提供方配置，包括接口地址、可用模型、默认模型和推理深度等信息。
- 智能体：保存主智能体、用户创建的其他智能体和可固化智能体定义。
- 插件：保存中心级插件清单、代码、配置、启用状态和调用记录。
- MCP：保存中心级 MCP Server 配置、运行参数、启用状态和调用记录。
- skills：保存中心级 skill 内容、说明、启用状态和调用记录。
- 运行环境：保存 Node.js、Python、Java、Maven、Git 等可执行运行环境配置。
- 会话：保存普通会话和项目会话的状态、消息、任务记录、项目关联信息和会话级配置。
- 临时：保存输入框粘贴、拖入或接收但尚未发送的临时附件文件和临时附件元数据。
- 日志：保存中心服务、客户端连接、任务执行、扩展能力调用和异常排查日志。

中心目录是中心服务的数据根目录。删除或迁移中心目录，就等于删除或迁移该中心服务的固化数据。

`临时` 目录不属于永久业务事实源，只用于保存尚未发送的输入框附件。中心服务启动和停止时需要执行一次临时目录清理，删除未绑定正式消息的临时文件，避免上次异常退出残留垃圾数据。

项目不作为中心目录下的独立顶层目录。项目身份、项目聊天、项目记忆、项目任务和项目扩展能力配置都归属于项目会话，由中心目录的“会话”目录统一承载。

项目根目录中的 `致心项目ID.md` 是例外。它用于跟随项目迁移，帮助中心服务识别项目身份；项目详细配置、项目聊天、项目记忆和项目扩展能力配置仍然存放在中心目录的“会话”目录下。

## 智能体体系

### 主智能体

系统内置一个主智能体，中文名为“致心”。

主智能体直接和用户对话，是用户进入平台后的默认智能体。主智能体可以派发任务给其他智能体，也可以在没有合适智能体时创建新的智能体协作完成任务。

主智能体有自己的记忆，可以由用户修改，但不能删除。

主智能体的承载供应商、模型和推理深度可以由用户修改。如果只有一个启用的供应商，则默认使用该供应商。

### 用户创建的智能体

用户可以手动创建多个智能体，主智能体和其他长期智能体也可以在对话中按任务需要创建新的长期智能体。

每个创建出来的智能体都必须固化为 Markdown 文件。每个智能体都需要保存：

- 智能体 ID。
- 智能体名称。
- 角色说明。
- 能力边界。
- 可用插件、MCP 和 skill 范围。
- 默认供应商、模型和推理深度配置。
- 记忆目录或记忆索引。
- 创建来源。
- 当前启用状态。

所有智能体默认都可以按权限使用中心服务管理的外部扩展能力，包括插件、MCP 和 skill。智能体定义中的可用插件、MCP 和 skill 范围用于描述该智能体的权限边界和默认启用范围，不表示外部扩展能力只能被某一个智能体独占。

项目级别对话中，当前项目提供的项目级插件、MCP 和 skill 也会进入该对话的可用能力范围。项目级能力与全局能力同名时，按项目级优先规则使用项目级能力。

项目级插件、MCP 和 skill 在项目对话页中不能常驻占用大块主内容区域。对话页只保留图标按钮或简短摘要入口，用户点击后打开“项目能力详情”弹框。弹框需要说明项目级插件、MCP 和 skill 由打开项目目录或登记项目上下文触发扫描，会注入当前项目对话上下文，不在顶部全局插件、MCP、skill 管理页中管理。

项目能力详情弹框需要展示当前项目 ID，并分别列出项目级插件、MCP 和 skill 的名称、来源、全局或项目级范围、启用状态以及不可用原因。没有扫描到项目级能力时，也需要说明当前项目暂无对应项目级能力，而不是把全局能力混入项目能力列表。

每个智能体都有自己的记忆。不同智能体的记忆需要隔离保存，不能混写到同一个智能体记忆文件中。

除不可删除的主智能体外，所有长期固化智能体都可以由用户统一管理，包括用户手动创建的智能体、主智能体创建的智能体，以及其他长期智能体创建的智能体。用户可以查看、添加、修改、停用和删除这些智能体。

删除智能体时，必须向用户提示删除影响。用户确认删除后：

- 删除或归档该智能体的专属记忆，具体策略需要由 UI 明确提示。
- 从正在使用该智能体的会话中移除后续可选入口。
- 移除该智能体的后续任务调度入口。
- 已经产生的历史对话内容按会话记录保留，但是不再绑定为可继续使用的智能体。

### 子智能体

任意主智能体或用户创建的智能体在执行任务时，都可以创建自己的子智能体。

子智能体用于执行一次性任务。子智能体只可以在任务执行过程中引导用户，不作为长期持续对话对象。

子智能体任务完成后需要关闭，只保留必要的短期窗口记忆和任务记录。需要沉淀为长期记忆的内容，必须通过创建它的智能体归纳后写入对应智能体记忆。

子智能体不能再创建新的子智能体，避免形成不可控的多级代理链路和无限套娃。所有需要继续拆分、派发或并行执行的任务，都必须回到创建它的主智能体或用户创建智能体统一调度。

### 智能体协作

用户发起一次对话后，主智能体和其他智能体都可以按任务需要激活其他智能体参与本轮处理。

智能体相互协作时，必须优先激活已经存在的长期智能体。只有现有智能体都不适合当前任务，或者用户明确要求创建新角色时，才允许创建新的长期智能体，避免无限制创建。

智能体之间可以相互通话。相互通话支持管线形式，一个智能体把阶段性结论、上下文或任务结果传给下一个智能体继续处理。

智能体之间也可以创建群聊，多个智能体在同一个协作讨论中共同分析、辩论、补充和收敛结论。

管线通话、群聊讨论、参与智能体、消息流向、当前状态和最终结果都需要在 UI 端显示出来，方便用户查看、介入、暂停、结束和管理。

### 智能体状态

所有 UI 需要同步展示智能体状态。

智能体状态至少包括：

- 空闲。
- 工作中。
- 排队中。
- 等待用户。
- 已结束。
- 失败。

UI 上需要能看到各个智能体和子智能体正在做什么，包括当前任务、阶段状态、是否等待用户、是否失败、是否已经结束。

多个对话框、多个项目会话或多个 IDE 页签可以并发执行，并且可以同时展示为“执行中”。“排队中”只表示当前对话框或当前页签内部的任务正在等待该对话内上一项处理完成，不表示全局所有对话只能串行执行。项目行、对话行、右侧任务状态和智能体状态的文案或 tooltip 必须明确这个作用域。

## 记忆系统

记忆系统使用 Markdown 文件保存，统一存放在中心目录的“记忆”子目录中。

用户记忆在一台中心电脑上只有一份，保存为：

```md
记忆/user.md
```

用户记忆用于记录用户是怎样一个人、偏好、长期目标和重要背景。

每个智能体都有自己的记忆。智能体永久记忆按文件夹组织，最后一层才是真正的 Markdown 文件：

```md
智能体名字/年/月/日.md
```

永久记忆每段格式为：

```md
# HH:mm:ss

## 关键词

## 总结

## 使用的电脑

## 用户说的

## 回答的
```

记忆写入以用户和 agent 之间的一轮完整对话为单位。一轮对话不是指 agent 和大模型之间的一次请求，也不是内部工具调用或子任务调用，而是指用户向 agent 发起一次输入后，agent 完成本次回应、引导、排队、子智能体协作和相关处理，直到需要等待用户下一次输入为止。

引导过程也属于这一轮对话的一部分，不能单独拆成多段记忆写入。

引导、审批和需要用户确认都归属于当前对话框当前轮次内的处理流程，不是全局功能。UI 在输入区、消息流或任务状态说明中需要明确该作用域，避免用户误解为所有对话都会被同一个全局确认流程阻塞。

每一轮用户和 agent 的对话完成后，才允许写入记忆。

记忆写入按智能体隔离。每个智能体自己的记忆写入必须单线程执行，避免同一个智能体的记忆文件竞争写入。

不同智能体之间可以并行写入各自的记忆。

记忆读取可以多线程执行。

记忆写入只能追加，不能覆盖或插入修改已有记忆内容。

附件文件仍然放在会话附件目录中，记忆内容中保存对正式附件的结构化引用关系。

永久记忆不能引用 `临时` 目录中的文件，也不能把附件二进制内容直接写进 Markdown。只有已经发送并转为正式会话附件的文件，才允许被永久记忆引用。

记忆写入时，如果本轮对话包含附件，需要在记忆中记录附件 ID、附件类型、文件名、所属会话 ID、所属消息 ID 和必要摘要，确保迁移中心目录后仍能从会话附件目录找到对应文件。

UI 需要支持快速查看用户记忆和各智能体记忆内容。

## 会话与对话轮次

中心服务维护普通会话和项目会话。

普通会话不绑定具体项目，适合个人事务、通用问答、知识整理、自动化协助等场景。

项目会话绑定某一个项目，适合代码理解、开发协作、上下文引用、IDE 插件交互和项目级扩展能力调用。

中心服务需要统计用户和 agent 之间的每一轮对话。

一轮对话的边界沿用记忆写入口径：用户向 agent 发起一次输入后，agent 完成本次回应、引导、排队、子智能体协作和相关处理，直到需要等待用户下一次输入为止。

每一轮对话需要保存：

- 轮次编号。
- 会话 ID。
- 用户消息 ID。
- 开始时间。
- 结束时间。
- 持续时长。
- 当前状态。
- 思考过程记录。
- 工具调用记录。
- 命令执行记录。
- 文件读取记录。
- 文件写入记录。
- 文件删除记录。
- 插件调用记录。
- MCP 调用记录。
- skill 使用记录。
- 子智能体创建和协作记录。
- 流式输出片段记录。

轮次编号按会话内用户发起的对话轮次递增。

对话执行过程中，桌面端、Web端和 IDE 插件需要实时显示本轮对话的时间信息。

实时显示的时间信息至少包含本轮开始时间、当前更新时间和已持续时间。

本轮对话结束后，时间显示固定为最终开始时间、结束时间和总持续时长，不再随客户端本地时钟继续变化。

对话中的所有执行详情都需要保存并可查看，包括调用了什么工具、执行了什么命令、读取或写入了什么文件、调用了哪些插件、MCP 和 skill、创建或激活了哪些智能体和子智能体，以及每一步的开始时间、结束时间、状态、输入摘要、输出摘要、错误原因和排查信息。

思考过程记录需要和普通回复、工具调用、命令执行等消息区分保存。UI 中思考过程默认折叠，用户可以手动展开查看；折叠状态属于本地 UI 状态，不强制同步到其他客户端。

agent 回复、思考过程、工具调用进度、命令输出和子智能体协作过程都需要支持流式传输显示。中心服务需要把流式片段同步给客户端，客户端按片段增量渲染，结束后固化为本轮对话的完整记录。

## 项目与项目聊天

项目是 IDE 插件和工程对话的核心概念。

项目使用 UUID 作为唯一 ID，不直接使用项目路径作为 ID。这样项目文件夹迁移到其他电脑后，只要项目 ID 文件还在，就可以继续识别为同一个项目。

每个项目根目录保存一个项目 ID 文件：

```md
致心项目ID.md
```

IDE 插件打开项目时，按下面规则处理：

- 如果存在 `致心项目ID.md`，读取其中的项目 ID。
- 如果不存在，创建新的 UUID，并写入 `致心项目ID.md`。
- 项目显示名称必须使用项目文件夹名。
- 项目文件夹名变化后，项目显示名称必须跟随新的文件夹名。
- 项目登记时，如果插件或客户端没有传入项目名称，但已经传入明确项目路径，中心服务必须从项目路径最后一级目录派生项目显示名称。
- 插件页面运行时读取 URL 参数时，如果缺少项目名称但存在明确项目路径，前端必须从项目路径最后一级目录派生项目显示名称，保证旧插件 URL 仍能进入当前项目对话。
- 项目名称和项目路径都无法得出项目文件夹名时，项目登记必须失败并提示登记信息不完整，不能用项目 ID、空字符串或其他占位内容冒充项目主名称。
- 用户可以设置项目别名或备注。
- 项目别名和备注只能作为备注信息展示，不能替代项目文件夹名作为项目名称。
- 桌面端、Web端和 IDE 插件展示项目主名称时必须使用项目文件夹名，别名或备注只在次要说明、tooltip 或详情中展示。

IDE 插件中的对话是项目级别的，只能看到当前打开项目下的聊天、智能体、任务和记忆，不显示其他项目的数据。

项目聊天支持项目级别的插件、MCP 和 skill。

项目聊天执行时，优先使用当前项目配置的插件、MCP 和 skill；只有当前项目没有同名能力时，才使用全局插件、MCP 和 skill。

IDE 插件中可以为同一个项目打开多个聊天页签，每个页签对应一个项目聊天会话。

在桌面端和 Web端中，项目聊天显示为项目下的子聊天。项目聊天内容、智能体状态、消息状态在 IDE 插件、桌面端和 Web端之间实时同步。

页签是否打开、当前正在查看哪个聊天、滚动位置和布局状态属于本地 UI 状态，不强制同步。

## 多端实时同步

所有 UI 实时同步中心服务中的数据状态。

同步的数据包括：

- 项目列表。
- 项目 ID。
- 项目名称。
- 普通聊天列表。
- 项目聊天列表。
- 聊天消息。
- 智能体状态。
- 智能体供应商、模型和推理深度配置。
- 任务状态。
- 记忆写入结果。
- 供应商配置状态。
- 插件配置状态。
- MCP 配置状态。
- skill 配置状态。

不同步的数据包括：

- 当前 UI 打开的窗口。
- 当前选中的页签。
- 当前滚动位置。
- 本地布局状态。

客户端和中心服务断开连接后需要自动重连，并展示连接状态。自动重连需要有明确的重试次数、重试间隔和最终停止状态，不能无限静默重试。

如果一直无法重连并进入停止状态，客户端中尚未发出的排队消息不能在恢复连接后自动发送。系统必须保留这些待发送消息，并提示用户确认后再发送、修改或取消，避免网络恢复后把用户已经失去上下文的消息自动发出去。

已经发送到中心服务并由中心服务接收的消息，由中心服务按照任务和会话状态继续处理；尚未成功发送到中心服务的本地排队消息，必须以“待用户确认”状态展示。

## 对话输入与上下文引用

对话输入框需要支持文本、附件和上下文引用的混合输入。

桌面端、桌面浏览器 Web 端和 IDE 插件工作区的对话输入区必须使用更宽、更高的稳定胶囊式输入框。输入区和运行中耗时提示宽度应适配到主对话区，输入框必须占满中间对话区可用宽度并与消息内容区左右对齐，桌面宽屏下不应显得过窄；输入框默认高度需要支持较长输入，并允许用户在合理高度范围内手动调整文本输入区高度。输入区必须固定在当前对话区底部，页面级无滚动，除输入区占用的稳定高度外，其余纵向空间全部分配给消息列表作为主滚动区域。

对话输入区或对话顶部需要提供当前会话的模型和推理深度选择。默认值优先来自当前启用供应商及其默认模型、模型列表和推理深度配置；如果供应商没有模型列表或模型列表刷新失败，允许用户选择或输入该供应商的默认模型，并明确说明模型名称由用户手动维护。输入框内部不能展示供应商选择，供应商只能作为模型来源或会话设置的说明展示。

对话输入框需要采用输入框内部三段入口和下方输入主体组合。三段入口固定为“任务 / 智能体 / 编辑”，必须位于输入框内部顶部，并和输入框左右边缘对齐，不再提供三段入口收起或展开按钮。下方主体需要包含附件入口、文本输入区、当前窗口上下文用量、底部控制条和发送按钮，不再提供独立“文件上下文”按钮。

当前窗口上下文用量展示格式为 `占用比例 / 窗口上限`，例如 `0% / 1M`。占用比例按当前对话窗口已使用上下文量除以所选模型窗口上限计算；当窗口记忆 history 数组过大时，占用比例允许超过 100%，例如 `200% / 1M`，不能强制截断到 100%。窗口上限来自所选模型的上下文窗口配置。前端刷新上下文用量时需要合并连续输入、引用和附件变化，避免每个输入字符都请求中心服务 token 统计接口；相同输入状态不重复请求，较早返回的统计结果不能覆盖较新的输入状态。

任务详情、智能体状态详情和编辑详情都从输入框内部入口向上打开浮层展示，浮层宽度跟随输入框内部内容宽度，默认由内容撑开，但最大高度为 `40vh`，超过后在浮层内部滚动。浮层不能撑高输入框本体，也不能向下挤压文本输入区。不再在输入框外部放置三段入口或收起展开按钮，避免输入区左右边界和消息内容错位。

“任务”入口用于体现当前会话的编排任务，展示本轮任务、阶段、状态和当前对话内排队、等待用户等状态语义。

“智能体状态”入口用于展示当前团队智能体和运行期子智能体状态。智能体状态弹框使用两级树：第一级展示团队里面所有智能体，包括主智能体“致心”和所有长期智能体；第二级展示各个长期智能体创建的各自子智能体。用户需要能看到每个智能体和子智能体的当前状态、任务摘要和层级关系。

智能体状态弹框中的每个智能体节点都可以点击查看详情。详情需要提供完整的对话查看和发送体验，包括消息列表、当前节点摘要、输入框和发送按钮。若中心服务暂时没有独立智能体会话 API，前端需要基于现有会话、消息和输入草稿实现“查看该智能体对话记录 + 向该智能体发送消息”的 UI 闭环，并在界面文案中明确消息仍通过当前会话发送，避免用户误解为已经进入独立智能体会话。

“编辑”入口用于展示本次任务所有新增文件和编辑文件，并支持查看本次编辑与上一次编辑之间的 diff。编辑文件列表和 diff 如果后端协议尚未齐备，前端可以使用单一临时占位约定展示，后续中心服务协议明确后替换。

输入框或输入区周边不展示“模型来源：暂无启用供应商，后续发送沿用中心服务当前默认处理。”这类文案。没有启用供应商时，前端可以保持模型控件为空或使用中心服务默认发送流程，但不能在输入框中展示该提示。

用户点击新增普通对话、项目对话或插件页签时，只进入本地待输入的新对话草稿状态，不立即创建中心服务可见会话。只有用户真实发送内容后，中心服务创建会话、消息和轮次，该会话才进入过去对话列表。

用户在当前会话中切换供应商、模型或推理深度，只影响后续发送的消息和后续任务，不回改历史消息、历史任务、历史用量统计或已有记忆。客户端可以先把该选择作为发送前配置展示；在中心服务真实执行模型切换能力补齐前，不能破坏现有发送流程。

附件类型不限制为多媒体文件，必须支持图片、音频、视频、PDF、Word、Excel、压缩包、文本文件和其他普通文件。

用户可以直接把剪贴板中的图片、文件或只有内存数据的附件内容粘贴到对话输入框中。

当粘贴内容没有稳定磁盘路径，或者来自剪贴板内存数据时，客户端必须立即把原始内容交给中心服务保存到中心目录的 `临时` 目录，并由中心服务生成临时附件 ID 和临时文件名。临时文件名需要避免依赖用户原始文件名，可以包含时间、随机 ID 和推断扩展名。

输入框中需要按附件类型展示预览：

- 图片附件展示图片缩略图或可见图片内容。
- 音频、视频、文档、压缩包和其他非图片文件展示为小方块附件标签，交互形态和 IDE 发送上下文引用的小方块保持一致。
- 文件附件标签需要展示文件名、文件类型图标或格式、文件大小和可删除入口。

临时附件在发送前只属于输入框草稿，不进入正式会话消息，也不能进入永久记忆。

用户取消发送、删除附件、关闭会话窗口或关闭客户端时，未发送的临时附件需要清理。中心服务启动和停止时也需要兜底清理 `临时` 目录中未绑定正式消息的文件。

用户真正发送消息后，中心服务需要把临时附件转为正式会话附件，并保存到中心目录的会话附件目录中。正式附件作为本轮用户消息的一部分进入对话上下文，消息中只保存正式附件 ID 和结构化元数据，不直接嵌入二进制内容。

正式附件需要保存：

- 附件 ID。
- 临时附件 ID。
- 原始文件名。
- 中心服务生成的存储文件名。
- MIME 类型。
- 文件大小。
- 图片宽高。
- 存储路径。
- 所属会话 ID。
- 所属消息 ID。
- 创建时间。
- 来源类型。

附件原始文件存放在中心目录的会话附件目录中，方便历史对话查看和迁移。

如果当前供应商或模型不支持图片输入，发送前需要在 UI 中提示用户，并禁止把图片附件发送给该模型。非图片文件是否能发送给模型，需要由当前模型能力和任务策略判断；不能发送给模型的文件仍可以作为消息附件或上下文引用保存。

输入框以 `/` 开头时，进入命令和 skill 检索模式。用户可以通过 `/` 搜索可用 skill，并把选中的 skill 加入本轮对话上下文。

skill 检索需要同时展示全局 skill 和当前项目可用 skill。项目级别会话中，如果项目级 skill 与全局 skill 同名，优先展示和使用项目级 skill。

对话窗口需要提供一个外部能力查看按钮。用户点击后，可以查看当前窗口、当前会话和当前智能体可用的插件、MCP 和 skill，包括全局能力、当前项目级能力、启用状态、权限范围、能力说明和不可用原因。

项目级别会话中，输入框需要支持 `@` 检索项目文件信息。用户输入 `@` 后，可以搜索当前项目下的文件或文件夹，并把选中的文件、文件夹或代码位置作为上下文引用加入对话框。

`@` 检索需要使用一个贴近输入框的小弹框展示可选结果。小弹框用于可视化选择文件、文件夹和代码位置，但不能阻塞输入框继续输入。

`@` 小弹框需要支持键盘上下选择、回车确认、Esc 关闭、鼠标点击选择。确认选择后，把引用标签插入到输入框当前位置或追加到输入框的引用区域。

`@` 文件检索只在有项目上下文的会话中启用。普通非项目会话不展示项目文件检索能力。

文件引用标签展示文件图标和文件名，并保留完整路径。

文件夹引用标签展示文件夹图标和文件夹名，并保留完整路径。

代码行引用在 UI 中显示为：

```md
文件名#L开始行-结束行
```

如果只选中一行，显示为：

```md
文件名#L行号
```

代码行引用需要保留：

- 项目 ID。
- 文件绝对路径。
- 文件相对项目路径。
- 开始行。
- 结束行。
- 选中的代码内容。

## 内部文件定位链接

插件页面、聊天消息和 Markdown 渲染内容中，需要支持类似 AI 插件里的内部文件定位链接。

内部文件定位链接用于定位当前项目中的文件、文件夹或代码行。点击后由已安装的 IDE 插件打开对应文件并跳转到指定行号或选区。

内部文件定位链接必须和外部链接区分处理。外部链接只包含普通 `http://`、`https://` 等网络地址，点击后按浏览器或系统默认方式打开。

内部文件定位链接必须携带项目 ID、相对路径、绝对路径、起始行和结束行等结构化信息，点击后走 IDE 插件内部跳转能力，不能按普通外部链接打开。

内部文件定位链接在 UI 显示上可以使用 `文件名`、`文件名#L行号` 或 `文件名#L开始行-结束行` 这种短标签形式，但底层数据不能只保存显示文本，必须保存完整定位信息，避免项目迁移、同名文件或路径变化时无法判断目标。

## 插件、MCP 和 skill 扩展能力

致心智能体需要支持类似 Codex 的扩展能力体系，包括插件系统、MCP 系统和 skill 系统。

这些能力由中心服务统一管理，供主智能体、用户创建的智能体和子智能体按权限使用。

所有智能体都可以使用外部扩展能力，但实际可用列表必须由中心服务根据全局启用状态、项目级能力、智能体权限、会话类型和用户配置计算，不能由客户端自行拼接或猜测。

项目级别会话需要在全局能力之外叠加当前项目的插件、MCP 和 skill。项目级能力参与当前项目对话和项目任务执行，不影响其他项目或普通非项目会话。

扩展能力系统要支持：

- 安装。
- 启用。
- 停用。
- 删除。
- 配置。
- 查看能力说明。
- 查看调用记录。
- 按项目或全局控制可用范围。
- 查看当前对话窗口可用能力清单。

扩展能力可以参与 agent 的任务执行，但是不能绕过中心服务直接操作核心记忆、项目数据和会话状态。

扩展能力支持全局级别和项目级别。

项目级别会话可以使用项目级别的插件、MCP 和 skill。

项目级扩展能力需要兼容市面上的通用项目目录规则。

项目级目录树：

```md
项目根目录
├─ 致心项目ID.md
└─ .agents
   ├─ rules
   ├─ skills
   ├─ plugins
   └─ mcp
```

项目级目录说明：

- `致心项目ID.md`：保存项目身份标识，跟随项目迁移，用于让中心服务把当前项目和中心目录中的项目会话关联起来。
- `.agents/rules`：保存项目级规则文件，例如 `AGENTS.md` 和 `AGENTS.overflow.md` 的项目作用域规则。
- `.agents/skills`：保存项目级 skill，方便跟随项目仓库一起迁移。
- `.agents/plugins`：保存项目级插件，供项目会话优先使用。
- `.agents/mcp`：保存项目级 MCP 配置，供项目会话优先使用。

中心目录可以保存项目级能力的扫描索引、启用状态和用户配置，但不强制把项目级能力复制出项目目录。项目的聊天、记忆、任务记录和项目会话配置仍然由中心目录的“会话”目录承载。

当项目级别和全局级别存在同名插件、MCP 或 skill 时，项目级别优先。

优先级规则：

- 项目级插件优先于全局插件。
- 项目级 MCP 优先于全局 MCP。
- 项目级 skill 优先于全局 skill。

### 插件系统

插件系统用于给致心智能体增加本地能力、业务能力或 UI 能力。

这里的插件是 agent 能力插件，不等同于 IDE 插件客户端。

插件需要有清晰的清单文件，描述插件名称、版本、入口、能力、配置项、权限和适用范围。

插件可以提供：

- 工具能力。
- UI 页面。
- 命令。
- 项目能力。
- 记忆处理能力。
- 第三方服务集成。

插件权限必须显式声明。涉及文件、网络、命令执行、记忆读写、项目访问等能力时，需要在安装或启用时向用户提示。

### MCP 系统

MCP 系统用于连接外部 MCP Server，让 agent 可以调用外部工具、资源和上下文。

中心服务负责管理 MCP Server 配置，包括：

- MCP Server 名称。
- 启动命令或连接地址。
- 环境变量。
- 是否启用。
- 可用工具列表。
- 可用资源列表。
- 适用范围。

MCP 工具调用需要进入调用记录，方便用户查看 agent 使用了哪些外部工具。

MCP Server 可以按全局启用，也可以按项目启用。IDE 插件中的项目聊天只能使用当前项目允许的 MCP 能力。

MCP 配置采用 JSON 格式，根字段为 `mcpServers`。

需要支持 HTTP 类型 MCP Server，例如：

```json
{
  "mcpServers": {
    "idea": {
      "type": "http",
      "url": "http://127.0.0.1:64342/stream"
    }
  }
}
```

需要支持 stdio 类型 MCP Server，例如：

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "chrome-devtools-mcp@latest",
        "--browserUrl",
        "http://127.0.0.1:9222",
        "--no-usage-statistics"
      ]
    }
  }
}
```

同一个 JSON 配置中可以配置多个 MCP Server。

中心服务需要能读取全局 MCP 配置，也需要能读取项目级 `.agents/mcp` 目录下的 MCP 配置。

### skill 系统

skill 系统用于给 agent 提供可复用的工作流程、专业知识和行为约束，整体设计参考 Codex 的 skill 机制。

每个 skill 需要有独立目录和说明文件，说明它的名称、用途、触发条件、工作流程和可用资源。

skill 可以包含：

- 使用说明。
- 工作流程。
- 模板。
- 脚本。
- 参考资料。
- 示例。

agent 在处理用户请求前，需要根据用户请求、当前项目、当前会话和系统状态判断是否需要使用 skill。

使用 skill 时，需要把 skill 作为本轮对话的执行上下文之一，但 skill 不能覆盖用户明确指令。

skill 可以全局安装，也可以按项目安装。项目级 skill 随项目迁移时可以继续使用。

## 供应商、代理与模型调用

供应商用于配置模型或能力提供方，由中心服务统一管理。

用户可以添加、修改、删除、启用、停用供应商。

供应商需要支持的信息：

- 供应商名称。
- 供应商类型。
- 接口地址。
- API Key。
- 可用模型列表。
- 默认模型。
- 可用推理深度列表。
- 默认推理深度。
- 代理策略。
- 是否启用。

API Key 等敏感信息只保存在中心电脑，由中心服务使用。桌面端和 Web端可以管理供应商配置，IDE 插件只使用中心服务提供的能力，不直接暴露 API Key 明文。

部分网络环境无法直接访问供应商接口，中心服务需要支持通过网络代理访问供应商、模型列表接口、推理深度接口和后续模型调用接口。

网络代理由中心服务统一管理，桌面端和 Web端只负责配置；IDE 插件不直接保存或使用代理账号密码。

代理协议至少支持：

- HTTP。
- HTTPS。
- SOCKS4。
- SOCKS4a。
- SOCKS5。

每个代理配置需要支持：

- 代理名称。
- 代理协议。
- 代理主机。
- 代理端口。
- 用户名。
- 密码。
- 是否启用。
- 备注。

代理用户名和密码允许为空。用户名和密码都为空时表示该代理不需要认证，中心服务必须按无认证代理连接，不能把空账号密码当作配置错误。

代理用户名和密码属于敏感信息，只能保存在中心电脑，由中心服务使用，客户端不能明文展示已经保存的密码。

系统需要支持全局默认代理。全局默认代理用于没有单独指定代理策略的供应商请求。

供应商需要支持代理策略：

- 不使用代理。
- 使用全局默认代理。
- 使用指定代理配置。

供应商的代理策略只影响后续供应商访问、模型列表刷新、推理深度刷新和模型调用，不回改历史对话、任务记录和调用记录。

代理连接失败时，需要在 UI 中提示失败原因，并保留排查信息。模型列表、推理深度或模型调用因为代理失败而失败时，错误原因需要能区分是代理连接失败、代理认证失败、供应商连接失败还是供应商接口返回失败。

创建或修改供应商后，中心服务需要自动向该供应商获取可用大模型列表和可用推理深度列表，并保存到供应商配置中。

模型列表获取成功后，用户可以从自动获取的模型列表中选择默认模型。

供应商配置弹框的“手填模型与上下文”区域需要提供“获取”按钮。用户点击后，客户端只请求中心服务，由中心服务使用当前已保存供应商配置和中心服务保存的 API Key 调用供应商模型列表接口；客户端不能直接调用供应商上游接口，也不能读取或保存 API Key 明文。

“获取”返回的模型列表需要按模型名称中匹配到的数字段降序排序，数字段不要求出现在模型名末尾；例如 `gpt-5.5` 排在 `gpt-5.4` 前，`gpt-5.10` 排在 `gpt-5.5` 前。排序后的第一项需要自动写入供应商默认模型，并进入默认模型下拉选项和“手填模型与上下文”文本区。获取模型时如果某个模型已有用户维护的上下文窗口，需要保留既有窗口；上游新模型没有返回窗口信息时，允许按 `200K` 写入可继续手动修改的默认窗口。

供应商管理 UI 中的默认模型必须优先使用下拉选择，选项来源于该供应商已经保存或刚刷新得到的模型列表，不能让用户在已有模型列表时仍只能手动输入。

推理深度列表获取成功后，用户可以从自动获取的推理深度列表中选择默认推理深度。

模型列表或推理深度列表获取失败时，需要在 UI 中提示失败原因，并允许用户手动刷新。

如果供应商不支持模型列表接口，可以允许用户手动填写模型名称；手动填写入口必须明确提示“该供应商未提供模型列表接口或当前刷新失败，模型名称由用户手动维护”。

如果供应商不支持推理深度列表接口，可以允许用户手动填写推理深度，或者标记为不支持推理深度。

供应商页面需要支持维护模型上下文窗口。用户手填模型名称时，也需要填写该模型窗口上下文上限；表单统一使用 `K` 作为输入单位，例如 `1000K` 表示约 `1M`，`200K` 表示 `200K`。中心服务保存为统一数值，前端展示时可格式化为 `1M`、`200K` 等短格式，供输入区上下文占用比例计算。手填模型保存后进入该供应商已保存模型列表，默认模型下拉仍从已保存模型列表选择。

供应商配置页面需要对供应商名称、协议插件、协议模式、接口地址、API Key、默认模型、模型列表来源、能力声明、代理策略和启用状态提供简洁解释，说明这些字段的来源、用途和影响范围。

如果只有一个启用的供应商，智能体默认使用这个供应商。

如果有多个启用的供应商，用户可以为每个智能体选择承载它的供应商、模型和推理深度。

供应商、模型或推理深度选择可以由用户修改。修改后，只影响后续对话和后续任务，不回改已经产生的历史对话、任务记录和记忆内容。

## 运行环境

中心服务需要统一管理可执行程序和工具链目录，方便插件、MCP、skill、任务执行和命令执行选择不同运行环境。

运行环境配置统一存放在中心目录的“运行环境”子目录中。

系统需要内置市面常用运行环境类型，至少包括：

- Node.js。
- Python。
- Java。
- Maven。
- Git。

pnpm、npm、yarn、npx、uv、pip 等包管理器或命令入口不作为独立运行环境类型内置。Node.js 包管理统一使用 pnpm，并通过所选 Node.js 运行环境执行。

Python 相关命令通过所选 Python 运行环境执行，例如 Python 可以使用 `python -m pip` 调用对应解释器环境下的 pip。

Java 项目依赖和构建统一使用 Maven，并通过登记的 Maven 运行环境执行。IDEA 插件工程自身按架构约定使用 Gradle IntelliJ Platform Plugin 构建。

每个运行环境配置需要支持：

- 环境名称。
- 环境类型。
- 可执行文件路径。
- 根目录路径。
- 版本号。
- 环境变量。
- PATH 追加目录。
- 是否默认。
- 是否启用。
- 备注。

用户可以在 UI 中添加、修改、删除、启用、停用运行环境配置。系统内置的常用类型只是默认模板，不限制用户新增其他工具链类型。

执行插件、MCP Server、skill 脚本或命令任务时，可以指定使用某个运行环境。未指定时使用该类型的默认启用环境。

运行环境选择只影响后续执行，不回改历史任务记录。任务记录中需要保存当时使用的运行环境名称、类型、路径和版本，方便排查问题。

## 用量统计

中心服务需要统计每个供应商的模型调用用量，方便用户查看不同供应商的使用情况。

供应商维度至少统计：

- 输入 token 数。
- 输出 token 数。
- 总 token 数。
- 缓存命中 token 数。
- 缓存未命中 token 数。
- 调用次数。
- 成功次数。
- 失败次数。
- 统计时间范围。

项目维度也需要统计模型调用用量。项目统计需要按项目 ID 归集，并支持查看某个项目在不同供应商、模型和时间范围内的 token 使用、缓存命中和缓存未命中情况。

普通非项目会话需要归入全局用量统计，不混入具体项目统计。

如果供应商返回了缓存命中、缓存未命中或类似字段，中心服务必须按供应商原始返回记录并转换为统一统计字段。

如果供应商没有返回缓存相关字段，统计中必须明确标记为供应商未提供，不能用 0 冒充实际缓存命中或未命中。

用量统计只追加记录，不回改历史调用记录。供应商、模型、项目别名或项目路径后续变化时，历史统计仍保留当时的供应商 ID、模型名称、项目 ID 和调用时间，避免统计口径漂移。

桌面端和 Web端需要提供用量统计查看界面，至少支持按供应商、模型、项目和时间范围筛选。

用量统计界面必须图形化展示统计结果，可以使用 ECharts 等图表库。图形化内容至少包括总量概览、各供应商维度和项目维度，不能只展示原始 JSON 或纯文本列表。

## 通知配置

用户可以配置对话完成后是否通知。

对话完成通知指用户发送一轮消息后，agent 完成本轮回复、工具调用、文件修改、记忆写入和状态收尾时，向用户发出完成提醒。

通知配置需要支持：

- 是否开启对话完成通知。
- 是否仅在窗口不活跃时通知。
- 是否通知普通对话。
- 是否通知项目对话。
- 是否通知智能体协作对话。
- 是否通知失败或需要用户处理的对话。

通知配置按客户端类型分别保存。桌面端、Web端、IDE 插件可以有不同的通知开关，不强制互相同步。

同一种客户端类型内部可以同步通知配置。例如多个桌面端窗口使用同一套桌面端通知配置，多个 IDE 插件窗口使用同一套 IDE 插件通知配置。

所有通知都由中心服务统一判断、生成和发送。客户端不自行判断是否需要产生通知。

中心服务需要直接调用中心电脑的系统通知通道发送通知，让用户在其他端、其他窗口或其他软件中工作时也能感知到 agent 已经完成或需要处理。

中心服务仍然需要把通知事件同步给相关客户端，用于客户端内展示、未读状态、跳转和 Web端二次通知。

中心服务发送和同步的通知事件需要包含：

- 通知 ID。
- 通知类型。
- 目标客户端类型。
- 目标会话 ID。
- 目标项目 ID。
- 目标智能体 ID。
- 标题。
- 内容摘要。
- 通知时间。
- 是否需要用户处理。
- 跳转目标。

Web端比较特殊。中心服务已经发送系统通知后，Web端收到通知事件时还需要在浏览器里再通知一次，可以使用浏览器通知或页面内通知，保证用户正在浏览器中工作时也能看到提醒。

如果中心电脑没有系统通知权限，桌面端需要提示用户开启系统通知权限，并把权限状态同步给中心服务。

如果浏览器没有通知权限，Web端需要提示用户开启浏览器通知权限；无法使用浏览器通知时，至少要在页面内显示通知，不能静默失败。

## 对话执行模式

每个对话窗口都可以选择执行模式。

执行模式会影响 agent 执行任务时是否需要用户审批。

执行模式不在桌面端、Web端、IDE 插件之间互相同步，而是按客户端类型分别保存和同步。同一种客户端类型内部统一同步，不按项目或会话拆分。

支持三种模式：

- 建议模式：最安全的选择，每一步都需要用户自己批准。
- 自动编辑：平衡模式，保持正常编辑流程，同时在需要时进行审批。
- 全自动：完全免审批，写文件和执行命令会立即生效，仅受沙箱限制。

执行模式选择控件不能只显示模式名称。下拉面板中必须同时展示每个模式的解释，说明建议模式需要逐步确认、自动编辑会自动执行低风险操作但高风险仍需确认、全自动会在权限范围内立即执行。

默认模式使用“全自动”。

用户可以在对话窗口中切换模式。切换后只影响当前客户端类型下的后续操作，不改变已经执行的历史操作。

## Web端与手机适配

Web端使用中心服务，不内置中心服务。

Web端页面由中心服务提供。桌面客户端启动中心服务后，本机浏览器访问 `http://127.0.0.1:端口` 即可打开 Web端页面；默认访问地址为 `http://127.0.0.1:8866`。

Web端需要兼容桌面浏览器和手机浏览器。手机浏览器访问属于 Web端能力，不单独拆分为移动端。

Web端桌面浏览器体验和桌面客户端保持一致，但不能包含 Electron 专属能力。

手机浏览器需要根据小屏幕做响应式适配，优先保证对话、消息查看、输入、通知和常用配置可用。复杂表格、宽屏状态栏和多列工作台不能直接塞进手机页面。

Web端如果从非本机访问中心服务，首次打开页面必须进入登录页，输入桌面端配置的账号和密码后才能访问。

登录成功后保存登录态。Web端登录态必须使用 Cookie 保存和传递，不能使用 `sessionStorage` 保存登录 token。

Cookie 由中心服务设置，并按访问方式配置合适的 `HttpOnly`、`SameSite`、`Secure` 和过期时间策略。

Web端不能配置中心服务启停、中心目录或 Web端访问账号密码，这些本机管理能力归桌面客户端负责。

## IDE 插件基座

IDE 插件最终需要打包为可安装到对应 IDE 中的插件。各 IDE 平台分别维护自己的 SDK 工程，但插件页面统一通过 WebView 加载中心服务提供的 `plugin.html`。

各 IDE 插件平台的工程名、插件名和安装包名统一使用 `zhixin-agent-平台名` 格式，平台名使用小写英文短名。例如 IDEA 平台使用 `zhixin-agent-idea`，后续 VSCode 平台使用 `zhixin-agent-vscode`。

IDE 插件打开项目时，先读取或创建 `致心项目ID.md`，再连接中心服务。

IDE 插件只展示当前项目的数据，不展示其他项目。

IDE 插件中可以配置中心服务端口，默认端口为 `8866`。

IDE 插件连接地址固定为：

```md
http://127.0.0.1:端口
```

IDE 插件提供的宿主能力包括：

- 获取项目路径。
- 获取项目名称。
- 读取或创建项目 ID。
- 把插件打开时的项目路径和项目 ID 发送给中心服务页面。
- 提供文件、文件夹、代码行、选区等上下文发送能力。
- 处理内部文件定位链接。

IDE 插件中的上下文发送入口必须模仿常见 AI 插件的右键菜单交互。

用户在编辑器选区、编辑器标签页、项目文件树文件、项目文件树文件夹等位置右键时，都可以看到发送到致心对话框的菜单项。

菜单点击后，不直接发起最终提问，而是先把对应文件、文件夹或代码行引用插入到致心对话框输入区，用户可以继续编辑文本后再发送。

编辑器选区右键菜单需要支持把选中代码发送到对话框。如果选区为空但光标在文件中，可以按当前文件或当前行范围提供发送入口。

项目文件树右键菜单需要支持把文件路径或文件夹路径发送到对话框。

编辑器标签页右键菜单需要支持把当前文件路径发送到对话框。

后续其他 IDE 插件也按同样交互设计实现。

IDEA 插件工程名、插件名和安装包名使用 `zhixin-agent-idea`。IDEA 插件包名和插件 ID 命名空间统一使用 `top.xzxsrq`。

IDEA 插件工具窗口名称使用“致心”，工具窗口打开后加载本机中心服务提供的 `plugin.html`。

IDEA 插件最终需要包含可安装到 IDEA 的真实插件骨架，包括 `META-INF/plugin.xml`、工具窗口入口、右键发送上下文 Action、中心服务连接配置和插件打包产物。

## 安全与访问控制

中心服务需要监听本机服务端口，默认端口为 `8866`。

桌面端负责启动和管理中心服务，也负责配置中心服务端口。

IDE 插件连接中心服务时，IP 固定为 `127.0.0.1`，只允许连接本机中心服务。

Web端访问中心服务时，如果访问来源是本机，可以直接进入已授权的本机使用流程。本机访问和非本机访问的判断必须由中心服务根据请求来源、监听地址和连接信息完成，前端不能只依赖 `window.location.hostname` 自行判断，否则容易被伪造或绕过。

Web端访问中心服务时，如果不是本机访问，第一次打开页面必须要求输入账号和密码。账号密码验证通过后，中心服务给 Web端发放登录态，后续请求使用登录态访问。

Web端账号密码只能在桌面端配置和修改，不能在 Web端自己配置账号密码。

账号密码等访问控制信息只保存在中心电脑，由中心服务校验。IDE 插件不使用账号密码登录，只使用本机 `127.0.0.1` 加端口连接。

## UI 与主题

主题支持暗黑和亮色。

各客户端首次打开时，默认主题必须匹配当前客户端环境。桌面端和 Web端默认跟随系统暗黑或亮色设置；IDE 插件端默认跟随 IDE 宿主当前主题，例如 IDE 是亮白主题时插件页面默认使用亮色，IDE 是暗黑主题时插件页面默认使用暗黑。

用户需要可以在界面中手动切换亮色和暗黑主题。用户手动切换后，当前客户端后续打开优先使用用户选择，不再被系统或 IDE 当前主题覆盖，除非用户再次切换。

桌面端、Web端桌面浏览器和后续插件页面的主工作区 UI 参考 Codex App 的工作台形态。

整体窗口、侧边栏、顶部栏、右侧状态栏、对话区和输入区必须使用 flex 布局组织，不能依赖页面级滚动撑开高度。

窗口级布局必须动态适配窗口大小变化。根容器固定为当前视口高度，页面主体禁止出现全局滚动条；侧边栏、顶部栏、右侧状态栏和输入框区域保持在可视区域内，只有对话消息列表区域允许纵向滚动。

滚动条视觉参考 Element Plus 的轻量滚动条风格：轨道尽量弱化，thumb 使用窄轨道、圆角和半透明颜色，hover 时更明显。消息列表、侧边栏列表、右侧状态栏列表、详情弹框内容、diff 区域、管理页滚动区和插件紧凑模式中的关键滚动容器都需要应用统一滚动条视觉。

桌面端应用内头部菜单用于切换“对话、智能体管理、供应商、网络代理、运行环境、用量统计、插件、MCP、skill、中心服务”主页面，连接状态放在头部区域。桌面浏览器 Web 端也需要展示“智能体管理”入口；IDE 插件紧凑模式不展示完整管理入口。

顶部菜单对应页面必须是真实独立页面或独立路由视图。不能通过 `<MainView initial-page="..."/>` 这类向同一个大组件传入初始页面值的方式伪装页面拆分；页面入口只能负责挂载对应页面和公共工作台壳，页面业务模板、状态加载和交互必须拆到各自页面组件中。

“智能体管理”页面用于管理主智能体和长期智能体。主智能体不可删除，长期智能体可查看、创建、修改、停用和删除；如果当前中心服务接口暂时不足，前端可以先展示管理语义和入口说明，但不能伪造删除等尚未落地的破坏性功能。

顶部“插件 / MCP / skill”页面是中心服务的全局扩展能力管理入口，只管理中心级或用户安装的全局能力，不展示成某个项目的能力管理页，也不要求用户在全局页填写项目 ID。

项目级插件、MCP 和 skill 只在打开项目目录或登记项目上下文时由中心服务扫描和计算，并在对应项目对话页通过图标按钮或简短摘要打开项目能力详情弹框；全局管理页不混入项目级安装、项目级配置或项目级能力列表。

IDE 插件紧凑模式不展示左侧整栏。项目多页签必须参考顶部页签样式展示在对话区顶部，不使用左侧项目页签栏。`plugin-compact` 只保留顶部项目页签、消息列表、输入框、文件或选区引用、任务状态、智能体状态和连接设置等必要能力。

桌面端中心服务启停和重启入口只放在桌面端“中心服务”页面，不在 Web端和插件页面展示。

桌面端对话页面左侧只展示对话导航，不再混入供应商、网络代理和运行环境入口。

对话导航分为“项目对话”和“普通对话”。项目对话按项目分组展示并支持收起或展开，普通对话以列表形式展示。

项目对话导航必须是两级树。第一级展示项目名称，项目行支持收起和展开；项目下面展示该项目的项目对话。项目主名称必须使用项目文件夹名，项目别名或备注只作为备注展示。项目行右侧常态展示项目级执行状态图标；如果项目没有执行任务，可以展示空闲图标或不展示状态图标。鼠标悬停项目行时，右侧切换为删除项目和新增项目对话两个图标按钮，按钮只使用图标表达。

项目对话行和普通对话行右侧需要常态展示对话时间节点，时间只允许显示在行右侧。两类对话行鼠标悬停时，通过 tooltip 展示用户发出的内容摘要、完整对话标题和 `YYYY-MM-DD HH:mm:ss` 格式时间，右侧切换为删除图标按钮。

项目对话分组标题右侧需要提供图标化新增入口，用于新增项目或新增项目对话。普通对话分组标题右侧需要提供图标化新增普通对话入口。

每轮对话的开始时间、结束时间和持续时长需要展示在对话内容内部，并放在本轮内容最后。当前轮次尚未结束时，输入框上方固定显示本轮已耗时，轮次结束后该固定耗时提示消失，最终耗时保留在对话内容中。

桌面端对话页面右侧状态栏只展示任务状态和智能体状态，不展示中心服务配置摘要。

桌面端、Web端和插件页面需要清晰展示各智能体和子智能体的任务状态、协作状态、等待用户状态和失败状态。

对话消息列表需要展示一轮对话中的完整执行过程。用户应能在对话中看到 agent 的回复、思考折叠块、工具调用卡片、命令执行卡片、文件读取卡片、文件写入卡片、文件删除卡片、插件调用卡片、MCP 调用卡片、skill 使用记录、智能体协作记录和错误详情。

所有 UI 展示时间必须统一使用 `YYYY-MM-DD HH:mm:ss` 格式，包括会话列表、消息、轮次、任务、事件、供应商、代理、运行环境、插件、MCP、skill、通知和用量统计页面；客户端不能直接展示 ISO 字符串或浏览器本地化时间字符串。

思考内容默认折叠展示，折叠标题需要显示所属智能体、阶段状态、耗时和是否仍在流式生成。工具调用、命令执行和文件操作需要展示执行状态、关键参数摘要、输出摘要、失败原因和排查 ID。

所有正在生成的回复、思考、工具过程、命令输出和智能体协作消息都需要按流式传输实时显示，不能等整轮对话完全结束后才一次性展示。

大模型回答返回的 Markdown 内容使用 GitHub Markdown 样式渲染，需要兼容暗黑和亮色主题。

## 应用图标与资源

应用图标固定文件名为 `图标.png`。

`图标.png` 用于 Electron 应用图标、系统托盘图标、任务栏图标、窗口图标和安装包图标。

项目身份文件固定为 `致心项目ID.md`。

对话框中的工具调用、文件读取、文件写入、文件删除等消息过程图标独立存放在项目根目录 `界面图标` 文件夹中，避免和应用主图标混用。

界面图标至少包括：

- `界面图标/mcp调用.svg`：用于表示 MCP 工具调用或 MCP 能力执行过程。
- `界面图标/读取文件.svg`：用于表示读取项目文件、加载文件内容或文件上下文引用过程。
- `界面图标/写入文件.svg`：用于表示写入、创建或修改项目文件过程。
- `界面图标/删除文件.svg`：用于表示删除项目文件过程。

界面图标使用 SVG 和 `currentColor`，由桌面端、Web端和插件页面根据当前主题统一控制颜色。

## 完整实现范围

本需求不按首期和后续拆分。开始编码时，应按本文件完整需求一次性实现，不把本文件中已经明确的能力降级为后续扩展。

完整实现范围包括：

- 构建交付一个包含中心服务和 Web端页面资源的 Electron 桌面端安装包。
- IDE 插件不包含在 Electron 桌面端安装包中，按各 IDE 平台单独构建和安装。
- 桌面客户端管理中心服务生命周期和本机配置。
- 中心服务作为唯一事实源。
- 桌面客户端启动中心服务后，本机可以通过 `http://127.0.0.1:8866` 打开 Web端页面。
- Web端兼容桌面浏览器和手机浏览器。
- IDE 插件作为 WebView 基座，并提供项目上下文发送能力。
- 主智能体、用户创建智能体和子智能体体系。
- 智能体定义和记忆固化为 Markdown 文件。
- 智能体协作、管线通话、群聊讨论和 UI 状态展示。
- 普通会话和项目会话。
- 项目 ID 文件和项目迁移识别。
- 对话输入框文本、附件、skill 检索和项目上下文引用。
- 插件、MCP 和 skill 扩展能力管理。
- 供应商、网络代理、运行环境、用量统计、通知和执行模式。
- 个人事务场景所需的插件、MCP 和 skill 接入能力。
- 项目开发场景所需的 IDE 插件基座、项目级上下文、项目级扩展能力、内部文件定位链接和项目会话能力。

## 个人事务与项目开发场景

个人事务和项目开发都属于本需求范围。

个人事务能力通过插件、MCP 和 skill 接入，但这些接入能力本身属于完整实现范围，需要支持：

- 日程管理。
- 待办事项。
- 个人知识库。
- 文件资料整理。
- 邮件、聊天软件或办公系统集成。
- 自动化工作流。
- 浏览器信息收集。
- 本地脚本和命令自动化。

项目开发场景通过 IDE 插件基座、项目级上下文、项目级扩展能力、内部文件定位链接和项目会话持续支持，也属于完整实现范围。
