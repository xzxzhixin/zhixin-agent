# 名字

中文名字：致心智能体

英文名字：zhixin-agent

## 架构

electron + vite + vue3 + vue-router + pinia + elementplus

系统不可能只有一个页面，桌面端、Web端和后续页面型插件都必须纳入路由模块管理，统一通过路由组织页面入口、页面跳转和页面级权限边界。

路由页面组件必须使用`() => import(...)`懒加载方式导入，不能在路由表顶部直接静态引入页面组件，避免首屏一次性加载所有页面。

系统页面的公共状态必须使用pinia保存，例如当前主题、中心服务连接状态、当前客户端类型、当前会话、当前项目、登录态、通知状态、执行模式和供应商公共选择等。

桌面端和桌面浏览器页面主要使用elementplus保持统一体验；Web端在手机浏览器访问时，移动端交互和组件使用vant适配，不能把桌面端复杂表格和大屏布局直接塞进手机页面。

大模型回答返回的Markdown内容使用GitHub Markdown样式渲染，需要兼容暗黑和亮色主题。

版本在package.json中要固化

系统需要有自己的应用图标，文件名固定为`图标.png`，图标风格需要贴合“致心智能体”的名称和智能体应用定位。

`图标.png`用于Electron应用图标、系统托盘图标、任务栏图标、窗口图标和安装包图标。

对话框中的工具调用、文件读取、文件写入、文件删除等消息过程图标独立存放在项目根目录`界面图标`文件夹中，避免和应用主图标混用。

`界面图标/mcp调用.svg`用于表示MCP工具调用或MCP能力执行过程。

`界面图标/读取文件.svg`用于表示读取项目文件、加载文件内容或文件上下文引用过程。

`界面图标/写入文件.svg`用于表示写入、创建或修改项目文件过程。

`界面图标/删除文件.svg`用于表示删除项目文件过程。

界面图标使用SVG和`currentColor`，由桌面端、Web端和插件页面根据当前主题统一控制颜色。

## 代码目录约定

代码层面需要按客户端和SDK拆分顶层目录，桌面端以及其他UI端都必须放在各自独立文件夹中，不能把不同端代码混在同一个根目录下。

目录示例：

```md
项目根目录
├─ 桌面端
├─ Web端
├─ IDEA_SDK
├─ 中心服务
└─ 共享
```

目录说明：

- 桌面端：保存Electron桌面端代码、桌面端UI、系统托盘、任务栏、窗口和安装包相关逻辑。
- Web端：保存浏览器Web端代码，需要同时兼容桌面浏览器和手机浏览器；UI体验和桌面端保持一致，但不能包含Electron专属能力。
- IDEA_SDK：保存最终打包安装到IDEA中的插件工程代码。IDEA插件负责连接IDE能力、本机中心服务和插件WebView，WebView统一加载中心服务提供的`plugin.html`作为插件页面入口，并向页面提供读取项目路径、项目名称、项目ID和发送上下文等能力。后续其他IDE插件同理，在各自SDK目录中独立打包安装到对应IDE。
- 中心服务：保存中心服务后端、数据读写、智能体调度、任务执行、记忆、会话和扩展能力管理逻辑。
- 共享：保存各端共用的类型、协议、工具函数、接口定义和渲染组件。

后续如果增加其他UI端或SDK，也必须在项目根目录下新增独立目录，例如独立App形态的`移动端`、`VSCode_SDK`等。手机浏览器访问属于Web端适配范围，不需要单独拆成移动端目录。

凡是包含`package.json`的目录，都必须使用`pnpm`作为Node.js包管理器，并使用项目统一的pnpm配置。优先使用项目根目录下的`.npmrc`，让子目录通过向上查找继承配置；如果某个工具链或执行方式无法继承根目录`.npmrc`，则需要在该`package.json`同级目录放置一份内容一致的`.npmrc`。

项目统一`.npmrc`至少需要包含npm镜像源配置和`save-exact=true`，确保依赖安装来源一致，并且写入`package.json`时固定精确版本。项目根目录必须提供`pnpm-workspace.yaml`管理Node.js工作区。

## 构建打包约定

中心服务源码保留在项目根目录的`中心服务`目录中，作为独立模块开发和维护。

桌面端构建时，必须把中心服务一起构建并打包进桌面端产物中。用户安装桌面端后，不需要再单独安装或手动启动中心服务。

桌面端负责启动、停止、重启和监控随包携带的中心服务，并继续负责中心服务端口、Web端账号密码和系统通知权限等本机配置。

Web端和IDE插件不内置中心服务，只连接桌面端启动并管理的中心服务。

## 主题

暗黑 + 白亮

这两个elementplus都是支持的，所以样式全部用el默认的即可

## 供应商

供应商用于配置模型或能力提供方，由中心服务统一管理。

用户可以添加、修改、删除、启用、停用供应商。

供应商需要支持的信息：

- 供应商名称
- 供应商类型
- 接口地址
- API Key
- 可用模型列表
- 默认模型
- 可用推理深度列表
- 默认推理深度
- 代理策略
- 是否启用

API Key等敏感信息只保存在中心电脑，由中心服务使用。桌面端和Web端可以管理供应商配置，IDE插件只使用中心服务提供的能力，不直接暴露API Key明文。

## 网络代理

部分网络环境无法直接访问供应商接口，中心服务需要支持通过网络代理访问供应商、模型列表接口、推理深度接口和后续模型调用接口。

网络代理由中心服务统一管理，桌面端和Web端只负责配置；IDE插件不直接保存或使用代理账号密码。

代理协议至少支持：

- HTTP
- HTTPS
- SOCKS4
- SOCKS4a
- SOCKS5

每个代理配置需要支持的信息：

- 代理名称
- 代理协议
- 代理主机
- 代理端口
- 用户名
- 密码
- 是否启用
- 备注

代理用户名和密码允许为空。用户名和密码都为空时表示该代理不需要认证，中心服务必须按无认证代理连接，不能把空账号密码当作配置错误。

代理用户名和密码属于敏感信息，只能保存在中心电脑，由中心服务使用，客户端不能明文展示已经保存的密码。

系统需要支持全局默认代理。全局默认代理用于没有单独指定代理策略的供应商请求。

供应商需要支持代理策略：

- 不使用代理
- 使用全局默认代理
- 使用指定代理配置

供应商的代理策略只影响后续供应商访问、模型列表刷新、推理深度刷新和模型调用，不回改历史对话、任务记录和调用记录。

代理连接失败时，需要在UI中提示失败原因，并保留排查信息。模型列表、推理深度或模型调用因为代理失败而失败时，错误原因需要能区分是代理连接失败、代理认证失败、供应商连接失败还是供应商接口返回失败。

创建或修改供应商后，中心服务需要自动向该供应商获取可用大模型列表和可用推理深度列表，并保存到供应商配置中。

模型列表获取成功后，用户可以从自动获取的模型列表中选择默认模型。

推理深度列表获取成功后，用户可以从自动获取的推理深度列表中选择默认推理深度。

模型列表或推理深度列表获取失败时，需要在UI中提示失败原因，并允许用户手动刷新。

如果供应商不支持模型列表接口，可以允许用户手动填写模型名称。

如果供应商不支持推理深度列表接口，可以允许用户手动填写推理深度，或者标记为不支持推理深度。

如果只有一个启用的供应商，智能体默认使用这个供应商。

如果有多个启用的供应商，用户可以为每个智能体选择承载它的供应商、模型和推理深度。

供应商、模型或推理深度选择可以由用户修改。修改后，只影响后续对话和后续任务，不回改已经产生的历史对话、任务记录和记忆内容。

## 用量统计

中心服务需要统计每个供应商的模型调用用量，方便用户查看不同供应商的使用情况。

供应商维度至少统计：

- 输入 token 数
- 输出 token 数
- 总 token 数
- 缓存命中 token 数
- 缓存未命中 token 数
- 调用次数
- 成功次数
- 失败次数
- 统计时间范围

项目维度也需要统计模型调用用量。项目统计需要按项目ID归集，并支持查看某个项目在不同供应商、模型和时间范围内的 token 使用、缓存命中和缓存未命中情况。

普通非项目会话需要归入全局用量统计，不混入具体项目统计。

如果供应商返回了缓存命中、缓存未命中或类似字段，中心服务必须按供应商原始返回记录并转换为统一统计字段；如果供应商没有返回缓存相关字段，统计中必须明确标记为供应商未提供，不能用0冒充实际缓存命中或未命中。

用量统计只追加记录，不回改历史调用记录。供应商、模型、项目别名或项目路径后续变化时，历史统计仍保留当时的供应商ID、模型名称、项目ID和调用时间，避免统计口径漂移。

桌面端和Web端需要提供用量统计查看界面，至少支持按供应商、模型、项目和时间范围筛选。

## 核心概念

致心智能体是中心服务中的主智能体，不绑定任何具体UI窗口。

桌面端、Web端、IDE插件都是连接中心服务的客户端。中心服务是唯一事实源，负责维护智能体、会话、项目、任务和记忆状态。

中心智能体只服务一台中心电脑。用户记忆在中心电脑上只有一份，但是记忆系统要支持迁移到其他电脑继续使用。

## 中心服务连接与访问控制

中心服务需要监听本机服务端口，默认端口为8866。

中心服务端口可以由用户配置。端口配置属于中心服务配置，修改后需要重启或重新启动中心服务后生效。

桌面端负责启动和管理中心服务，也负责配置中心服务端口。

IDE插件连接中心服务时，IP固定为127.0.0.1，只允许连接本机中心服务。IDE插件中可以配置端口，默认端口为8866。

IDE插件最终连接地址格式为：

```md
http://127.0.0.1:端口
```

Web端访问中心服务时，如果访问来源是本机，可以直接进入已授权的本机使用流程。本机访问和非本机访问的判断必须由中心服务根据请求来源、监听地址和连接信息完成，前端不能只依赖`window.location.hostname`自行判断，否则容易被伪造或绕过。

Web端访问中心服务时，如果不是本机访问，第一次打开页面必须要求输入账号和密码。账号密码验证通过后，中心服务给Web端发放登录态，后续请求使用登录态访问。

Web端登录态必须使用Cookie保存和传递，不能使用`sessionStorage`保存登录 token。Cookie由中心服务设置，并按访问方式配置合适的`HttpOnly`、`SameSite`、`Secure`和过期时间策略。

Web端账号密码只能在桌面端配置和修改，不能在Web端自己配置账号密码。

账号密码等访问控制信息只保存在中心电脑，由中心服务校验。IDE插件不使用账号密码登录，只使用本机127.0.0.1加端口连接。

## 中心目录

所有固化信息都存放在中心服务的中心目录下，方便统一管理、迁移和删除。

如果中心服务运行目录是：

```md
D:/中心
```

那么固化数据统一存放在这个目录的子目录中。

中心级目录树：

```md
D:/中心
├─ 记忆
├─ 供应商
├─ 智能体
├─ 插件
├─ MCP
├─ skills
├─ 运行环境
├─ 会话
└─ 日志
```

中心级目录说明：

- 记忆：保存主智能体、团队智能体集合中的智能体、子智能体和会话沉淀出的长期记忆。
- 供应商：保存模型或能力提供方配置，包括接口地址、可用模型、默认模型和推理深度等信息。
- 智能体：保存主智能体、团队智能体集合中的智能体和子智能体的定义、权限、默认模型选择和扩展能力可用范围。
- 插件：保存中心级插件的清单、代码、配置、启用状态和调用记录。
- MCP：保存中心级MCP Server配置、运行参数、启用状态和调用记录。
- skills：保存中心级skill内容、说明、启用状态和调用记录。
- 运行环境：保存Node.js、Python、Java、Git等可执行运行环境配置。
- 会话：保存普通会话和项目会话的状态、消息、任务记录、项目关联信息和会话级配置。
- 日志：保存中心服务、客户端连接、任务执行、扩展能力调用和异常排查日志。

中心目录是中心服务的数据根目录。删除或迁移中心目录，就等于删除或迁移该中心服务的固化数据。

项目不作为中心目录下的独立顶层目录。项目身份、项目聊天、项目记忆、项目任务和项目扩展能力配置都归属于项目会话，由“会话”目录统一承载。

项目根目录中的“致心项目ID.md”是例外。它用于跟随项目迁移，帮助中心服务识别项目身份；项目的详细配置、项目聊天、项目记忆和项目扩展能力配置仍然存放在中心目录的“会话”目录下。

## 运行环境配置

中心服务需要统一管理可执行程序和工具链目录，方便插件、MCP、skill、任务执行和命令执行选择不同运行环境。

运行环境配置统一存放在中心目录的“运行环境”子目录中。

系统需要内置市面常用运行环境类型，至少包括：

- Node.js
- Python
- Java
- Maven
- Git

pnpm、npm、yarn、npx、uv、pip等包管理器或命令入口不作为独立运行环境类型内置。Node.js包管理统一使用pnpm，并通过所选Node.js运行环境执行。Python相关命令通过所选Python运行环境执行，例如Python可以使用`python -m pip`调用对应解释器环境下的pip。Java项目依赖和构建统一使用Maven，并通过登记的Maven运行环境执行。

每个运行环境配置需要支持的信息：

- 环境名称
- 环境类型
- 可执行文件路径
- 根目录路径
- 版本号
- 环境变量
- PATH追加目录
- 是否默认
- 是否启用
- 备注

用户可以在UI中添加、修改、删除、启用、停用运行环境配置。系统内置的常用类型只是默认模板，不限制用户新增其他工具链类型。

如果用户使用nvm、pyenv、conda、asdf、sdkman等版本管理工具，中心服务允许用户把不同版本的Node、Python、Java、Maven等运行环境分别登记为独立配置。例如用户本机默认Node是16版本，但某个MCP Server、插件或skill需要Node 20时，可以选择Node 20对应的运行环境执行；某个IDEA插件或Java能力需要指定Maven版本时，可以选择对应Maven运行环境执行。

执行插件、MCP Server、skill脚本或命令任务时，可以指定使用某个运行环境。未指定时使用该类型的默认启用环境。

运行环境选择只影响后续执行，不回改历史任务记录。任务记录中需要保存当时使用的运行环境名称、类型、路径和版本，方便排查问题。

## 会话初始化规则

每次创建新的会话时，agent默认读取当前作用域下的AGENTS.md规则文件。

AGENTS.md文件名不区分大小写，例如下面文件名都视为同一个规则文件：

```md
AGENTS.md
agents.md
Agents.md
```

如果同时存在AGENTS.overflow.md，则读取AGENTS.md后再读取AGENTS.overflow.md。

AGENTS.overflow.md用于覆盖AGENTS.md中的规则。出现冲突时，以AGENTS.overflow.md为准。

AGENTS.overflow.md文件名同样不区分大小写。

项目会话优先读取项目作用域下的规则文件，项目作用域规则优先放在：

```md
.agents/rules
```

普通会话读取中心目录或用户配置作用域下的规则文件。

## 扩展能力系统

致心智能体需要支持类似Codex的扩展能力体系，包括插件系统、MCP系统和skill系统。

这些能力由中心服务统一管理，供主智能体、团队智能体集合中的智能体和子智能体按权限使用。

扩展能力系统要支持：

- 安装
- 启用
- 停用
- 删除
- 配置
- 查看能力说明
- 查看调用记录
- 按项目或全局控制可用范围

扩展能力可以参与agent的任务执行，但是不能绕过中心服务直接操作核心记忆、项目数据和会话状态。

扩展能力支持全局级别和项目级别。

项目级别会话可以使用项目级别的插件、MCP和skill。

项目级扩展能力需要兼容市面上的通用项目目录规则。

项目级目录树：

```md
项目根目录
├─ 致心项目ID.md
└─ .agents
   ├─ rules
   ├─ skills
   ├─ plugins
   └─ mcp
```

项目级目录说明：

- 致心项目ID.md：保存项目身份标识，跟随项目迁移，用于让中心服务把当前项目和中心目录中的项目会话关联起来。
- .agents/rules：保存项目级规则文件，例如AGENTS.md和AGENTS.overflow.md的项目作用域规则。
- .agents/skills：保存项目级skill，方便跟随项目仓库一起迁移。
- .agents/plugins：保存项目级插件，供项目会话优先使用。
- .agents/mcp：保存项目级MCP配置，供项目会话优先使用。

中心目录可以保存项目级能力的扫描索引、启用状态和用户配置，但不强制把项目级能力复制出项目目录。项目的聊天、记忆、任务记录和项目会话配置仍然由中心目录的“会话”目录承载。

当项目级别和全局级别存在同名插件、MCP或skill时，项目级别优先。

优先级规则：

- 项目级插件优先于全局插件
- 项目级MCP优先于全局MCP
- 项目级skill优先于全局skill

如果项目级能力不存在，才回退使用全局能力。

### 插件系统

插件系统用于给致心智能体增加本地能力、业务能力或UI能力。

这里的插件是agent能力插件，不等同于IDE插件客户端。

插件需要有清晰的清单文件，描述插件名称、版本、入口、能力、配置项、权限和适用范围。

插件可以提供：

- 工具能力
- UI页面
- 命令
- 项目能力
- 记忆处理能力
- 第三方服务集成

插件权限必须显式声明。涉及文件、网络、命令执行、记忆读写、项目访问等能力时，需要在安装或启用时向用户提示。

### MCP系统

MCP系统用于连接外部MCP Server，让agent可以调用外部工具、资源和上下文。

中心服务负责管理MCP Server配置，包括：

- MCP Server名称
- 启动命令或连接地址
- 环境变量
- 是否启用
- 可用工具列表
- 可用资源列表
- 适用范围

MCP工具调用需要进入调用记录，方便用户查看agent使用了哪些外部工具。

MCP Server可以按全局启用，也可以按项目启用。IDE插件中的项目聊天只能使用当前项目允许的MCP能力。

MCP配置采用JSON格式，根字段为mcpServers。

需要支持HTTP类型MCP Server，例如：

```json
{
  "mcpServers": {
    "idea": {
      "type": "http",
      "url": "http://127.0.0.1:64342/stream"
    }
  }
}
```

需要支持stdio类型MCP Server，例如：

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "chrome-devtools-mcp@latest",
        "--browserUrl",
        "http://127.0.0.1:9222",
        "--no-usage-statistics"
      ]
    }
  }
}
```

同一个JSON配置中可以配置多个MCP Server。

中心服务需要能读取全局MCP配置，也需要能读取项目级“.agents/mcp”目录下的MCP配置。

### skill系统

skill系统用于给agent提供可复用的工作流程、专业知识和行为约束，整体设计参考Codex的skill机制。

每个skill需要有独立目录和说明文件，说明它的名称、用途、触发条件、工作流程和可用资源。

skill可以包含：

- 使用说明
- 工作流程
- 模板
- 脚本
- 参考资料
- 示例

agent在处理用户请求前，需要根据用户请求、当前项目、当前会话和系统状态判断是否需要使用skill。

使用skill时，需要把skill作为本轮对话的执行上下文之一，但skill不能覆盖用户明确指令。

skill可以全局安装，也可以按项目安装。项目级skill随项目迁移时可以继续使用。

## 多端实时同步

所有UI实时同步中心服务中的数据状态。

同步的数据包括：

- 项目列表
- 项目ID
- 项目名称
- 普通聊天列表
- 项目聊天列表
- 聊天消息
- 智能体状态
- 智能体供应商、模型和推理深度配置
- 任务状态
- 记忆写入结果
- 供应商配置状态
- 插件配置状态
- MCP配置状态
- skill配置状态

不同步的数据包括：

- 当前UI打开的窗口
- 当前选中的页签
- 当前滚动位置
- 本地布局状态

也就是说，对话数据实时同步，但是其他客户端不会被强制同步打开同一个窗口或同一个页签。

客户端和中心服务断开连接后需要自动重连，并展示连接状态。自动重连需要有明确的重试次数、重试间隔和最终停止状态，不能无限静默重试。

如果一直无法重连并进入停止状态，客户端中尚未发出的排队消息不能在恢复连接后自动发送。系统必须保留这些待发送消息，并提示用户确认后再发送、修改或取消，避免网络恢复后把用户已经失去上下文的消息自动发出去。

已经发送到中心服务并由中心服务接收的消息，由中心服务按照任务和会话状态继续处理；尚未成功发送到中心服务的本地排队消息，必须以“待用户确认”状态展示。

## 对话输入框

对话输入框需要支持文本、图片和上下文引用的混合输入。

用户可以直接把剪贴板中的图片粘贴到对话输入框中。粘贴后图片以附件预览形式展示，发送后作为本轮用户消息的一部分进入对话上下文。

图片附件需要保存以下信息：

- 附件ID
- 文件名
- MIME类型
- 文件大小
- 图片宽高
- 存储路径
- 所属会话ID
- 所属消息ID

图片原始文件存放在中心目录的会话附件目录中，方便历史对话查看和迁移。

如果当前供应商或模型不支持图片输入，发送前需要在UI中提示用户，并禁止把图片附件发送给该模型。

输入框以“/”开头时，进入命令和skill检索模式。用户可以通过“/”搜索可用skill，并把选中的skill加入本轮对话上下文。

skill检索需要同时展示全局skill和当前项目可用skill。项目级别会话中，如果项目级skill与全局skill同名，优先展示和使用项目级skill。

项目级别会话中，输入框需要支持“@”检索项目文件信息。用户输入“@”后，可以搜索当前项目下的文件或文件夹，并把选中的文件、文件夹或代码位置作为上下文引用加入对话框。

“@”检索需要使用一个贴近输入框的小弹框展示可选结果。小弹框用于可视化选择文件、文件夹和代码位置，但不能阻塞输入框继续输入；用户在弹框打开时仍然可以继续编辑文本、删除“@”查询内容或关闭弹框。

“@”小弹框需要支持键盘上下选择、回车确认、Esc关闭、鼠标点击选择。确认选择后，把引用标签插入到输入框当前位置或追加到输入框的引用区域。

“@”文件检索只在有项目上下文的会话中启用。普通非项目会话不展示项目文件检索能力。

## 通知配置

用户可以配置对话完成后是否通知。

对话完成通知指用户发送一轮消息后，agent完成本轮回复、工具调用、文件修改、记忆写入和状态收尾时，向用户发出完成提醒。

通知配置需要支持：

- 是否开启对话完成通知
- 是否仅在窗口不活跃时通知
- 是否通知普通对话
- 是否通知项目对话
- 是否通知团队智能体集合中的智能体对话
- 是否通知失败或需要用户处理的对话

通知配置按客户端类型分别保存。桌面端、Web端、IDE插件可以有不同的通知开关，不强制互相同步。

同一种客户端类型内部可以同步通知配置。例如多个桌面端窗口使用同一套桌面端通知配置，多个IDE插件窗口使用同一套IDE插件通知配置。

所有通知都由中心服务统一判断、生成和发送。客户端不自行判断是否需要产生通知。

中心服务需要直接调用中心电脑的系统通知通道发送通知，让用户在其他端、其他窗口或其他软件中工作时也能感知到agent已经完成或需要处理。

中心服务仍然需要把通知事件同步给相关客户端，用于客户端内展示、未读状态、跳转和Web端二次通知。

中心服务发送和同步的通知事件需要包含：

- 通知ID
- 通知类型
- 目标客户端类型
- 目标会话ID
- 目标项目ID
- 目标智能体ID
- 标题
- 内容摘要
- 通知时间
- 是否需要用户处理
- 跳转目标

目标客户端类型用于区分桌面端、Web端和IDE插件。中心服务根据目标客户端类型和通知配置决定系统通知内容和通知事件要同步给哪些客户端连接。

桌面端和IDE插件收到中心服务通知事件后，主要用于客户端内未读状态、提示条和跳转定位，不再负责产生系统级通知。

Web端比较特殊。中心服务已经发送系统通知后，Web端收到通知事件时还需要在浏览器里再通知一次，可以使用浏览器通知或页面内通知，保证用户正在浏览器中工作时也能看到提醒。

如果中心电脑没有系统通知权限，桌面端需要提示用户开启系统通知权限，并把权限状态同步给中心服务。

如果浏览器没有通知权限，Web端需要提示用户开启浏览器通知权限；无法使用浏览器通知时，至少要在页面内显示通知，不能静默失败。

## 对话执行模式

每个对话窗口都可以选择执行模式。

执行模式会影响agent执行任务时是否需要用户审批。

执行模式不在桌面端、Web端、IDE插件之间互相同步，而是按客户端类型分别保存和同步。同一种客户端类型内部统一同步，不按项目或会话拆分。

例如：

- IDEA插件中的模式会同步到整个IDEA插件，不会同步到桌面端或Web端
- 桌面端中的模式会同步到桌面端自己的窗口，不会同步到IDEA插件或Web端
- Web端中的模式会同步到Web端自己的窗口，不会同步到IDEA插件或桌面端

支持三种模式：

- 建议模式：最安全的选择，每一步都需要用户自己批准。
- 自动编辑：平衡模式，保持正常编辑流程，同时在需要时进行审批。
- 全自动：完全免审批，写文件和执行命令会立即生效，仅受沙箱限制。

默认模式使用“全自动”。

用户可以在对话窗口中切换模式。切换后只影响当前客户端类型下的后续操作，不改变已经执行的历史操作。

## 智能体

- 主智能体和团队智能体集合中的智能体都可以引导用户，也可以和用户持续对话。
- 子智能体只可以在任务执行过程中引导用户，不作为持续对话对象。
- UI端需要重点展示主智能体对话，同时允许用户点进其他团队智能体的对话、管线通话和群聊讨论查看详情。
- 子智能体任务完成后就关闭。如果子智能体已经结束但用户仍在查看对话，UI端保留该对话窗口，直到用户关闭后再移除。


### 主智能体

名字：致心

角色：直接和用户对话

功能：派发任务给子智能体或者是创建其他的智能体去工作

记忆：有自己的记忆


主智能体可以由用户修改，但是不能删除。

主智能体的承载供应商、模型和推理深度可以由用户修改。如果只有一个启用的供应商，则默认使用该供应商。

### 团队智能体

“团队智能体”是用户给一组智能体的总体名称，不是低于主智能体的权限级别。团队智能体集合里包含的是一个一个独立智能体，建议使用中文名字展示为团队人员。

团队智能体集合中的智能体可以由主智能体在对话中创建，也可以由用户手动添加。

主智能体创建或用户添加的团队智能体都要用md固化下来，每个团队智能体都有自己的记忆、供应商、模型和推理深度配置。

团队智能体的权限和主智能体一致，可以引导用户、和用户持续对话、拉起其他团队智能体协作，也可以创建子智能体派发一次性任务。确实没有合适团队智能体时，才允许创建新的团队智能体。

团队智能体可以由用户添加、管理、修改、删除。

团队智能体的承载供应商、模型和推理深度可以由用户修改。如果只有一个启用的供应商，则默认使用该供应商。

删除团队智能体时，必须向用户提示删除影响。用户确认删除后：

- 删除该团队智能体的记忆
- 从正在使用该团队智能体的对话中移除
- 移除该团队智能体的后续可选入口
- 已经产生的历史对话内容按会话记录保留，但是不再绑定为可继续使用的团队智能体

### 智能体协作

用户发起一次对话后，主智能体和团队智能体都可以按任务需要拉起其他队员参与本轮处理。

拉起其他队员时，必须优先从团队智能体集合中的现有智能体中选择。只有现有团队智能体都不适合当前任务时，才允许创建新的团队智能体，避免无限制创建智能体。

智能体之间可以相互通话。相互通话支持管线形式，一个智能体把阶段性结论、上下文或任务结果传给下一个智能体继续处理。

智能体之间也可以创建群聊，多个智能体在同一个协作讨论中共同分析、辩论、补充和收敛结论。

管线通话、群聊讨论、参与智能体、消息流向、当前状态和最终结果都需要在UI端显示出来，方便用户查看、介入、暂停、结束和管理。

### 子智能体

子智能体可以由主智能体或者团队智能体创建，用于执行一次性任务。子智能体只可以在任务执行过程中引导用户，不可以作为持续对话对象；任务使用完成后需要关闭，只保留短期窗口记忆。

子智能体不能再创建新的子智能体。所有需要继续拆分、派发或并行执行的任务，都必须回到创建它的主智能体或团队智能体统一调度，避免形成不可控的多级代理链路。

### 智能体状态

智能体需要有明确状态，方便所有UI同步展示。

- 空闲
- 工作中
- 排队中
- 等待用户
- 已结束
- 失败

## 项目

项目是IDE插件和工程对话的核心概念。

项目使用UUID作为唯一ID，不直接使用项目路径作为ID。这样项目文件夹迁移到其他电脑后，只要项目ID文件还在，就可以继续识别为同一个项目。

每个项目根目录保存一个项目ID文件：

```md
致心项目ID.md
```

IDE插件打开项目时，按下面规则处理：

- 如果存在“致心项目ID.md”，读取其中的项目ID
- 如果不存在，创建新的UUID，并写入“致心项目ID.md”
- 项目显示名称默认使用项目文件夹名
- 项目文件夹名变化后，项目显示名称默认跟随新的文件夹名
- 用户可以设置项目别名
- 设置项目别名后，桌面端、Web端和IDE插件优先显示项目别名
- 未设置项目别名时，项目显示名称继续使用项目文件夹名

## 项目聊天

项目聊天属于某一个项目。

IDE插件中的对话是项目级别的，只能看到当前打开项目下的聊天、智能体、任务和记忆，不显示其他项目的数据。

项目聊天支持项目级别的插件、MCP和skill。

项目聊天执行时，优先使用当前项目配置的插件、MCP和skill；只有当前项目没有同名能力时，才使用全局插件、MCP和skill。

IDE插件中可以为同一个项目打开多个聊天页签，每个页签对应一个项目聊天会话。

在桌面端和Web端中，项目聊天显示为项目下的子聊天。项目聊天内容、智能体状态、消息状态在IDE插件、桌面端和Web端之间实时同步。

页签是否打开、当前正在查看哪个聊天、滚动位置和布局状态属于本地UI状态，不强制同步。

## 功能

### 桌面端

electron桌面端，可以开启中心服务。

桌面端可以配置中心服务端口，默认端口为8866。

桌面端可以配置Web端非本机访问所需的账号和密码。账号密码只能在桌面端配置和修改。

#### 主页

左侧分成普通对话和工程对话。

工程对话按项目分组展示，每个项目下面展示该项目的子聊天。

### Web端

使用中心服务，需要兼容桌面浏览器和手机浏览器。手机浏览器访问属于Web端能力，不单独拆分为移动端；UI体验和桌面端保持一致，并根据小屏幕做响应式适配。

Web端可以看到所有普通对话和所有项目对话。

Web端如果从非本机访问中心服务，首次打开页面必须进入登录页，输入桌面端配置的账号和密码后才能访问。登录成功后保存登录态。

## 记忆系统

使用.md文件存放所有记忆。

记忆统一存放在中心目录的“记忆”子目录中。

如果中心目录是：

```md
D:/中心
```

那么记忆目录就是：

```md
D:/中心/记忆
```

记忆要支持迁移。永久记忆中记录“使用的电脑”，表示当时产生这段记忆的中心电脑。

用户可以在UI中快速查看记忆内容。

记忆写入以用户和agent之间的一轮对话为单位。一轮对话不是指agent和大模型之间的一次请求，也不是内部工具调用或子任务调用，而是指用户向agent发起一次输入后，agent完成本次回应、引导、排队、子智能体协作和相关处理，直到需要等待用户下一次输入为止。

引导过程也属于这一轮对话的一部分，不能单独拆成多段记忆写入。

每一轮用户和agent的对话完成后，才允许写入记忆。

记忆写入按智能体隔离。每个智能体自己的记忆写入必须单线程执行，避免同一个智能体的记忆文件竞争写入。

不同智能体之间可以并行写入各自的记忆。

记忆读取可以多线程执行。

记忆写入只能追加，不能覆盖或插入修改已有记忆内容。

### 用户记忆

user.md

记住用户是怎么一个人。

用户记忆在一台中心电脑上只有一份。

### 短期记忆

也就是会话窗口，不生成md文件。

### 长期记忆

### 永久记忆

按照文件夹的形式，最后一层才是真的文件：智能体名字/年/月/日.md

媒体文件如何处理

每段永久记忆格式，一个对话完成了才写入记忆系统，写入必须用固定脚本单线程写入，避免竞争。

```md
# 时间：YYYY-MM-DD HH:mm:ss

## 关键词

## 总结

## 使用的电脑

## 用户说的

## 回答的
```

## 插件

- 各个平台使用webview作为基座，提供完成功能的SDK
  - 获取项目路径
  - 获取项目名称
  - 读取或创建项目ID
  - 插件打开就是发送项目路径和项目ID
- 打开插件就是引入中心服务提供的`plugin.html`。
- `plugin.html`是插件页面的统一承载入口，页面运行时通过已安装到IDE中的插件调用IDE或宿主端提供的能力。
- IDEA插件最终需要打包为可安装到IDEA中的插件；其他平台的IDE插件同理，分别打包安装到对应IDE，并统一通过WebView加载`plugin.html`承载插件页面。
- 实现一套插件页面代码供各个平台使用

### IDE插件上下文发送

IDE插件需要支持把当前项目中的文件、文件夹和代码行发送到对话框，作为本次项目聊天的上下文引用。

IDE插件的项目级别会话输入框也需要支持“@”检索当前项目文件信息。用户可以通过“@”搜索并选择文件、文件夹或代码位置，选择后以文件引用标签、文件夹引用标签或代码行引用标签展示在输入框中。

IDE插件中的“@”检索同样使用贴近输入框的小弹框展示可选结果。小弹框不能阻塞输入框继续输入，用户可以一边输入一边筛选，也可以继续编辑原文本或取消检索。

IDE插件中的上下文发送入口必须模仿常见AI插件的右键菜单交互：用户在编辑器选区、编辑器标签页、项目文件树文件、项目文件树文件夹等位置右键时，都可以看到发送到致心对话框的菜单项。菜单点击后，不直接发起最终提问，而是先把对应文件、文件夹或代码行引用插入到致心对话框输入区，用户可以继续编辑文本后再发送。

编辑器选区右键菜单需要支持把选中代码发送到对话框；如果选区为空但光标在文件中，可以按当前文件或当前行范围提供发送入口。项目文件树右键菜单需要支持把文件路径或文件夹路径发送到对话框。编辑器标签页右键菜单需要支持把当前文件路径发送到对话框。后续其他IDE插件也按同样交互设计实现。

选中文件或文件夹时，可以把路径发送到对话框。

发送到对话框后，UI以引用标签展示：

- 文件显示文件图标和文件名
- 文件夹显示文件夹图标和文件夹名
- 引用标签中保留完整路径，鼠标悬停或详情中可以查看完整路径

选中代码行时，可以把当前文件路径和选中行范围发送到对话框。

代码行引用在UI中显示为：

```md
文件名#L开始行-结束行
```

如果只选中一行，显示为：

```md
文件名#L行号
```

代码行引用需要保留的信息：

- 项目ID
- 文件绝对路径
- 文件相对项目路径
- 开始行
- 结束行
- 选中的代码内容

插件页面、聊天消息和Markdown渲染内容中，需要支持类似AI插件里的内部文件定位链接。内部文件定位链接用于定位当前项目中的文件、文件夹或代码行，点击后由已安装的IDE插件打开对应文件并跳转到指定行号或选区。

内部文件定位链接必须和外部链接区分处理。外部链接只包含普通`http://`、`https://`等网络地址，点击后按浏览器或系统默认方式打开；内部文件定位链接必须携带项目ID、相对路径、绝对路径、起始行和结束行等结构化信息，点击后走IDE插件内部跳转能力，不能按普通外部链接打开。

内部文件定位链接在UI显示上可以使用“文件名”“文件名#L行号”或“文件名#L开始行-结束行”这种短标签形式，但底层数据不能只保存显示文本，必须保存完整定位信息，避免项目迁移、同名文件或路径变化时无法判断目标。

文件、文件夹和代码行引用都属于消息输入内容的一部分，发送后需要进入对应项目聊天，并参与桌面端、Web端和IDE插件的实时同步。

### idea

IDEA插件打开项目时，先读取或创建“致心项目ID.md”，再连接中心服务。

IDEA插件只展示当前项目的数据，不展示其他项目。
