---
description: 本文件记录项目全部单一功能、功能边界、上下游依赖、影响范围与回归测试关系，用于在修改任一功能时快速识别可能受影响的关联功能，避免局部改动导致其他功能失效，并支持 AI 与人工进行影响分析、回归检查和自动测试生成。每条记录至少应明确：当前功能是什么、依赖哪些功能、被哪些功能依赖、修改后必须联动检查哪些页面/接口/流程、最小回归测试范围是什么。记录方式建议保持一行一个功能，但每行除“功能-关联功能”外，还应补充功能说明、依赖方向、影响范围和测试关注点。本文件由AI全权管理。文件关系固定为：`需求.md` 提供功能存在的业务目标与验收边界，`设计.md` 提供功能拆分方式、交互流程与方案约束，`架构.md` 提供功能对应的技术模块、接口边界与调用链路，`功能清单与关系.md` 负责把前三者映射成可检索的功能清单、依赖图、影响面与最小回归测试范围。若这里记录的功能关系与前三者冲突，不得以本文件反向定义需求、设计或架构，而必须先修正上游文档后再同步本文件。文档改动完成后不做额外校验，直接提交并推送。
---

- 记忆召回与身份问答 - 依赖 `services/center/src/model-gateway-runtime.ts` 的 `memory_index` 检索、`services/center/src/memory-engine.ts` 的 `mem0` 适配和 `services/center/src/domain/session-turn-effects.ts` 的长期记忆过滤；影响普通对话身份问答、长期记忆写入和错误记忆拦截；修改后至少回归 `你叫什么？`、`我是谁？你叫什么？`、错误回复不再写回记忆。
- 轮次失败收尾与运行中恢复 - 依赖 `services/center/src/deepagents-agent.ts` 异常收尾、`services/center/src/events.ts` 终态事件输出和前端会话页轮次状态展示；影响“当前轮次已耗时”、失败终态和控制台日志；修改后至少回归工具/模型失败时轮次不再卡死、页面从运行中进入失败或完成。
- 对话页面真实内容展示边界 - 依赖中心服务过程事件产出边界与前端消息流渲染规则；影响聊天消息列表、过程卡片和验收展示；修改后至少回归页面只显示模型真实文字、真实工具调用和真实工具结果，不展示中心服务写死阶段文案。
- 桌面壳开发启动链路 - 依赖 `scripts/dev-desktop-shell.mjs` 和 `apps/desktop-shell/src/main.ts` 对用户系统 Node、中心服务健康检查和日志输出的处理；影响 `dev:desktop-shell`、8866 监听、Electron 主进程稳定性；修改后至少回归通过用户环境 Node 拉起桌面壳和中心服务、窗口正常加载、无 `EPIPE` 主进程弹错。