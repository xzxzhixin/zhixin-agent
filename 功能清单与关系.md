---
description: 本文件记录项目全部单一功能、功能边界、上下游依赖、影响范围与最小回归测试关系，是每次修改后的强制同步清单，用于在修改任一功能时快速识别关联影响，避免局部改动导致其他功能失效，并支持 AI 与人工进行影响分析、回归检查和自动测试生成。任何代码修改、Bug 修复、新增需求、设计调整、架构调整、接口协议变化、依赖版本变化、测试或验收口径变化完成后，都必须立即回查本文件；如果本次变化新增、删除、拆分、合并或改变了任何可独立回归的单一功能，必须同步新增或修订对应条目；如果判断无需更新，最终回复必须明确说明“不需要更新功能清单与关系.md”及原因。记录粒度必须是“单一可回归功能”，不能只写大模块或临时过程；每条记录至少明确当前功能是什么、依赖哪些功能、被哪些功能依赖、修改后必须联动检查哪些页面/接口/流程、最小回归测试范围是什么。记录方式保持一行一个功能，每行除“功能-关联功能”外，还必须补充功能说明、依赖方向、影响范围和测试关注点。本文件由 AI 全权管理。文件关系固定为：`需求.md` 提供功能存在的业务目标与验收边界，`设计.md` 提供功能拆分方式、交互流程与方案约束，`架构.md` 提供功能对应的技术模块、接口边界与调用链路，`功能清单与关系.md` 负责把前三者映射成可检索的功能清单、依赖图、影响面与最小回归测试范围。若这里记录的功能关系与前三者冲突，不得以本文件反向定义需求、设计或架构，而必须先修正上游文档后再同步本文件。文档改动完成后不做额外校验，直接提交并推送。
---

- 记忆召回与身份问答 - 依赖 `services/center/src/model-gateway-runtime.ts` 的 `memory_index` 检索、`services/center/src/memory-engine.ts` 的 `mem0` 适配和 `services/center/src/domain/session-turn-effects.ts` 的长期记忆过滤；影响普通对话身份问答、长期记忆写入和错误记忆拦截；修改后至少回归 `你叫什么？`、`我是谁？你叫什么？`、错误回复不再写回记忆。
- 轮次失败收尾与运行中恢复 - 依赖 `services/center/src/deepagents-agent.ts` 异常收尾、`services/center/src/domain/turn-runtime-cancel-registry.ts` 运行时取消注册、`services/center/src/domain/session-domain.ts` 终态覆盖保护、`services/center/src/events.ts` 终态事件输出和前端会话页轮次状态展示；影响“当前轮次已耗时”、失败终态、取消终态、后台迟到完成和控制台日志；修改后至少回归工具/模型失败时轮次不再卡死、点击停止后 Deep Agents 后台运行被取消、已取消轮次不被迟到完成覆盖、页面从运行中进入失败/完成/取消终态。
- Deep Agents 命令工具真实调用链路 - 依赖 `services/center/src/deepagents-agent.ts` 的 Deep Agents agent 装配、`services/center/src/AgentMiddleware/CenterToolChoiceMiddleware.ts` 的工具选择诊断与空工具名恢复、`services/center/src/StructuredTool/tool-choice-policy.ts` 的命令工具安全名常量和 `services/center/src/StructuredTool/CommandStructuredTool.ts` 的命令执行；影响用户要求查看本机 Node/npm/pnpm/Git 版本、执行命令、命令过程卡片和工具结果回填；修改后至少回归模型必须自主返回 `builtin_command_run` 结构化 `tool_calls`、日志出现 `model.tool.requested`/`tool.command.completed`/`model.tool.result.appended`、页面展示真实命令输出，且中心服务源码中不得保留按用户文本固定解析并强制命令工具选择的入口；供应商返回空工具名时只能按结构化参数唯一匹配恢复命令工具名并记录 `model.tool_call.name_restored`。
- Deep Agents MCP 官方 adapter 真实调用链路 - 依赖 `services/center/src/deepagents-agent.ts` 的 Deep Agents agent 装配、`services/center/src/AgentMiddleware/CenterToolChoiceMiddleware.ts` 的工具选择诊断与空工具名恢复、`services/center/src/StructuredTool/mcp-adapter-config.ts` 的官方 `MultiServerMCPClient` 配置转换和 `services/center/src/StructuredTool/McpAdapterStructuredTool.ts` 的中心服务审计包装；影响 MCP 管理页已配置工具在对话中的真实调用、MCP 过程卡片、工具结果回填和空工具名兼容供应商；修改后至少回归模型必须自主返回目标 MCP adapter 工具结构化 `tool_calls`、日志出现 MCP 工具请求/完成/结果回填，中心服务源码中不得保留自建 `DynamicMcpStructuredTool`、自写 `tools/call` 主路径、按用户文本固定解析、强制选择 `idea.get_all_open_file_paths` 或把空工具名修正为固定 MCP 工具的入口。
- 命令工具过程卡片与轮次终态展示 - 依赖中心服务 `model.tool.*`、`tool.command.*` 事件、`services/center/src/StructuredTool/CenterStructuredToolBase.ts` 的结构化工具取消检查、`services/center/src/StructuredTool/deepagents-tool-runtime.ts` 的运行时取消信号传递和 `apps/frontend/src/stores/app-conversation-actions.ts` 的终态识别；影响停止按钮、当前轮次耗时、命令卡片完成态、工具失败态、停止后的工具结果回填和最终回复展示；修改后至少回归工具完成、工具失败、轮次失败和取消时按钮恢复“发送”，当前轮次耗时不残留运行态，停止后不继续追加该轮次工具结果回填或助手消息，同一 `toolCallId` 的请求、执行、输出、完成和结果回填仍聚合在同一张过程卡片。
- 对话页面真实内容展示边界 - 依赖中心服务过程事件产出边界与前端消息流渲染规则；影响聊天消息列表、过程卡片和验收展示；修改后至少回归页面只显示模型真实文字、真实工具调用和真实工具结果，不展示中心服务写死阶段文案。
- LangChain/Deep Agents 直接依赖边界 - 依赖 `services/center/package.json` 显式声明 `langchain`、`deepagents` 和 `@langchain/*` 版本，并依赖 pnpm lockfile 固化解析结果；影响中心服务依赖安装、Deep Agents middleware import、严格依赖解析和命令工具调用运行；修改 LangChain Agent API、Deep Agents middleware 或相关依赖版本后至少回归 `pnpm install --lockfile-only`、中心服务通过 `dev:desktop-shell` 启动和命令工具结构化调用。
- 桌面壳开发启动链路 - 依赖 `scripts/dev-desktop-shell.mjs` 和 `apps/desktop-shell/src/main.ts` 对用户系统 Node、中心服务健康检查和日志输出的处理；影响 `dev:desktop-shell`、8866 监听、Electron 主进程稳定性；修改后至少回归通过用户环境 Node 拉起桌面壳和中心服务、窗口正常加载、无 `EPIPE` 主进程弹错。
