---
description: 本文件记录项目全部单一功能、功能边界、上下游依赖、影响范围与最小回归测试关系，是每次修改前的影响分析入口和每次修改后的强制同步清单，用于在修改任一功能前先定位关联链路、共用状态、跨端表现、终态事件、事实源文档和最小回归面，避免局部改动导致其他功能失效或同类问题反复出现，并支持 AI 与人工进行影响分析、回归检查和自动测试生成。任何代码修改、Bug 修复、新增需求、设计调整、架构调整、接口协议变化、依赖版本变化、测试或验收口径变化开始前，都必须先检索本文件中是否已有对应单一功能或相邻依赖；实现完成后必须再次回查本文件。如果本次变化新增、删除、拆分、合并或改变了任何可独立回归的单一功能，必须同步新增或修订对应条目；如果判断无需更新，最终回复必须明确说明“不需要更新功能清单与关系.md”及原因。记录粒度必须是“单一可回归功能”，不能只写大模块、临时过程或一次性修复说明；每条记录至少明确当前功能是什么、依赖哪些功能、被哪些功能依赖、修改后必须联动检查哪些页面/接口/流程、最小回归测试范围是什么。记录方式保持一行一个功能，每行除“功能-关联功能”外，还必须补充功能说明、依赖方向、影响范围和测试关注点；涉及跨端、取消、终态、实时事件、运行态恢复、工具闭环、记忆写入等高风险链路时，条目必须显式列出中心服务事实源、前端状态收敛点、事件类型和最小回归动作。本文件由 AI 全权管理。文件关系固定为：`需求.md` 提供功能存在的业务目标与验收边界，`设计.md` 提供功能拆分方式、交互流程与方案约束，`架构.md` 提供功能对应的技术模块、接口边界与调用链路，`功能清单与关系.md` 负责把前三者映射成可检索的功能清单、依赖图、影响面与最小回归测试范围。若这里记录的功能关系与前三者冲突，不得以本文件反向定义需求、设计或架构，而必须先修正上游文档后再同步本文件。文档改动完成后不做额外校验，直接提交并推送。
---

- 记忆召回与身份问答 - 依赖 `services/center/src/model-provider/MainAgentMemoryPrompt.ts` 的 `memory_index` 检索、`services/center/src/memory-engine.ts` 的 `mem0` 适配和 `services/center/src/domain/session-turn-effects.ts` 的长期记忆过滤；影响普通对话身份问答、长期记忆写入和错误记忆拦截；修改后至少回归 `你叫什么？`、`我是谁？你叫什么？`、错误回复不再写回记忆。
- 富文本输入框结构化消息事实 - 依赖 `packages/ui/src/index.ts` 草稿协议、`apps/frontend/src/views/Chat/components/ChatConversationPanel.vue` 输入区和 `apps/frontend/src/stores/app-conversation-actions.ts` 发送链路；影响附件、项目引用、代码引用和消息展示；修改后至少回归纯文本发送、附件发送、代码引用插入、发送前草稿不入记忆。
- 归档附件引用 - 依赖 `services/center/src/domain/AttachmentArchiveService.ts`、`services/center/src/domain/usage-domain.ts` 和 `attachments` 表；影响临时附件转正、会话删除、中心目录迁移和附件记忆来源；修改后至少回归附件只保存一份、会话删除不删除归档文件、会话消息仍能展示附件、恶意 `temporaryRelativePath` 不能越过 `temp/{temporaryAttachmentId}/` 或移动中心目录外文件。
- 附件记忆写入 - 依赖 `services/center/src/domain/AttachmentMemoryService.ts`、`services/center/src/domain/session-turn-effects.ts`、`services/center/src/domain/agent-domain.ts` 和 `services/center/src/memory-engine.ts`；影响 Markdown 记忆、SQLite `memory_index` 和 Mem0 metadata；修改后至少回归附件来源写入、附件摘要成功追加、附件摘要失败不影响轮次。
- 跨窗口长期记忆召回 - 依赖 `services/center/src/model-provider/MainAgentMemoryPrompt.ts`、`services/center/src/memory-engine.ts` 和 SQLite `memory_index`；影响普通对话、项目对话和多窗口上下文；修改后至少回归窗口 A 说“我今天吃了龙虾”、窗口 B 问“我今天吃了什么”能召回，且源码不保留固定身份或业务词硬编码召回。
- 轮次失败收尾与运行中恢复 - 依赖 `services/center/src/deepagents-agent.ts` 异常收尾、`services/center/src/domain/turn-runtime-cancel-registry.ts` 运行时取消注册、`services/center/src/domain/session-domain.ts` 终态覆盖保护、`services/center/src/events.ts` 终态事件输出、`services/center/src/api/sync-route.ts` 的 `session.turn.state` 轻量轮次状态请求、`apps/frontend/src/stores/TurnStateReconciler.ts` 状态收敛器和前端会话页轮次状态展示；影响“当前轮次已耗时”、失败终态、取消终态、等待用户终态、后台迟到完成、多端终态同步和控制台日志；修改后至少回归工具/模型失败时轮次不再卡死、中心服务不生成失败总结助手消息、不写兜底事实说明、点击停止后 Deep Agents 后台运行被取消、已取消轮次不被迟到完成覆盖、收到 `turn.state.changed` 后多端进入终态、未收到助手消息且轮次非终态时触发 `20ms` 疑似卡住快轮询 `session.turn.state`、页面从运行中进入失败/完成/取消/等待用户终态。
- 停止取消与多端运行态收敛 - 依赖 `services/center/src/api/sync-route.ts` 的 `session.turn.cancel` 请求、`services/center/src/domain/turn-runtime-cancel-registry.ts` 的运行时取消原因和取消残留异常识别、`services/center/src/deepagents-agent.ts` 的 Deep Agents/LangGraph 收集器取消吞噬、`services/center/src/domain/session-domain.ts` 的取消终态写入、`turn.updated` 与 `turn.state.changed` 终态事件、`apps/frontend/src/stores/TurnStateReconciler.ts` 的强制终态收敛和 `apps/frontend/src/stores/app-conversation-actions.ts` 的 WebSocket 终态事件处理；影响浏览器端、桌面端、IDE 插件端停止按钮、“当前轮次已耗时”、过程卡片取消态、后台迟到工具结果、控制台取消噪声和多端快照刷新；修改后至少回归发送端点击停止后按钮恢复“发送”、其他端收到 `cancelled` 终态后不残留运行态、Deep Agents/LangGraph 因用户停止抛出的普通 `Error: 用户点击停止当前执行。` 不写入 `message.turn.failed`、取消后的迟到完成不覆盖已取消轮次、过程卡片保留已取消终态、专项 `task.updated` 和事件版 `turn.state.changed` 都能触发当前会话快照刷新。
- Deep Agents 命令工具真实调用链路 - 依赖 `services/center/src/deepagents-agent.ts` 的 Deep Agents agent 装配、`services/center/src/AgentMiddleware/CenterToolChoiceMiddleware.ts` 的工具选择诊断与空工具名缺失失败收尾、`services/center/src/StructuredTool/tool-choice-policy.ts` 的命令工具安全名常量和 `services/center/src/StructuredTool/CommandStructuredTool.ts` 的命令执行；影响用户要求查看本机 Node/npm/pnpm/Git 版本、执行命令、命令过程卡片和工具结果回填；修改后至少回归模型必须自主返回 `builtin_command_run` 结构化 `tool_calls`、OpenAI 兼容供应商流式首段工具名不能被后续空名称参数分片覆盖、日志出现 `model.tool.requested`/`tool.command.completed`/`model.tool.result.appended`，供应商空工具名时 `model.tool_call.name_missing` 必须带上 `rawModelMessage` 原始模型返回摘要，页面展示真实命令输出，且中心服务源码中不得保留按用户文本固定解析、强制命令工具选择或恢复空工具名的入口；供应商返回空工具名时只能记录 `model.tool_call.name_missing` 并失败收尾。
- Deep Agents MCP 官方 adapter 真实调用链路 - 依赖 `services/center/src/deepagents-agent.ts` 的 Deep Agents agent 装配、`services/center/src/AgentMiddleware/CenterToolChoiceMiddleware.ts` 的工具选择诊断与空工具名缺失失败收尾、`services/center/src/StructuredTool/mcp-adapter-config.ts` 的官方 `MultiServerMCPClient` 配置转换、`services/center/src/StructuredTool/McpToolProvider.ts` 的官方 adapter 工具发现、`services/center/src/StructuredTool/McpToolWrapperStructuredTool.ts` 的中心服务审计包装和 `services/center/src/StructuredTool/McpToolResultNormalizer.ts` 的结果规范化；影响 MCP 管理页已配置工具在对话中的真实调用、MCP 过程卡片、工具结果回填和空工具名协议错误处理；修改后至少回归模型必须自主返回目标 MCP adapter 工具结构化 `tool_calls`、工具快照包含模型实际可见工具名和描述、供应商空工具名时记录 `model.tool_call.name_missing` 并失败收尾、MCP 工具描述和参数按官方 adapter tool 原样进入执行链路、MCP 工具结果只把官方 adapter 的 content 文本回填模型、MCP adapter 普通失败结果只形成失败过程卡片并回填模型、不直接导致整轮失败，structuredContent 与声明 schema 不一致时不导致中心服务停机或轮次卡住、日志出现 MCP 工具请求/完成/失败/结果回填，中心服务源码中不得保留自建 `DynamicMcpStructuredTool`、自写 `tools/call` 主路径、按用户文本固定解析、硬编码具体 MCP 工具名、描述增强、参数补全或把空工具名修正为任何 MCP 工具的入口。
- 模型调用原始响应日志 - 依赖 `services/center/src/deepagents-agent.ts` 的 Deep Agents agent 装配、`services/center/src/AgentMiddleware/CenterModelCallLogMiddleware.ts` 的模型返回后日志、`services/center/src/model-provider/ModelProviderRuntimeFactory.ts` 的请求地址摘要和内部 OpenAI/Anthropic 模型协议运行时的原始响应摘要；影响供应商协议诊断、模型原始返回排查、OpenAI 兼容工具调用、Anthropic tool_use 识别和中心服务文件日志；修改后至少回归 Deep Agents 装配处直接 `new CenterModelCallLogMiddleware(context)`，日志包含供应商 ID、模型协议、模型名、调用 URL、原始响应摘要、工具调用诊断和用量字段，源码不得用供应商名称、用户提示词或具体工具名猜测协议。
- 命令工具过程卡片与轮次终态展示 - 依赖中心服务 `model.tool.*`、`tool.command.*` 事件、`services/center/src/StructuredTool/CenterStructuredToolBase.ts` 的结构化工具取消检查、`services/center/src/StructuredTool/deepagents-tool-runtime.ts` 的运行时取消信号传递和 `apps/frontend/src/stores/app-conversation-actions.ts` 的终态识别；影响停止按钮、当前轮次耗时、命令卡片完成态、工具失败态、停止后的工具结果回填和最终回复展示；修改后至少回归工具完成、工具失败、轮次失败和取消时按钮恢复“发送”，当前轮次耗时不残留运行态，停止后不继续追加该轮次工具结果回填或助手消息，同一 `toolCallId` 的请求、执行、输出、完成和结果回填仍聚合在同一张过程卡片。
- MCP 工具过程卡片去重展示 - 依赖中心服务 `model.tool.requested`、`tool.mcp.*`、`model.tool.result.appended` 事件、`services/center/src/StructuredTool/CenterStructuredToolBase.ts` 的 LangChain 工具调用 ID 继承和 `apps/frontend/src/views/Chat/chat-view-helpers.ts` 的过程正文合并规则；影响 MCP 工具请求、执行输出、完成摘要、结果回填和前端单卡片展示；修改后至少回归同一 `toolCallId` 的 MCP 请求、输出、完成和结果回填只显示一张过程卡片，完整工具输出优先于截断回填摘要，前缀相同或包含关系的正文不重复拼接。
- 中心服务统一日志配置 - 依赖 `services/center/src/logger.ts` 统一日志管线、中心服务配置系统配置文件、中心目录 `logs` 文件轮转和中心服务页面配置入口；影响控制台日志、文件日志、流式输出日志、敏感字段脱敏、开发环境默认等级、生产/打包默认等级和用户显式动态覆盖；修改后至少回归同一条日志在等级允许时同步进入控制台和中心目录 `logs` 文件、日志等级使用 pino 标准 `trace/debug/info/warn/error/fatal` 字符串、开发环境默认 `debug`、生产或打包默认 `info`、中心服务页面修改后动态生效且不写 Electron `desktop-config.json`、流式输出只受日志等级控制不被日志管线硬过滤、供应商 API Key、代理用户名密码、Web 访问账号密码、Cookie、令牌和授权头等敏感字段写入前已脱敏。
- Deep Agents 直接终态收尾 - 依赖 `services/center/src/deepagents-agent.ts` 的 Deep Agents 原生 graph 执行、内部模型协议运行时和 `services/center/src/AgentMiddleware/CenterToolChoiceMiddleware.ts` 的模型诊断；影响 Deep Agents graph 结束后的最终助手消息、长期记忆写入、用量记录、任务终态和异常失败收尾；修改后至少回归模型返回结构化 `tool_calls` 时由 Deep Agents/LangGraph 原生工具循环推进、模型同时返回过程文本和结构化 `tool_calls` 时过程文本可展示且工具继续执行、普通工具计划失败只作为观测事件和工具结果交给 Deep Agents 继续决策，不能由中心服务观测层直接提升为整轮失败，中心服务不得保留 `TurnFailureSummaryService`、外部失败总结、兜底失败说明、`AgentCompletionGate`、`DeepAgentTurnSupervisor`、`AgentRunCandidate` 或监督预算，最终助手消息只创建一次。
- Deep Agents 原生子智能体委派 - 依赖 `deepagents` 内置 `task` 工具、`createDeepAgent` 默认 subagent middleware、`services/center/src/deepagents-agent.ts` 的 Deep Agents agent 装配和 `services/center/src/StructuredTool/DeepAgentsToolFactory.ts` 的中心服务工具注入；影响短期子智能体委派、工具可见列表、中心服务工具权限边界和子智能体运行事件；修改后至少回归 `node_modules/deepagents` 暴露 `task`/`createSubAgentMiddleware` 原生能力、中心服务不再注入 `CreateSubAgentStructuredTool`、统一工具注册表不再包含 `builtin.agent.createSubAgent`、长期智能体不再因中心服务子智能体工具获得创建类权限、team 工具仍保留为中心服务会话级能力。
- Deep Agents write_todos 任务步骤桥接 - 依赖 `services/center/src/deepagents-agent.ts` 的 Deep Agents 原生 `write_todos` 工具完成事件、`services/center/src/domain/session-domain.ts` 的 `createTaskStep`/`updateTaskStep`、`apps/frontend/src/stores/app-conversation-actions.ts` 的 `task.step.*` 快照刷新和 `apps/frontend/src/views/Chat/useChatConversation.ts` 的 `source = todoList` 过滤；影响长任务拆解步骤是否进入页面任务入口、旧中心服务 todoList 工具链是否回归、运行中任务步骤实时刷新和过程卡片边界；修改后至少回归原生 `write_todos` 的 `output.update.todos` 转为 `task_steps`、页面任务入口显示拆解步骤、`task.step.*` 事件触发当前会话快照刷新、`builtin.deepagents.write_todos`/`builtin.todo.list`/`deepagents-todo-tool.ts` 不恢复。
- 对话过程事件实时渲染 - 依赖中心服务 `event.appended` 广播、`apps/frontend/src/stores/app-conversation-actions.ts` 的 `replaceRealtimeEvent`、`writeMergedEvents` 与 `mergeSnapshotEvents`、`apps/frontend/src/stores/app.ts` 的会话快照合并、`apps/frontend/src/views/Chat/useChatConversation.ts` 的事件计算入口和 `apps/frontend/src/views/Chat/chat-view-helpers.ts` 的过程行生成；影响运行中模型 Markdown、工具过程卡片、快照兜底和最终消息展示；修改后至少回归 WebSocket 收到过程事件后消息流立即出现卡片、实时事件刷新 `events` 新引用并递增 `eventsRevision`、旧快照不覆盖新事件、轮次终态后卡片保留完成或失败状态。
- 对话页面真实内容展示边界 - 依赖中心服务过程事件产出边界与前端消息流渲染规则；影响聊天消息列表、过程卡片和验收展示；修改后至少回归页面只显示模型真实文字、真实工具调用和真实工具结果，不展示中心服务写死阶段文案。
- 内部模型协议/Deep Agents 直接依赖边界 - 依赖 `services/center/package.json` 显式声明 `langchain`、`deepagents`、`@langchain/anthropic` 和必要的 OpenAI 官方 SDK 版本，依赖 `services/center/src/model-provider/ModelProviderRuntimeFactory.ts` 与内部 OpenAI/Anthropic 模型运行时的模型创建约束，并依赖 pnpm lockfile 固化解析结果；影响中心服务依赖安装、Deep Agents middleware import、严格依赖解析、OpenAI 兼容供应商工具调用、Anthropic tool_use 和命令工具调用运行；修改 LangChain Agent API、Deep Agents middleware、模型创建参数或相关依赖版本后至少回归 `pnpm install --lockfile-only`、中心服务通过 `dev:desktop-shell` 启动和命令工具结构化调用。
- 供应商数据库化与内部模型协议 - 依赖 `services/center/src/model-provider` 的 `ModelProviderRepository`、`ModelProviderRuntimeFactory`、内部 OpenAI/Anthropic 模型运行时，依赖 SQLite 表 `model_providers`、`model_provider_models`、`model_provider_settings`、`model_provider_capabilities`、`model_provider_checks`，依赖 `apps/frontend/src/views/Providers/RouterIndex.vue` 和 `/api/model-provider/*`；影响供应商新增、修改、删除、启用、模型列表、默认模型、能力声明、模型检测、Deep Agents 模型创建、模型调用日志、用量统计和智能体默认供应商；修改后至少回归供应商不是插件、页面不展示插件 ID/协议模式/runtime/wire API/第三方模型运行包字段、旧 `/api/provider/*` 和 `providers/*.json` 不再作为事实源、只支持 `model_protocol = openai` 和 `model_protocol = anthropic`、OpenAI 协议检测先探测 `/responses` 再探测 `/chat/completions`、检测结果写入 `selected_runtime_mode` 和能力矩阵、运行时按 `selected_runtime_mode` 自动选择 Responses 或 Chat Completions 兼容路径、OpenAI 对内统一 Responses 语义并按原始结构兼容 `/responses` 返回 Chat Completions 或 `/chat/completions` 返回 Responses、Anthropic 协议直接通过 `ChatAnthropic` 调用、供应商空工具名或畸形 text 工具块仍按协议错误处理、用量统计和智能体默认模型读取新表。
- 桌面壳开发启动与退出链路 - 依赖 `scripts/dev-desktop-shell.mjs`、`apps/desktop-shell/src/main.ts`、`services/center/src/index.ts` 和 `services/center/src/manager-lifecycle-watch.ts` 对用户系统 Node、中心服务健康检查、中心目录 `.zhixin-center.lock`、单实例锁、退出清理、桌面托管管理者监护和日志输出的处理；影响 `dev:desktop-shell`、8866 监听、Electron 主进程稳定性、发布后双击启动行为、复用遗留中心服务后的退出、隐藏到托盘、开发脚本停止和桌面壳强杀后的中心服务自退出；修改后至少回归通过用户环境 Node 拉起桌面壳和中心服务、窗口正常加载、无 `EPIPE` 主进程弹错、第二次启动只唤醒已有窗口且不重复拉起中心服务、第二次启动复用遗留中心服务后直接退出会关闭中心服务、隐藏到托盘不关闭中心服务、停止 `dev:desktop-shell` 后 Electron 和当前开发中心服务都不残留、IDEA 强制停止启动进程、任务管理器强杀 Electron 或桌面壳崩溃后中心服务自动退出并释放 `8866` 和 `.zhixin-center.lock`、端口相同但中心目录不匹配时不误杀其他中心服务。
