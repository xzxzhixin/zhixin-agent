# 致心智能体开发计划

> 本计划由新版 `需求.md` 和新版 `架构.md` 拆解而来，是后续开发、审查和交接的唯一任务表。
> 每次调整实现范围、增加或修改需求时，必须同步更新本文件。

## 计划维护规则

- `需求.md` 是产品需求源，`架构.md` 是技术架构和工程边界源，`计划.md` 是实施拆解和任务状态源。
- `需求.md`、`架构.md` 和 `计划.md` 必须随着最近代码和真实实现持续保持一致；完成代码、规则、架构或验收口径调整后，都要评估是否需要同步这三个文件。
- 新增、删除、修改 `需求.md` 中的需求时，必须同时评估并更新本计划。
- 修改架构、目录边界、构建打包、接口规范或存储边界时，必须同步评估并更新本计划。
- `架构.md` 中的目录树、模块清单、接口清单和构建产物清单，说明必须写在对应条目旁边或紧跟对应条目之后，避免读者按行号或顺序远距离对照。
- 每完成一个任务，必须把对应任务从 `[ ]` 改为 `[x]`。
- 如果任务被拆分、延期、替换或废弃，必须保留原任务记录，并说明原因和后续处理方式。
- 不允许只改代码不改计划；不允许只改需求不改计划。
- 默认不提交 Git，除非用户明确要求。

## 当前总体状态

- 项目名称：致心智能体 / zhixin-agent。
- 正式需求已重写为“个人中心服务 Agent”方向，`需求.md` 是当前产品需求源。
- 新版 `架构.md` 已确认采用：中心服务内核、Worker 执行进程、统一前端、多宿主壳、混合存储、事件日志、LangChain 模型运行时、内联工具体系、英文职责目录和绿色版 zip 交付。
- 本轮最新架构已确认：插件当前阶段全部内联，插件开发只作为后续目标；OpenAI 和 Anthropic 协议直接使用 LangChain；对话页全部改 WebSocket；长任务自动续跑、小任务连续展示和中途任务重规划合并纳入当前实施；中心服务对外 API、domain、tools、data-access 和智能体类层级需要重整。
- 本轮最新需求已确认：思考卡片没有真实可公开思考正文时只显示标题和耗时；工具过程卡片标题单行省略，正文内容区最大高度 `200px`，运行中默认展开、完成或失败后默认折叠；命令执行卡片必须与普通工具、MCP、插件和 skill 明显区分；命令输出需要修复 Windows 中文错误或 traceback 乱码；全量验收使用项目根目录 `项目对话测试`；执行 `dev:desktop-shell` 前必须先关闭旧桌面壳和中心服务进程，浏览器验收必须使用 Chrome DevTools 按人的方式点击、输入和提交。

## 本轮阶段：中心服务目录整理与会话级 team 工具

目标：整理 `services/center/src` 模块边界，把 API、domain、tools 和 data-access 分层落稳，并为主智能体补齐会话级 team 创建、解散、添加成员和移除成员工具。

- [x] 更新 `本次计划.md`，固化本轮目录整理、team 生命周期、成员边界和工具颗粒度。
- [x] 更新 `需求.md`、`架构.md` 和 `计划.md`，固化会话级 team 需求和中心服务源码目录边界。
- [x] 编写并运行中心服务目录边界红灯检查，证明根级 domain、根级 tool-runtime 和大路由文件仍未拆分。
- [x] 建立 `services/center/src/domain` 边界，把领域模块集中到 domain 文件夹并保留聚合出口。
- [x] 按 `/api/{资源}` 拆分 `services/center/src/api/api-routes.ts`，由统一入口注册。
- [x] 把根级 `tool-runtime*.ts` 迁入 `services/center/src/tools`，并保持工具运行时聚合出口。
- [x] 编写并运行会话级 team 红灯检查，证明缺少 team 数据表、仓储和 4 个主智能体 team 工具。
- [x] 实现会话级 team 数据表、仓储、领域方法和事件记录；会话删除和解散 team 时物理删除 team 记录和成员关系。
- [x] 实现 `create-agent-team`、`disband-agent-team`、`add-agent-team-member`、`remove-agent-team-member` 四个工具，只允许主智能体使用。
- [x] 使用允许的自动化检查验证目录边界和 team 工具；涉及浏览器可见交互时用 Chrome DevTools 按人的方式验收。
- [x] 检查 `提示词模板.md` 图片引用；仅删除提示词实际引用的临时图片，不删除应用图标、favicon 或界面过程图标。（当前未发现图片引用，无需删除资源）
- [ ] 完成后重试远端检查；若远端领先先拉取，再提交并推送工作区全部非忽略改动。

验收口径：中心服务根级不再散落 domain 和工具运行时实现；API 按 `/api/{资源}` 拆成独立文件；team 跟随会话生命周期；主智能体具备 4 个独立 team 工具；team 成员只来自启用长期智能体；解散 team 物理删除 team 记录，历史消息和事件保留；TDD 红灯和绿灯验证通过。

## 本轮阶段：长任务自动续跑、WebSocket 对话页、LangChain 与智能体架构重整

目标：按最新确认口径修复 agent loop 达到工具循环上限后停住的问题，建立长任务子步骤展示、任务重规划合并、对话页 WebSocket-only 通道、LangChain 模型运行时、内联工具目录和智能体类层级。

- [x] 更新 `本次计划.md`、`需求.md`、`架构.md` 和 `计划.md`，固化本轮最新需求、架构边界、TDD 验收和执行顺序。
- [ ] 编写并运行长任务工具循环红灯检查，证明当前 `MODEL_TOOL_LOOP_LIMIT_EXCEEDED` 会直接失败并要求用户手动继续。
- [ ] 实现单批工具预算到达后的内部自动续跑，继续沿用同一 `turnId`、`taskId` 和已完成工具结果。
- [ ] 编写并运行任务步骤展示红灯检查，证明长任务拆解子任务没有在同一任务下连续追加/更新。
- [ ] 实现同一任务下子步骤连续追加和右侧任务状态区展示。
- [ ] 编写并运行任务重规划红灯检查，证明用户中途修改需求后旧拆解不会被合并、过期步骤不会被替换。
- [ ] 实现 `task.plan.revised` 事件和稳定子任务 ID 合并规则。
- [x] 编写并运行 WebSocket-only 红灯检查，证明对话页首屏、历史、补齐、发送、附件或事件仍通过 REST。
- [x] 迁移对话页数据和动作到 WebSocket 请求、响应、初始快照和增量事件。（本轮已补齐 `chat.bootstrap.snapshot` 和 `tokenizer.count`，对话页供应商、模型列表、智能体列表和上下文 token 统计不再触发 REST）
- [ ] 编写并运行滚动规则红灯检查，证明 token 流贴底、用户接管、100px 阈值和右下角方向箭头不满足。
- [ ] 实现消息区“贴底跟随 + 用户接管 + 最近滚动方向箭头”规则。
- [ ] 编写并运行 LangChain 模型运行时红灯检查，证明 OpenAI/Anthropic 仍走自研或插件协议适配路径。
- [ ] 接入 LangChain OpenAI 和 Anthropic ChatModel，并用供应商配置映射初始化参数。
- [ ] 编写并运行插件内联化红灯检查，证明当前阶段仍依赖插件安装、动态扫描或 `plugins/builtin-model-*` 模型适配器。
- [ ] 将当前阶段插件能力收敛为内联工具、MCP、skill 或中心服务领域模块；保留“插件开发”后续目标但不实施。
- [ ] 编写并运行中心服务目录边界红灯检查，证明对外 API、工具和智能体类仍散落在旧文件中。
- [ ] 建立 `services/center/src/api`、`services/center/src/tools` 和 `services/center/src/agents` 边界，工具按单工具单文件拆分。
- [ ] 编写并运行智能体类层级红灯检查，证明主智能体、长期智能体、子智能体没有按基类和派生类限制创建工具。
- [ ] 实现 `BaseAgent`、`MainAgent`、`LongTermAgent`、`SubAgent`，并注入创建长期智能体和创建子智能体工具；子智能体不得获得创建工具。
- [x] 使用 Chrome DevTools 按人的方式验证长任务自动续跑、右侧任务子步骤、对话页 WebSocket-only、流式滚动箭头和智能体创建工具边界。（已真实新增普通对话并发送 WebSocket-only 验收消息，网络仅保留本机授权 REST，无对话页 provider、agent、tokenizer、session 业务 REST）
- [ ] 检查 `提示词模板.md` 图片引用；仅删除提示词实际引用的临时图片，不删除应用图标、favicon 或界面过程图标。
- [ ] 完成后重试远端检查；若远端领先先拉取，再提交并推送工作区全部非忽略改动。

验收口径：长任务达到单批工具预算后自动续跑，不要求用户手动“继续”；同一轮任务右侧展示连续追加/更新的子步骤；用户中途修改需求后任务拆解能合并并替换过期步骤；对话页全部通过 WebSocket；消息区滚动符合贴底跟随和用户接管规则；OpenAI/Anthropic 直接使用 LangChain；插件当前阶段内联化；中心服务对外 API、工具和智能体类边界清晰；浏览器验收通过。

## 本轮阶段：过程卡片折叠、命令区分与乱码修复

目标：按截图反馈修复对话过程卡片标题、折叠、高度、命令类型区分和命令输出编码。

- [x] 更新 `本次计划.md`、`需求.md`、`架构.md` 和 `计划.md`，固化本轮最新过程卡片和命令输出口径。
- [x] 编写并运行过程卡片红灯检查，证明当前标题没有单行省略、内容区没有 `200px` 上限、完成或失败后没有默认折叠、命令卡片没有专属类型区分。
- [x] 修复前端过程卡片聚合类型、折叠状态、标题单行省略、正文最大高度和命令专属样式。
- [x] 编写并运行命令输出红灯检查，证明当前命令输出直接按 UTF-8 解码导致 Windows 非 UTF-8 输出可能出现替换字符乱码。
- [x] 修复中心服务命令输出解码，确保 PowerShell、Python traceback 和中文错误信息以可读文本写入事件。
- [x] 使用 Chrome DevTools 按人的方式验证项目对话中的思考卡片、命令卡片折叠、标题省略、正文滚动、命令类型区分和乱码修复。
- [x] 检查 `提示词模板.md` 图片引用；仅删除提示词实际引用的临时图片，不删除应用图标、favicon 或界面过程图标。
- [ ] 完成后重试远端检查；若远端领先先拉取，再提交并推送工作区全部非忽略改动。

验收口径：没有真实可公开思考正文时思考卡片只显示标题和耗时；工具过程卡片标题单行省略；过程卡片正文内容区最大高度为 `200px` 且内部滚动；工具卡片运行中默认展开，完成或失败后默认折叠；命令卡片与普通工具、MCP、插件和 skill 有明显区分；Windows 命令输出不显示替换字符乱码；浏览器验收通过。
- 旧版 `桌面端`、`Web端`、`中心服务`、`共享`、`IDEA_SDK` 目录只作为历史实现参考，不再作为新增开发目标。
- 后续实施必须优先建立新版目录，再按阶段迁移可复用代码。

## 本轮阶段：协议适配器动态扫描与真实思考正文口径

目标：按最新确认口径移除 OpenAI 兼容插件包，保留 `OpenAI 内置` 固定项，动态扫描 `plugins/builtin-model-*` 模型适配器，并同步思考卡片正文来源规则。

- [x] 固化并落地需求口径：思考正文只展示供应商返回的真实可公开自然语言思考；没有真实可公开思考时只显示标题和耗时，不把上下文统计、任务状态、供应商数量、扩展能力快照或事件摘要作为正文。
- [x] 固化协议口径：中心服务内部只使用 OpenAI Chat Completions，外部 Anthropic 等协议通过模型协议适配器转换进入。
- [x] 将供应商页字段文案从“协议插件”调整为“协议适配器”，并保留 `OpenAI 内置` 固定项。
- [x] 删除 `plugins/builtin-model-openai-compatible` 包，OpenAI 兼容能力改由中心服务固定内置适配器承担。
- [x] 保留 `plugins/builtin-model-anthropic-messages`，展示名统一为“Anthropic 适配器”，构建后生成可供中心服务扫描的适配器声明。
- [x] 限定协议适配器动态来源只扫描 `plugins/builtin-model-*`，其他 `plugins/builtin-*` 类型插件不进入供应商页协议适配器下拉。
- [x] `dev:desktop-shell` 启动前自动构建所有内置插件，并同步到中心目录 `center-data/plugins`；绿色版启动时由中心服务把随包 `resources/plugins` 同步到当前中心目录 `plugins`。
- [x] 固化验收规则：执行 `dev:desktop-shell` 前必须关闭旧桌面壳和中心服务进程；浏览器端必须使用 Chrome DevTools 按真实用户点击、输入、选择、提交和等待反馈的方式测试。

验收口径：仓库不再存在 `plugins/builtin-model-openai-compatible`；供应商页显示“协议适配器”；协议适配器下拉包含固定 `OpenAI 内置` 和中心目录 `plugins/builtin-model-*` 动态项；Anthropic 展示名为“Anthropic 适配器”；中心服务不再把固定上下文摘要作为思考正文；需要启动桌面壳验收时必须先关闭旧进程，再用 Chrome DevTools 按真实用户路径验证浏览器页面。

## 本轮阶段：思考卡片、首轮标题总结与管理弹框修复

目标：修复对话思考卡片标题和耗时、会话标题总结覆盖时机、供应商删除语义和管理配置弹框滚动布局。

- [x] 更新 `本次计划.md`、`需求.md`、`架构.md` 和 `计划.md`，固化本轮最新需求、架构边界、TDD 验收和执行顺序。
- [x] 编写并运行思考卡片红灯检查，证明当前标题仍是“思考中/思考过程”且没有独立“已思考（用时 X 秒）”口径。
- [x] 修复思考卡片聚合和渲染，展示真实可展示思考或阶段摘要，运行中默认展开，完成后默认折叠并显示耗时。
- [x] 编写并运行标题总结红灯检查，证明当前每轮完成都会自动覆盖标题。
- [x] 修复标题总结逻辑，只在第一次对话完成后自动总结，后续轮次不覆盖标题。
- [x] 编写并运行供应商删除与管理弹框红灯检查，证明删除只是停用、刷新按钮在弹框内、管理弹框没有共享固定 footer 滚动外壳。
- [x] 修复供应商删除语义，删除后列表刷新并移除已删除供应商，停用仍保留在列表中。
- [x] 新增管理配置弹框共享外壳，并迁移供应商、网络代理、运行环境、插件、MCP、skill 和智能体管理弹框到内容区滚动、底部按钮固定布局。
- [x] 使用 Chrome DevTools 按人的方式验证首轮对话标题总结、思考卡片展开折叠和耗时、供应商删除后列表刷新、管理弹框内容滚动和底部按钮固定。
- [x] 检查 `提示词模板.md` 图片引用；仅删除提示词实际引用的临时图片，不删除应用图标、favicon 或界面过程图标。
- [ ] 完成后重试远端检查；若远端领先先拉取，再提交并推送工作区全部非忽略改动。

验收口径：思考卡片不出现硬编码假内容，运行中默认展开且完成后默认折叠并显示耗时；标题总结只在第一次对话完成后自动生成；删除供应商后中心服务列表不再返回该供应商；管理配置弹框在小视口下底部按钮可见且内容区可滚动；浏览器验收通过。

## 本轮阶段：LangGraph 全量执行图与 OpenAI 协议统一

目标：移除内部模型协议包和旧文本工具协议，把真实对话执行拆入 LangGraphJS 多节点图，并修复供应商保存、启用和提示展示口径。

- [x] 更新 `本次计划.md`、`需求.md`、`架构.md` 和 `计划.md`，固化本轮最新需求、架构边界、TDD 验收和执行顺序。
- [x] 编写并运行 LangGraph 全量节点红灯检查，证明当前 `langgraph-runner` 仍是单节点套壳。
- [x] 将当前真实对话 agent loop 拆入 LangGraphJS 多节点和条件边，覆盖上下文整理、模型流、工具调用、工具执行、工具结果回填、消息固化、记忆、用量和失败收尾。
- [x] 编写并运行 OpenAI 协议红灯检查，证明当前仍依赖 `packages/model-protocol` 和旧 `ModelRequest`、`ModelMessage`、`ModelStreamEvent` 内部协议。
- [x] 删除 `packages/model-protocol` 包和所有工作区引用，中心服务内部模型请求、工具定义、工具回填和流式解析全部改用 OpenAI Chat Completions 规范。
- [x] 删除历史文本 JSON 工具调用兼容逻辑，模型没有 OpenAI 结构化 `tool_calls` 时按不完整工具意图失败处理。
- [x] 编写并运行供应商保存和提示红灯检查，证明当前只填写 `baseUrl` 和 API Key 的供应商无法保存或提示会被弹框遮挡。
- [x] 允许供应商不完整保存但禁止启用，所有保存、获取模型、启用、停用、删除、失败和警告提示统一使用 `ElMessage`。
- [x] 检查 `提示词模板.md` 图片引用；当前没有实际图片引用，不删除应用图标、favicon 或界面过程图标。
- [x] 使用项目根目录 `项目对话测试` 进行全量项目对话测试；执行 `dev:desktop-shell` 前先关闭旧桌面壳和中心服务进程，并使用 Chrome DevTools 验证浏览器端。（验收发现并修复轮次已完成但默认任务仍为 `running` 的收尾问题，复测项目会话 `turnStatus=completed`、`taskStatus=completed`、浏览器显示任务 `1/1` 和右侧“已完成”）
- [ ] 完成后重试远端检查；若远端领先先拉取，再提交并推送工作区全部非忽略改动。

验收口径：`services/center` 真实对话发送链路不再只通过单个 LangGraph 节点包裹旧闭环；仓库不再存在 `packages/model-protocol` 包和 `@zhixin/model-protocol` 依赖；模型工具调用闭环只使用 OpenAI `tools`、`tool_calls`、`tool_call_id` 和 `role: "tool"`；供应商只填 `baseUrl` 和 API Key 可保存但不可启用；所有管理提示使用 `ElMessage`；项目对话测试目录可完成全量对话验收。

## 阶段 0：新版工程骨架与迁移边界

目标：建立新版英文职责目录、pnpm 工作区、共享包和资源目录，避免继续在旧目录上扩展。

- [x] 建立新版顶层目录：`apps`、`services`、`packages`、`plugins`、`docs`、`assets`、`scripts`。
- [x] 建立 `apps/frontend`，作为唯一业务前端入口。
- [x] 建立 `apps/desktop-shell`，作为 Electron 桌面壳入口。
- [x] 建立 `services/center`，作为中心服务入口。
- [x] 建立 `packages/shared`，保存共享常量、枚举、协议类型和纯工具。
- [x] 建立 `packages/api-client`，保存 REST API 客户端和 WebSocket 客户端。
- [x] 建立 `packages/ui`，保存跨入口复用 UI 组件。
- [x] 建立 `packages/model-protocol`，保存内部模型协议类型。（本轮已按最新需求删除该包，中心内部统一改为 OpenAI Chat Completions 规范；保留历史任务记录用于说明阶段 0 当时完成过旧骨架）
- [x] 建立 `packages/plugin-sdk`，保存插件清单、权限声明和插件 API 类型。
- [x] 建立 `plugins/idea`，作为 IDEA 插件新版工程目录。
- [x] 建立系统内置插件目录：历史阶段曾建立 `plugins/builtin-model-openai-compatible`、`plugins/builtin-model-anthropic-messages`、`plugins/builtin-automation`、`plugins/builtin-browser-collector`、`plugins/builtin-office-integration`、`plugins/builtin-file-organizer`；本轮已按最新口径删除 `plugins/builtin-model-openai-compatible`，OpenAI 改为中心服务固定内置适配器。
- [x] 建立资源目录：`assets/app-icon/图标.png`、`assets/ui-icons/mcp-call.svg`、`assets/ui-icons/file-read.svg`、`assets/ui-icons/file-write.svg`、`assets/ui-icons/file-delete.svg`。
- [x] 更新根 `pnpm-workspace.yaml`，纳入 `apps/*`、`services/*`、`packages/*`、`plugins/builtin-*`。
- [x] 更新根 `package.json`，提供新版开发、构建、检查脚本。
- [x] 清理根 `package.json` 中旧端入口废弃提示脚本、静态检查命令和各 Node 子包 `build` 脚本中的 `tsc --noEmit` 语法检查；项目不再通过命令行校验 TS 文件，确需校验时使用 IDE 能力，相关约束已同步到 `AGENTS.md`。
- [x] 保留旧目录作为历史参考，并在 README 或迁移说明中标记旧目录不再新增开发。
- [x] 各阶段代码迁移并验证完成后，删除对应旧目录，避免新旧实现长期并存。（`共享` 已迁移到 `packages/shared` 并删除；`中心服务` 已迁移到 `services/center` 并删除；`Web端` 旧 `apps/frontend/legacy-web` 已删除；`桌面端` 旧 `apps/desktop-shell/legacy-desktop` 已删除；`界面图标` 已迁移到 `assets/ui-icons` 并删除。用户已明确要求迁移完成后移除旧版所有文件）
- [x] 新版 IDEA 插件工程迁移完成后，刷新 IDEA 项目并确认 IDEA 模块指向新版 `plugins/idea`。（已更新 `.idea/gradle.xml`，IDEA 已能读取 `plugins/idea`；当前 IDEA 模块列表同时残留旧模块缓存，需在 IDEA 中 Reload Gradle Project 或重开项目后清理显示）

验收口径：新版目录和工作区可被 pnpm 识别；旧目录没有新增业务能力；资源文件路径和架构一致。

## 阶段 1：共享协议与基础类型

目标：先固化跨端协议，避免前端、中心服务、Worker 和插件各自定义字段。

- [x] 在 `packages/shared` 定义应用名称、默认端口、中心目录名、客户端类型、入口模式、任务状态、智能体状态、执行模式、统一响应包和错误结构。
- [x] 在 `packages/model-protocol` 定义 `ModelRequest`、`ModelMessage`、`ModelContentPart`、`ModelToolSpec`、`ModelToolCall`、`ModelStreamEvent`、`ModelUsage` 和 `ModelError`。（本轮已按最新需求废弃该内部协议包，中心服务改由 `services/center/src/openai-chat-protocol.ts` 定义 OpenAI Chat Completions 内部规范）
- [x] 在 `packages/plugin-sdk` 定义插件清单、插件来源、适用范围、权限枚举、配置 schema、调用记录和插件 API 类型。
- [x] 在 `packages/shared` 定义项目、会话、消息、轮次、任务、事件、附件、通知、用量、运行环境和个人事务协议。
- [x] 定义内部文件定位链接协议，区分外部链接和项目文件定位链接。
- [x] 定义 REST API 统一响应包和 WebSocket 事件包，不允许路径参数协议。
- [x] 补充协议字段中文注释，说明来源、含义、格式、默认值或约束。

验收口径：共享类型能覆盖中心服务、前端、Worker 和插件 SDK 的基础协议；字段命名单一明确，无候选字段兜底。

## 阶段 2：中心服务基础设施

目标：建立 Fastify 中心服务、center-data 初始化、SQLite 迁移、统一 API 和启动锁。

- [x] 在 `services/center` 建立 Node.js + TypeScript + Fastify 基础入口。
- [x] 实现中心目录 `center-data` 初始化。
- [x] 创建中心目录子目录：`db`、`config`、`memory`、`agents`、`providers`、`plugins`、`mcp`、`skills`、`runtimes`、`sessions/attachments`、`personal`、`temp`、`logs`。
- [x] 接入 better-sqlite3，保证只有中心服务主进程打开数据库连接。
- [x] 实现 SQLite 迁移机制和迁移记录表。
- [x] 创建核心状态表：`projects`、`sessions`、`messages`、`conversation_turns`、`tasks`、`task_steps`、`agents_index`、`agent_runtime_states`、`attachments`、`notifications`、`usage_records`、`usage_daily_stats`、`todos`、`calendar_events`、`knowledge_items`、`plugin_installs`、`extension_call_records`、`sync_clients`。
- [x] 创建 `events` 事件日志表，并实现同一轮次内 `sequence` 递增。
- [x] 实现中心服务启动锁，避免同一中心目录多实例。
- [x] 实现统一响应包和 API 错误格式。
- [x] 实现 `GET /api/health` 和 `GET /api/bootstrap/state`。
- [x] 实现日志写入和 `traceId` 生成。
- [x] 实现 `temp` 目录启动清理和停止清理。

验收口径：中心服务可启动，首次启动能创建中心目录和数据库；迁移可重复执行；错误响应带 `traceId`。

## 阶段 3：访问控制与客户端连接

目标：实现本机访问、远程 Web 登录、Cookie 登录态、IDE 本机连接限制和 WebSocket 基础同步。

- [x] 实现服务端本机访问识别，不依赖前端 `window.location.hostname` 判断。
- [x] 实现远程 Web 登录接口，校验桌面壳配置的账号密码摘要。
- [x] 实现 Cookie 登录态，按访问方式配置 `HttpOnly`、`SameSite`、`Secure` 和过期时间。
- [x] 实现本机访问授权流程。
- [x] 实现 IDE 插件连接限制，只允许 `127.0.0.1:端口` 的本机连接模式。
- [x] 实现 WebSocket `client.hello` 和 `server.ready` 握手。
- [x] 实现 WebSocket 订阅范围校验。
- [x] 实现 `event.appended`、`task.updated`、`agent.state.changed`、`notification.created` 和 `connection.state` 推送。
- [x] 实现断线重连后 `POST /api/session/event/list` 补齐缺失事件。
- [x] 实现尚未成功发送消息的本地待确认策略所需服务端状态。

验收口径：本机访问便利，远程 Web 必须登录，IDE 插件只能连接本机中心服务；WebSocket 能按订阅范围推送事件。

## 阶段 4：统一前端应用

目标：建立一个 Vue 前端应用，支持 workspace、mobile 和 plugin-compact 三种布局。

- [x] 在 `apps/frontend` 建立 Vite + Vue 3 基础工程。
- [x] 配置 Vue Router，构建 `index.html` 和 `plugin.html` 多入口。
- [x] 配置 Pinia，只保存 UI 状态和订阅状态。
- [x] 引入 Element Plus、Vant、GitHub Markdown 样式和 Markdown 渲染能力。
- [x] 实现客户端能力适配层 `ClientCapabilities`。
- [x] 实现 `desktop-shell`、`web-local`、`web-remote`、`web-mobile`、`ide-plugin` 运行时识别。
- [x] 实现 workspace 布局：顶部主导航、左侧对话导航、中间对话区、右侧任务和智能体状态栏。
- [x] 实现 mobile 布局，优先支持对话、消息查看、输入、通知、待办、日程和常用设置。
- [x] 实现 plugin-compact 布局，只展示当前项目页签、消息列表、输入框、文件引用、任务状态和连接设置。
- [x] 调整 `plugin-compact` 布局为无左侧整栏，项目页签放在对话区顶部，并保留消息列表、输入框、文件或选区引用、任务状态、智能体状态和连接设置。（代码已落地；仍需通过 chrome-devtools 或 windows-mcp 在紧凑 WebView 中验证无右侧整栏挤占）
- [x] 使用 chrome-devtools 或 windows-mcp 验证 `plugin-compact` 紧凑 WebView：项目页签、消息列表、输入框、窗口上下文用量、任务状态、智能体状态和连接设置在顶部或输入区附近可见，且不出现固定右侧整栏。（原“文件上下文入口”验收口径已被本轮需求替换为“窗口上下文用量”；项目文件引用仍通过 `@` 检索进入草稿。本轮已用 Chrome DevTools 验证 `plugin.html` 无左侧对话栏和固定右侧状态栏）
- [x] 实现页面根容器固定视口高度，禁止页面级滚动。
- [x] 实现消息列表作为主滚动区域。
- [x] 实现 WebSocket 客户端和断线重连提示。
- [x] 实现远程 Web 登录页和 Cookie 登录态检查。
- [x] 实现主题自动匹配和手动切换：桌面端/Web 默认跟随系统主题，IDE 插件默认跟随宿主 IDE 主题，用户手动切换后当前客户端优先使用本地保存主题。
- [x] 补齐本轮页面全量拆分：移除各管理页 `<MainView initial-page="..."/>` 入口模式，拆出真实页面组件和公共工作台壳插槽，`MainView.vue` 不再集中维护所有顶部菜单业务模板。

验收口径：同一前端应用能按入口和客户端类型展示不同布局；桌面壳才显示中心服务管理入口；插件入口不显示完整管理页面。

## 阶段 5：桌面壳与绿色版交付

目标：实现 Electron 壳、中心服务生命周期、本机配置、托盘、通知权限和绿色版 zip。

- [x] 在 `apps/desktop-shell` 建立 Electron 主进程和 preload。
- [x] 桌面壳启动时拉起随包中心服务。
- [x] 桌面壳退出时停止由它管理的中心服务进程。
- [x] 实现中心服务重启能力。
- [x] 实现中心服务端口配置。
- [x] 实现中心目录配置，默认使用解压目录下 `center-data`。
- [x] 调整桌面端“中心服务”页面为进入后直接展示编辑面板，不再通过弹窗维护端口、中心目录、Web 远程访问账号密码和系统通知状态；中心目录切换按停止当前中心服务、校验或初始化新目录、再重启中心服务处理，旧目录不自动删除。（已通过桌面壳桥接和 Chrome DevTools 桌面态 mock 验证直接编辑面板、选择中心目录入口和无配置弹框）
- [x] 实现 Web 远程访问账号和密码配置。
- [x] 实现系统通知权限检测，并同步给中心服务。
- [x] 创建主窗口并加载 `apps/frontend` 的 `index.html`。
- [x] 创建系统托盘，使用 `图标.png`。
- [x] 打包绿色版 zip，内含桌面壳、中心服务产物、前端资源、内置插件和图标。（已生成包含前端资源、中心服务入口、内置插件、应用图标和界面图标的绿色版目录/zip；正式 Electron 二进制后续由 Electron 打包器替换当前开发期启动壳）
- [x] 补齐本轮桌面端动态装配中心服务：中心服务只在桌面端打包阶段进入 `resources/center`，Web 前端 dist 不携带中心服务入口、SQLite、本地数据库驱动或桌面壳专属文件。
- [x] UI 明确提示外部中心目录不会随程序目录删除。

验收口径：绿色版 zip 解压即可运行；默认删除解压目录可清理程序和默认中心数据；外部中心目录有明确提示。

## 阶段 6：项目、会话、轮次与消息

目标：中心服务维护普通会话、项目会话、IDE 多页签、轮次、消息和任务基础状态。

- [x] 实现项目登记接口 `POST /api/project/register`。
- [x] 实现项目详情接口 `POST /api/project/detail`。
- [x] 实现普通会话创建、列表和详情接口。
- [x] 实现项目会话创建、列表和详情接口。
- [x] 实现 IDE 插件每个页签对应一个独立项目会话。
- [x] 支持同一项目多个会话页签并行执行。
- [x] 实现消息创建和查询。
- [x] 实现 `conversation_turns` 轮次创建、结束、失败、取消和等待用户状态。
- [x] 实现任务和任务步骤状态流转。
- [x] 实现轮次开始时间、当前更新时间、结束时间和持续时长保存。
- [x] 实现 UI 中轮次时间实时显示和结束后固定显示。

验收口径：普通会话和项目会话都由中心服务维护；IDE 插件多页签可并行；轮次和任务状态可实时展示。

## 阶段 7：IDEA 插件

目标：实现 IDEA 插件宿主能力和 `plugin.html` WebView 承载。

- [x] 在 `plugins/idea` 建立 Gradle IntelliJ Platform Plugin 工程。
- [x] 插件名、工程名和安装包名使用 `zhixin-agent-idea`。
- [x] 插件包名和插件 ID 命名空间使用 `top.xzxsrq`。
- [x] 工具窗口名称使用“致心”，默认加载 `http://127.0.0.1:端口/plugin.html`。
- [x] 支持配置中心服务端口，默认 `8866`。
- [x] 读取或创建项目根目录 `致心项目ID.md`。
- [x] 打开项目时向 `plugin.html` 提供项目 ID、项目路径和项目名称。
- [x] 实现 `getProjectContext`、`insertContextReference`、`openInternalFileLink`、`readSelectionReference`、`readCurrentFileReference`、`readFolderReference` 和 `updateCenterPort` 宿主桥接。
- [x] 实现编辑器选区右键发送到致心对话框。
- [x] 实现编辑器标签页右键发送到致心对话框。
- [x] 实现项目文件树文件右键发送到致心对话框。
- [x] 实现项目文件树文件夹右键发送到致心对话框。
- [x] 右键菜单只插入引用到输入框，不直接发送问题。
- [x] 支持内部文件定位链接打开文件并跳转到行号或选区。
- [x] 支持插件多页签状态标志：执行中、等待用户、失败、未读。（已在 IDEA 插件侧补充状态枚举和工具窗口标题桥接；plugin-compact 内部多页签 UI 由统一前端继续承载）

验收口径：IDEA 插件只展示当前项目数据；右键上下文能插入输入框；多页签对应多个项目会话。

## 阶段 8：Agent Worker 与执行引擎

目标：实现中心服务调度、Worker IPC、基于 LangGraphJS 的 Agent 执行链路和任务审计。

- [x] 实现 `worker-manager`，支持启动、复用、取消、心跳和终止 Worker。（当前以中心服务内 Worker 生命周期状态、消息入口、取消和失败收尾形成最小闭环；真实独立子进程池后续可替换该管理层）
- [x] 定义 Worker JSON-RPC 消息协议。
- [x] 实现 `worker.ready`、`task.start`、`task.cancel`、`heartbeat`。
- [x] 实现 `context.request`，由中心服务返回会话、项目、智能体、记忆索引、可用工具和权限。
- [x] 实现 `event.emit`，由中心服务统一写入事件日志并广播。
- [x] 实现 `task.complete` 和 `task.failed`。
- [x] 实现 Worker 崩溃、超时或心跳丢失后的任务失败收尾。
- [x] 实现 `turn-runner` 轮次执行编排。（已提供中心服务内最小执行编排入口，串联轮次、任务、上下文、模型编排事件、工具计划、协作事件、记忆提交和用量采集）
- [x] 实现 `context-builder` 上下文组装。
- [x] 实现 `agent-router` 智能体选择和子智能体调度。
- [x] 实现 `model-orchestrator` 模型调用和流式解析。（本轮已替换为 OpenAI Chat Completions 内部请求、SSE 片段、`tool_calls` 和用量归一化口径）
- [x] 实现 `tool-planner` 工具调用计划和审批请求。
- [x] 实现 `collaboration-engine` 管线通话和群聊讨论。
- [x] 实现 `memory-committer` 轮次结束后的记忆归纳请求。
- [x] 实现 `usage-collector` 模型用量归集。
- [x] 替换当前 `tool-runtime.ts` 中按用户文本硬编码识别 Node/Python 版本命令的临时工具意图逻辑，改为模型或 Agent 执行引擎基于结构化工具定义自主提出工具调用请求。（已移除用户文本关键词触发命令的执行分支，保留旧入口但固定不触发工具）
- [x] 建立中心服务统一工具定义协议，覆盖工具名称、能力说明、输入参数 schema、权限要求、风险等级、适用范围、审批策略、展示文案和不可用原因，并把可用工具随上下文提供给 Agent 执行链路。（对外模型工具名已转换为协议安全名称，内部工具 ID 通过 `sourceToolId` 保留）
- [x] 实现模型工具调用闭环：模型请求工具，中心服务校验工具名称和参数 schema，按执行模式审批或自动执行，工具结果回填给模型，模型基于结果继续生成最终回复。（当前首批接入命令工具，已通过真实 OpenAI 兼容供应商浏览器闭环验证；审批 UI 和其他执行器继续按后续项完善）
- [x] 修复对话执行过程实时展示：会话发送接口后台执行时每次事件落库后立即 WebSocket 推送，完成后只补发未推送事件，不再等整轮结束后从头整批广播。
- [x] 修复命令工具 UI 聚合：每次命令调用按 `toolCallId`、命令和输入摘要生成独立过程卡片，同一轮 Node.js 和 Python 等多个命令不会合并到同一个框，也不会显示到用户问题上方。
- [x] 实现命令工具 stdout/stderr 事件级流式输出：命令执行改为异步进程，输出数据块追加为 `tool.command.output`，前端在同一命令卡片内持续追加显示。
- [x] 引入 `@langchain/langgraph` 和本地 checkpointer 依赖，建立 `langgraph-runner`，让当前轮次核心执行图直接由 LangGraphJS `StateGraph` 驱动，保留中心服务事件、任务步骤、`payload.graph`、SQLite 和 Markdown 事实源。
- [ ] 把文件读取、文件写入、文件删除、插件调用和 skill 使用统一接入工具调用闭环，不再把插件/skill 长期记录为不可用占位事件。（原任务中的 MCP 调用已在本轮拆出并完成；文件、插件和 skill 仍保留待处理）
- [x] 补齐 HTTP MCP 真实调用闭环：中心服务从 `mcp/global.json` 读取 HTTP MCP Server 配置，启动模型调用前通过 MCP `tools/list` 生成动态模型工具，模型请求动态 MCP 工具后由中心服务执行 `tools/call`，写入 `tool.mcp.started`、`tool.mcp.completed` 或失败事件，并把 MCP 结果回填模型生成最终回复。（本轮用户补充要求继续支持 stdio MCP，已拆入下一项）
- [x] 补齐 stdio MCP 真实调用闭环与 CCGUI 式过程展示：中心服务读取 `type: "stdio"`、`command`、`args` 和环境变量配置后托管 MCP 子进程，通过 JSON-RPC 执行 `initialize`、`tools/list`、`tools/call`，前端 MCP 管理页展示工具列表，对话消息流按 `toolCallId` 或 `serverId + toolName` 显示独立 MCP 过程卡片，并持续展示调用、执行、读写、输出摘要、失败原因和排查 ID。（已用 Chrome DevTools 验证 MCP 页展示 `stdio · chrome-devtools` 工具列表；对话内通过 `chrome-devtools.new_page` 打开 Google 首页并展示独立 MCP 过程卡片）
- [ ] 工具调用事件和 UI 展示继续补齐审批状态、文件操作、插件/MCP/skill 真实执行结果，以及工具结果如何影响最终回复的完整可视化链路。
- [x] 调整 MCP 管理页为 Server 级列表与按需工具查看：同一个 `mcpServers` JSON 中的每个 Server 单独成行，移除配置文件列，工具列表通过当前行“查看工具”按钮异步加载并展示 loading 状态，再次点击收起；操作列编辑按钮打开当前配置完整 JSON，避免同一文件其他 Server 丢失。（已用 Chrome DevTools 验证列移除、工具展开/收起和编辑弹框；该完整 JSON 编辑口径已被下一项最新需求替换）
- [x] 修正 MCP 管理页全局单服务编辑保存：MCP 管理页全部是全局配置，列表移除作用域列；新增和编辑弹框只维护单个 Server ID 与单个服务配置 JSON；`POST /api/mcp/save` 支持单服务合并写回 `mcp/global.json`，保存一个 Server 时不得覆盖其他 Server；继续保留旧完整 `mcpServers` 保存能力作为兼容入口；浏览器测试覆盖列移除、工具展开/收起、编辑弹框只显示当前 Server 配置和保存后其他 Server 不丢失。（已用 Chrome DevTools 验证表格无配置文件列和作用域列、`chrome-devtools` 编辑弹框只显示单服务配置、保存后 `idea` 与 `chrome-devtools` 均保留、工具按钮 loading/展开/收起正常）
- [x] 修正 MCP 配置编辑 JSON 形态：MCP 新增和编辑弹框 textarea 参考标准 MCP 配置示例，展示完整 `{"mcpServers":{"服务 ID":{...}}}` JSON；前端保存时要求该 JSON 只包含一个 Server，并抽取该 Server 走单服务合并保存，继续保证不会覆盖全局文件中的其他 Server；浏览器测试覆盖编辑弹框显示完整 JSON、保存后列表仍保留两个服务。（已用 Chrome DevTools 验证 `chrome-devtools` 编辑弹框显示完整 JSON 且只包含当前服务，保存后 `idea` 和 `chrome-devtools` 均保留；新增配置默认展示完整 `mcpServers` 根结构）

验收口径：Worker 不直接写核心数据；所有副作用操作经中心服务；任务失败保留事件和 `traceId`。

## 阶段 9：模型网关、供应商与代理

目标：实现内部统一模型调用规范、协议适配器、供应商配置、代理策略和用量记录。（历史目标中的“两个内置模型协议插件”已被新口径替换：OpenAI 为固定内置项，Anthropic 通过 `plugins/builtin-model-*` 适配器进入。）

- [x] 实现 `model-gateway`，只接收内部 `ModelRequest`。（本轮已按最新需求替换为只接收 OpenAI Chat Completions 内部请求；原 `ModelRequest` 任务保留为历史记录）
- [x] 实现 `builtin-model-openai-compatible` 模型协议插件。（本轮已按最新需求删除该包，OpenAI 能力改由中心服务 `openai-builtin` 固定内置项承担）
- [x] 支持 `responses` 模式。（本轮已按最新需求废弃，中心内部和供应商配置只保留 `chat-completions`）
- [x] 支持 `chat-completions` 模式。
- [x] 实现 `builtin-model-anthropic-messages` 模型协议插件。（本轮已改为“Anthropic 适配器”，通过中心目录 `plugins/builtin-model-*` 扫描进入供应商页）
- [x] 两个内置模型协议插件不可卸载，但可配置、启用或停用。（本轮已替换为：`OpenAI 内置` 不作为插件包卸载；`builtin-model-*` 适配器按系统内置插件规则管理）
- [x] 实现供应商配置引用 `protocolPluginId` 和 `protocolMode`。
- [x] 实现 API Key 敏感信息保存，不向客户端返回明文。
- [x] 实现供应商列表、新增、修改、删除、启用、停用接口。
- [x] 实现模型列表刷新和推理深度刷新。
- [x] 补齐供应商弹框模型获取和数字版本排序：新增“获取”按钮，通过中心服务 `/api/provider/model-fetch` 调用上游模型列表，按模型名中匹配出的数字段降序排序，自动把第一项设为默认模型，并保留既有模型上下文窗口。
- [x] 实现供应商能力声明：图片、工具调用、JSON 输出、推理深度、缓存用量字段、模型列表接口和流式输出。
- [x] 实现真实供应商 token/SSE 级逐字模型回复、模型流式解析和前端逐字/逐片段助手回复展示；模型网关已启用 `stream: true`，按 OpenAI Chat Completions 和适配器转换后的 SSE delta 事件逐片段写入 `model.stream.delta`，不再使用同步完整 HTTP 响应伪造流式事件。
- [x] 实现网络代理配置，支持 HTTP、HTTPS、SOCKS4、SOCKS4a、SOCKS5。
- [x] 代理用户名和密码允许为空，二者都为空时按无认证代理处理。
- [x] 实现供应商代理策略：不使用代理、使用全局默认代理、使用指定代理配置。
- [x] 区分代理失败、认证失败、供应商连接失败、供应商接口失败和协议解析失败。
- [x] 写入 `usage_records` 原始用量记录。
- [x] 实现 `usage_daily_stats` 聚合刷新或增量维护。

验收口径：Agent 内部只使用 OpenAI Chat Completions 规范；OpenAI 兼容供应商直接进入，其他供应商通过适配器转换后进入；敏感信息不泄露；用量可追踪。

## 阶段 10：智能体、记忆与协作

目标：实现主智能体、长期智能体、子智能体、Markdown 记忆和协作过程展示。

- [x] 初始化不可删除主智能体“致心”。
- [x] 主智能体定义缺失或损坏时按内置模板恢复。
- [x] 实现智能体 Markdown 定义文件 `agents/{agentId}.md`。
- [x] 智能体定义保存 ID、名称、角色、能力边界、可用插件范围、可用 MCP 范围、可用 skill 范围、默认供应商、默认模型、推理深度、记忆索引、创建来源和启用状态。
- [x] 智能体定义中的可用插件、MCP 和 skill 范围作为权限边界和默认启用范围保存。
- [x] 智能体执行时最终可用能力由当前会话窗口、项目上下文、权限和审批策略动态决定。
- [x] 实现长期智能体创建、修改、停用和删除。
- [x] 删除长期智能体时提示记忆处理、调度入口和历史会话影响。
- [x] 实现用户记忆 `memory/user.md`。
- [x] 实现智能体记忆目录 `memory/agents/{agentId}/{year}/{month}/{day}.md`。
- [x] 记忆写入以一轮完整对话为边界。（执行引擎最小闭环已在轮次编排后触发记忆提交；手动记忆接口仍保留用于检查和管理）
- [x] 记忆写入只追加，不覆盖或插入已有内容。
- [x] 每个智能体记忆写入使用单写队列。
- [x] SQLite 保存记忆索引、关键词、来源会话、来源轮次和附件引用。
- [x] 接入 Mem0 OSS 作为语义记忆引擎，默认索引和 history 文件放在中心目录 `memory/mem0` 下；Markdown 记忆和 SQLite `memory_index` 继续作为长期记忆事实源，Mem0 索引可随中心目录迁移或从 Markdown 记忆重建。（已建立中心服务 `memory-engine` 适配层和 `memory.mem0.skipped` 审计；默认不启用外部 LLM/Embedding，后续通过中心服务供应商网关或本地 embedding 提供方再开启真实语义抽取）
- [x] 实现子智能体一次性任务，不落为长期定义。
- [x] 子智能体不能再创建子智能体。
- [x] 实现智能体管线通话事件展示。
- [x] 实现智能体群聊讨论事件展示。

验收口径：智能体定义和记忆保持 Markdown 可读；协作过程可审计；子智能体只存在于任务上下文。

## 阶段 11：插件、MCP、skill 与内置个人事务

目标：实现插件作为总扩展包，MCP 和 skill 可独立安装或被插件携带，个人事务能力按一等领域和内置插件拆分。

- [x] 实现插件清单 schema。
- [x] 实现插件安装、启用、停用、删除、配置和调用记录。
- [x] 实现插件来源：`system-builtin`、`user-installed`、`project-local`。
- [x] 系统内置插件不可卸载，部分可停用。
- [x] 实现插件权限声明和权限校验。
- [x] 插件缺少权限声明时禁止执行敏感操作。
- [x] 实现插件携带 MCP Server 配置。
- [x] 实现独立 MCP 配置，根字段为 `mcpServers`。
- [x] 支持 HTTP 类型 MCP Server。
- [x] 支持 stdio 类型 MCP Server。
- [x] 实现插件携带 skill。
- [x] 实现独立 skill 安装。
- [x] 实现项目级插件、MCP、skill 扫描和注册。
- [x] 实现能力优先级：当前项目级能力 > 用户安装全局能力 > 系统内置能力。
- [x] 实现待办一等领域模块。
- [x] 实现日程一等领域模块。
- [x] 实现个人知识库一等领域模块。
- [x] 实现 `builtin-file-organizer` 内置插件。
- [x] 实现 `builtin-browser-collector` 内置插件。
- [x] 实现 `builtin-office-integration` 内置插件。
- [x] 实现 `builtin-automation` 内置插件。
- [x] 所有插件、MCP、skill 调用写入 `extension_call_records` 和 `events`。

验收口径：扩展能力不能绕过中心服务；项目级能力优先；个人事务核心数据可迁移。

## 阶段 12：输入框、附件与上下文引用

目标：实现统一输入框，支持文本、附件、图片、skill 检索、项目文件引用和 IDE 上下文插入。

- [x] 实现统一输入框组件。
- [x] 输入框补齐“任务 / 子代理 / 编辑”三段入口，分别展示编排任务、子智能体状态树和本次新增/编辑文件 diff。（代码已落地；子代理真实发送协议待中心服务明确后替换当前前端临时引导草稿）
- [x] 修复输入框三段入口审查问题：子代理弹窗“发送引导”写入当前输入草稿；第三轮移除子代理和编辑 diff 演示数据，协议未齐备时显示“暂无子代理状态 / 暂无本次编辑”；文件上下文按钮复用现有项目引用候选，并支持真实项目会话、未发送项目草稿和插件运行时项目上下文。
- [x] 补齐本轮输入区小弹框、智能体状态树、智能体对话闭环和统一滚动条回归：输入区三段入口更新为“任务 / 智能体状态 / 编辑”，三者详情都使用小弹框；智能体状态弹框展示主智能体、长期智能体和各自子智能体两级树；每个智能体节点可查看当前会话消息并发送消息，中心服务没有独立智能体会话 API 时仍通过当前会话发送；编辑详情改为小弹框；滚动条统一为 Element Plus 风格窄轨道和圆角 thumb。插件端只落实代码，不测试、不构建；项目对话验证使用项目根目录 `对话测试` 目录；由桌面端拉起中心服务。本轮进一步把任务详情、智能体状态/对话、编辑详情和项目能力详情拆成独立弹框组件，并把顶部菜单非对话页迁入独立页面宿主组件，`MainView.vue` 只负责布局、菜单切换、传参和事件回调；静态回归脚本已覆盖该边界。
- [x] 调整输入区三入口最终布局：输入框占满中间对话区可用宽度并与消息内容区左右对齐；“任务 / 智能体 / 编辑”入口固定在输入框内部顶部，不提供收起展开按钮；三入口详情从入口条向上打开浮层，不撑高输入框本体，内容默认撑开且最大高度 `40vh`，超过后在浮层内部滚动。静态回归脚本已覆盖满宽、向上浮层和 40vh 限高；测试闭环必须由桌面端 `dev:desktop-shell` 拉起中心服务，不使用 `dev:center:new`。
- [x] 输入框移除“模型来源：暂无启用供应商，后续发送沿用中心服务当前默认处理。”提示，不再渲染该文案。
- [x] 支持文本输入和多行编辑。
- [x] 支持剪贴板图片粘贴。
- [x] 支持附件临时保存到 `temp`。
- [x] 发送后将附件转入正式会话附件目录。
- [x] 图片发送前由中心服务基于模型能力判断是否支持。
- [x] 支持 `/` skill 检索。
- [x] skill 检索展示当前会话可用 skill。
- [x] 支持 `@` 项目文件、文件夹和代码位置检索。
- [x] 普通非项目会话不展示项目文件检索。
- [x] 文件引用保存项目 ID、绝对路径、相对路径和显示名。
- [x] 文件夹引用保存项目 ID、绝对路径、相对路径和显示名。
- [x] 代码引用保存项目 ID、绝对路径、相对路径、开始行、结束行和选中代码。
- [x] 输入框引用标签保留完整结构化信息。
- [x] 支持 IDE 插件插入上下文引用。
- [x] 发送消息后中心服务保存附件和引用元数据。

验收口径：输入内容、附件和引用都作为消息事实进入中心服务；未发送临时附件可清理。

## 阶段 13：通知、执行模式与审批

目标：中心服务统一判断通知和审批策略，各客户端只展示结果。

- [x] 实现按客户端类型保存通知配置。
- [x] 通知配置支持对话完成、失败、需要用户处理、普通对话、项目对话和智能体协作对话。
- [x] 桌面壳检测系统通知权限并同步给中心服务。
- [x] 中心服务统一判断是否发送系统通知。
- [x] 中心服务同步通知事件给相关客户端。
- [x] Web 端收到通知事件后使用浏览器通知或页面内通知。
- [x] 浏览器无通知权限时至少显示页面内通知。
- [x] 实现按客户端类型保存执行模式。
- [x] 支持建议模式、自动编辑、全自动。
- [x] 默认执行模式为全自动。
- [x] 补齐本轮执行模式下拉说明：建议模式、自动编辑和全自动在下拉面板内展示对应审批语义解释。
- [x] 执行模式只影响后续操作，不回改历史任务。
- [x] 建议模式下副作用步骤需要用户确认。
- [x] 自动编辑下低风险读操作和部分编辑流程自动执行，高风险操作审批。
- [x] 全自动在沙箱和权限范围内自动执行。
- [x] 审批结果写入事件日志。

验收口径：通知由中心服务统一生成；审批策略由中心服务执行；客户端不自行判断副作用权限。

## 阶段 14：用量统计与审计查看

目标：用户可以查看供应商、模型、项目、任务和扩展能力调用记录。

- [x] 实现用量原始记录查询。
- [x] 实现按供应商、模型、项目、会话和时间范围聚合。
- [x] 供应商未返回缓存字段时标记为未提供，不能用 0 冒充。
- [x] 普通非项目会话归入全局统计。
- [x] 项目会话按项目 ID 归集。
- [x] 历史统计保留当时供应商 ID、模型、项目 ID 和调用时间。
- [x] 实现事件日志查看接口。
- [x] 实现任务步骤审计查看。
- [x] 实现插件、MCP、skill 调用记录查看。
- [x] 前端提供用量统计页面。
- [x] 前端提供任务和事件详情查看。

验收口径：统计只追加不回改；审计记录能追踪模型调用、工具调用、命令执行、文件操作和扩展能力调用。

## 阶段 15：构建、迁移与质量门槛

目标：形成可运行、可打包、可迁移、可验收的完整交付链路。

- [x] 根构建脚本按顺序构建共享包、中心服务、前端、桌面壳和内置插件。
- [x] 桌面壳打包前自动构建中心服务和前端。
- [x] 绿色版 zip 包含中心服务产物、前端资源、内置插件、应用图标和界面图标。（当前绿色版 zip 已复制前端资源、中心服务入口、内置插件、应用图标和界面图标；正式 Electron 二进制后续由 Electron 打包器替换当前开发期启动壳）
- [x] IDEA 插件构建使用 Gradle IntelliJ Platform Plugin。
- [x] IDEA 插件 Java 编译输出固定 Java 21 字节码。
- [x] IDEA 插件无 Settings 页时禁用 `buildSearchableOptions`。
- [x] Gradle 仓库优先 `mavenLocal()`，再阿里云公共镜像，再 Maven Central。
- [x] 完成所有新版目录迁移后删除旧 `桌面端`、`Web端`、`中心服务`、`共享`、`IDEA_SDK` 和 `界面图标` 目录。（用户已明确要求移除旧版所有文件；根旧目录和迁移残留 `legacy-web`、`legacy-desktop` 已删除）
- [x] 删除旧目录后刷新 IDEA 项目，确认 IDEA 不再使用旧模块。（`.idea/gradle.xml` 已指向 `plugins/idea`；已补 IDEA 插件静态检查覆盖新版工程）
- [x] 建立 `center-data` 迁移验收清单。
- [x] 建立手动验收清单，覆盖桌面壳、Web、本机访问、远程访问、手机 Web、IDE 插件、中心目录迁移和绿色版删除。
- [x] 建立中心服务基础测试，覆盖配置、SQLite 迁移、事件日志、访问控制、供应商、记忆写入和 Worker 失败收尾。
- [x] 建立前端基础检查，覆盖入口模式、登录、消息展示、插件紧凑模式、移动模式和原桌面工作台形态，确保顶部主菜单、左侧项目/普通对话导航、右侧任务/智能体状态栏和胶囊式输入框不会退化为临时骨架。
- [x] 建立前端主题检查，覆盖系统默认主题、IDE 宿主主题参数、手动切换和本地保存。
- [x] 建立前端分包检查，覆盖页面路由懒加载、第三方依赖 vendor 拆包和构建产物 chunk 大小，避免 `index.html`、`plugin.html` 共用入口的主文件过大。
- [x] 修复桌面壳 `dev:desktop-shell` 启动白屏问题，并建立前端入口路由、资源相对路径、中心服务 HTTP 托管前端资源、桌面端开发服务器加载和本机免登录检查，覆盖桌面壳加载中心服务 `index.html`、IDE 插件打开 `plugin.html`、桌面端开发期启动前端 dev server、桌面壳本机入口不能渲染空白页或误进远程登录页的回归场景。
- [x] 建立 IDEA 插件检查，覆盖项目 ID 读取/创建、右键引用和内部文件定位链接。（已建立 `scripts/check-idea-plugin-static.mjs`；Gradle 构建仍依赖 IDEA Gradle 环境或补齐 Wrapper）
- [x] 已完成修正/验收补齐：补齐 `apps/frontend` 工作台顶部“供应商、网络代理、运行环境、用量统计”页面真实中心服务功能；右侧状态栏恢复为任务状态、智能体状态和事件/审计摘要语义；新增并加强 `scripts/check-frontend-management-pages.mjs`，覆盖管理页不能退化为空页面、必须走 API 客户端和右栏审计摘要，并补充供应商完整更新、敏感字段不回显、用量会话筛选和供应商不存在错误处理的静态回归检查。本轮补齐本机开发前端跨端口访问中心服务的 CORS 验收，中心服务只允许本机 Vite 开发来源，不放开任意公网来源；管理页接口失败由 store 层记录可见错误并保留控制台排查信息，避免 UI 事件出现未捕获 Promise；新增 `scripts/check-center-local-dev-cors.ts` 并扩展 `scripts/check-frontend-management-pages.mjs` 覆盖该约定。本轮修复桌面壳 preload 指向 `.ts` 源码导致 `zhixinDesktop` 桥接未注入、前端误判为 `web-local` 的问题，新增 CommonJS preload 和桌面运行时检查；Windows MCP 截图已验收 `desktop-shell open`、六项顶部菜单、中心服务配置页和对话页右侧状态栏。本轮继续修复桌面/前端 UI 与中心服务启动锁回归：顶部固定展示“对话、供应商、网络代理、运行环境、用量统计、中心服务”六项，主题切换改为最右侧图标按钮，客户端类型、连接状态、任务状态和轮次状态统一中文展示，输入框恢复三种执行模式和四档推理深度中文说明；管理页网络失败统一提示“无法连接中心服务，请确认中心服务已启动后重试。”；中心服务启动锁读取 `.zhixin-center.lock` 的 `pid/createdAt` 并跨平台判活，只清理确认已退出或损坏的陈旧锁，仍存活进程继续阻止重复启动；新增 `scripts/check-center-startup-lock.ts` 并纳入总检查，扩展前端工作台和管理页静态回归检查。
- [x] 建立用户反馈回归检查 `scripts/check-frontend-feedback-regressions.mjs`，覆盖桌面端六项菜单、浏览器端不展示中心服务管理入口、主题切换图标化、客户端类型和连接状态中文化、输入框执行模式和推理深度下拉完整说明。本检查已能复现当前未修复项，后续程序员修复 UI 后应转为通过。
- [x] 补齐用户反馈回归修正：顶部移除桌面端、本机 Web 等端类型标志，只保留中文连接状态；工作台新增“插件、MCP、skill”菜单；前端提供插件安装、启停、配置和删除管理，MCP 全局/项目级配置列表与保存，skill 全局/项目级安装与列表；API 客户端接入插件、MCP 和 skill 管理接口；中心服务新增 `/api/mcp/list` 并扫描中心目录 MCP 配置，skill 列表改为读取中心目录已安装内容；`scripts/check-frontend-feedback-regressions.mjs` 已转为通过。
- [x] 补齐本轮全局扩展、供应商模型选择、用量图表和时间格式回归：顶部“插件 / MCP / skill”管理页只展示全局管理，不混入项目级能力；项目级能力只在项目对话上下文体现；供应商默认模型改为来源于已保存或刷新模型列表的下拉选择，并保留不支持模型列表时的手动填写说明；供应商配置字段提供解释；用量统计接入图形化总量、供应商维度和项目维度；前端所有展示时间统一通过 `YYYY-MM-DD HH:mm:ss` 格式化函数；插件端本轮只落实代码，不执行插件端测试。
- [x] 补齐浏览器测试窄范围修复回归：`scripts/check-frontend-management-pages.mjs` 覆盖用量统计不能直出 ISO 时间、项目对话页必须展示“项目级插件 / MCP / skill”能力摘要、桌面壳开发期必须启动当前 `services/center`；用量原始记录和聚合统计展示前递归格式化时间字段；项目对话页新增项目级插件、MCP 和 skill 摘要区，空数据时说明由打开项目目录扫描且不在全局页管理；`scripts/check-center-extensions-usage.ts` 补充 `/api/provider/model-list` 注入检查，确认当前中心服务源码接口可用，浏览器 `API_NOT_FOUND` 属于旧进程/旧运行环境风险。
- [x] 修复 Windows 客户端供应商默认模型下拉识别回归：`scripts/check-frontend-management-pages.mjs` 增加默认模型字段必须使用 `el-select`、`el-option`、`allow-create`、`default-first-option` 且不得回退 `el-input` 的静态检查；供应商页默认模型控件固定为可创建下拉，选项来自 `providerModelOptions`，无模型列表时仍通过下拉手动维护模型名称。
- [x] 补齐本轮项目对话树 UI 回归：`需求.md` 明确项目对话两级树、项目/对话状态图标、hover 图标按钮、tooltip 时间展示和两个分组标题新增入口；前端左侧导航按项目名称分组，项目行可展开收起，对话列表右侧不再常态展示时间；新增静态回归检查覆盖 `data-nav-kind` 标识、标题图标按钮、状态图标和 tooltip 时间格式。
- [x] 修复旧中心服务 `/api/project/list` 返回 `API_NOT_FOUND` 时前端初始化中断的问题：`loadNavigationData` 先加载会话再加载项目，`loadProjects` 捕获项目列表失败并记录排查信息，项目对话树从项目会话 `projectId` 构造旧中心服务兼容兜底项目节点，普通会话继续渲染；`scripts/check-frontend-workspace-ui.mjs` 已补充 RED/GREEN 回归检查。
- [x] 补齐本轮反馈修复：`需求.md`、`架构.md` 明确“智能体管理”顶部菜单、当前会话模型选择、`queued` 只限定当前对话内部、引导/审批/确认归属当前轮次，以及项目级插件/MCP/skill 通过详情弹框展示；前端新增项目能力详情弹框、状态作用域说明、输入区供应商/模型/推理深度选择和智能体管理页入口；静态回归检查覆盖上述 UI/语义点。插件端本轮只尝试 IDEA 插件构建，不做 UI 测试。
- [x] 补齐本轮对话体验反馈：项目主名称固定使用文件夹名，别名/备注只作备注；新增普通/项目对话只创建本地草稿，真实发送后才创建可见会话；对话导航行右侧展示时间节点并在 hover 展示用户发送摘要；轮次时间移入对话内容末尾，运行中耗时固定显示在输入框上方；输入框内部移除供应商选择，仅保留模型、推理深度、执行模式和必要发送设置。
- [x] 修复本轮对话体验回归：发送前新建会话不插入可见历史列表；项目缺少登记名称时显示“未登记项目名称”并在 tooltip 暴露项目 ID；运行中轮次只在输入框上方显示耗时，结束后才在消息内容末尾显示最终时间。
- [x] 补齐本轮工作区输入框和项目默认名称回归：统一前端 workspace 输入区改为更宽、更高的稳定胶囊输入框；中心服务项目登记在缺少 displayName 但 latestPath 明确时从路径最后一级目录派生项目名称；插件运行时在缺少 projectName 但 projectPath 明确时派生项目名称；无法从名称或路径得出文件夹名时仍返回登记错误。
- [x] 补齐本轮记忆时间、页面路由和智能体状态计数回归：`需求.md` 与 `架构.md` 已同步永久记忆段标题只保留 `HH:mm:ss`；`AGENTS.md` 已写入 `views/页面/RouterIndex.vue` 和别名动态路由规范；中心服务新增记忆 Markdown 标题先按当轮旧口径改为 `# 时间：HH:mm:ss`，随后已被下一条最新需求校准为 `# HH:mm:ss`，数据库时间仍保留 ISO；前端路由改为 `@views/.../RouterIndex.vue` 懒加载并注册对话、登录、智能体管理、供应商、网络代理、运行环境、用量统计、插件、MCP、skill、中心服务 URL；管理 URL 进入完整工作台壳并按页面加载数据；`agentStatusProgressText` 只统计主智能体和长期智能体，不把子智能体计入分母；已扩展静态回归检查覆盖路由别名、页面入口、记忆标题、智能体状态计数、管理页拆分和用量图表容器。插件端本轮只落实代码，不测试、不构建；浏览器端通过桌面端拉起中心服务后用 Chrome DevTools 验证。
- [x] 补齐本轮最新需求校准：永久记忆段标题从 `# 时间：HH:mm:ss` 改为 `# HH:mm:ss`；对话页专属弹框迁入 `views/Chat/dialogs`，弹框专属样式写回 Vue scoped style；输入区移除独立“文件上下文”按钮，改为展示当前窗口上下文用量 `占用比例 / 模型窗口上限`；供应商模型列表支持手填模型名称和 `K` 单位上下文窗口，中心服务保存为 `contextWindowTokens`；静态回归检查已覆盖上述口径。Chrome DevTools 已验证浏览器端 `/agent-management`、`/proxies`、`/runtimes`、`/center`、`/login`、`/chat` 非白屏；中心服务仍按规则未直接启动，桌面壳未拉起中心服务时仅验证前端壳和可见错误状态。
- [x] 补齐本轮 Web 包瘦身、页面全量拆分和执行模式说明回归：新增 `本次计划.md`，更新需求与架构边界；静态检查需覆盖 Web 端 dist 不含中心服务、桌面端动态装配中心服务、管理页不再通过 `initial-page` 传参复用大组件、执行模式下拉选项展示解释；浏览器端使用 Chrome DevTools 验证独立路由页面和执行模式下拉。本轮继续把 `MainView.vue` 收敛为公共工作台壳和嵌套路由出口，各 `views/页面/RouterIndex.vue` 直接承载页面主体，不再自行包裹公共壳；静态回归已通过。依赖中心服务的联调暂未完成，原因是本机缺少 Visual Studio C++ Build Tools，`better-sqlite3` 原生绑定无法生成，桌面壳拉起中心服务后 `8866` 不能监听，待补齐本机编译环境后继续。
- [x] 补齐本轮真实功能回归 P01：建立代码文件行数治理，拆分超过 1500 行或接近 1500 行的源码，并新增脚本防止单个代码文件超过 1500 行。
- [x] 补齐本轮真实功能回归 P02：修复真实对话链路，串联会话发送、启用供应商、模型网关调用、流式片段、最终消息固化、任务状态和错误事件，确保可使用 `AI账号.md` 完成真实供应商调用验证。
- [x] 补齐本轮真实功能回归 P03：补齐模型协议插件脚手架和导入注册，确保 OpenAI 兼容与 Anthropic Messages 内置协议插件可被中心服务注册并在供应商页选择。（本轮已按最新需求替换为：OpenAI 作为 `OpenAI 内置` 固定项，Anthropic 作为 `Anthropic 适配器` 从中心目录 `plugins/builtin-model-*` 扫描进入）
- [x] 补齐本轮真实功能回归 P04：修复供应商模型与协议能力，覆盖模型列表刷新、手填模型、上下文窗口 `K` 单位转 token、推理深度、能力声明和 API Key 脱敏保存。
- [x] 补齐本轮真实功能回归 P05：修复 `8866` 前端资源刷新策略，确保桌面端拉起中心服务托管页面能反映最新前端资源或清晰复用 `5173` HMR。
- [x] 补齐本轮真实功能回归 P06：修复会话删除功能，覆盖中心服务删除接口、前端删除按钮、项目对话与普通对话列表刷新和删除失败可见错误。
- [x] 补齐本轮真实功能回归 P07：修复会话标题总结功能，真实对话完成后生成或更新标题摘要并同步列表、详情和实时事件。
- [x] 补齐本轮真实功能回归 P08：修复用量统计功能，真实模型调用后写入用量原始记录和聚合统计，前端展示真实总量、供应商维度、项目维度和原始记录。
- [x] 补齐本轮真实功能回归 P09：修复顶部菜单激活样式，确保点击和直接访问路由后当前菜单有明确激活态并能刷新保持。
- [x] 补齐本轮真实功能回归 P10：修复网络代理协议与认证字段，支持 HTTP、HTTPS、SOCKS4、SOCKS4a、SOCKS5、用户名密码、无认证代理和供应商代理策略关联。
- [x] 补齐本轮真实功能回归 P11：补齐智能体管理真实功能，覆盖主智能体不可删除、长期智能体创建、修改、停用、删除、记忆影响提示和列表持久化刷新。
- [x] 补齐本轮文档维护规则回归：`AGENTS.md` 写入 `需求.md`、`架构.md`、`计划.md` 随最近代码和真实实现持续同步的规则；`架构.md` 工程目录说明改为紧跟目录条目展示，避免目录树和解释远距离对照；`计划.md` 同步维护规则和本完成项。
- [x] 补齐本轮中心目录说明回归：`架构.md` 的 `center-data` 目录树已为 `db`、`config`、`memory`、`sessions`、`personal` 等目录和关键文件补齐同一行就近解释，避免目录结构与用途说明分离。
- [x] 补齐本轮对话输入区和任务编排回归：输入区文本区域支持用户手动调整高度并固定在底部，剩余空间全部给消息列表；任务入口和右侧任务状态只统计当前轮次或最新轮次任务编排，不再把历史对话次数累计为任务数量；上下文 token 统计通过节流、请求签名去重和请求序号防止连续输入时反复请求或旧响应覆盖新状态；`需求.md`、`架构.md` 和静态回归检查已同步上述验收口径。
- [x] 修复 `dev:desktop-shell` 在 `5173` 已被前端 Vite 占用时无法继续拉起 `8866` 的回归：开发编排脚本启动前先探测 `http://127.0.0.1:5173`，已有可访问服务时复用并继续启动 Electron；新增静态检查覆盖复用逻辑和 IDEA 运行窗口提示，已通过 IDEA 运行配置验证 `8866` 监听和 `/api/health` 返回 200。
- [x] 修复桌面壳窗口加载早于中心服务监听的启动竞态：主窗口加载中心服务 URL 前先轮询 `GET /api/health`，健康检查通过后再 `loadURL`，超时或加载失败时写入诊断日志并展示错误页，避免 `ERR_CONNECTION_REFUSED` 和 `UnhandledPromiseRejectionWarning`；新增 `scripts/check-desktop-shell-center-health.mjs` 并纳入 `scripts/check-all.mjs`。
- [x] 补齐任务编排和工具调用真实闭环：中心服务发送一轮消息后创建思考、模型、工具计划、命令工具执行、插件/MCP/skill 状态记录等 `task_steps`；明确命令意图写入 `tool.plan.created`、`tool.command.started/output/completed`，插件、MCP 和 skill 未解析到执行器时写入不可用事件和原因；修复任务步骤创建接口缺少 `findTask` 导入的问题；扩展 `scripts/check-center-turn-task.ts` 覆盖真实步骤和事件。
- [x] 补齐本轮输入区重叠和小浮层回归：对话页消息列表与输入区改为严格 flex 边界，输入区外层手柄调整高度并禁止 textarea 自身撑破布局；“任务 / 智能体状态 / 编辑”详情从居中弹框改为贴近输入区的小浮层，任务和右侧状态栏同步当前轮次真实任务，智能体状态右侧摘要同步浮层节点，编辑入口按真实 diff 展示 `+新增 -删除` 摘要；静态回归和 Chrome DevTools 布局测量已覆盖。
- [x] 补齐本轮输入区拖拽和三入口小弹框回归：输入区作为对话页底部 flex 子项，拖高时只压缩上方消息列表不重叠；三入口可见文案调整为“任务 / 智能体 / 编辑”，任务、智能体和编辑小弹框移除标题栏与关闭按钮，支持点击原入口或外部区域关闭；右侧智能体摘要与智能体小弹框同源展示；静态回归和 Chrome DevTools 浏览器闭环需覆盖。
- [x] 补齐本轮三入口外置和删除确认回归：输入区三入口移到输入框外部并支持收起/展开，输入框、消息内容和三类小浮层左右对齐；任务浮层只展示任务列表和状态，智能体浮层展示长期智能体与子智能体树并可点开完整智能体对话弹窗，编辑浮层展示本轮文件说明和列表；普通对话与项目对话删除统一进入对应确认弹框；已使用 `项目对话测试` 目录完成项目对话发送和浏览器删除确认闭环。
- [x] 补齐本轮最新输入区和项目删除回归：按反馈把“任务 / 智能体 / 编辑”三入口收回输入框内部顶部，三入口和输入框左右边缘对齐，移除收起/展开按钮；任务、智能体和编辑三个输入区浮层由内容撑开并限制最大高度 `40vh`；中心服务新增项目删除接口，前端项目行 hover 展示删除项目和新增项目对话图标，删除项目时清理中心服务项目索引及项目会话事实，不删除项目目录或 `致心项目ID.md`；测试闭环必须由桌面端拉起中心服务，不使用 `dev:center:new`。
- [x] 修复本轮自然语言工具调用和对话排序回归：模型网关已解析 OpenAI 兼容 `chat-completions` 标准 `message.tool_calls` 数组，允许工具调用响应 `content` 为空，并支持同次响应内多个命令工具调用后一次性回填完整 assistant `tool_calls` 与多条 tool 结果消息，避免自然语言请求查看 Node/Python 版本时触发 `PROVIDER_RESPONSE_TEXT_EMPTY` 或只执行第一个工具；前端对话区改为按轮次合并用户消息、思考过程、工具/模型过程和助手回复，确保过程卡片显示在用户问题下方、助手回复上方；新增 `scripts/check-chat-turn-render-order.ts` 与 `scripts/check-center-model-tool-loop.ts` 标准多工具调用回归检查。
- [x] 补齐本轮中心服务生命周期、项目文件夹选择和真实 SSE 流式回归：删除误生成的 `services/center/center-data`；根脚本移除独立中心服务启动入口，规范明确中心服务启停只能由桌面端或 `dev:desktop-shell` 管理；新增项目对话分组入口改为先选择文件夹并登记项目，浏览器端测试固定使用 `项目对话测试`；新增 `scripts/check-current-streaming-and-project-policy.mjs` 覆盖独立启动禁用、SSE/token 级流式、项目文件夹选择和本轮规范同步。
- [x] 补齐本轮对话过程流式展示回归：思考过程按 `thinkingId` 或阶段拆成独立卡片，生成中默认展开、完成后默认折叠；模型 SSE delta 不再渲染为“模型输出”过程卡片，而是拼接为运行中临时助手消息，固化助手消息到达后自动替代；工具卡片标题收敛为核心执行对象，例如 `node -v`；同一轮过程按事件序号交错排序，耗时条和输入框左右对齐；扩展 `scripts/check-chat-turn-render-order.ts` 和 `scripts/check-chat-experience-regressions.mjs` 覆盖该口径。
- [x] 补齐本轮工具调用可视化和半截回复防护回归：插件/MCP/skill 不可用占位不进入对话消息流，只有真实调用才显示过程卡片；模型输出“准备继续执行、改用命令、重新查询、调用工具”等半截工具意图但没有结构化工具调用时，中心服务写入 `message.turn.incomplete`、标记轮次失败并禁止固化助手消息；新增 `scripts/check-center-incomplete-tool-intent.ts` 覆盖该口径。
- [x] 补齐本轮排队发送、引导移除、发送按钮双态和执行期 token 统计回归：运行中按 Enter 发送进入输入框顶部本地排队消息区，点击“引导”立即移除排队项并作为当前轮次补充引导提交；发送按钮仅显示“发送/停止”；用户输入、引用和附件变更不触发 token 统计，token 总览只在执行中或完成后刷新；新增 `scripts/check-chat-queue-send-and-token-regressions.mjs` 覆盖该口径。
- [x] 补齐本轮 token 外显、三入口激活大小和下拉描述回归：输入区 token 外层只显示进度圈和百分比，hover 只展示已用 token、窗口上限和占用比例；任务、智能体、编辑三个入口固定为激活入口大小，浮层内容自动撑开且最大 `40vh`；执行模式和推理深度下拉选项展示已有 description；新增 `scripts/check-chat-composer-ui-regressions.mjs` 覆盖该口径，浏览器端用 Chrome DevTools 验证。
- [x] 修正本轮输入区视觉反馈：token hover 改为 `K/M` 短单位的 `用量 / 上限` 与百分比；三入口条和三个浮层与输入框左右边缘对齐；入口视觉高度提升为激活框大小，顶部圆角与输入框一致；智能体浮层移除内部独立滚动条，仅由外层浮层承载滚动；`scripts/check-chat-composer-ui-regressions.mjs` 已同步覆盖。
- [x] 修复本轮输入区浮层滚动、编辑文字化、焦点释放和圆角回归：任务、智能体和编辑三个输入区浮层内部不再自带滚动条，统一由外层最大 `40vh` 浮层承载；编辑浮层改为文字摘要列表，不再展示旧 diff 预览块；打开任一浮层时释放输入框真实焦点；三入口条不裁剪输入框顶部两个圆角；引用反馈的临时图片已从工作区删除。
- [x] 修复本轮输入区截图反馈：移除三入口按钮固定 `52px` 高度；智能体浮层树不再启用 Element Plus 当前节点高亮，也不显示自定义激活态；上下文百分比圈改用 Element Plus 圆形进度组件且 0% 显示空进度；输入框底部工具栏继承外壳底部圆角，避免左右下角样式缺失。
- [x] 补齐本轮智能体浮层、智能体独立对话和编辑真实闭环：智能体浮层只展示名称与状态并移除 hover 边框激活；主对话抽出 `ChatConversationPanel.vue` 完整复用内容区、左侧时间线和输入区，智能体弹框按 `parentSessionId + agentId` 读取独立子对话；中心服务新增 `agent_sub_conversation_messages`、`pending_edit_records`、智能体子对话接口和待确认编辑保存/撤回/diff 接口；保存只接受当前真实编辑，撤回在文件仍等于编辑后内容时写回编辑前内容，文件缺失或再次变更时标记冲突；IDEA 插件新增 `openEditDiff` 原生对比桥接，Web 端无宿主桥接时使用前端 diff 弹框。
- [x] 修复本轮智能体浮窗与子对话弹框布局回归：智能体浮层节点改为名称和状态同一行展示；智能体子对话弹框增加稳定外壳和消息内容区 `40vh` 最小高度，确保没有历史消息时也显示完整内容区空态。
- [x] 修正本轮智能体浮层状态位置回归：智能体状态紧跟智能体名称后方展示，不再使用两端对齐把状态推到浮层右侧；点击节点打开复用完整对话组件的智能体子对话弹框逻辑保持不变。
- [x] 补齐本轮项目对话身份文件闭环：浏览器端新增项目对话通过 `showDirectoryPicker({ mode: "readwrite" })` 读取或创建项目根目录 `致心项目ID.md`，文件内容固定为 UUID 加换行，非法 UUID 阻断登记；桌面端新增 `selectProjectDirectoryAndEnsureIdentity` 白名单桥接，原生目录选择后读写同一身份文件并返回 `{ projectId, displayName, latestPath }`；前端优先使用桌面桥接，没有桥接时使用浏览器 File System Access API，两者都不可用时展示错误；IDEA 插件读取已有身份文件时增强 UUID 校验，禁止目录名伪造项目 ID。
- [x] 补齐本轮多端会话删除同步、历史事件恢复和浏览器 URL 规范：`/api/session/delete` 删除成功后复用中心服务 `session.deleted` 事件广播给其他端，实时层转发 `session.deleted` 专项 WebSocket 包；前端收到删除包后刷新导航，当前会话被删时清空详情、事件和编辑状态并进入可继续输入的草稿；历史会话加载统一走 `loadActiveSessionSnapshot()` 拉取详情和事件，恢复过程卡片；桌面壳窗口入口固定中心服务地址，中心服务开发期重定向把业务路径规范到 hash，避免 `/chat?port=8866#/chat`。
- [x] 补齐本轮长期智能体和记忆闭环：长期智能体 Markdown 定义保存可用插件、MCP 和 skill 范围字段；主智能体 `main` 明确不可停用；正常会话完成并固化助手消息后追加主智能体长期记忆，`memory_index` 绑定 `source_session_id`、`source_turn_id` 和附件引用，Worker 上下文请求可读取主智能体记忆索引，后续普通模型请求会注入主智能体最近长期记忆 system 消息；`scripts/check-center-domains.ts` 已覆盖主智能体停用失败、记忆写入、来源绑定和第二轮模型请求消费记忆。
- [x] 修复本轮方案 B 项目删除和任务图恢复回归：前端项目删除不再写入 Pinia getter `projectCapabilitySummary`，删除成功或中心服务返回 `PROJECT_NOT_FOUND` 时都会清理本地项目、项目会话、展开态、当前详情和编辑状态，避免左侧残留幽灵项目；中心服务新增对话图 checkpoint 元数据，任务步骤、模型流、模型工具请求、命令工具、工具结果回填、扩展状态和消息固化事件写入 `payload.graph`，用 `sessionId` 作为 thread、`turnId` 作为 graph run、`superstep` 和 `checkpointId` 支持按节点边界恢复；新增 `scripts/check-center-graph-checkpoint-regression.mjs` 并扩展删除回归覆盖该口径。
- [x] 修复本轮永久记忆电脑信息和思考过程展示回归：长期记忆 `## 使用的电脑` 不再写固定 `center`，改为写入当前中心服务运行环境的 OS 类型、平台、版本和架构，用于迁移中心目录后识别写入环境；思考过程正文片段不再携带或渲染状态标签，状态只保留在思考卡片标题后方；`scripts/check-center-domains.ts` 和 `scripts/check-chat-turn-render-order.ts` 已按 TDD 先红灯后绿灯覆盖。
- [x] 补齐本轮记忆本机时间和开发脚本职责回归：`需求.md` 明确永久记忆标题和日期目录使用中心电脑本机时间，并新增“需求事实源与同步”口径，要求后续未覆盖新需求必须写入 `需求.md`、冲突时最新需求优先、`需求.md`/`计划.md`/`架构.md` 与真实代码保持一致；`AGENTS.md` 只保留该口径的执行流程；`dev:frontend` 独立拉起前端 Vite，`dev:desktop-shell` 只启动桌面壳并由桌面壳拉起中心服务，不再拉起前端；`scripts/check-frontend-workspace-ui.mjs`、`scripts/check-desktop-dev-startup.mjs` 和 `scripts/check-current-streaming-and-project-policy.mjs` 已覆盖该口径。
- [x] 修复本轮窗口记忆与思考固定模板回归：历史实现曾把当前会话统计、任务记录、供应商和扩展能力数量显示为思考正文；本轮已按最新口径收紧为只有供应商真实可公开思考内容才显示正文，没有真实思考时只显示标题和耗时。
- [x] 修复本轮桌面端对话体验回归：当前窗口上下文 token 统计改为统计已进入上下文的全量对话消息并在模型响应过程刷新；工具卡片在工具开始事件到达时立即出现，响应回来后追加响应内容；桌面端外部链接交由系统默认浏览器打开，主窗口不跳转离开致心工作台。已按规则关闭旧桌面壳和中心服务进程后重启 `dev:desktop-shell`，并用 Chrome DevTools 与桌面端窗口验证思考正文、工具卡片、token 请求体和主窗口仍停留在致心工作台。
- [x] 修复本轮 `tool-runtime.ts` 单文件超过 1500 行回归：按现有中心服务工具边界拆出 `tool-runtime-command.ts` 和 `tool-runtime-mcp.ts`，保留 `tool-runtime.ts` 作为统一入口、工具注册表和 OpenAI 工具意图转换兼容导出；同步相关静态检查脚本扫描新模块，未改变命令工具和 MCP 工具业务逻辑。

验收口径：绿色版 zip 可独立运行；复制 `center-data` 可迁移数据；核心模块有明确验证方式。

