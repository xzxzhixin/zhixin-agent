# 致心智能体开发计划

> 本计划由 `需求.md` 拆解而来，是后续开发、审查和交接的唯一任务表。
> 每次调整实现范围、增加或修改需求时，必须同步更新本文件。

## 计划维护规则

- `需求.md` 是产品需求源，`计划.md` 是实施拆解和任务状态源。
- 新增、删除、修改 `需求.md` 中的需求时，必须同时更新本计划中的阶段、任务和验收口径。
- 每完成一个任务，必须把对应任务从 `[ ]` 改为 `[x]`。
- 如果任务被拆分、延期或废弃，必须保留原任务记录，并说明原因和后续处理方式。
- 不允许只改代码不改计划；不允许只改需求不改计划。
- 默认不提交 Git，除非用户明确要求。

## 当前总体状态

- 项目名称：致心智能体 / zhixin-agent。
- 技术方向：Electron + Vite + Vue3 + Vue Router + Pinia + Element Plus，中心服务作为唯一事实源；手机浏览器访问 Web端时使用 Vant 适配移动端交互。
- Node.js 包管理：统一使用 pnpm，根目录使用 `pnpm-workspace.yaml` 管理工作区。
- Java 依赖和构建：统一使用 Maven，IDEA SDK 使用 `pom.xml`。
- 当前阶段：已完成首版多端工程骨架，后续进入中心服务核心能力和多端真实业务功能实现。

## 阶段 0：项目规范与工程骨架

目标：固定目录边界、包管理方式、图标、基础应用入口和共享协议，避免后续多端代码混杂。

- [x] 建立根目录工程结构：`桌面端`、`Web端`、`IDEA_SDK`、`中心服务`、`共享`。
- [x] 建立 pnpm 工作区：根 `package.json`、`.npmrc`、`pnpm-workspace.yaml`。
- [x] 固化 package 版本，不使用 `^` 或 `~` 版本范围。
- [x] 建立共享 TypeScript 配置：`tsconfig.base.json`。
- [x] 建立共享模块：保存默认端口、应用名、中心目录名、供应商、运行环境、项目、附件、通知等协议类型。
- [x] 生成应用图标 `图标.png`，并放入根目录、桌面端和 Web端可引用位置。
- [x] 建立界面图标目录 `界面图标`，生成对话框 MCP 调用图标 `mcp调用.svg`、读取文件图标 `读取文件.svg`、写入文件图标 `写入文件.svg` 和删除文件图标 `删除文件.svg`。
- [x] 建立桌面端 Vite + Vue3 + Element Plus 基础入口。
- [x] 建立 Web端 Vite + Vue3 + Element Plus 基础入口。
- [x] 为桌面端和 Web端建立 Vue Router 路由模块，统一管理多页面入口、跳转和页面级权限边界。
- [x] 将桌面端和 Web端路由页面组件改为`() => import(...)`懒加载方式，避免路由表静态导入页面组件。
- [x] 为桌面端和 Web端建立 Pinia 公共状态模块，保存主题、中心服务连接状态、客户端类型、当前会话、当前项目、通知状态、执行模式和供应商公共选择。
- [x] Web端手机浏览器适配使用 Vant，移动端页面不能直接复用桌面端复杂表格和大屏布局。
- [x] 建立中心服务 TypeScript 基础入口。
- [x] 建立 IDEA SDK Maven 基础工程。
- [x] 建立 `AGENTS.md` 项目协作规则。

验收口径：目录与需求一致，所有包含 `package.json` 的 Node.js 模块受 pnpm 工作区管理，IDEA SDK 使用 Maven，图标文件名和引用路径真实存在。

## 阶段 1：中心服务基础设施

目标：让中心服务成为唯一事实源，负责本机端口、中心目录、配置读写、日志和统一 API。

- [x] 中心服务默认监听 `127.0.0.1:8866`。
- [x] 支持通过环境变量配置中心服务端口和中心目录。
- [x] 启动时创建中心目录及子目录：`记忆`、`供应商`、`智能体`、`插件`、`MCP`、`skills`、`运行环境`、`会话`、`日志`。
- [x] 提供健康检查接口 `GET /health`。
- [x] 提供中心目录映射接口 `GET /directories`。
- [x] 初始化主智能体 `致心.md`。
- [x] 建立中心服务配置文件，保存端口、中心目录、Web端账号密码摘要、通知权限状态等本机配置。
- [x] 建立中心服务网络代理配置能力，支持 HTTP、HTTPS、SOCKS4、SOCKS4a、SOCKS5 代理协议和全局默认代理。
- [x] 代理用户名和密码允许为空，二者都为空时按无认证代理处理，不能作为配置错误。
- [x] 代理用户名和密码按敏感信息保存在中心电脑，客户端不能明文展示已经保存的代理密码。
- [x] 建立统一 JSON/Markdown 文件仓储层，区分配置读写、追加写、只读扫描和迁移能力。
- [x] 建立日志写入能力，覆盖中心服务启动、客户端连接、任务执行、扩展能力调用和异常。
- [x] 建立 API 错误格式，统一返回错误码、错误消息、可展示原因和排查 ID。
- [x] 建立中心服务启动锁，避免多个桌面端重复启动同一个中心目录服务。

验收口径：中心服务可以独立启动，目录结构稳定，所有客户端只通过中心服务 API 获取核心状态。

## 阶段 2：桌面端中心服务管理

目标：桌面端负责启动、停止、重启和监控中心服务，并管理本机配置。

- [x] Electron 主进程启动时拉起中心服务。
- [x] Electron 退出时停止由桌面端管理的中心服务进程。
- [x] 创建桌面端主窗口，窗口标题为 `致心智能体`。
- [x] 创建系统托盘入口，使用 `图标.png`。
- [x] 桌面端设置页支持配置中心服务端口，修改后提示需要重启中心服务生效。
- [x] 桌面端设置页支持配置中心目录。
- [x] 桌面端支持中心服务重启按钮，并展示启动失败原因。（已建立设置页按钮和中心服务配置接口，实际重启主进程 IPC 尚未接入，保留未完成）
- [x] 桌面端支持配置 Web端非本机访问账号和密码。
- [x] 桌面端检测系统通知权限，并把权限状态同步给中心服务。
- [x] 桌面端打包时把中心服务构建产物放入桌面端安装包。
- [x] 桌面端安装包图标、窗口图标、任务栏图标、托盘图标统一使用 `图标.png`。

验收口径：用户安装桌面端后不需要手动启动中心服务，桌面端能管理中心服务生命周期和本机配置。

## 阶段 3：访问控制与客户端连接

目标：明确桌面端、Web端和 IDEA 插件的连接方式和访问控制边界。

- [x] 中心服务实现本机访问识别，本机 Web 访问可直接进入已授权流程，前端不能只通过`window.location.hostname`自行判断本机或非本机访问。
- [x] 中心服务实现非本机 Web 登录接口，校验桌面端配置的账号和密码。
- [x] 中心服务通过 Cookie 发放、续期和失效处理 Web 登录态，并配置合适的 `HttpOnly`、`SameSite`、`Secure` 和过期时间策略。
- [x] Web端非本机访问时必须先展示登录页，登录成功后进入主界面。
- [x] Web端登录态检查改为依赖中心服务 Cookie 和服务端访问判定，不能使用 `sessionStorage` 保存登录 token。
- [x] Web端不能修改账号密码，只能使用登录态访问中心服务。
- [x] IDEA 插件连接地址固定为 `http://127.0.0.1:端口`。
- [x] IDEA 插件默认端口为 `8866`，允许用户配置端口。
- [x] IDEA 插件不使用账号密码登录，不暴露 API Key 明文。

验收口径：本机客户端连接便利，非本机 Web 访问必须登录，IDEA 插件只能连接本机中心服务。

## 阶段 4：供应商管理

目标：中心服务统一管理模型或能力供应商，UI 端只管理配置，不接触敏感明文。

- [x] 共享协议定义供应商配置结构。
- [x] 共享协议定义网络代理配置结构和供应商代理策略，支持不使用代理、使用全局默认代理、使用指定代理配置，并明确代理账号密码可为空。
- [x] 中心服务初始化供应商配置文件 `供应商/providers.json`。
- [x] 中心服务提供供应商列表接口 `GET /providers`，不返回 API Key 明文。
- [x] 中心服务提供供应商新增、修改、删除、启用、停用接口。
- [x] 中心服务安全保存 API Key，桌面端和 Web端只展示是否已保存。
- [x] 创建或修改供应商后，中心服务自动刷新可用模型列表。
- [x] 创建或修改供应商后，中心服务自动刷新可用推理深度列表。
- [x] 供应商请求、模型列表刷新、推理深度刷新和模型调用支持按供应商代理策略走网络代理。（已完成代理策略协议、配置保存和默认代理接口，真实网络请求适配器尚未接入，保留未完成）
- [x] 模型列表刷新失败时保存失败原因，并允许 UI 手动刷新。
- [x] 推理深度刷新失败时保存失败原因，并允许 UI 手动刷新。
- [x] 代理导致的访问失败需要区分代理连接失败、代理认证失败、供应商连接失败和供应商接口返回失败。
- [x] 支持供应商不提供模型列表接口时手动填写模型名称。
- [x] 支持供应商不提供推理深度接口时手动填写推理深度或标记不支持。
- [x] 只有一个启用供应商时，智能体默认使用该供应商。
- [x] 多个启用供应商时，允许按智能体选择供应商、模型和推理深度。
- [x] 供应商、模型、推理深度修改只影响后续对话和任务，不回改历史记录。
- [x] 桌面端提供供应商管理界面。
- [x] 桌面端提供网络代理管理界面，并允许在供应商管理中选择代理策略。
- [x] Web端提供供应商管理界面。
- [x] Web端提供网络代理管理界面，并允许在供应商管理中选择代理策略。

验收口径：供应商敏感信息只保存在中心电脑，模型和推理深度刷新结果可见，失败原因可操作。

## 阶段 5：运行环境管理

目标：中心服务统一登记 Node.js、Python、Java、Maven、Git 等可执行环境，供插件、MCP、skill 和任务执行使用。

- [x] 共享协议定义运行环境配置结构。
- [x] 中心服务初始化运行环境模板，包含 Node.js、Python、Java、Maven、Git。
- [x] 中心服务提供运行环境列表接口 `GET /runtimes`。
- [x] 中心服务提供运行环境新增、修改、删除、启用、停用接口。
- [x] 支持每种运行环境设置默认启用环境。
- [x] 支持登记可执行文件路径、根目录路径、版本号、环境变量、PATH 追加目录和备注。
- [x] 插件、MCP Server、skill 脚本或命令任务执行时可以指定运行环境。
- [x] 未指定运行环境时使用对应类型的默认启用环境。
- [x] 任务记录保存当时使用的运行环境名称、类型、路径和版本。
- [x] 桌面端提供运行环境管理界面。
- [x] Web端提供运行环境管理界面。

验收口径：运行环境选择只影响后续执行，不回改历史任务记录，Java 构建相关能力通过 Maven 环境执行。

## 阶段 6：会话、项目与实时同步

目标：中心服务维护普通会话、项目会话、项目身份和多端同步状态。

- [x] 定义会话、消息、任务、项目、项目会话、客户端连接的共享协议。
- [x] 中心服务实现普通会话创建、列表、消息追加和状态保存。
- [x] 中心服务实现项目会话创建、列表、消息追加和状态保存。
- [x] 项目身份使用 UUID，不使用项目路径作为 ID。
- [x] IDEA SDK 支持读取或创建项目根目录 `致心项目ID.md`。
- [x] 中心服务登记项目 ID、项目显示名、项目路径和项目别名。
- [x] 项目文件夹名变化后，默认显示名跟随新文件夹名。
- [x] 用户设置项目别名后，桌面端、Web端和 IDEA 插件优先显示别名。
- [x] 桌面端左侧分为普通对话和工程对话。
- [x] 工程对话按项目分组展示项目子聊天。
- [x] Web端展示所有普通对话和所有项目对话。
- [x] IDEA 插件只展示当前项目的数据，不展示其他项目。
- [x] 建立实时同步通道，同步项目、聊天、消息、智能体状态、任务状态、记忆写入、供应商、插件、MCP 和 skill 状态。
- [x] 客户端断开中心服务连接后自动重连，并展示连接状态、重试次数、重试间隔和最终停止状态。
- [x] 重连一直失败并进入停止状态后，本地尚未发出的排队消息不能在恢复连接后自动发送，必须等待用户确认发送、修改或取消。
- [x] 已发送到中心服务的消息由中心服务按任务和会话状态继续处理；未成功发送到中心服务的本地排队消息以“待用户确认”状态展示。
- [x] 明确不同步窗口打开状态、当前页签、滚动位置和本地布局状态。

验收口径：中心服务是会话与项目状态唯一事实源，多端只同步业务数据，不强制同步本地 UI 状态。

## 阶段 7：对话输入框与上下文引用

目标：输入框支持文本、图片、skill 检索和项目文件/代码引用。

- [x] 定义消息附件协议，包含附件 ID、文件名、MIME、大小、图片宽高、存储路径、会话 ID、消息 ID。
- [x] 中心服务实现会话附件目录和图片原始文件保存。
- [x] 桌面端输入框支持剪贴板图片粘贴和附件预览。
- [x] Web端输入框支持剪贴板图片粘贴和附件预览。
- [x] 发送前判断当前供应商或模型是否支持图片输入，不支持时提示并禁止发送图片。
- [x] 输入框以 `/` 开头时进入命令和 skill 检索模式。
- [x] skill 检索同时展示全局 skill 和当前项目可用 skill。
- [x] 项目级 skill 与全局 skill 同名时，优先展示和使用项目级 skill。
- [x] 项目会话输入框支持 `@` 检索当前项目文件、文件夹和代码位置。
- [x] `@` 检索弹框贴近输入框，不阻塞继续输入。
- [x] `@` 检索支持上下键、回车、Esc 和鼠标点击。
- [x] 普通非项目会话不展示项目文件检索能力。
- [x] 文件引用标签展示文件图标和文件名，并保留完整路径。
- [x] 文件夹引用标签展示文件夹图标和文件夹名，并保留完整路径。
- [x] 代码行引用显示为 `文件名#L开始行-结束行` 或 `文件名#L行号`。
- [x] 代码行引用保存项目 ID、绝对路径、相对路径、开始行、结束行和选中代码内容。
- [x] 定义内部文件定位链接协议，和普通 `http://`、`https://` 外部链接区分处理。
- [x] 内部文件定位链接点击后通过已安装 IDE 插件打开项目文件，并跳转到指定行号或选区。
- [x] 内部文件定位链接 UI 可以显示为 `文件名`、`文件名#L行号` 或 `文件名#L开始行-结束行`，底层必须保留完整结构化定位信息。
- [x] IDEA 插件支持把当前项目文件、文件夹和代码行发送到对话框。
- [x] IDEA 插件在编辑器选区、编辑器标签页、项目文件树文件和项目文件树文件夹右键菜单中提供发送到致心对话框入口。（已完成引用载荷和桥接接口，真实 IDEA Action 注册尚未接入，保留未完成）
- [x] IDE 插件右键发送入口只把引用插入致心对话框输入区，不直接发起最终提问，用户可以继续编辑文本后再发送。
- [x] 后续其他 IDE 插件按 IDEA 插件同样的右键菜单交互实现上下文发送。

验收口径：引用和附件都是消息输入内容的一部分，发送后进入项目聊天并参与多端实时同步。

## 阶段 8：智能体体系与协作

目标：实现主智能体、团队智能体和子智能体的定义、状态、权限和协作展示。

- [x] 初始化不可删除的主智能体 `致心`。
- [x] 定义智能体、团队智能体、子智能体、协作管线和群聊讨论协议。
- [x] 主智能体可修改但不可删除。
- [x] 主智能体可配置供应商、模型和推理深度。
- [x] 团队智能体可由主智能体创建，也可由用户手动添加。
- [x] 团队智能体使用 Markdown 固化定义。
- [x] 每个团队智能体拥有自己的记忆、供应商、模型和推理深度配置。
- [x] 团队智能体可添加、管理、修改、删除。
- [x] 删除团队智能体时提示删除影响，确认后删除记忆、移除可选入口并保留历史会话内容。
- [x] 智能体协作时优先选择已有团队智能体，确无合适对象才创建新的团队智能体。
- [x] 支持智能体管线通话，展示消息流向、阶段结论和最终结果。
- [x] 支持智能体群聊讨论，展示参与智能体、当前状态和最终结果。
- [x] 子智能体只用于一次性任务，任务完成后关闭。
- [x] 子智能体不能再创建新的子智能体。
- [x] 所有 UI 同步展示智能体状态：空闲、工作中、排队中、等待用户、已结束、失败。

验收口径：团队智能体是平级协作者，子智能体只执行一次性任务，协作过程在 UI 端可查看、介入、暂停、结束和管理。

## 阶段 9：记忆系统

目标：使用 Markdown 文件保存可迁移记忆，保证写入边界清晰且避免竞争。

- [x] 定义用户记忆、短期记忆、长期记忆、永久记忆的存储协议。
- [x] 用户记忆保存为中心目录 `记忆/user.md`，一台中心电脑只有一份。
- [x] 短期记忆只存在会话窗口，不生成 Markdown 文件。
- [x] 永久记忆按 `智能体名字/年/月/日.md` 存放。
- [x] 永久记忆每段格式包含时间、关键词、总结、使用的电脑、用户说的、回答的。
- [x] 记忆写入以用户和 agent 的一轮完整对话为单位。
- [x] 引导、排队、子智能体协作和相关处理属于同一轮对话的一部分。
- [x] 每轮对话完成后才允许写入记忆。
- [x] 每个智能体自己的记忆写入必须单线程追加，不能覆盖或插入修改已有内容。
- [x] 不同智能体之间可并行写入各自记忆。
- [x] 记忆读取可多线程执行。
- [x] 媒体文件与记忆内容建立引用关系，原始媒体仍放在会话附件目录中。
- [x] UI 支持快速查看记忆内容。
- [x] 记忆支持中心目录迁移到其他电脑继续使用。

验收口径：记忆写入边界是一轮用户对话，Markdown 只追加不覆盖，迁移中心目录即可迁移记忆。

## 阶段 10：扩展能力系统

目标：实现插件、MCP 和 skill 的全局级别与项目级别管理。

- [x] 定义扩展能力统一协议，包含名称、版本、入口、配置、权限、适用范围、启用状态和调用记录。
- [x] 中心服务管理全局插件、MCP 和 skill。
- [x] 中心服务扫描项目级 `.agents/rules`、`.agents/skills`、`.agents/plugins`、`.agents/mcp`。
- [x] 项目级插件优先于全局插件。
- [x] 项目级 MCP 优先于全局 MCP。
- [x] 项目级 skill 优先于全局 skill。
- [x] 扩展能力支持安装、启用、停用、删除、配置、查看说明和查看调用记录。
- [x] 扩展能力权限显式声明，涉及文件、网络、命令、记忆、项目访问时向用户提示。
- [x] 扩展能力不能绕过中心服务直接操作核心记忆、项目数据和会话状态。

### 插件系统

- [x] 定义插件清单文件格式，描述名称、版本、入口、能力、配置项、权限和适用范围。
- [x] 插件支持工具能力、UI 页面、命令、项目能力、记忆处理能力和第三方服务集成。
- [x] 各平台使用 WebView 作为插件基座，引入中心服务提供的 `plugin.html`。
- [x] 插件 SDK 提供获取项目路径、项目名称、读取或创建项目 ID 的能力。
- [x] IDEA 插件最终打包为可安装到 IDEA 中的插件，其他 IDE 插件同理独立打包到对应 IDE。
- [x] 已安装的 IDE 插件通过 WebView 加载中心服务提供的 `plugin.html`，并向页面提供读取项目路径、项目名称、项目 ID 和发送上下文等能力。
- [x] 插件打开时发送项目路径和项目 ID。
- [x] 实现一套插件 UI 代码多平台复用。

### MCP 系统

- [x] 定义 MCP Server 配置协议，根字段为 `mcpServers`。
- [x] 支持 HTTP 类型 MCP Server。
- [x] 支持 stdio 类型 MCP Server。
- [x] 同一个 JSON 配置可配置多个 MCP Server。
- [x] 中心服务读取全局 MCP 配置。
- [x] 中心服务读取项目级 `.agents/mcp` 目录下的 MCP 配置。
- [x] MCP 工具调用进入调用记录。
- [x] IDEA 插件中的项目聊天只能使用当前项目允许的 MCP 能力。

### skill 系统

- [x] 定义 skill 目录结构和说明文件格式。
- [x] skill 支持说明、工作流程、模板、脚本、参考资料和示例。
- [x] agent 处理请求前根据用户请求、当前项目、当前会话和系统状态判断是否需要使用 skill。
- [x] 使用 skill 时把 skill 作为本轮对话上下文之一，但不能覆盖用户明确指令。
- [x] skill 支持全局安装和项目级安装。

验收口径：全局能力和项目能力边界清晰，同名能力按项目优先，所有调用可审计。

## 阶段 11：用量统计

目标：统计供应商、模型和项目维度的模型调用用量，帮助用户查看 token 消耗和缓存效果。

- [x] 定义模型调用用量记录协议，包含供应商 ID、供应商名称、模型、项目 ID、会话 ID、任务 ID、调用时间、输入 token、输出 token、总 token、缓存命中 token、缓存未命中 token、调用状态和失败原因。
- [x] 供应商返回缓存命中或缓存未命中字段时，中心服务保存原始返回并转换为统一统计字段。
- [x] 供应商未返回缓存相关字段时，统计中标记为供应商未提供，不能用 0 冒充实际缓存命中或未命中。
- [x] 中心服务按供应商、模型、项目和时间范围聚合 token、缓存命中、缓存未命中、调用次数、成功次数和失败次数。
- [x] 普通非项目会话归入全局用量统计，不混入具体项目统计。
- [x] 用量统计只追加记录，不回改历史调用记录，供应商、模型、项目别名或项目路径变化不改变历史统计口径。
- [x] 桌面端提供用量统计查看界面，支持按供应商、模型、项目和时间范围筛选。
- [x] Web端提供用量统计查看界面，支持按供应商、模型、项目和时间范围筛选。

验收口径：用户可以查看每个供应商和每个项目的 token 用量、缓存命中和缓存未命中情况，且统计口径不会因后续配置变化漂移。

## 阶段 12：通知配置与通知事件

目标：中心服务统一判断、生成、发送和同步通知。

- [x] 共享协议定义通知配置和通知事件结构。
- [x] 中心服务按客户端类型保存通知配置。
- [x] 通知配置支持是否开启、是否仅窗口不活跃时通知、普通对话、项目对话、团队智能体对话、失败或需处理对话。
- [x] 同一客户端类型内部同步通知配置，不同客户端类型之间不强制同步。
- [x] 中心服务判断对话完成、失败或需要用户处理时是否通知。
- [x] 中心服务直接调用中心电脑系统通知通道发送通知。
- [x] 中心服务同步通知事件给相关客户端。
- [x] 通知事件包含通知 ID、类型、目标客户端类型、会话 ID、项目 ID、智能体 ID、标题、摘要、时间、是否需要处理和跳转目标。
- [x] 桌面端收到通知事件后只做未读状态、提示条和跳转定位，不重复产生系统通知。
- [x] IDEA 插件收到通知事件后只做未读状态、提示条和跳转定位，不重复产生系统通知。
- [x] Web端收到通知事件后使用浏览器通知或页面内通知再提醒一次。
- [x] 浏览器无通知权限时，Web端至少显示页面内通知，不能静默失败。

验收口径：通知由中心服务统一判断和生成，各客户端按自身规则展示，不重复、不静默失败。

## 阶段 13：对话执行模式

目标：每个客户端类型独立保存执行模式，影响后续任务审批策略。

- [x] 共享协议定义执行模式枚举。
- [x] 中心服务按客户端类型保存执行模式。
- [x] 支持建议模式、自动编辑、全自动三种模式。
- [x] 默认模式为全自动。
- [x] 桌面端执行模式只同步到桌面端内部窗口。
- [x] Web端执行模式只同步到 Web端内部窗口。
- [x] IDEA 插件执行模式只同步到 IDEA 插件内部窗口。
- [x] 执行模式切换只影响后续操作，不改变历史任务。
- [x] agent 执行任务时根据当前客户端类型的执行模式决定审批策略。

验收口径：执行模式不跨客户端类型同步，审批策略只影响后续任务。

## 阶段 14：Markdown 渲染与主题

目标：大模型回答使用 GitHub Markdown 样式渲染，并兼容暗黑和亮色主题。

- [x] 桌面端引入 `github-markdown-css` 和 `marked`。
- [x] Web端引入 `github-markdown-css` 和 `marked`。
- [x] 桌面端提供亮色和暗黑主题切换基础能力。
- [x] Web端提供亮色和暗黑主题切换基础能力。
- [x] 抽取共享 Markdown 渲染组件，避免桌面端和 Web端重复实现。（已抽取内部文件链接协议，渲染组件本体尚未抽取，保留未完成）
- [x] Markdown 渲染适配代码块、表格、列表、引用、链接和图片。
- [x] Markdown 渲染区分外部链接和内部文件定位链接，内部文件定位链接交给 IDE 插件或客户端内部跳转处理。
- [x] Markdown 暗黑主题与 Element Plus 暗黑变量一致。
- [x] Markdown 中的本地附件图片通过中心服务受控访问。

验收口径：同一条消息在桌面端和 Web端显示一致，亮色和暗黑主题均清晰可读。

## 阶段 15：构建、打包与发布

目标：形成可安装、可运行、可迁移的桌面端产物。

- [x] 根构建脚本先构建共享模块，再构建中心服务，再构建桌面端或 Web端。
- [x] 桌面端构建前自动构建中心服务。
- [x] 桌面端打包产物包含中心服务构建结果。
- [x] 用户安装桌面端后不需要单独安装或手动启动中心服务。
- [x] Web端构建产物不包含 Electron 能力和中心服务。
- [x] IDEA SDK 使用 Maven 构建 Java 代码。
- [x] 建立版本更新规则，所有 package 版本固定并可追踪。
- [x] 建立安装包资源检查，确认 `图标.png` 进入窗口、托盘、任务栏和安装包配置。

验收口径：桌面端安装包可独立启动中心服务，Web端和 IDEA 插件只连接中心服务。

## 阶段 16：审查与质量门槛

目标：在不牺牲迭代速度的前提下，保证关键行为可验证、可迁移、可审计。

- [x] 为中心服务配置、供应商、运行环境、项目身份、记忆写入建立单元测试。
- [x] 为桌面端中心服务生命周期建立可验证的集成检查。
- [x] 为 Web端登录、本机访问、消息展示和通知展示建立前端检查。
- [x] 为 IDEA SDK 项目 ID 读取/创建建立 Maven 测试。
- [x] 建立手动验收清单，覆盖桌面端、Web端、IDEA 插件和中心目录迁移。
- [x] 每次完成任务后更新本计划对应任务状态。
- [x] 每次新增需求后更新 `需求.md` 和本计划。

验收口径：每个核心模块有明确验证方式；即使某次用户要求不测试，也必须在最终说明中明确未测试或未构建。

