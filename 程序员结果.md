## 主线程摘要

- 状态：需复测。
- 完成计划项：P05 启动反馈修复。
- 未完成计划项：无。
- 修改文件：`services/center/src/service.ts`、`本次测试计划.md`、`程序员结果.md`。
- 是否需要启动子代理刷新环境：是。
- 需要复测项：T02；同时需要启动子代理重新刷新环境后解除 T01 至 T10 浏览器端验证前置阻塞。
- 阻塞归属：无。
- 下一步：主线程派发启动子代理通过 `dev:desktop-shell` 重启或刷新桌面端宿主，复测 `http://127.0.0.1:8866/` 是否跳转到 `http://127.0.0.1:5173/?port=8866` 并可进入浏览器端测试。

## 修改详情

- 任务编号：P05 / T02 启动反馈修复。
- 修改文件：`services/center/src/service.ts`。
- 核心变更：中心服务开发期非 API 页面请求继续重定向到 Vite dev server，但 Fastify `reply.redirect` 调用改为 URL 参数在前、302 状态码在后。
- 关键边界处理：保留 `/api/*` 和 `/assets/*` 不重定向的既有逻辑，避免 API 调用和已构建静态资源被开发期页面跳转影响。
- 未处理项：未启动桌面端，未执行浏览器端复测。

## 计划同步

- 已勾选的本轮计划任务：无；`P05 修复 8866 前端资源刷新策略` 在本次修复前已处于完成态，本轮只处理启动反馈回归。
- 已同步勾选的正式计划任务：无；正式计划中对应 P05 已处于完成态，本轮不重复修改。
- 未获授权时建议勾选的任务行：无。
- 无法勾选时的原因：无。

## 测试反馈处理

- 涉及测试项编号：T02。
- 修复摘要：修复中心服务开发期前端重定向参数顺序，避免访问 `http://127.0.0.1:8866/` 时把 `http://127.0.0.1:5173/?port=8866` 当作非法状态码导致 500。
- 建议状态：已修复待复测。
- 需要复测的验证端：浏览器端。

## 风险与阻塞

- 当前阻塞项：无。
- 阻塞归属：无。
- 需要主线程派发的下一角色：启动子代理。

## 校验记录

- 已运行：IDE 错误检查 `services/center/src/service.ts`，通过，未发现错误。
- 未运行：未运行测试，未运行构建，未启动服务，未运行浏览器测试；原因是本任务要求不要运行未授权测试或构建，且不能直接启动 `services/center` 绕过桌面端启动规则。
