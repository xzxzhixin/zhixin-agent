# 致心智能体手动验收清单

## 桌面端

- 启动桌面端后，中心服务自动监听 `127.0.0.1:8866`。
- 点击设置页“重启中心服务”，失败时展示中心服务启动错误摘要。
- 托盘、窗口、任务栏和打包配置使用 `图标.png`。
- 粘贴图片后出现附件标签，当前供应商未声明支持图片时禁止发送。

## Web端

- 本机访问直接进入主界面，非本机访问先进入登录页。
- 登录态通过中心服务 Cookie 维持，刷新页面后仍由 `/auth/status` 判定。
- 浏览器通知无权限时，页面内展示通知提示。
- 手机浏览器进入 Vant 适配页面。

## IDEA 插件

- 打开项目时读取或创建 `致心项目ID.md`。
- 项目会话只请求当前项目 ID 下的数据。
- 编辑器选区、编辑器标签页、项目文件树文件和文件夹右键入口生成结构化引用载荷。
- WebView 统一加载中心服务的 `/plugin.html`。

## 中心目录迁移

- 迁移中心目录后，`记忆`、`供应商`、`智能体`、`插件`、`MCP`、`skills`、`运行环境`、`会话` 和 `日志` 结构保持一致。
- `记忆/user.md` 和永久记忆 Markdown 内容可读取。
- 会话附件目录中的原始媒体文件仍能通过中心服务附件接口访问。
