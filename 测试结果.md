# 测试结果

## 主线程摘要

- 状态：失败。
- 是否全部必测通过：否。
- 阻塞归属：程序员子代理。
- 需要派发给：程序员子代理。
- 涉及测试项编号：T01、T03、T04、T05、T09、T10。
- 关键证据：浏览器端真实对话发送后审计摘要出现 `model.failed`，错误为 `invokeProviderModelGateway is not defined`；项目对话新增入口无可见效果，无法验证项目对话删除；智能体管理页显示“中心服务暂未返回主智能体定义”；用量统计页无真实调用数据。T02、T06、T07、T08 已通过。
- 下一步：程序员子代理修复真实模型网关调用、项目对话创建/删除链路、主智能体返回和真实调用后的标题/用量落库后，启动子代理刷新环境，再交测试子代理复测未通过项。

## 测试详情

### T01 真实对话调用供应商

- 验证端：浏览器端。
- 使用工具：chrome-devtools。
- 当前状态：失败待修复。
- 最近一次测试结果：失败。已按 `AI账号.md` 将现有供应商切换为 OpenAI 兼容协议、测试账号地址、启用状态和脱敏 API Key；在 `/chat` 发送“浏览器真实调用测试：请只回复‘真实调用成功’。”后，任务状态为失败。
- 失败原因：页面审计摘要出现 `model.failed`，错误为 `invokeProviderModelGateway is not defined`；未产生供应商返回内容，仍不能证明真实模型调用链路可用。
- 复现步骤：进入 `/providers` 确认供应商已启用并保存测试账号；进入 `/chat` 发送短消息；观察右侧审计摘要和任务状态。
- 反馈对象：程序员子代理。
- 复测结果：未复测。
- 临时图片清理结果：未全部通过，未清理。

### T02 8866 前端资源刷新

- 验证端：浏览器端。
- 使用工具：chrome-devtools。
- 当前状态：通过。
- 最近一次测试结果：通过。打开 `http://127.0.0.1:8866/#/providers` 后进入开发期 `http://127.0.0.1:5173/?port=8866#/providers`，刷新后仍显示最新供应商页，控制台无资源加载错误。
- 失败原因：无。
- 复现步骤：从 8866 入口打开供应商页并刷新，确认跳转到 5173 HMR 页面且页面可渲染。
- 反馈对象：无。
- 复测结果：通过。
- 临时图片清理结果：未全部通过，未清理。

### T03 对话删除

- 验证端：浏览器端。
- 使用工具：chrome-devtools。
- 当前状态：失败待修复。
- 最近一次测试结果：部分通过。普通对话 hover 后出现删除按钮，删除刚创建的失败测试对话后，该对话从列表消失，刷新后未再显示该测试消息。
- 失败原因：项目对话列表为空；点击“新增项目对话”无可见效果，无法创建项目对话，也无法验证项目对话删除功能。验收标准要求普通对话和项目对话都有效。
- 复现步骤：进入 `/chat`；点击“新增项目对话”；观察项目对话区域仍显示“暂无项目对话”；无法继续执行项目对话删除验证。
- 反馈对象：程序员子代理。
- 复测结果：未复测。
- 临时图片清理结果：未全部通过，未清理。

### T04 标题总结

- 验证端：浏览器端。
- 使用工具：chrome-devtools。
- 当前状态：失败待修复。
- 最近一次测试结果：失败。真实对话 T01 未完成，发送测试消息后列表标题仍为“新的对话”，未出现与问题相关的摘要标题。
- 失败原因：模型调用失败，且会话标题没有从默认标题更新为摘要标题。
- 复现步骤：进入 `/chat` 发送主题明确的短消息；等待轮次结束；观察左侧会话标题仍为默认标题。
- 反馈对象：程序员子代理。
- 复测结果：未复测。
- 临时图片清理结果：未全部通过，未清理。

### T05 用量统计

- 验证端：浏览器端。
- 使用工具：chrome-devtools。
- 当前状态：失败待修复。
- 最近一次测试结果：失败。进入 `/usage` 后总量概览、供应商维度、项目维度均显示暂无真实模型调用用量。
- 失败原因：T01 真实供应商调用失败，未产生真实用量原始记录和聚合统计。
- 复现步骤：完成 T01 真实对话发送失败后进入 `/usage`，点击查询统计，观察页面仍为空数据。
- 反馈对象：程序员子代理。
- 复测结果：未复测。
- 临时图片清理结果：未全部通过，未清理。

### T06 顶部菜单激活样式

- 验证端：浏览器端。
- 使用工具：chrome-devtools。
- 当前状态：通过。
- 最近一次测试结果：通过。直接访问 `/providers`、`/proxies`、`/agent-management`、`/usage` 时，对应顶部菜单分别出现 `active` 样式；供应商页还带 `aria-current="page"`。
- 失败原因：无。
- 复现步骤：依次直接访问各管理路由，读取 `button.top-menu-item.active`。
- 反馈对象：无。
- 复测结果：通过。
- 临时图片清理结果：未全部通过，未清理。

### T07 供应商协议插件与模型能力

- 验证端：浏览器端。
- 使用工具：chrome-devtools。
- 当前状态：通过。
- 最近一次测试结果：通过。`/api/provider/protocol-plugin/list` 返回 OpenAI 兼容和 Anthropic Messages 两个内置协议插件；供应商页显示协议插件、协议模式、默认模型、手填模型与上下文、推理深度、能力声明和 API Key 新值字段；供应商列表仅显示 API Key 已保存，不回显明文。
- 失败原因：无。
- 复现步骤：进入 `/providers`，检查协议插件接口返回和供应商页表单、脱敏展示。
- 反馈对象：无。
- 复测结果：通过。
- 临时图片清理结果：未全部通过，未清理。

### T08 网络代理协议和认证字段

- 验证端：浏览器端。
- 使用工具：chrome-devtools。
- 当前状态：通过。
- 最近一次测试结果：通过。`/proxies` 协议下拉包含 HTTP、HTTPS、SOCKS4、SOCKS4a、SOCKS5；用户名和密码字段可见；创建无认证代理 `browser-test-noauth-1659` 后列表显示“无认证”，未泄露密码。
- 失败原因：无。
- 复现步骤：进入 `/proxies`，展开协议下拉；填写代理名称、主机、端口并保持用户名密码为空；保存后检查列表展示。
- 反馈对象：无。
- 复测结果：通过。
- 临时图片清理结果：未全部通过，未清理。

### T09 智能体管理

- 验证端：浏览器端。
- 使用工具：chrome-devtools。
- 当前状态：失败待修复。
- 最近一次测试结果：失败。`/agent-management` 可显示长期智能体创建表单和删除影响提示，但主智能体区域显示“中心服务暂未返回主智能体定义”。
- 失败原因：验收标准要求显示主智能体且不可删除；当前中心服务或前端未返回主智能体真实定义，不能证明主智能体不可删除约束。
- 复现步骤：进入 `/agent-management`，观察主智能体区域。
- 反馈对象：程序员子代理。
- 复测结果：未复测。
- 临时图片清理结果：未全部通过，未清理。

### T10 行数治理功能回归

- 验证端：浏览器端。
- 使用工具：chrome-devtools。
- 当前状态：失败待修复。
- 最近一次测试结果：失败。`/providers`、`/proxies`、`/usage`、`/agent-management` 可渲染，但多次路由切换出现 Vue 异常 `Unhandled error during execution of component update`；`/chat` 的真实对话链路失败，核心对话功能未通过。
- 失败原因：拆分后的页面仍有运行时异常和真实对话功能回归，不能判定页面功能回归未破坏对话、管理页和路由。
- 复现步骤：在测试专用页签依次访问 `/chat`、`/providers`、`/proxies`、`/usage`、`/agent-management`；观察控制台 Vue 异常和 T01 真实对话失败。
- 反馈对象：程序员子代理。
- 复测结果：未复测。
- 临时图片清理结果：未全部通过，未清理。
