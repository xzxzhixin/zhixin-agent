# 项目启动

## 主线程摘要

- 状态：完成。
- 是否可进入测试：是。
- 阻塞归属：无。
- 需要派发给：测试子代理。
- 涉及任务编号：P12。
- 关键证据：已停止旧 `dev:desktop-shell` 宿主树并确认 `5173`、`8866` 释放；随后通过 IDEA Run Configuration `dev:desktop-shell` 重新拉起宿主链路，`5173` 前端服务可访问，`8866` 中心服务健康，T09 事件接口已出现 `tool.command.output`。
- 下一步：执行浏览器端测试，测试 URL 使用 `http://127.0.0.1:5173/?port=8866#/chat`。

## 启动详情

- 启动方式：通过项目约定桌面宿主刷新环境，使用 IDEA Run Configuration `dev:desktop-shell` 重新拉起宿主链路。
- 使用的命令或 IDE Run Configuration：IDEA Run Configuration `dev:desktop-shell`，实际脚本为根目录 `npm run dev:desktop-shell` -> `node scripts/dev-desktop-shell.mjs` -> `pnpm --filter @zhixin/frontend dev -- --host 127.0.0.1 --strictPort` 与 `pnpm --filter @zhixin/desktop-shell dev`。
- 未使用的启动方式：未直接运行 `services/center`，未绕过桌面端或项目约定宿主。
- 实际访问 URL：`http://127.0.0.1:5173/?port=8866#/chat`。
- 服务是否由宿主入口拉起：是；新进程链为 IDEA PID 24504 -> `npm run dev:desktop-shell` PID 15724 -> `scripts/dev-desktop-shell.mjs` PID 15320 -> Electron 宿主 PID 24836 -> `services/center/node_modules/.bin/tsx.CMD` PID 5496 -> Node 中心服务 PID 19372。
- 本轮是否重启宿主入口：是。
- 本轮是否重新拉起服务：是；旧宿主根进程树 PID 6828 已停止，`5173` 与 `8866` 曾确认释放后重新启动。
- 重启原因：T09 需要运行中的 `8866` 覆盖 P09 新增 `tool.command.output` 事件；按项目约束只能通过桌面端或项目约定宿主刷新，不能直接运行 `services/center`。
- 可复用服务：刷新后 `5173` 和 `8866` 均健康可复用；未启动备用前端端口。
- 当前运行状态是否覆盖最新代码：是；`8866` 为刷新后的新 Node 进程 PID 19372，命令行通过 `tsx` 加载 `D:\CODE\project\self\zhixin-agent\services\center\src\index.ts`。
- 后续代码变更后是否需要重新启动：涉及中心服务、桌面壳、preload、Electron 主进程、静态资源托管、CORS、数据库迁移、中心服务装配或启动锁时需要通过宿主重新刷新。
- 前端端口复用决策：`5173` 已监听并返回 HTTP 200，按项目规则复用默认前端开发端口，不启动 `5174`、`5175` 或其他备用端口。
- 宿主旧实例关闭确认：已关闭旧宿主根进程树 PID 6828，随后确认 `5173` 与 `8866` 端口释放。
- 宿主新实例 PID：Electron PID 24836；宿主编排 Node PID 15320。
- 端口：`5173` 监听 PID 2344；`8866` 监听 PID 19372。
- 关键日志位置：`center-data/logs/desktop-center-runtime.log`、`center-data/logs/center.log`。
- 测试前置状态：可进入浏览器端测试；T09 所需 `tool.command.output` 已由刷新后宿主中心服务暴露。
- 停止方式：在 IDEA Run Configuration `dev:desktop-shell` 对应运行窗口停止宿主，或停止宿主编排根进程；不要直接叠加启动中心服务，不要使用 `dev:center:new` 绕过宿主。
- 当前阻塞项：无。

## 健康检查记录

- 检查时间：2026-06-04 16:56:06 +08:00。
- 环境来源：IDEA Run Configuration `dev:desktop-shell` 启动后，通过 IDEA Terminal 执行端口检查、HTTP 健康检查和宿主进程链确认；当前时间戳通过代理 shell 获取，仅用于记录。
- `5173` 监听：通过，`0.0.0.0:5173` Listen，PID 2344。
- `5173` HTTP 检查：通过，`GET http://127.0.0.1:5173/?port=8866#/chat` 返回 HTTP 200。
- `8866` 监听：通过，`127.0.0.1:8866` Listen，PID 19372。
- `8866` 健康检查：通过，`GET http://127.0.0.1:8866/api/health` 返回 `success: true`、端口 `8866`、中心目录 `D:\CODE\project\self\zhixin-agent\center-data`。
- 进程摘要：PID 2344 为 Vite 前端进程；PID 19372 为 Electron 宿主链路拉起的 `tsx services/center/src/index.ts` 中心服务进程。
- T09 事件覆盖检查：通过；浏览器发送 Node 版本命令后，`/api/session/event/list` 返回 `tool.command.started`、`tool.command.output`、`tool.command.completed`，输出摘要为 `v20.9.0`。
- 启动约束确认：未直接运行 `services/center` 绕过桌面端或项目约定宿主；未执行桌面端测试项；未执行 IDEA 插件端测试项；未读取、重置或更新 `浏览器测试窗口.md`。

## 已知验证状态

- 已通过：`GET http://127.0.0.1:5173/?port=8866#/chat` 返回 HTTP 200。
- 已通过：`GET http://127.0.0.1:8866/api/health` 返回 `success: true`。
- 已通过：`node scripts/check-dialog-agent-workflow-regression.mjs`。
- 已通过：`node scripts/check-chat-render-regression.mjs`。
- 已通过：`node scripts/check-current-plan-regressions.mjs`。
- 已通过：`node scripts/check-code-file-line-limit.mjs`。
- 未执行：桌面端测试项和 IDEA 插件端测试项，原因是用户明确本轮不执行。
- 未执行：`tsc --noEmit`、`vue-tsc` 或等价 TypeScript 编译器检查，原因是项目规则禁止把它们作为质量门槛。
- 当前结论：浏览器端测试可使用 `http://127.0.0.1:5173/?port=8866#/chat`。
