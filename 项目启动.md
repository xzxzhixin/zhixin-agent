# 项目启动

## 主线程摘要

- 状态：完成。
- 是否可进入测试：是。
- 阻塞归属：无。
- 需要派发给：测试子代理。
- 涉及任务编号：P05、T02；解除 T01 至 T10 浏览器端验证前置阻塞。
- 关键证据：已通过 IDE Run Configuration `dev:desktop-shell` 重启桌面端宿主；`5173` 与 `8866` 均监听；`GET http://127.0.0.1:8866/api/health` 返回 200；`GET http://127.0.0.1:8866/` 不再返回 500，跟随重定向后最终访问 `http://127.0.0.1:5173/?port=8866` 返回 200；Chrome DevTools 打开 `http://127.0.0.1:8866/` 后进入 `http://127.0.0.1:5173/?port=8866#/chat`，页面标题为“致心智能体”。
- 下一步：进入浏览器端测试阶段，测试子代理按 `本次测试计划.md` 串行执行 T01 至 T10。

## 启动详情

- 启动方式：IDE Run Configuration。
- 使用的命令或 IDEA Run Configuration：`dev:desktop-shell`。
- 实际访问 URL：
  - 中心服务 Web 入口：`http://127.0.0.1:8866/`。
  - 开发期实际页面：`http://127.0.0.1:5173/?port=8866#/chat`。
  - 前端开发服务：`http://127.0.0.1:5173/?port=8866#/chat`。
  - 中心服务健康接口：`http://127.0.0.1:8866/api/health`。
- 服务是否由宿主入口拉起：是，中心服务由 Electron 桌面壳启动链路拉起；本轮未直接运行 `services/center` 启动中心服务。
- 中心服务是否由桌面端拉起：是。
- 本轮是否重启宿主入口：是。
- 本轮是否重新拉起服务：是。
- 重启原因：程序员子代理完成 P05 启动反馈修复，变更中心服务开发期前端重定向逻辑；旧中心服务进程未加载该修复，必须通过桌面端宿主重启刷新运行环境。
- 可复用服务：无。旧宿主、旧前端和旧中心服务已关闭后重新启动。
- 当前运行状态是否覆盖最新代码：是。当前中心服务进程由重启后的桌面端宿主拉起，已覆盖 P05 修复；`8866/` 已从上轮 500 转为开发期 302 重定向并最终 200。
- 后续代码变更后是否需要重新启动：涉及中心服务、桌面壳、preload、静态资源托管、启动锁、数据库迁移、访问控制或 CORS 变更时需要重新启动；仅普通前端页面、样式、路由或交互变更可复用 `5173` 并刷新浏览器。
- 前端端口复用决策：复用项目默认前端端口 `5173`；旧实例释放后由 `dev:desktop-shell` 重新启动，没有启用备用端口。
- 宿主旧实例关闭确认：已关闭。旧 `dev:desktop-shell` 进程树通过 `taskkill /pid 16724 /t /f` 结束，随后 `5173` 和 `8866` 端口释放。
- 宿主新实例 PID：Electron 主进程 PID 为 `4076`；中心服务监听进程 PID 为 `31480`；前端 Vite 监听进程 PID 为 `24760`。
- 端口：
  - 前端端口：`5173`，监听地址 `0.0.0.0:5173`，`curl http://127.0.0.1:5173/?port=8866` 返回 200。
  - 中心服务端口：`8866`，监听地址 `127.0.0.1:8866`，`curl http://127.0.0.1:8866/api/health` 返回 200。
  - 宿主相关进程端口：Electron 桌面壳自身无独立 HTTP 端口，开发期加载 `http://127.0.0.1:5173?port=8866`。
- 关键日志位置：
  - `center-data/logs/desktop-center-runtime.log`，最后更新时间为 `2026-06-02 16:50:52`。
  - `center-data/logs/center.log`，最后更新时间为 `2026-06-02 16:50:53`。
- 测试前置状态：已满足。浏览器端可从 `http://127.0.0.1:8866/` 进入开发期页面，当前实际地址为 `http://127.0.0.1:5173/?port=8866#/chat`；可交给测试子代理执行浏览器端验证。
- 停止方式：
  - 优先在 IDEA Run Configuration `dev:desktop-shell` 中停止。
  - 如需命令行强制停止，可结束本轮新宿主进程树：`taskkill /pid 4076 /t /f`；如需从脚本根进程停止，可按 IDE 终端中的 `dev:desktop-shell` 运行进程树处理。
- 当前阻塞项：无。
