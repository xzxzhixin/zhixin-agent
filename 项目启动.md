# 项目启动

## 主线程摘要

- 状态：完成。
- 是否可进入测试：是。
- 阻塞归属：无。
- 需要派发给：测试子代理。
- 涉及任务编号：启动准备接力、P12。
- 关键证据：已停止本轮修改前存在的旧 `dev:desktop-shell` 宿主进程树，并通过 IDEA Run Configuration `dev:desktop-shell` 重新拉起宿主；`5173` 前端服务可访问，`8866` 中心服务健康；`8866` 进程链显示由 Electron 宿主拉起中心服务，未直接运行 `services/center` 绕过宿主。
- 下一步：执行浏览器端测试，不执行桌面端测试项，不执行 IDEA 插件端测试项。

## 启动详情

- 启动方式：通过项目约定桌面宿主刷新环境，使用 IDEA Run Configuration `dev:desktop-shell` 重新拉起宿主链路。
- 使用的命令或 IDE Run Configuration：IDEA Run Configuration `dev:desktop-shell`，实际脚本为根目录 `npm run dev:desktop-shell` -> `node scripts/dev-desktop-shell.mjs` -> `pnpm --filter @zhixin/frontend dev -- --host 127.0.0.1 --strictPort` 与 `pnpm --filter @zhixin/desktop-shell dev`。
- 未使用的启动方式：未执行 IDEA Run Configuration `dev:center:new`，未直接运行 `services/center`，未绕过桌面端或项目约定宿主。
- 实际访问 URL：`http://127.0.0.1:5173`、`http://127.0.0.1:5173/?port=8866#/chat`、`http://127.0.0.1:8866`。
- 服务是否由宿主入口拉起：是；新进程链为 IDEA PID 24504 -> `npm run dev:desktop-shell` PID 6828 -> `scripts/dev-desktop-shell.mjs` PID 2816 -> Electron 宿主 PID 10708 -> `services/center/node_modules/.bin/tsx.CMD` PID 19412 -> Node 中心服务 PID 34724。
- 本轮是否重启宿主入口：是。
- 本轮是否重新拉起服务：是；旧宿主根进程树 PID 13344 已停止，`5173` 与 `8866` 曾确认释放后重新启动。
- 重启原因：`项目启动.md` 原记录说明 `8866` 是本轮修改前已存在的长驻进程；浏览器端测试需要使用最新中心服务源码，因此通过宿主入口刷新。
- 可复用服务：刷新后 `5173` 和 `8866` 均健康可复用；未启动备用前端端口。
- 当前运行状态是否覆盖最新代码：是；`8866` 为刷新后的新 Node 进程 PID 34724，命令行通过 `tsx` 加载 `D:\CODE\project\self\zhixin-agent\services\center\src\index.ts`。
- 后续代码变更后是否需要重新启动：涉及中心服务、桌面壳、preload、Electron 主进程、静态资源托管、CORS、数据库迁移、中心服务装配或启动锁时需要通过宿主重新刷新。
- 前端端口复用决策：刷新前健康 `5173` 属于旧宿主编排子进程；为刷新中心服务源码，停止旧宿主树时该端口随树释放，随后由同一 `dev:desktop-shell` 宿主配置重新拉起 `5173`，未使用 `5174`、`5175` 或其他备用端口。
- 宿主旧实例关闭确认：已关闭旧宿主根进程树 PID 13344，随后确认 `5173` 与 `8866` 端口释放。
- 宿主新实例 PID：Electron PID 10708；宿主编排 Node PID 2816。
- 端口：`5173` 监听 PID 25568；`8866` 监听 PID 34724。
- 关键日志位置：`center-data/logs/desktop-center-runtime.log`、`center-data/logs/center.log`。
- 关键日志摘要：`desktop-center-runtime.log` 在 `2026-06-04T03:19:29.080Z` 记录通过 `services/center/node_modules/.bin/tsx.CMD` 拉起 `services/center/src/index.ts`，端口 `8866`，中心目录 `D:\CODE\project\self\zhixin-agent\center-data`；`center.log` 在 `2026-06-04T03:19:33.972Z` 记录 `center.server.listening`，端口 `8866`。
- 测试前置状态：可进入浏览器端测试。
- 停止方式：在 IDEA Run Configuration `dev:desktop-shell` 对应运行窗口停止宿主，或停止宿主编排根进程；不要直接叠加启动中心服务，不要使用 `dev:center:new` 绕过宿主。
- 当前阻塞项：无。

## 健康检查记录

- 检查时间：2026-06-04 11:20:02 +08:00。
- 环境来源：IDE Run Configuration `dev:desktop-shell` 启动后，通过 IDEA Terminal 执行端口检查、HTTP 健康检查和宿主进程链确认。
- `5173` 监听：通过，`0.0.0.0:5173` Listen，PID 25568。
- `5173` HTTP 检查：通过，`GET http://127.0.0.1:5173` 返回 HTTP 200。
- `8866` 监听：通过，`127.0.0.1:8866` Listen，PID 34724。
- `8866` 健康检查：通过，`GET http://127.0.0.1:8866/api/health` 返回 `success: true`、端口 `8866`、中心目录 `D:\CODE\project\self\zhixin-agent\center-data`。
- `8866` bootstrap 检查：通过，`GET http://127.0.0.1:8866/api/bootstrap/state` 返回 `success: true`、`ready: true`，核心表包含 `projects`、`sessions`、`messages`、`conversation_turns`、`tasks`、`task_steps`、`events`、`pending_messages` 等。
- `8866` 项目列表检查：通过，`POST http://127.0.0.1:8866/api/project/list` 返回 `success: true`。
- 宿主来源证据：PID 34724 的父进程链最终归属 Electron 宿主 PID 10708；`desktop-center-runtime.log` 记录该宿主通过 `services/center/node_modules/.bin/tsx.CMD` 拉起 `services/center/src/index.ts`。
- 启动约束确认：未直接运行 `services/center` 绕过桌面端或项目约定宿主；未执行桌面端测试项；未执行 IDEA 插件端测试项；未读取、重置或更新 `浏览器测试窗口.md`。

## 已知验证状态

- 已通过：IDE Run Configuration `dev:desktop-shell` 可启动并保持运行；启动命令超时是因为开发服务为长驻进程，不代表失败。
- 已通过：`GET http://127.0.0.1:5173` 返回 HTTP 200。
- 已通过：`GET http://127.0.0.1:8866/api/health` 返回 `success: true`。
- 已通过：`GET http://127.0.0.1:8866/api/bootstrap/state` 返回 `success: true`、`ready: true`。
- 已通过：`POST http://127.0.0.1:8866/api/project/list` 返回 `success: true`。
- 已通过：新 `8866` 进程 PID 34724 的父链归属 Electron 宿主 PID 10708。
- 未执行：`tsc --noEmit`、`vue-tsc` 或等价 TypeScript 编译器检查，原因是项目规则禁止把它们作为质量门槛。
- 未执行：桌面端测试项和 IDEA 插件端测试项，原因是本轮最终测试范围只包含浏览器端。
- 当前结论：浏览器端测试可使用 `http://127.0.0.1:5173/?port=8866#/chat` 和刷新后的宿主中心服务 `http://127.0.0.1:8866`。
