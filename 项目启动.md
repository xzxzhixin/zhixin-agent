# 项目启动

## 主线程摘要

- 状态：完成。
- 是否可进入测试：是。
- 阻塞归属：无。
- 需要派发给：测试子代理。
- 涉及任务编号：启动刷新；本轮浏览器端验证前置环境。
- 关键证据：已关闭旧桌面宿主开发进程树并释放旧 `5173`、`8866`；随后通过 IDEA Run Configuration `dev:desktop-shell` 重新启动项目约定宿主入口。新环境中 `5173` 已监听并返回 `200`，页面包含 `#app`；`8866` 已监听并且 `GET http://127.0.0.1:8866/api/health` 返回 `200`。进程链显示中心服务由 Electron 桌面壳子进程通过 `services/center/node_modules/.bin/tsx.CMD services/center/src/index.ts` 拉起，未直接运行 `services/center` 绕过宿主。
- 下一步：测试子代理按本轮限定范围执行浏览器端验证；不执行桌面端测试项，不执行 IDEA 插件端测试项。

## 启动详情

- 启动方式：通过项目约定宿主入口刷新；使用 Electron 桌面壳拉起中心服务。
- 使用的命令或 IDE Run Configuration：IDEA Run Configuration `dev:desktop-shell`，根脚本为 `node scripts/dev-desktop-shell.mjs`。
- 实际访问 URL：前端 `http://127.0.0.1:5173/?port=8866#/chat`；中心服务 `http://127.0.0.1:8866`；健康检查 `http://127.0.0.1:8866/api/health`。
- 服务是否由宿主入口拉起：是。中心服务进程链为 Electron 桌面壳 `electron.exe` 子进程 `cmd.exe /c services/center/node_modules/.bin/tsx.CMD services/center/src/index.ts`，再派生 `tsx` 和 Node 运行进程。
- 本轮是否重启宿主入口：是。
- 本轮是否重新拉起服务：是。
- 重启原因：服务；程序员子代理已修复中心服务源码语法阻塞，需要刷新宿主拉起的浏览器端复测环境。
- 可复用服务：前端开发服务 `5173` 已由本轮宿主入口重新拉起并健康；中心服务 `8866` 已由本轮宿主入口重新拉起并健康。
- 当前运行状态是否覆盖最新代码：是。前端由 Vite 开发服务提供，中心服务由宿主入口通过 `services/center/src/index.ts` 源码入口拉起。
- 后续代码变更后是否需要重新启动：涉及中心服务、桌面壳、preload、静态资源托管、CORS、数据库迁移、中心服务装配或启动锁时需要重新通过宿主入口刷新；普通 Vue 页面、样式、路由或前端交互变更可优先复用 `5173` 并刷新浏览器。
- 前端端口复用决策：使用项目默认端口 `5173`；未启动 `5174`、`5175` 或其他备用端口。
- 宿主旧实例关闭确认：已关闭旧桌面宿主开发进程树；旧 `5173`、旧 `8866` 监听释放后才启动新环境。
- 宿主新实例 PID：Electron 主宿主 PID `7156`；Electron 启动命令包装进程 PID `5112`。
- 端口：前端端口 `5173`，监听 PID `18104`；中心服务端口 `8866`，监听 PID `14764`。
- 关键日志位置：`center-data/logs/desktop-center-runtime.log`；IDEA Run Configuration `dev:desktop-shell` 输出窗口。
- 测试前置状态：可进入浏览器端复测。
- 停止方式：在 IDEA Run Configuration 或 IDE 终端停止 `dev:desktop-shell`；若残留开发进程，按宿主进程树停止，避免叠加启动。
- 当前阻塞项：无。

## 健康检查记录

- 检查时间：2026-06-03 17:45:00 +08:00。
- 环境来源：IDEA Run Configuration `dev:desktop-shell` 与本轮 PowerShell 端口/HTTP 检查。
- 旧实例处理：启动前发现旧 `5173` 监听 PID `31716`，以及旧 Electron 宿主进程 `10548`、`17984`、`24056`、`33176` 和包装进程 `30400` 等仍指向当前项目；已按进程树停止旧宿主和旧 Vite，随后确认 `5173`、`8866` 无旧监听。
- 新启动命令：通过 IDEA Run Configuration `dev:desktop-shell` 执行 `node scripts/dev-desktop-shell.mjs`；脚本先启动 Vite，再启动 Electron 桌面壳；Electron 桌面壳以 `services/center/src/index.ts` 为入口拉起中心服务。
- `5173` 监听：`0.0.0.0:5173`，PID `18104`，进程名 `node.exe`。
- `5173` HTTP 检查：GET `http://127.0.0.1:5173/?port=8866#/chat` 返回 `200`，响应包含 `id="app"`。
- `8866` 监听：`127.0.0.1:8866`，PID `14764`，进程名 `node.exe`。
- `8866` 健康检查：GET `http://127.0.0.1:8866/api/health` 返回 `200`，响应 `success=true`，端口 `8866`，中心目录 `D:\CODE\project\self\zhixin-agent\center-data`。
- `8866` 前端资源策略检查：GET `http://127.0.0.1:8866/` 返回 `302`，跳转到 `http://127.0.0.1:5173/?port=8866`，符合开发期由中心服务清晰复用 `5173` HMR 的约定。
- 宿主来源证据：`center-data/logs/desktop-center-runtime.log` 记录 `2026-06-03T09:43:30.092Z` 启动中心服务命令，入口为 `D:\CODE\project\self\zhixin-agent\services\center\src\index.ts`，端口 `8866`，中心目录 `D:\CODE\project\self\zhixin-agent\center-data`。
- 进程链证据：Electron 主宿主 PID `7156` 派生 `cmd.exe` PID `24940`，命令为 `services\center\node_modules\.bin\tsx.CMD services\center\src\index.ts`；后续派生 `tsx` PID `32868` 和中心服务监听 PID `14764`。
- 启动约束确认：本轮只准备浏览器端复测环境；未执行桌面端测试项；未执行 IDEA 插件端测试项；未直接运行 `services/center`；未启动备用前端端口；未修改业务代码；未修改 `需求.md`、`架构.md`、`计划.md`、`本次计划.md`、`本次测试计划.md`、`程序员结果.md`、`测试结果.md`、`浏览器测试窗口.md`；只覆盖更新 `项目启动.md`。
