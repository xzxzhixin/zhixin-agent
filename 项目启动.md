# 项目启动

## 本轮范围

- 检查时间：2026-06-01。
- 本轮场景：根据 `本次计划.md` 和当前修复结果刷新全量验证运行环境。
- 本轮修复背景：已修改 `apps/desktop-shell/src/main.ts`，新增开发期 Electron 会话缓存清理和窗口实际加载 URL / 页内导航日志，旧 Electron 桌面端实例不能作为复测依据。
- 本轮限制：不修改业务代码，不提交，不执行 `git add`，不删除文件，不清理数据库，不直接运行 `services/center`。
- 启动原则：需要中心服务参与测试时，只能启动桌面端，由桌面端拉起中心服务。

## 启动方式

- 优先方式：IDEA Run Configuration。
- 实际使用配置：`dev:desktop-shell`。
- 配置类型：npm 运行配置。
- 根脚本：`package.json` 的 `dev:desktop-shell`，内容为 `node scripts/dev-desktop-shell.mjs`。
- 启动编排：脚本先启动前端 Vite 开发服务，再启动 Electron 桌面壳。
- 桌面端前端地址：脚本注入 `ZHIXIN_FRONTEND_DEV_URL=http://127.0.0.1:5173`。
- 中心服务拉起方式：桌面壳主进程拉起中心服务；本轮没有直接执行 `services/center`。
- 本轮处理方式：首次启动发现旧链路占用 `5173/8866`，新实例退到 `5174` 并复用旧中心服务；随后只停止当前仓库路径下旧 `dev:desktop-shell` 相关进程，再通过 IDEA `dev:desktop-shell` 重新启动。
- 中心服务拉起证据：`8866` 监听进程 `1100` 的父链为 `1100 node.exe` <- `7964 node.exe tsx` <- `4988 cmd.exe` <- `20328 electron.exe .` <- `680 node.exe electron cli` <- `4244 cmd.exe electron .` <- `20424 pnpm --filter @zhixin/desktop-shell dev` <- `13880 node scripts/dev-desktop-shell.mjs` <- IDEA `idea64.exe`。

## 当前服务状态

- 前端 Vite：可访问，`http://127.0.0.1:5173` 返回 HTTP 200。
- 桌面端：已通过 IDEA `dev:desktop-shell` 重新启动，运行配置仍在 IDEA 运行窗口中。
- 桌面窗口：Windows 桌面工具确认“致心智能体”窗口已打开并处于最大化状态。
- 中心服务：可访问，`http://127.0.0.1:8866/api/health` 返回 HTTP 200。
- 健康检查结果：响应包含 `appName` 为“致心智能体”、`version` 为 `0.1.0`、`port` 为 `8866`、`centerDirectory` 为 `C:\CODE\privateProject\zhixin-agent\center-data`。
- 中心服务是否由桌面端拉起：是，进程父链显示由本轮新启动的 Electron 桌面端拉起。
- 本轮是否重启桌面端：是。
- 本轮是否重新拉起中心服务：是，由桌面端重新拉起。
- 本轮重启原因：Electron 主进程新增开发期会话缓存清理和窗口实际加载 URL / 页内导航日志；该类修改必须重启桌面端才能进入复测基线。
- 当前运行状态是否覆盖最新代码：
  - 前端：`5173` 来自当前仓库的 Vite 进程，可通过 HMR 或浏览器刷新获取前端源码更新。
  - 桌面壳：本轮已通过 IDEA Run Configuration 重新启动，覆盖当前 `apps/desktop-shell/src/main.ts`。
  - 中心服务：`8866` 运行进程命令行指向当前仓库 `services/center/src/index.ts`，并由桌面端拉起。
- 后续代码变更后是否需要重新启动：
  - 只改前端页面、样式、路由或交互时，可复用 Vite 服务并刷新浏览器。
  - 改桌面壳、preload、Electron 主进程、中心服务启动链路、中心服务代码、静态资源托管、CORS 或数据库迁移时，需要重新启动 `dev:desktop-shell`。

## 访问信息

- 浏览器验证首选 URL：`http://127.0.0.1:5173`
- 桌面端加载的前端开发 URL：`http://127.0.0.1:5173`
- 中心服务 Web URL：`http://127.0.0.1:8866`
- 中心服务健康检查 URL：`http://127.0.0.1:8866/api/health`
- 中心服务插件入口 URL：`http://127.0.0.1:8866/plugin.html`
- 端口状态：
  - `5173`：已监听，HTTP 200，监听进程 `25372`。
  - `8866`：已监听，健康检查 HTTP 200，监听进程 `1100`。

## 关键日志位置

- 桌面壳和 Vite 输出：IDEA `dev:desktop-shell` 运行窗口。
- 中心服务运行日志：`center-data/logs/desktop-center-runtime.log`。
- 当前中心服务启动日志：`[2026-06-01T13:02:24.531Z] start command=...services\center\node_modules\.bin\tsx.CMD; node=C:\CODE\node\node.exe; entry=...\services\center\src\index.ts; port=8866; centerDir=...\center-data`。
- Vite 加载日志证据：`[2026-06-01T13:02:24.727Z] window-load-url http://127.0.0.1:5173?port=8866`。
- 页内导航日志证据：`[2026-06-01T13:02:25.070Z] window-did-navigate-in-page http://127.0.0.1:5173/?port=8866#/chat`。

## 测试前置状态

- 浏览器端页面验证可以使用 `http://127.0.0.1:5173` 开始。
- 依赖中心服务接口的验证可以使用 `http://127.0.0.1:8866` 和 `http://127.0.0.1:8866/api/health`。
- IDE 插件或插件紧凑入口验证可使用 `http://127.0.0.1:8866/plugin.html`。
- 本轮已刷新 Electron 桌面端和由桌面端管理的中心服务，可以作为后续浏览器测试和桌面测试的复测基线。
- 本轮已确认桌面端实际加载 Vite 地址 `http://127.0.0.1:5173?port=8866`，不是旧 `5174` 退避实例。
- 本轮未运行构建，不能用构建产物证明 Web dist 边界。
- 本轮未直接启动 `services/center`，符合启动约束。

## 停止方式

- 首选停止方式：在 IDEA Run 窗口停止 `dev:desktop-shell`。
- 桌面窗口停止方式：关闭 Electron 桌面窗口，并按关闭确认选择退出或隐藏到托盘。
- 预期收尾：停止桌面壳后，由桌面壳和启动脚本回收其管理的中心服务与子进程。
- 不建议单独结束 `1100` 中心服务进程；如需重启中心服务，应停止并重启 `dev:desktop-shell`。

## 阻塞项

- 当前启动刷新无阻塞项。
- 未执行构建，构建产物相关验证仍需后续单独执行。
- 若后续测试修改中心服务或 Electron 相关代码，当前运行状态会过期，需要重新通过 `dev:desktop-shell` 刷新启动环境。
