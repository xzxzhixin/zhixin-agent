# 项目启动

## 当前状态

- 检查时间：2026-06-01。
- 启动约束：本轮只能通过桌面端链路让中心服务参与测试，不能直接运行 `services/center`。
- 结论：前端 Vite 开发服务可复用，桌面壳进程已在运行，但中心服务未成功进入 `8866` 监听。

## 启动方式

- 使用的方式：`dev:desktop-shell`。
- 运行配置：IDEA 只识别到 `dev:desktop-shell`，类型为 npm 运行配置，指向根 `package.json` 的 `dev:desktop-shell` 脚本。
- 根脚本：`package.json` 中 `dev:desktop-shell` 为 `node scripts/dev-desktop-shell.mjs`。
- 编排逻辑：`scripts/dev-desktop-shell.mjs` 先启动前端 `@zhixin/frontend dev`，再启动 `@zhixin/desktop-shell dev`，并注入 `ZHIXIN_FRONTEND_DEV_URL=http://127.0.0.1:5173`。
- 桌面壳入口：`apps/desktop-shell/package.json` 的 `dev` 为 `electron .`，由 `scripts/electron-main.mjs` 进入 `apps/desktop-shell/src/main.ts`。
- 中心服务拉起方式：由桌面壳主进程调用 `startCenterService()` 间接拉起，不直接运行 `services/center`。

## 当前状态

- 可复用前端服务：是。
- 可复用桌面壳：是，当前已有 `electron.exe` 和 `node scripts/dev-desktop-shell.mjs` 相关进程在跑。
- 可复用中心服务：否，`8866` 未监听。
- 中心服务是否由桌面端拉起：已尝试由桌面端拉起，但未成功进入可用状态。
- 本轮是否重启桌面端：是，已重新发起 `dev:desktop-shell`。
- 本轮是否重新拉起中心服务：是，桌面壳内部已尝试重新拉起。
- 当前运行状态是否覆盖最新代码：前端覆盖到最新开发服务；中心服务未覆盖到可用状态。
- 后续代码变更后是否需要重新启动：涉及前端页面变更可复用前端服务；涉及桌面壳、preload、主进程或中心服务代码时，需要重启桌面端链路。

## 访问信息

- 前端访问 URL：`http://127.0.0.1:5173`
- 中心服务访问 URL：`http://127.0.0.1:8866`
- 前端端口：`5173`
- 中心服务端口：`8866`

## 关键日志位置

- `center-data/logs/desktop-center-runtime.log`
- 桌面壳标准输出 / 标准错误输出

## 测试前置状态

- 前端：可访问，`5173` 已监听。
- 桌面壳：已启动，可复用。
- 中心服务：不可用，`8866` 未监听。
- 直接验证限制：本轮不得直接运行 `services/center`。

## 停止方式

- 通过退出 `dev:desktop-shell` 对应进程或关闭 Electron 窗口停止桌面壳。
- 桌面壳退出后，应一并停止它管理的中心服务进程。

## 阻塞项

- 中心服务启动失败，`8866` 未监听。
- 日志显示 `better-sqlite3` 原生绑定未正确加载，中心服务初始化数据库时退出。
- 当前环境下 `dev:desktop-shell` 运行配置直接报 `pnpm` 不可用，IDE 运行链路与外部终端实际进程状态不一致，需要以可复用的桌面壳进程和前端监听结果为准。

## 浏览器端验证结果

- 验证方式：浏览器测试子代理使用 chrome-devtools 验证前端开发服务。
- 已验证 URL：`#/chat`、`#/agent-management`、`#/providers`、`#/proxies`、`#/runtimes`、`#/usage`、`#/plugins`、`#/mcp`、`#/skills`、`#/center`、`#/login`。
- 可见结果：上述页面均可直达且非白屏，页面内容不是纯空壳入口；顶部菜单切换会同步 hash URL；`/chat` 执行模式下拉可见审批语义说明。
- 失败结果：控制台仍有 `http://127.0.0.1:8866` 连接被拒绝，管理页只能展示壳层与错误提示，不能完成真实中心服务数据联调。
- 风险处理：`MainView.vue` 的 `<router-view>` 已增加 `route.fullPath` key，降低路由切换时旧页面实例复用导致内容错位的风险。
