# 项目启动

## 本轮范围

- 检查时间：2026-06-01。
- 本轮场景：只执行浏览器端验证准备。
- 本轮限制：不创建桌面测试子代理，不运行构建，不直接运行 `services/center`。
- 启动原则：需要中心服务参与时，只能启动桌面端，由桌面端拉起中心服务。

## 启动方式

- 优先方式：IDEA Run Configuration。
- 实际使用配置：`dev:desktop-shell`。
- 配置类型：npm 运行配置。
- 根脚本：`package.json` 的 `dev:desktop-shell`，内容为 `node scripts/dev-desktop-shell.mjs`。
- 启动编排：脚本先启动前端 Vite 开发服务，再启动 Electron 桌面壳。
- 桌面端前端地址：脚本注入 `ZHIXIN_FRONTEND_DEV_URL=http://127.0.0.1:5173`。
- 中心服务拉起方式：桌面壳主进程调用中心服务启动逻辑拉起，未直接执行 `services/center`。

## 当前服务状态

- 前端 Vite：可访问。
- 桌面端：已通过 `dev:desktop-shell` 启动，运行配置仍在 IDEA 运行窗口中。
- 中心服务：不可访问，`8866` 未监听。
- 中心服务是否由桌面端拉起：是，桌面端已尝试拉起中心服务。
- 本轮是否重启桌面端：是。
- 本轮是否重新拉起中心服务：是，由桌面端尝试拉起。
- 本轮是否覆盖最新代码：
  - 前端：`5173` 和 `5174` 均来自当前仓库的 Vite 进程；浏览器验证优先使用 `5173`，可通过刷新获取当前前端源码更新。
  - 桌面壳：本轮已通过 IDEA Run Configuration 重新启动。
  - 中心服务：未进入可用状态，不能确认运行中服务覆盖最新代码。
- 后续代码变更后是否需要重新启动：
  - 只改前端页面、样式、路由或交互时，可复用 Vite 服务并刷新浏览器。
  - 改桌面壳、preload、Electron 主进程、中心服务启动链路或中心服务代码时，需要重新启动 `dev:desktop-shell`。

## 访问信息

- 浏览器验证首选 URL：`http://127.0.0.1:5173`
- 本轮额外 Vite URL：`http://127.0.0.1:5174`
- 中心服务 URL：`http://127.0.0.1:8866`
- 前端端口状态：
  - `5173`：已监听，HTTP 200。
  - `5174`：已监听，HTTP 200。
- 中心服务端口状态：
  - `8866`：未监听，连接被拒绝。

## 关键日志位置

- 桌面壳和 Vite 输出：IDEA `dev:desktop-shell` 运行窗口。
- 中心服务运行日志：`center-data/logs/desktop-center-runtime.log`。

## 测试前置状态

- 浏览器端页面验证可以使用 `http://127.0.0.1:5173` 开始。
- 涉及真实中心服务数据、接口联调、供应商、智能体、插件、MCP、skill 或中心服务管理状态的验证当前会被 `8866` 不可用阻塞。
- 本轮未运行构建，不能用构建产物证明 Web dist 边界。
- 本轮未直接启动 `services/center`，符合启动约束。

## 停止方式

- 首选停止方式：在 IDEA Run 窗口停止 `dev:desktop-shell`。
- 桌面窗口停止方式：关闭 Electron 桌面窗口。
- 预期收尾：停止桌面壳后，由桌面壳和启动脚本回收其管理的中心服务与子进程。
- 额外说明：当前 `5174` 是本轮因 `5173` 已被占用而新开的额外 Vite 端口，可通过停止本轮 `dev:desktop-shell` 运行配置释放。

## 阻塞项

- 中心服务阻塞：`8866` 未监听。
- 失败原因：桌面端拉起中心服务时，`better-sqlite3` 原生绑定缺失，初始化数据库阶段退出。
- 关键错误：`Could not locate the bindings file`，缺失 `better_sqlite3.node`。
- 影响范围：浏览器端可验证前端页面壳、路由和静态交互；不能完成依赖中心服务的真实数据联调。
- 本轮不处理项：不修改本机 Node.js / pnpm / 编译工具环境，不运行构建，不直接运行中心服务。
