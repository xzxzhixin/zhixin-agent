# 本次计划

- 目标：整理 `services/center/src` 代码结构，并补齐主智能体团队协作相关工具，先完成需求细节对齐，再按 TDD 执行。
- 用户最新指令：使用 `/using-superpowers`；和用户对齐所有细节后再落实 TDD；不使用视觉伴侣；主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复；确认过程中持续更新 `本次计划.md`；普通说明使用 `-`，任务项使用 `- [ ]`，完成后改为 `- [x]`；浏览器使用 Chrome DevTools 按人的方式点击、输入等测试；任务完成再做 git 处理；gitignore 忽略的不要强制加入；发现远端领先先拉取；工作区所有改动全部自动推送；不校验 `提示词模板.md` 是否被修改，直接提交；任务完成再删除提示词里面引用的图片；执行 `dev:desktop-shell` 前必须先关闭之前进程；服务使用系统环境里的 node 启动。
- 本次需求：整理 `services/center/src` 下的代码；`domain` 放到一个文件里面；`routers` 放一个文件夹里面；API 文件按 URL 拆解，例如 `/api/auth` 这类路径中 `api` 只是根前缀，`auth` 独立为 `auth.ts`，其他同理，统一放在 `api` 文件夹下；`tool` 开头的工具代码放到 `tool` 文件夹；配置 `config` 等模块一起考虑，做好分模块和分包处理。
- 本次需求：针对主智能体补齐两个团队工具方向。一个是在对话窗口里创建 team 的工具，team 队员全部来自长期智能体；如果没有合适长期智能体，可能临时创建长期智能体并拉进 team；既然能创建 team，就要能解散 team；既然能在 team 里拉人，就要能移除人；团队工具还有细节需要对齐后再实现。
- 硬性约束：先确认需求细节再实现；实现前必须 TDD 红灯；中心服务不能独立启动；浏览器验收必须通过桌面壳拉起中心服务；TS 文件不使用命令行编译器校验；新增或修改代码必须补中文注释；不回滚用户已有改动。
- 已读取技能：`using-superpowers`、`brainstorming`、`test-driven-development`、`writing-plans`、`dispatching-parallel-agents`、`project-dev-standards`。
- 已读取项目事实源：`需求.md`、`架构.md`、`计划.md`、`AGENTS.md`。
- 当前工作区状态：`main...origin/main`，可见已有改动为 `提示词模板.md`；不会回滚无关改动。
- 当前代码现状：`services/center/src` 同时存在根级旧文件和新目录。根级有 `agent-domain.ts`、`provider-domain.ts`、`session-domain.ts`、`api-routes.ts`、`provider-routes.ts`、`tool-runtime.ts`、`tool-runtime-command.ts`、`tool-runtime-mcp.ts` 等；目录已有 `api`、`agents`、`data-access`、`tools`。
- 当前代码现状：根级 `*-routes.ts` 多数已经变成兼容转出口，真实路由集中在 `services/center/src/api`，但 `api/api-routes.ts` 仍包含大量 `/api/*` 注册，尚未按 `/api/auth -> api/auth.ts` 这种资源维度拆开。
- 当前代码现状：工具目录已有 `tools/create-long-term-agent-tool.ts` 和 `tools/create-sub-agent-tool.ts`，但运行时仍以根级 `tool-runtime*.ts` 为主；已确认统一迁入既有 `tools` 目录。
- 当前代码现状：智能体类目录已有 `agents/base-agent.ts`、`agents/main-agent.ts`、`agents/long-term-agent.ts`、`agents/sub-agent.ts`，团队协作工具尚未对齐。

## 目标设计待对齐

- 目录整理建议按职责拆为稳定边界：`api` 放 HTTP/WebSocket 请求入口；`domain` 放领域编排；`data-access` 放 SQLite/文件仓储；`tools` 或 `tool` 放内联工具与工具运行时；`config` 放配置解析、静态资源、访问控制等启动配置；根级只保留 `index.ts`、`service.ts` 和少量聚合出口。
- `domain 放到一个文件里面` 已确认按 `domain` 文件夹集中领域模块处理，不物理合并为单个大文件；可用 `domain/index.ts` 作为聚合出口。
- `routers 放一个文件夹里面` 已确认沿用 `services/center/src/api`，不新增 `routers` 文件夹；当前大路由中的 `/api/{资源}` 必须全部拆成独立路由文件。
- API 拆解按 `/api/{资源}` 拆成 `api/{资源}.ts`，例如 `auth.ts`、`access.ts`、`project.ts`、`session.ts`、`agent.ts`、`tokenizer.ts`、`memory.ts`、`mcp.ts`、`skill.ts`、`notification.ts`、`worker.ts`、`approval.ts`、`file.ts` 等，并由 `api/index.ts` 或 `api/register.ts` 统一注册。
- 团队工具已确认建模为 4 个独立内联工具：`create-agent-team`、`disband-agent-team`、`add-agent-team-member`、`remove-agent-team-member`，每个工具行为单一、单独测试和审计。
- team 队员来源只允许长期智能体；创建 team 时优先从已启用长期智能体中选择。没有合适长期智能体时，主智能体可先调用创建长期智能体工具，再调用添加成员工具加入 team；创建长期智能体按现有权限和执行模式走审批。
- team 成员边界已确认：team 成员只能是启用状态的长期智能体；主智能体“致心”作为创建者、协调者和队长，不作为普通成员记录；子智能体不能加入 team，也不能管理 team；长期智能体可以被添加或移除，但本轮不开放长期智能体自行创建或解散 team。
- team 解散口径已确认：解散 team 时物理删除 team 记录和当前成员关系；历史消息、工具过程卡片和已写入的会话事件日志保留，不删除长期智能体或长期智能体记忆。
- team 移除成员口径已确认：移除成员只影响后续协作，不删除该长期智能体，也不改它的记忆；创建、解散、添加成员、移除成员都写事件日志并在对话过程卡片中展示。
- team 生命周期已确认跟随会话：team 绑定会话保存，会话未删除则 team 一直存在；会话删除时 team 随会话事实一起清理，不作为全局长期团队复用对象。

## 任务拆分

- [x] 读取本轮适用技能和项目必读文档。
- [x] 查看 `services/center/src` 当前文件结构、现有 `api`、`agents`、`tools`、`data-access` 目录和 Git 状态。
- [x] 重写 `本次计划.md`，固化本轮目标、用户指令、硬性约束、现状和待对齐问题。
- [x] 和用户逐项确认目录整理边界、`domain`/`routers`/`tool` 命名、API 拆分粒度和 team 工具行为。
- [x] 用户确认后同步 `需求.md`、`架构.md` 和 `计划.md`，把本轮新增能力和目录边界写入事实源。
- [x] 使用 writing-plans 输出本轮实现计划，拆出 TDD 红灯、实现、验证、文档同步、审查和提交推送步骤。
- [x] 视任务独立性分派子代理：一个只读梳理 API 拆分映射，一个只读梳理工具运行时迁移点，一个只读梳理 team 数据模型和既有智能体能力。
- [x] TDD 红灯：新增或扩展回归检查，证明 API 未按资源文件拆分、工具运行时未进入工具目录、team 工具缺失。
- [x] 绿灯：按确认方案移动和拆分中心服务代码，保持导入路径、构建入口和现有行为不变。
- [x] 绿灯：按确认方案实现 team 工具、持久化或会话态、权限边界和事件记录。
- [x] 回看 `本次计划.md`，确认没有偏移后再进入验证。
- [x] 运行允许的自动化检查，不使用 `tsc` / `vue-tsc`。
- [x] 如涉及浏览器可见 team 操作，关闭旧进程后使用系统环境 Node 启动 `dev:desktop-shell`，用 Chrome DevTools 真实操作验证。
- [x] 检查 `提示词模板.md` 图片引用，任务完成后删除提示词引用的临时图片。（当前未发现图片引用，无需删除资源）
- [x] 远端检查；如远端领先先拉取。（已 fetch，`HEAD..origin/main` 为 0，无需拉取）
- [ ] 提交并推送全部非忽略改动。

## 待确认问题

- [x] `domain 放到一个文件里面` 是指建立 `domain` 文件夹集中领域模块，还是物理上合并为一个 `domain.ts` 聚合文件？（已确认：建 `domain` 文件夹集中，允许聚合出口）
- [x] 路由文件夹是否沿用现有架构的 `services/center/src/api`，还是必须改名为 `routers`？（已确认：沿用 `api`）
- [x] 工具目录是否必须命名为单数 `tool`，还是沿用现有架构的复数 `tools`？（已确认：沿用 `tools`）
- [x] API 拆分是否按 `/api/{资源}` 作为文件名，例如 `/api/auth/login` 归 `api/auth.ts`，`/api/provider/*` 归 `api/provider.ts`？（已确认：全部按资源拆出来）
- [x] “缺少两个 tool”是要两个工具：`创建/解散 team` 和 `添加/移除成员`，还是每个动作一个工具，共四个工具？（已确认：4 个独立工具）
- [x] team 是只在当前对话窗口临时存在，还是要长期固化并可复用？（已确认：team 跟随会话，会话未删除则一直存在）
- [x] 创建 team 时没有合适长期智能体，是否允许主智能体自动创建长期智能体并加入，还是必须先请求用户确认？（已确认：允许按执行模式和审批创建后加入）
- [x] team 成员是否只能是启用状态的长期智能体，主智能体自己是否可以作为队长但不作为成员？（已确认：长期智能体成员，主智能体为队长不占成员位，子智能体不参与）
- [x] team 的解散和移除成员是否需要写入审计事件、是否需要用户审批？（已确认：写事件和过程卡片；解散物理删除 team 记录；普通解散和移除不额外审批，创建长期智能体按执行模式审批）

## 当前状态

- [x] 正在需求对齐阶段，尚未修改业务代码。
- [x] 已确认本轮不是视觉任务，按用户要求不使用视觉伴侣。
- [x] 已确认第一个关键分歧：`domain` 建文件夹集中领域模块，不物理合并为单文件。
- [x] 已确认路由沿用现有架构的 `api` 文件夹，并按 `/api/{资源}` 全部拆分。
- [x] 已确认工具目录沿用现有架构的 `tools`，根级 `tool-runtime*.ts` 后续迁入该目录。
- [x] 已确认 team 工具采用 4 个独立工具。
- [x] 已确认 team 生命周期跟随会话。
- [x] 已确认没有合适长期智能体时，主智能体可按执行模式和审批创建长期智能体并加入 team。
- [x] 已确认 team 成员边界和主智能体队长关系。
- [x] 已确认 team 解散、移除成员、审计和 UI 展示口径；解散 team 物理删除 team 记录。
- [x] 已完成用户确认、本轮 TDD 实现、自动化验证、构建验证和 Chrome DevTools 浏览器验收。

## 校准记录

- 2026-06-09：用户提出整理 `services/center/src` 代码结构，并补齐主智能体 team 工具；明确要求先对齐需求细节后 TDD 执行，不使用视觉伴侣。
- 2026-06-09：已读取项目事实源和当前中心服务源码目录，发现已有部分新边界但仍存在根级旧文件和 `api/api-routes.ts` 大路由文件。
- 2026-06-09：用户确认 `domain` 采用文件夹集中领域模块的方案，不合并成单个大文件。
- 2026-06-09：用户确认路由沿用 `services/center/src/api`，并把当前大路由里的 `/api/{资源}` 全部拆成独立文件。
- 2026-06-09：用户确认工具目录沿用 `services/center/src/tools`，不改为单数 `tool`。
- 2026-06-09：用户确认 team 工具采用 4 个独立工具：创建、解散、添加成员、移除成员。
- 2026-06-09：用户确认 team 跟随会话生命周期，会话未删除则 team 一直存在。
- 2026-06-09：用户确认 team 优先复用已启用长期智能体；没有合适长期智能体时，可按执行模式和审批创建长期智能体并加入 team。
- 2026-06-09：用户确认 team 成员只能是启用状态的长期智能体；主智能体作为队长不占成员位；子智能体不能加入或管理 team。
- 2026-06-09：用户确认 team 解散时物理删除 team 记录；历史消息和事件保留；移除成员不删除长期智能体和记忆。

- 2026-06-09：已完成中心服务目录拆分、会话级 team 数据与 4 个主智能体 team 工具实现；已通过自动化检查、构建和 Chrome DevTools 真实点击验收，桌面壳与中心服务进程已关闭。

