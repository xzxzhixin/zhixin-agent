# 本次计划

## 当前执行状态

- 本轮任务场景：程序员任务：继续修复前端路由主体残留问题。
- 本轮计划性质：窄范围前端 hash 路由、顶层 RouterView 和授权同步回归修复。
- 本轮已完成运行时取证：`#/center -> #/chat`、`#/providers`、`#/usage`、`#/login` 失败时，`window.location.hash` 和 Vue Router `currentRoute` 已变化，但 DOM 标题仍停留在“中心服务”，目标页面组件资源已加载，说明问题在顶层渲染主体未跟随原生 hashchange 重绘。
- 本轮已确认根因：顶层 `App.vue` 的 `<router-view>` 未对当前 route 建立强重建边界，授权逻辑只在一次性挂载阶段执行；外部直接修改 hash 时 Vue Router 状态可变化，但顶层和工作台宿主仍可能停在旧主体。
- 本轮已修复：`App.vue` 顶层 RouterView 改为 slot + route key + 原生 `hashchange` 渲染版本桥接，授权同步改为 watch 当前 route 和远程登录能力；`main.ts` 挂载前等待 `router.isReady()`；`router.ts` 合并重复根路由；`MainView.vue` 工作台主体增加稳定 route 宿主。
- 本轮已新增回归：扩展 `scripts/check-frontend-browser-route-auth.mjs` 覆盖顶层 RouterView keyed、授权 watch、router ready、原生 hashchange 桥接和重复根路由约束。
- 本轮验证结果：`node scripts/check-frontend-browser-route-auth.mjs` 已先 RED 后 GREEN；`node scripts/check-frontend-workspace-ui.mjs`、`node scripts/check-frontend-management-pages.mjs` 和 `pnpm --filter @zhixin/frontend build` 已通过；Chrome DevTools 已复测失败链路，DOM 标题随 `#/chat`、`#/providers`、`#/usage` 切换，本机访问 `#/login` 自动回到 `#/chat`。
- 本轮未同步正式 `计划.md`，原因是用户明确禁止修改正式 `计划.md`。
- 本轮未修改 `需求.md` 和 `架构.md`。

## 来源与目标

- 需求分析来源：`本次需求分析.md`。
- 最新正式需求来源：`需求.md`。
- 协作规则来源：`AGENTS.md`、`提示词模板.md##公共模板`、`提示词模板.md##架构师子代理提示词`。

目标：

1. 把本轮全量验证拆成可执行的子代理任务。
2. 明确启动、浏览器测试、桌面测试和最终检验的依赖顺序。
3. 确保中心服务只由桌面端拉起，不允许直接运行 `services/center`。
4. 确保浏览器测试使用 chrome-devtools，并读取或维护 `浏览器测试窗口.md`。
5. 确保桌面测试使用 windows-mcp 或当前可用桌面工具。
6. 确保检验子代理只做最终复核，不修改业务代码；只有启动、浏览器测试、桌面测试和检验全部通过后，才允许把对应需求标题追加 `-完成`。
7. 如发现代码缺陷，后续应回流程序员子代理；本轮架构师只负责计划。

## 需求边界

- `本次需求分析.md` 已定位两段未完成临时需求：
  - `当前修改的需求-2026年6月1日11:30:04`
  - `当前修改的需求-2026年6月1日17:51:44`
- 本轮用户要求的是全量验证调度，不是新增或修改产品需求。
- 本轮不修改 `需求.md`。
- 本轮不修改 `架构.md`。
- 本轮不修改正式 `计划.md`。
- 本轮不修改业务代码、启动脚本、前端页面、中心服务或桌面壳。

## 文件范围

### 本轮允许维护

- `本次计划.md`

### 后续启动子代理允许维护

- `项目启动.md`

### 后续浏览器测试子代理允许维护

- `浏览器测试窗口.md`

### 后续检验子代理在全部通过后允许维护

- `提示词模板.md`
  - 仅允许把已通过验收的 `## 当前修改的需求-时间` 标题追加 `-完成`。
  - 不允许改写需求正文、模板规则或业务说明。

### 本轮和后续验证阶段禁止修改

- `需求.md`
- `架构.md`
- `计划.md`
- 业务代码目录：`apps`、`services`、`packages`、`plugins`
- 依赖目录：`node_modules`
- Git 暂存区和提交记录

## 阶段 1：启动子代理任务

### 任务 S1：通过桌面端拉起中心服务并维护启动记录

**子代理类型：启动子代理**

**执行前必须读取：**

- `提示词模板.md##公共模板`
- `提示词模板.md##启动子代理提示词`
- `本次计划.md`

**允许文件范围：**

- 创建或更新：`项目启动.md`

**执行要求：**

- 优先通过 IDEA Run Configuration 或 IDEA Terminal 启动项目，并写明采用方式。
- 禁止直接运行 `services/center` 启动中心服务。
- 需要中心服务参与测试时，只能启动桌面端，由桌面端拉起中心服务。
- 确认中心服务端口，默认应为 `8866`，以实际启动结果为准。
- 记录浏览器访问 URL，例如 `http://127.0.0.1:8866`，以实际端口为准。
- 记录桌面端窗口状态、中心服务拉起证据、关键日志位置和停止方式。
- 启动子代理完成后不默认关闭服务，除非主线程另行要求。

**`项目启动.md` 必填内容：**

- 启动方式。
- 使用的命令或 IDEA Run Configuration。
- 实际访问 URL。
- 中心服务是否由桌面端拉起。
- 本轮是否重启桌面端。
- 本轮是否重新拉起中心服务。
- 重启原因。
- 可复用服务。
- 当前运行状态是否覆盖最新代码。
- 后续代码变更后是否需要重新启动。
- 端口。
- 关键日志位置。
- 测试前置状态。
- 停止方式。
- 当前阻塞项。

**通过标准：**

- `项目启动.md` 已存在且信息完整。
- 可确认中心服务由桌面端拉起，而不是直接运行 `services/center`。
- 浏览器测试和桌面测试都有可复用的启动信息。

**失败回流：**

- 如果桌面端无法启动、中心服务未监听、`8866` 请求被拒绝、中心服务不是桌面端拉起，后续应回流程序员或启动链路处理。
- 架构师不修代码，只在计划中标明回流规则。

## 阶段 2：浏览器测试子代理任务

### 任务 B1：维护浏览器测试专用窗口

**子代理类型：浏览器测试子代理**

**依赖：**

- 必须先完成任务 S1，并读取 `项目启动.md`。

**执行前必须读取：**

- `提示词模板.md##公共模板`
- `提示词模板.md##浏览器测试子代理提示词`
- `本次计划.md`
- `项目启动.md`

**允许文件范围：**

- 创建或更新：`浏览器测试窗口.md`

**执行要求：**

- 只使用 chrome-devtools。
- 使用 chrome-devtools 前必须先读取或创建 `浏览器测试窗口.md`。
- 只能操作 `浏览器测试窗口.md` 记录的测试专用页签。
- 不导航、不刷新、不覆盖用户当前激活页或未记录的业务页面。
- 测试结束后不默认关闭测试页。

**`浏览器测试窗口.md` 必填内容：**

- DevTools 页面 ID。
- 当前 URL。
- 页面标题。
- 页面用途：浏览器测试专用。
- 是否允许覆盖导航：是，仅限本测试页。
- 创建时间。
- 最后使用时间。
- 最近一次使用者：浏览器测试子代理。
- 当前状态：可复用 / 已失效 / 需要重建。

**通过标准：**

- 浏览器测试专用页签可复用，且不会影响用户当前页面。

### 任务 B2：浏览器端全量可见验证

**子代理类型：浏览器测试子代理**

**依赖：**

- S1 已完成。
- B1 已完成。

**验证范围：**

- 本机 Web 入口可打开。
- 顶部菜单切换时 hash URL 同步变化。
- 直接访问或刷新以下页面不白屏：
  - `/chat`
  - `/agent-management`
  - `/providers`
  - `/proxies`
  - `/runtimes`
  - `/usage`
  - `/plugins`
  - `/mcp`
  - `/skills`
  - `/center`
- 浏览器端不展示不该出现的桌面专属能力；如当前运行模式是桌面壳托管页面，应按实际客户端能力记录。
- 执行模式下拉面板中可见：
  - 建议模式说明。
  - 自动编辑说明。
  - 全自动说明。
- 页面根容器无全局滚动条异常，消息列表作为主滚动区域。
- 菜单按钮中线对齐、激活样式和整体布局无明显回归。
- 管理页不是空白页，也不是明显的伪拆分壳。
- 项目对话如需使用测试项目，统一使用仓库根目录下的 `对话测试` 目录。
- 核心对话功能如需账号配置，只可使用本地 `AI账号.md` 作为配置依据，不输出敏感内容。

**必须检查：**

- 浏览器控制台关键错误。
- 关键网络请求错误。
- 页面可见错误提示。
- 路由刷新后的页面状态。

**输出要求：**

- 已测项。
- 未测项。
- 失败项。
- 复现步骤。
- 关键工具观察结果。
- 浏览器控制台关键错误。
- 网络请求关键错误。
- 是否阻塞验收。

**失败回流：**

- 如果发现页面白屏、路由错误、控制台阻塞级错误、请求 `ERR_CONNECTION_REFUSED`、`API_NOT_FOUND`、未捕获 Promise 或布局明显回归，后续应回流程序员。
- 如果失败原因是启动状态过期或中心服务未由桌面端拉起，应先回流启动子代理刷新 `项目启动.md`。

## 阶段 3：桌面测试子代理任务

### 任务 D1：桌面客户端全量可见验证

**子代理类型：桌面测试子代理**

**依赖：**

- 必须先完成任务 S1，并读取 `项目启动.md`。
- D1 可与 B1/B2 在 S1 完成后并行。

**执行前必须读取：**

- `提示词模板.md##公共模板`
- `提示词模板.md##桌面测试子代理提示词`
- `本次计划.md`
- `项目启动.md`

**工具要求：**

- 使用 windows-mcp 或当前环境可用的桌面验证工具。
- 如果当前环境没有可用桌面验证工具，必须明确报告未测原因。

**验证范围：**

- 桌面客户端窗口可打开。
- 桌面端启动默认最大化。
- 顶部菜单按钮中线对齐、激活样式和整体布局无明显回归。
- 桌面端包含中心服务管理入口。
- 桌面端能通过自身生命周期拉起中心服务。
- 中心服务不是直接运行 `services/center`。
- 关闭按钮触发退出或隐藏托盘选择；如功能未完成，应记录为失败项。
- 如已支持记住关闭选择，应验证再次关闭时行为符合记录；如未支持，应记录为失败项。
- 桌面端可见错误状态和关键日志无阻塞级错误。
- 对话入口、供应商、网络代理、运行环境、用量统计、插件、MCP、skill、智能体管理和中心服务页面可见状态与需求一致。

**必须检查：**

- 桌面端关键错误。
- 中心服务拉起证据。
- 窗口可见状态。
- 关闭行为。
- 是否存在影响后续浏览器测试的服务异常。

**输出要求：**

- 已测项。
- 未测项。
- 失败项。
- 复现步骤。
- 关键工具观察结果。
- 桌面端关键错误。
- 中心服务拉起证据。
- 是否阻塞验收。

**失败回流：**

- 如果桌面壳未拉起中心服务、关闭时未停止由桌面端管理的中心服务、窗口行为不符合需求或 UI 明显回归，后续应回流程序员。
- 如果桌面工具不可用，应记录为阻塞，不允许因此标记需求完成。

## 阶段 4：最终检验子代理任务

### 任务 V1：最终复核与完成标记判断

**子代理类型：检验子代理**

**依赖：**

- S1 通过。
- B1/B2 通过。
- D1 通过。
- 如 B 或 D 发现代码缺陷，必须先回流程序员修复并重新执行相关启动和测试任务；未重新通过前不得进入完成标记。

**执行前必须读取：**

- `提示词模板.md##公共模板`
- `提示词模板.md##检验子代理提示词`
- `本次需求分析.md`
- `本次计划.md`
- `项目启动.md`
- `浏览器测试窗口.md`
- 各测试子代理结构化结果。

**复核范围：**

- `本次计划.md` 中任务是否都已有结果。
- `本次需求分析.md` 中定位的两个需求标题是否均已覆盖验证。
- 本轮是否遵守不修改 `需求.md`、不修改 `架构.md`、不修改正式 `计划.md` 的约束。
- 是否存在未暂存文件、无关改动、不应提交文件或危险操作痕迹。
- 是否存在未测试、未构建或环境阻塞项。
- 浏览器测试和桌面测试是否均通过。
- 是否可以把对应 `当前修改的需求-时间` 标题追加 `-完成`。

**允许操作：**

- 不修改业务代码。
- 只有全部验证通过且无阻塞项时，才允许把 `提示词模板.md` 中对应需求标题追加 `-完成`。
- 如果任一验证未通过、未执行或存在阻塞项，不允许标记 `-完成`。

**输出要求：**

- 计划任务完成情况。
- 需求同步情况。
- 架构同步情况。
- `计划.md` 同步情况。
- 当前修改需求是否可标记完成。
- 已追加 `-完成` 的需求标题。
- 未暂存文件或无关改动。
- 不应提交文件。
- 未测试项。
- 未构建项。
- 阻塞项和建议处理方式。

## 可并行任务清单

- S1 必须先执行，不能与测试任务并行。
- S1 通过后，B1/B2 和 D1 可以并行执行。
- V1 必须在 S1、B1/B2、D1 都通过后执行。

## 不可并行任务清单

- S1 与 B1/B2/D1 不可并行，因为测试依赖 `项目启动.md` 的可信启动信息。
- V1 与任何启动或测试任务不可并行，因为检验依赖最终测试结果。
- 发现代码缺陷后的程序员修复与检验不可并行，必须先修复、重启、复测，再检验。
- 涉及同一运行环境重启的启动刷新与浏览器/桌面测试不可并行，必须先刷新启动状态。

## 验收和测试建议

- 全量验证必须包含浏览器测试和桌面测试。
- 浏览器测试只使用 chrome-devtools。
- 桌面测试使用 windows-mcp 或当前可用桌面工具。
- 中心服务只允许由桌面端拉起。
- 不直接运行 `services/center`。
- 测试截图只作为即时观察，不作为长期证据保存。
- 如工具强制生成截图文件，创建者必须在输出前删除；无法确认或无法删除时列为待用户确认清理。
- 核心对话验证可以读取本地 `AI账号.md` 作为配置依据，但不得输出账号、密钥或敏感内容。
- 本轮计划阶段不运行任何测试、构建或启动命令。

## 风险与阻塞项

- 如果桌面端无法拉起中心服务，浏览器测试和桌面测试都不能通过全量验收。
- 如果 chrome-devtools 不可用，浏览器验证阻塞。
- 如果 windows-mcp 或桌面验证工具不可用，桌面验证阻塞。
- 如果 `AI账号.md` 配置不可用，核心对话验证可能阻塞；输出中不得泄露敏感内容。
- 如果测试发现代码缺陷，本轮架构师不修复，后续必须回流程序员。
- 如果任一测试未执行、未通过或存在阻塞项，检验子代理不得标记需求完成。

## 计划同步说明

- 是否读取了最新 `需求.md`：已读取。
- 是否建议或已经同步 `需求.md`：未同步，本轮用户未要求，且要求不要修改正式需求。
- 是否建议同步 `架构.md`：本轮不建议同步；当前任务是验证调度计划。
- 是否已同步 `计划.md`：未同步，用户明确要求不要同步正式 `计划.md`。
- `计划.md` 中删除或替换的冲突旧计划摘要：无，本轮未修改正式 `计划.md`。

## 结论

- 本次 `本次计划.md` 已更新为全量验证调度计划。
- 后续主线程应先创建启动子代理维护 `项目启动.md`。
- 启动通过后，可并行创建浏览器测试子代理和桌面测试子代理。
- 所有测试通过后，再创建检验子代理做最终复核。
- 只有全部通过且无阻塞项时，检验子代理才允许把对应需求标题追加 `-完成`。

## 程序员任务补充-桌面验证阻塞项

- [x] 已定位桌面端启动窗口状态阻塞根因：`apps/desktop-shell/src/main.ts` 创建 `BrowserWindow` 时只设置 `width: 1280` 和 `height: 820`，没有显式执行默认最大化。
- [x] 已新增最小回归检查：`scripts/check-desktop-window-behavior.mjs`，覆盖启动默认最大化、关闭按钮询问退出或隐藏托盘、关闭偏好持久化和托盘恢复。
- [x] 已确认 RED：修复前执行 `node scripts/check-desktop-window-behavior.mjs` 返回 1，并提示缺少最大化、关闭事件、偏好持久化、关闭弹框和隐藏托盘行为。
- [x] 已修复桌面壳行为：主窗口创建后执行 `mainWindow.maximize()`；首次关闭弹出“隐藏到托盘 / 直接退出 / 取消”选择；勾选记住后写入桌面本机配置；托盘退出走统一退出流程。
- [x] 已确认 GREEN：修复后执行 `node scripts/check-desktop-window-behavior.mjs` 返回 0。
- [x] 已由桌面测试子代理使用 windows-mcp 复测真实 Electron 窗口状态、菜单主体同步和关闭按钮交互；Electron 窗口最大化、菜单切换无旧 DOM 残留，关闭确认框已出现并选择取消。
