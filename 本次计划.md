# 本次计划

## 主线程摘要

- 状态：待启动验证。
- 是否需要程序员阶段：否。
- 未完成程序员任务编号：无。
- 程序员连续执行任务编号：P01 至 P09 已完成。
- 是否可以进入启动阶段：是，需启动子代理确认浏览器端测试环境。
- 是否可以进入测试阶段：否，需启动子代理确认 `5173` 与宿主拉起的 `8866` 健康后再进入测试。
- 测试范围：最终只执行浏览器端验证，不执行桌面端测试项，不执行 IDEA 插件端测试项。
- 阻塞归属：无。
- 下一步：派发启动子代理刷新前端和中心服务环境，再派发测试子代理按 `本次测试计划.md` 串行执行浏览器端 T01 至 T11。

## 本轮范围

- 需求来源：`当前需要实现的需求.md` 中未标记 `-完成` 的一级标题 `# 2026年6月3日15:38:08`。
- 续接结论：上一轮浏览器端供应商配置弹框复测已在 `测试结果.md` 记录为通过；上一轮 T08 相关补修不再作为未完成项继承。本轮只承接新标题提出的 dialog、对话执行过程、任务编排、智能体层级、引导、排队、自动工具使用和中心服务数据访问层分离诉求。
- 正式文档边界：用户明确禁止修改 `需求.md`、`架构.md`、`计划.md`；因此本轮只覆盖更新 `本次计划.md` 与 `本次测试计划.md`，不同步正式需求、正式架构或正式计划。
- 代码边界：程序员可修改前端、中心服务、共享协议、API 客户端、UI 组件、静态回归脚本和必要依赖配置；不得修改正式需求、正式架构、正式计划，不得读取或更新 `浏览器测试窗口.md`，不得标记临时需求 `-完成`。
- 启动边界：涉及中心服务、数据访问层、对话执行链路、工具调用或 WebSocket 流式状态的变更后，必须经启动子代理重新确认环境；测试最终只覆盖浏览器端。

## 执行任务

- [x] P01 统一弹框全局行为：检查 Element Plus 弹框封装、页面专属弹框和跨页面弹框，确保默认宽度统一为 `80vw`、默认追加到 `body`、整体最大高度为 `80vh`，滚动限制在弹框内容容器内部而不是页面根滚动；目标文件包括 `apps/frontend/src` 下 dialog 相关组件、各 `views/*/dialogs` 目录和必要全局样式；完成口径为所有未特殊说明的弹框满足默认行为，页面根容器仍固定视口高度无全局滚动。
- [x] P02 补齐主对话思考与流式输出展示：在对话消息渲染、事件订阅、消息状态和中心服务事件流中补齐思考片段、流式回复片段、折叠思考块、生成中状态和完成后固化展示；目标文件包括 `apps/frontend/src/views/Chat`、对话消息组件、`packages/api-client`、`services/center` 会话事件相关模块；完成口径为浏览器发送对话时可见思考过程和逐步输出，不再等待整轮结束后一次性显示。
- [x] P03 补齐任务编排入口语义：修正输入区“任务”入口和任务详情弹框，使截图中显示的数量表示编排出来的任务进度而不是对话数量；任务列表必须展示当前轮次或当前会话内任务、步骤、状态、耗时、失败原因和排查信息；完成口径为任务入口外部计数、弹框标题和详情文案均明确任务编排语义。
- [x] P04 实现自动工具使用可见闭环：按中心服务唯一入口补齐命令行、插件、skill、MCP 自动工具调用流程，浏览器端可发起或展示工具过程卡片；命令调用至少用输出 Node.js 版本作为浏览器端验证场景；工具调用需遵守 Codex 风格过程记录，展示开始、结束、输入摘要、输出摘要、失败原因和 traceId；完成口径为命令、插件、skill、MCP 至少各有明确入口、状态或不可用原因，不再静默缺失。
- [x] P05 修正智能体状态两级展示：智能体状态弹框只展示长期智能体及其对应子智能体两级树，主智能体不再显示在该状态树中；长期智能体节点和子智能体节点都支持点击打开对话或引导弹窗；完成口径为两级结构、节点操作、空状态和运行状态均符合需求截图语义。
- [x] P06 补齐智能体对话弹窗完整对话能力：修复智能体节点打开后的对话弹窗，使其具备完整消息列表、输入、发送、引导、任务状态、流式输出和错误展示能力；目标文件包括 `apps/frontend/src/views/Chat/dialogs` 中智能体相关弹框和会话发送状态管理；完成口径为弹窗内不是静态详情，而是可完成一次真实对话或引导流程。
- [x] P07 补齐主对话引导与排队功能：在主对话框输入区、消息流或任务状态区域提供引导能力，并实现当前对话当前轮次内的排队语义；排队只表示当前对话内部等待上一项任务处理，不代表全局串行；完成口径为浏览器端可创建待引导状态、可提交引导内容、可见排队中任务并在前一任务完成后继续处理。
- [x] P08 推进中心服务 SQL 与代码分离：在不引入 `tsc --noEmit`、`vue-tsc` 或命令行 TS 质量门槛的前提下，扩展 Node 端数据访问层，将高频领域的内联 SQL 从业务服务中拆出到仓储或查询模块；至少覆盖会话、任务/事件、智能体、供应商中的两个领域，保持 better-sqlite3 仅由中心服务主进程持有连接；完成口径为业务路由/服务不再直接散落复杂 SQL，新增或更新静态检查能约束后续新增 SQL 边界。
- [x] P09 更新程序员结果和计划状态：程序员完成 P01 至 P08 后，逐项把本文件对应任务从 `[ ]` 改为 `[x]`，并在 `程序员结果.md` 写明修改范围、运行过的静态检查、未运行浏览器测试原因、是否需要启动子代理刷新环境；不得修改 `需求.md`、`架构.md`、`计划.md`，不得标记临时需求 `-完成`。

## 验收口径

- P01 至 P09 均勾选完成，且 `程序员结果.md` 说明实现范围、静态检查和未测试项后，才允许进入启动阶段。
- 启动子代理必须确认 `5173` 前端可访问、中心服务由桌面端或项目约定宿主拉起、`8866` 健康，并覆盖更新 `项目启动.md` 后，才允许进入测试阶段。
- `本次测试计划.md` 中所有浏览器端必测项均为 `[x]` 且详情状态为“通过”后，测试子代理才允许按工作流判断临时需求是否可标记 `-完成`。
- 本轮最终测试范围只包含浏览器端；桌面端和 IDEA 插件端不执行，不作为本轮通过条件。

## 风险与阻塞

- 思考、流式输出、工具调用、排队和引导涉及中心服务事件、前端订阅和任务状态多模块联动，需避免只做静态 UI 假展示。
- 命令、插件、skill、MCP 自动工具使用必须经过中心服务权限与事件审计，不能让浏览器端或 Worker 绕过中心服务直接执行。
- SQL 与代码分离需保持现有 SQLite 迁移和唯一连接边界，不能引入需要额外本机编译或破坏当前 Node 20/pnpm 9 约束的重型 ORM。
- 弹框全局样式涉及多个页面，程序员需要确保 80vw/80vh 默认值不破坏已有特定修改弹框。
