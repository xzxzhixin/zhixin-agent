# 本次计划：永久记忆电脑信息与思考过程展示修正

## 目标

- 永久记忆 `## 使用的电脑` 写入当前操作系统信息，不能再写固定 `center`。
- 对话中的思考过程状态只显示在思考过程标题后面，思考过程正文只显示思考内容。
- 按 TDD 先写失败检查，再做最小实现，最后验证并同步 `计划.md`。

## 用户最新指令

- 使用 `/using-superpowers`。
- 先和用户对齐需求所有细节，再落实 TDD 执行。
- 不使用视觉伴侣。
- 大任务必须本轮持续推进到完成；上下文复杂时创建或更新 `本次计划.md`。
- 主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复。
- 能隔离的执行工作交给子代理；有共享文件风险时按阶段处理。
- 本次测试要求重启保证最新代码生效；重启前查看并关闭运行中的代码。
- 浏览器使用 chrome-devtools 测试。
- 完成后：gitignore 忽略文件不强制加入；发现远端领先先拉取；工作区改动自动推送；删除提示词引用的 `img.png`。
- 最新校准：关闭所有服务；本轮后续不再启动前端、桌面壳或中心服务。
- 最新校准：记忆时间必须用本机时间，这是产品需求，写入 `需求.md`。
- 最新校准：`dev:frontend` 独立拉起前端，`dev:desktop-shell` 只启动桌面壳和中心服务且不拉前端，这写入 `AGENTS.md`。
- 最新校准：后续用户提到的需求如果 `需求.md` 未覆盖就必须加入；若冲突，以最新需求优先。

## 需求校准

- `## 使用的电脑` 是迁移识别字段，含义是本轮记忆写入发生时的当前操作系统信息。
- 当前实现写入 `center` 是错误的，因为 `center` 表示中心服务角色，不代表电脑或操作系统。
- 思考过程标题可以带所属智能体、阶段状态、耗时、流式生成状态等状态信息。
- 思考过程正文只能展示思考文本本身，不再在正文每段前显示状态标签。

## 硬性约束

- 开始实现前必须读取 `需求.md`、`架构.md`、`计划.md` 和 `AGENTS.md`。
- 未获得用户明确要求时不修改 `需求.md`。
- 不允许只改代码不改 `计划.md`。
- 中心服务不能独立启动；浏览器联调必须通过桌面端或 `dev:desktop-shell` 管理中心服务。
- TS 文件不通过 `tsc --noEmit` 或 `vue-tsc` 做命令行质量门槛。
- 新增或修改代码必须补中文注释；函数注释说明用途、关键逻辑、参数和返回值。
- 多属性对象、数组、参数拆成多行；Vue 属性每个一行；CSS 声明一行一个。
- 单个代码文件不得超过 1500 行。
- 不回滚或覆盖用户已有改动，当前已知用户改动为 `提示词模板.md`。

## 任务拆分

- [x] 任务 1：中心服务记忆电脑字段 TDD。
  - 红灯：检查脚本断言长期记忆不能写入固定 `center`，必须包含操作系统信息片段。
  - 绿灯：中心服务写入当前 OS 信息。
  - 验证：运行相关领域脚本。
- [x] 任务 2：前端思考过程正文 TDD。
  - 红灯：检查脚本断言思考过程正文 segment 不再暴露 `statusLabel`，标题或行级状态仍保留。
  - 绿灯：模板只在 summary 展示状态，正文只渲染 segment summary。
  - 验证：运行相关前端渲染顺序检查。
- [x] 任务 3：计划同步、运行时验证、收尾。
  - 同步 `计划.md`。
  - 原计划要求重启并用 chrome-devtools 验证；已被用户最新“关闭所有服务”要求替换，本轮改为静态检查和构建验证，不再启动服务。
  - 删除 `img.png`。
  - 检查远端状态，必要时先拉取，再提交并推送本轮改动。
- [x] 任务 4：最新用户校准处理。
  - 已关闭所有服务并确认 8866/5173 无监听输出。
  - 已把记忆本机时间需求写入 `需求.md`。
  - 已把 `dev:frontend` / `dev:desktop-shell` 启动职责和需求写入规范写入 `AGENTS.md`。
  - 已更新开发脚本和静态检查。

## 子代理分工

- 子代理 A：只读定位永久记忆 `## 使用的电脑` 字段现有实现和检查入口。
- 主线程：更新本次计划、实现 TDD、审查子代理结论、合并验证和收尾。

## 验收方式

- `scripts/check-center-domains.ts` 或新增/调整的中心服务领域检查先红灯再绿灯。
- `scripts/check-chat-turn-render-order.ts` 或新增/调整的前端检查先红灯再绿灯。
- `node scripts/check-all.mjs` 或等价项目检查通过；若因环境失败，记录具体原因。
- chrome-devtools 打开最新桌面端管理的页面，确认思考过程正文只显示思考内容。
- `计划.md` 新增完成项并勾选。

## 当前状态

- 已读取 `需求.md`、`架构.md`、`计划.md`、`AGENTS.md`。
- 已读取并应用相关技能：`using-superpowers`、`brainstorming`、`test-driven-development`、`systematic-debugging`、`writing-plans`、`dispatching-parallel-agents`、`subagent-driven-development`、`using-user-environment`。
- 用户已明确不使用视觉伴侣，因此跳过视觉伴侣。
- 已定位到中心服务当前在 `services/center/src/agent-domain.ts` 的 `writeAgentMemory` 中写入固定 `center`。
- 已定位到前端当前在 `apps/frontend/src/views/Chat/components/ChatConversationPanel.vue` 的思考过程正文中渲染 `segment.statusLabel`。
- 已发现工作区存在用户已有改动：`提示词模板.md`，本轮不覆盖不回滚。
- 已发现提示词引用图片 `img.png` 存在，收尾阶段删除。
- 已在 `scripts/check-center-domains.ts` 写入红灯断言，并用 IDE 终端 Node v20.18.0 确认因固定 `center` 失败。
- 已在 `scripts/check-chat-turn-render-order.ts` 写入红灯断言，并确认因思考片段携带状态标签失败。
- 已在 `services/center/src/agent-domain.ts` 新增 `formatMemoryComputerIdentity()`，长期记忆写入当前 OS 类型、平台、版本和架构。
- 已在 `apps/frontend/src/views/Chat/chat-view-helpers.ts` 与 `ChatConversationPanel.vue` 移除思考正文片段状态标签。
- 已验证 `pnpm exec tsx scripts/check-chat-turn-render-order.ts` 通过。
- 已验证 IDE 终端 `pnpm exec tsx scripts/check-center-domains.ts` 通过。
- 用户最新要求关闭所有服务后，已停止 8866/5173 相关进程，后续不再启动服务做浏览器验证。
- 已按 TDD 为本机时间、开发脚本职责和 AGENTS 新规则写入红灯检查，并确认失败。
- 已把 `formatMemoryTimeTitle()` 和记忆日期目录改为本机时间。
- 已把 `scripts/dev-desktop-shell.mjs` 改为只检查 `dev:frontend` 是否已启动，不再拉起前端。
- 已同步 `需求.md`、`架构.md`、`AGENTS.md` 和 `计划.md`。
- 只读代码审查建议已处理：`scripts/check-center-domains.ts` 改为读取记忆写入返回的 `relativePath`，避免跨本机 0 点重算路径；任务 3 已按最新“关闭所有服务”约束收敛。
