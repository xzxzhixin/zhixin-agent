# 本次计划

- 目标：修复并完善对话思考卡片、首轮标题总结和供应商管理体验，确保思考内容展示中心服务拿到的全部真实可展示思考或阶段摘要且不硬编码假思考，标题只在第一次对话完成后总结，供应商删除后列表刷新且配置弹窗内容区滚动、底部按钮固定可见。
- 用户最新指令：使用 `/using-superpowers`；和用户对齐需求所有细节后再落实 TDD；不使用视觉伴侣；任务明显过大或上下文复杂时先创建或更新 `本次计划.md`；主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复；能隔离的执行工作全部交给子代理；存在强顺序依赖或共享文件风险的工作按阶段分批；每完成一个阶段必须回看 `本次计划.md`、最新用户要求和当前文件状态；浏览器使用 chrome-devtools 按人的方式点击、输入测试；任务完成再做 git 处理；gitignore 忽略的不要强制加入；发现远端领先先拉取；工作区里面所有改动全部自动推送；不校验 `提示词模板.md` 是否被修改，直接提交；任务完成再删除提示词里面引用的图片；执行 `dev:desktop-shell` 前必须先关闭之前的进程；服务启用使用用户环境的 node 启动。
- 硬性约束：中心服务不能独立启动；`dev:frontend` 只负责前端 Vite；`dev:desktop-shell` 只启动桌面壳并由桌面壳拉起中心服务；不使用 `tsc` 或 `vue-tsc` 做命令行质量门槛；TS 文件如需检查只用 IDE 检查；新增或修改代码必须有中文注释；多属性对象、数组和参数拆多行；Vue 属性每个一行；CSS 声明一行一个；单文件不超过 1500 行；不回滚用户已有改动；不删除应用图标或界面图标资源。
- 已读取技能：`using-superpowers`、`brainstorming`、`test-driven-development`、`systematic-debugging`、`using-user-environment`、`dispatching-parallel-agents`、`subagent-driven-development`、`writing-plans`。
- 已读取项目事实源：`需求.md`、`架构.md`、`计划.md`、`AGENTS.md`。
- 当前工作区状态：`main` 分支；本地已有 `提示词模板.md` 修改，按用户要求不校验该文件内容且不回滚；本轮尚未检查远端是否领先，提交前必须重试远端检查。
- 当前截图校准：`img.png` 和 `img_1.png` 展示 DeepSeek 思考卡片，生成中标题为“正在思考”，完成后标题为“已思考（用时 X 秒）”，正文为可公开思考摘要并可折叠；`img_2.png` 展示第一次对话完成后把会话标题总结为简短主题；`img_3.png` 展示供应商删除提示出现但列表仍残留已删除供应商；`img_4.png` 展示供应商配置弹窗底部按钮和错误区域被裁掉，面板不可滚动。
- 需求同步口径：本轮用户明确提出产品能力、交互口径和验收口径，必须先同步 `需求.md`，并评估更新 `架构.md`、`计划.md`，再落代码。
- TDD 口径：每个实现阶段先写失败检查或测试脚本，确认红灯，再写最少实现，确认绿灯；缺陷修复必须先有可复现失败检查，不能先改生产代码。
- 浏览器验收口径：启动前关闭旧桌面壳和中心服务进程；使用用户环境的 node 执行 `dev:desktop-shell`；用 chrome-devtools 点击、输入、删除供应商、打开配置弹窗、发送首轮对话并观察思考卡片和标题。
- 子代理分工：思考卡片探索子代理只读梳理中心事件、前端渲染和真实思考摘要来源；标题总结探索子代理只读梳理首轮标题生成与会话列表同步；供应商体验探索子代理只读梳理删除刷新、列表刷新按钮位置和配置弹窗滚动边界。

## 目标设计草案

- 思考卡片目标设计：
  - 当前轮次每个独立思考过程按真实执行阶段单独成卡，不合并成一个总块。
  - 流式生成时默认展开，标题显示“正在思考”或同等运行态，正文实时追加当前执行链路产生的可公开阶段摘要。
  - 结束后默认折叠，标题显示“已思考（用时 X 秒）”，用户可以手动展开查看。
  - 正文必须来自真实执行链路或模型可公开阶段摘要，不能写固定模板、系统提示词、执行说明，不能在代码中硬编码假思考内容。
- 标题总结目标设计：
  - 只在会话第一次对话完成后生成或更新标题总结。
  - 标题来源应基于本轮用户输入和助手最终回复生成简短主题，不能每轮重复覆盖用户后续修改或已有标题。
  - 标题更新后需要同步会话详情、左侧普通对话和项目对话列表，以及跨端实时订阅。
- 供应商体验目标设计：
  - 删除供应商成功后立即刷新列表，删除成功提示不能替代真实列表更新。
  - 供应商列表刷新按钮放在列表上方，作为显式更新入口。
  - 配置弹窗内容区必须在窗口内可滚动，底部保存、取消、报错或提示区域固定在弹窗底部可见且不会被裁掉。
  - 需要检查其他管理弹窗是否也存在内容不可滚动或底部按钮不可见问题；如果多个弹窗存在同类布局风险，抽出可复用弹窗壳统一处理。
  - 成功、失败、警告提示继续统一使用 Element Plus `ElMessage`，弹窗内阻塞性错误或接口返回原因也必须在可滚动面板内可见。

## 任务拆分

- [x] 读取 `using-superpowers` 和本轮适用技能。
- [x] 重读 `需求.md`、`架构.md`、`计划.md`、`AGENTS.md`。
- [x] 查看当前截图和 Git 工作区状态。
- [x] 更新 `本次计划.md` 固化本轮目标、约束、任务拆分和校准记录。
- [x] 并行分派只读子代理梳理思考卡片、标题总结和供应商体验影响面。
- [x] 汇总子代理结论，提出 2-3 种实现方案和推荐方案。
- [x] 向用户确认关键需求边界后进入实现。
- [x] 检查其他前端管理弹窗是否也存在内容区不可滚动、底部按钮不可见的问题。
- [x] 同步 `需求.md`：新增本轮思考卡片、首轮标题总结、供应商列表刷新和弹窗滚动口径。
- [x] 按影响面评估并同步 `架构.md`。
- [x] 同步 `计划.md`：新增本轮实施项，完成后勾选。
- [x] TDD 红灯 1：新增思考卡片真实摘要和折叠标题检查，证明当前实现不满足截图口径或真实摘要口径。
- [x] 绿灯 1：修复思考事件生成和前端思考卡片展示。
- [x] TDD 红灯 2：新增首轮标题总结检查，证明当前标题没有只在首轮完成后总结或同步不足。
- [x] 绿灯 2：修复首轮标题总结生成和列表同步。
- [x] TDD 红灯 3：新增供应商删除刷新、刷新按钮位置和弹窗滚动检查，证明当前 UI 缺陷。
- [x] 绿灯 3：修复供应商管理页面和配置弹窗。
- [x] 阶段审查：主线程回看 `本次计划.md`、最新用户要求和文件状态，确认没有偏移。
- [x] 运行项目允许的回归检查，不使用 `tsc` / `vue-tsc`。
- [x] 关闭旧进程后执行 `dev:desktop-shell`，用 chrome-devtools 按人的方式完成浏览器验收。
- [x] 检查 `提示词模板.md` 是否存在图片引用；只删除提示词引用的图片，不删除应用图标、界面图标或资源文件。
- [ ] 最终重试远端检查；如远端领先先拉取并处理冲突。
- [ ] 提交全部工作区非忽略改动，包括 `提示词模板.md`，不强制添加 gitignore 忽略文件。
- [ ] 推送到远端。

## 待确认问题

- [x] 思考卡片展示范围：用户确认“全部展示”，但不允许硬编码假思考；落地口径为展示中心服务拿到的全部真实可展示思考或阶段摘要，不展示不存在的内容。
- [x] 标题总结范围：用户确认只在第一次对话完成后总结。
- [x] 供应商弹窗布局：用户确认按钮应 flex 固定在下面，内容可以滚动；还要检查其他弹窗是否同类问题，如普遍存在则抽成组件复用。

## 当前状态

- [x] 已确认本轮包含多个子系统和 UI 缺陷，使用 `本次计划.md` 作为恢复事实源。
- [x] 已确认用户明确不使用视觉伴侣，截图只作为需求参考和验收口径。
- [x] 已确认当前只有 `提示词模板.md` 是已有未提交修改，后续不校验内容、不回滚。
- [x] 已收到只读子代理返回影响面结论。
- [x] 已收到用户确认设计边界，并补充弹窗底部固定与复用要求。

## 校准记录

- 2026-06-08：用户要求不使用视觉伴侣，先对齐需求所有细节再 TDD 执行。
- 2026-06-08：用户新增 DeepSeek 思考卡片参考，要求本项目思考卡片内容一定来自大模型真实思考过程，代码里不要有任何修改。
- 2026-06-08：用户新增标题总结参考，要求标题总结只在第一次对话完成后总结。
- 2026-06-08：用户反馈供应商删除后列表没有更新，列表更新按钮需要放在列表上面，供应商配置弹窗因为无法滚动导致看不到报错按钮。
- 2026-06-08：用户确认思考卡片应全部展示真实可展示内容但不允许硬编码假思考；标题总结只首轮；供应商弹窗按钮固定在底部、内容区滚动，并检查其他弹窗是否也需要抽复用组件。
- 2026-06-08：代码审查发现供应商真实删除需要阻断 providerId 路径穿越并同步删除 API Key secret；已按删除语义补充校验和回归检查。
- 2026-06-08：Chrome DevTools 已验证供应商删除后列表移除、provider JSON/model cache/secret 清理、管理弹窗底部按钮固定、首轮标题总结和第二轮不覆盖标题。
