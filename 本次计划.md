# 本次计划

- 目标：实现中心服务真实 MCP 调用闭环，让模型请求 MCP 工具时可以按中心服务保存的 MCP 配置连接服务器并返回结果。
- 用户最新指令：先读取 `提示词模板.md` 严格执行；先读“公共”；测试只做“浏览器测试”；任务完成后走“任务完成执行”；执行 `dev:desktop-shell` 前必须先关闭之前进程；使用给定 IDEA MCP 配置测试。
- MCP 测试配置：

```json
{
  "mcpServers": {
    "idea": {
      "type": "http",
      "url": "http://127.0.0.1:64342/stream"
    }
  }
}
```

- 测试内容：通过 MCP 获取当前打开项目的路径，并读取项目根目录 `提示词模板.md` 的内容。
- 硬性约束：不使用视觉伴侣；浏览器测试使用 chrome-devtools；不运行非浏览器测试或构建；中心服务不独立启动；文档、注释和回复使用中文；新增或修改代码补中文注释。
- 需求同步：当前 `需求.md` 与 `计划.md` 已覆盖“MCP 不可长期只记录不可用占位，需要接入工具调用闭环”的目标；本轮只需在完成后勾选对应计划项并校准必要说明。
- 验收方式：先用浏览器复现 MCP 工具不可用或无法返回 IDEA 项目路径的红灯，再通过浏览器保存 MCP 配置、发送测试消息，验证对话过程出现真实 MCP 调用并返回当前项目路径和 `提示词模板.md` 内容摘要。

## 任务拆分

- [x] 读取 `提示词模板.md`、`需求.md`、`架构.md`、`计划.md` 和 `AGENTS.md`。
- [x] 读取并使用 `using-superpowers`、项目开发规范、MCP 构建、TDD、用户环境和完成前验证相关技能。
- [x] 定位中心服务 MCP 配置、统一工具注册表、模型工具闭环和前端过程卡片渲染。
- [x] 通过浏览器红灯确认旧中心服务仍返回 `MCP_SERVER_NOT_RESOLVED`，且单轮工具回填会导致后续 MCP 读取文件无法继续。
- [x] 实现 MCP 配置解析、HTTP MCP 连接、工具调用事件和模型工具结果回填。
- [x] 更新 `计划.md` 对应任务状态。
- [x] 关闭旧 `dev:desktop-shell` 相关进程，并通过 IDEA 或用户可见环境重新拉起最新桌面端。
- [x] 只用 Chrome DevTools 完成浏览器红绿回归验证。
- [x] 按“任务完成执行”走 git 规则，删除提示词引用图片，检查远端领先并提交推送。

## 当前状态

- [x] 已完成项目必读文档和模板读取。
- [x] 已确认当前 `builtin.mcp.call` 仍是不可用能力，模型工具定义不会携带 MCP 工具。
- [x] 已完成浏览器红灯：旧中心服务能力接口返回 `MCP_SERVER_NOT_RESOLVED`，首轮 MCP 后无法继续读取文件。
- [x] 已完成浏览器绿灯：中心服务通过 `mcp/global.json` 连接 IDEA MCP，连续调用 `get_all_open_file_paths`、`execute_terminal_command`、`get_file_text_by_path`，最终回复项目路径 `C:\CODE\privateProject\zhixin-agent` 和 `提示词模板.md` 内容。
