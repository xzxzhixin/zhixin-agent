# 本次计划

- 目标：修复对话过程卡片展示体验，覆盖真实思考展示、卡片标题单行省略、乱码输出、工具内容高度和默认折叠、命令与其他工具卡片区分。
- 用户最新指令：使用 `/using-superpowers`；先和用户对齐需求所有细节后再落实 TDD；不使用视觉伴侣；任务大时主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复；确认过程中持续更新 `本次计划.md`；普通说明使用 `-`，任务项使用 `- [ ]`；浏览器使用 Chrome DevTools 按人的方式点击、输入等测试；任务完成再做 git 处理；gitignore 忽略的不要强制加入；发现远端领先先拉取；工作区里面所有改动全部自动推送；不校验 `提示词模板.md` 是否被修改，直接提交；任务完成再删除提示词里面引用的图片；执行 `dev:desktop-shell` 前必须先关闭之前进程；服务启用使用用户环境的 node 启动。
- 本次需求：
  - 思考卡片需要确认“是否就是没有思考”：没有供应商真实可公开思考正文时，只展示标题和耗时，不显示假正文；如果模型或供应商返回真实可公开思考摘要，则展示真实内容。
  - 卡片标题只占用一行，超出部分使用 `...` 显示，不能把长命令标题撑满过程卡片。
  - UI 上的乱码必须修复，命令输出或错误详情应按真实文本可读展示。
  - 卡片内容最大高度为 `200px`，超出内容在卡片内部滚动。
  - 工具卡片完成后默认折叠，运行中可以展开显示进度，完成后用户可手动展开查看。
  - 命令执行卡片要和普通工具、文件、MCP、插件、skill 卡片有明显区分。
- 硬性约束：中心服务不能独立启动；`dev:frontend` 只负责前端 Vite；`dev:desktop-shell` 只启动桌面壳并由桌面壳拉起中心服务；不使用 `tsc` 或 `vue-tsc` 做命令行质量门槛；TS 文件如需检查只用 IDE 检查；新增或修改代码必须有中文注释；多属性对象、数组和参数拆多行；Vue 属性每个一行；CSS 声明一行一个；单文件不超过 1500 行；不回滚用户已有改动；不删除应用图标或界面图标资源。
- 已读取技能：`using-superpowers`、`brainstorming`、`test-driven-development`、`writing-plans`、`dispatching-parallel-agents`、`subagent-driven-development`、`verification-before-completion`、`project-dev-standards`、`systematic-debugging`、`using-user-environment`。
- 已读取项目事实源：`需求.md`、`架构.md`、`计划.md`、`AGENTS.md`。
- 当前工作区状态：`main` 分支；本地已有大量未提交改动，包含上一轮协议适配、文档、锁文件、`提示词模板.md` 等内容；本轮不会回滚无关改动；提交前必须重试远端检查。
- 截图校准：
  - `img.png` 显示思考卡片只有标题“已思考（用时 0 秒）”，命令标题过长并占多行，命令内容直接摊开且高度过大。
  - `img_1.png` 显示命令输出区域出现大量 `�` 乱码，疑似命令输出编码或 Python traceback 编码被错误解码。
- 根因调查方向：
  - 前端：`apps/frontend/src/views/Chat/chat-view-helpers.ts` 聚合工具过程标题、日志、响应内容；`apps/frontend/src/views/Chat/components/ChatConversationPanel.vue` 渲染思考卡片和过程卡片。
  - 中心服务：`services/center/src/tool-runtime-command.ts` 负责命令 spawn、stdout/stderr 解码和 `tool.command.output` 事件。
  - 回归脚本：`scripts/check-chat-thinking-card-regression.mjs`、`scripts/check-chat-turn-render-order.ts`、`scripts/check-chat-experience-regressions.mjs` 可能需要扩展。
- 需求同步口径：本轮用户明确提出产品能力、交互口径和验收口径，必须先同步 `需求.md`，并评估更新 `架构.md`、`计划.md`，再落生产代码。
- TDD 口径：每个实现阶段先写失败检查或测试脚本，确认红灯，再写最少实现，确认绿灯；缺陷修复必须先有可复现失败检查，不能先改生产代码。
- 浏览器验收口径：启动前关闭旧桌面壳和中心服务进程；使用用户环境的 node 执行 `dev:desktop-shell`；用 Chrome DevTools 真实点击、输入、发送能触发命令的项目对话，观察思考卡片、命令卡片折叠、高度、标题省略、乱码和类型区分。
- 子代理分工：
  - 前端过程卡片探索：只读梳理过程卡片标题、内容、高度、折叠状态和类型样式影响面。
  - 命令乱码探索：只读梳理中心服务命令 stdout/stderr 解码、事件负载和前端日志渲染影响面。
  - 回归检查探索：只读梳理已有脚本能覆盖哪些口径、需要新增哪些红灯断言。

## 方案选项

- 方案 A：只改前端样式和折叠。优点是快；缺点是乱码若来自中心服务解码仍无法根治，命令与工具区分只能停留在视觉层。
- 方案 B：前端过程卡片结构化区分 + 中心服务命令输出按平台编码解码 + 回归脚本覆盖。优点是覆盖截图所有问题且符合现有事件事实源；缺点是涉及前后端和脚本。
- 方案 C：重做过程卡片组件体系，把命令、文件、MCP、插件、skill 拆成独立组件。优点是长期边界最清楚；缺点是本轮改动范围大，容易和现有未提交改动冲突。
- 推荐方案：方案 B。它能用最小架构变更修复本轮所有症状，并保留后续拆分独立组件的空间。

## 目标设计草案

- 思考卡片：沿用当前“真实可公开思考正文”口径；如果没有正文，完成态只保留“已思考（用时 X 秒）”标题，正文区域不渲染占位。
- 过程卡片：改为可折叠卡片，运行中默认展开，完成或失败后默认折叠；标题行单行显示，超出省略。
- 命令卡片：使用命令专属视觉标识和状态文案，标题优先展示规范化后的实际命令；正文内分 stdout、stderr 或日志流片段展示，最大高度 `200px`。
- 非命令工具卡片：保留工具、MCP、插件、skill 语义，但标题和内容高度遵守同一上限，完成后默认折叠。
- 乱码处理：中心服务优先保证 Windows 命令输出按可读编码解码，前端只展示中心服务给出的 UTF-8 文本，不在 UI 里二次猜测业务编码。

## 任务拆分

- [x] 读取 `using-superpowers` 和本轮适用技能。
- [x] 重读 `需求.md`、`架构.md`、`计划.md`、`AGENTS.md`。
- [x] 查看当前截图、Git 工作区状态和初步代码位置。
- [x] 更新 `本次计划.md` 固化本轮目标、约束、任务拆分和校准记录。
- [ ] 并行分派只读子代理梳理前端过程卡片、命令乱码和回归检查影响面。
- [ ] 汇总子代理结论，向用户确认推荐方案和关键需求边界。
- [x] 用户确认后同步 `需求.md`。
- [x] 按影响面评估并同步 `架构.md`。
- [x] 同步 `计划.md` 新增本轮实施项。
- [x] TDD 红灯 1：新增过程卡片 UI 结构检查，证明标题单行省略、内容最大 200px、完成后默认折叠、命令类型区分当前不满足。
- [x] 绿灯 1：修复前端过程卡片聚合和样式。
- [x] TDD 红灯 2：新增命令输出编码检查，证明 Windows 中文错误输出或 traceback 会出现替换字符乱码。
- [x] 绿灯 2：修复命令输出解码与事件负载。
- [x] TDD 红灯 3：新增思考空正文检查，证明没有真实思考正文时不会渲染假内容或空占位。
- [x] 绿灯 3：按真实思考口径修正必要边界。
- [x] 阶段审查：回看 `本次计划.md`、最新用户要求和文件状态，确认没有偏移。
- [x] 运行项目允许的回归检查，不使用 `tsc` / `vue-tsc`。
- [x] 关闭旧进程后执行 `dev:desktop-shell`，用 Chrome DevTools 按人的方式完成浏览器验收。
- [x] 检查 `提示词模板.md` 是否存在图片引用；只删除提示词引用的图片，不删除应用图标、界面图标或资源文件。
- [ ] 最终重试远端检查；如远端领先先拉取并处理冲突。
- [ ] 提交全部工作区非忽略改动，包括 `提示词模板.md`，不强制添加 gitignore 忽略文件。
- [ ] 推送到远端。

## 待确认问题

- [x] 是否确认采用方案 B：前端结构化修复过程卡片，同时修中心服务命令输出解码，并用回归脚本覆盖？
- [x] 命令卡片完成后是否统一默认折叠，失败状态是否也默认折叠但保留红色失败标识？
- [x] 命令输出最大 `200px` 是仅内容区高度，还是包含标题和状态栏的整张卡片高度？当前推荐为仅内容区最大 `200px`。

## 当前状态

- [x] 已确认本轮为对话过程卡片和命令输出体验修复，属于行为变更，需要 TDD。
- [x] 已确认用户明确不使用视觉伴侣，截图只作为需求参考和验收口径。
- [x] 已确认工作区已有大量未提交改动，后续只追加本轮必要修改，不回滚无关内容。
- [x] 用户已确认方案 B、失败卡片默认折叠、`200px` 为正文内容区最大高度。
- [x] 已同步 `需求.md`、`架构.md` 和 `计划.md`。
- [x] 已完成红灯和绿灯自动化检查，准备浏览器验收。
- [x] 已使用 Chrome DevTools 验证新命令卡片默认折叠、命令类型类存在、标题单行省略、正文内容区最大高度 `200px`、中文命令输出无新乱码。
- [x] 已确认 `提示词模板.md` 引用 `img.png` 和 `img_1.png`，准备删除这两个 gitignore 忽略的临时截图文件。

## 校准记录

- 2026-06-08：用户要求使用 `/using-superpowers`，先对齐需求所有细节后再 TDD 执行，不使用视觉伴侣。
- 2026-06-08：用户基于 `img.png` 和 `img_1.png` 提出五点反馈：确认是否没有思考、卡片标题单行省略、UI 乱码、内容最大 200px 且工具卡片完成后默认折叠、命令执行卡片与其他卡片区分。
- 2026-06-08：用户确认采用方案 B；失败命令卡片同样默认折叠但保留失败标识；`200px` 指卡片正文内容区最大高度，不包含标题栏。
- 2026-06-08：新增 `scripts/check-chat-process-card-regression.mjs` 先红灯，随后修复前端过程卡片和中心服务命令输出解码后变绿；相关思考、对话顺序、工具闭环和半截工具意图检查已通过。
- 2026-06-08：浏览器验收通过：发送 PowerShell 中文输出命令后，新命令卡片完成后默认折叠，展开后只显示一次“你好，致心”，computed style 显示标题 `nowrap`、`ellipsis`、`hidden`，正文 `max-height: 200px` 且 `overflow: auto`。
