# 本次计划

- 目标：修复对话消息流中思考过程不真实、工具调用卡片开始时机滞后、输入框内当前窗口全量对话 token 统计展示，以及桌面端点击外部链接后错误在桌面端自身打开的问题。
- 用户最新指令：使用 `/using-superpowers`；先对齐需求细节再按 `TDD` 执行；不使用视觉伴侣；任务再大也必须本轮持续推进完成；先读取根目录 `提示词模板.md`；先读“公共”；测试只做浏览器测试；完成后走“任务完成执行”；执行 `dev:desktop-shell` 前必须先关闭之前的进程。
- 已读文件：`提示词模板.md`、`需求.md`、`架构.md`、`计划.md`、`AGENTS.md`。
- “公共”校准：仓库根目录没有独立 `公共` 文件；当前按公共工作台壳 `apps/frontend/src/views/MainView.vue` 与公共协作规则理解并读取。
- 硬性约束：中心服务不能独立启动；浏览器测试使用 chrome-devtools；不使用视觉伴侣；代码和文档保持中文注释；不使用 `tsc` 或 `vue-tsc` 作为质量门槛。
- 需求同步：用户提出的三项现象属于产品交互和验收口径，需要先同步 `需求.md` 与 `计划.md`；若实现触及事件协议、前端聚合规则或 token 统计接口，需要同步 `架构.md`。
- TDD 口径：先通过 Chrome DevTools 浏览器复现红灯，或者用浏览器测试脚本驱动页面验证失败；再实现修复；最后用浏览器测试验证绿灯。
- 验收方式：桌面壳拉起中心服务后，在浏览器端发送真实对话，确认思考正文来自真实阶段摘要而非固定模板；工具调用开始事件到达时立即显示工具卡片，工具响应到达后在同一卡片追加响应内容；输入框 token 展示当前对话窗口所有已进入上下文的消息 token 汇总，刷新时机绑定大模型每次响应过程或响应完成；桌面端点击外部链接不让主窗口跳转到该链接页面。

## 任务拆分

- [x] 读取 `提示词模板.md` 与项目四个必读文档。
- [x] 读取并使用 `using-superpowers`、需求对齐、TDD、系统化调试、项目规范、用户环境和完成前验证相关技能。
- [x] 排查公共工作台壳、对话过程渲染、中心服务思考事件、token 统计链路和桌面端链接打开策略。
- [x] 同步 `需求.md`、`架构.md` 和 `计划.md` 的本轮验收口径。
- [x] 用 Chrome DevTools 浏览器测试形成红灯证据。
- [x] 实现思考过程、工具调用卡片和输入框 token 统计修复。
- [x] 关闭旧 `dev:desktop-shell` 相关进程并重新拉起最新桌面端。
- [x] 用 Chrome DevTools 浏览器完成绿灯验证。
- [x] 按“任务完成执行”处理 git 规则并删除提示词引用图片。

## 当前状态

- [x] 已确认当前 `提示词模板.md` 是本轮最新执行模板。
- [x] 已确认工作区已有 `提示词模板.md` 修改，作为用户或本轮模板变更保留，不擅自回滚。
- [x] 根因调查已完成：中心服务公开思考摘要、前端工具卡片聚合、token 统计请求体和桌面壳外链导航守卫均已定位并修复。

## 校准记录

- 2026-06-07：用户要求不使用视觉伴侣，且测试只做浏览器测试；本轮不使用静态检查或构建作为验收结论。
- 2026-06-07：用户校准 token 统计不是输入草稿 token，而是当前窗口所有对话上下文 token 汇总；展示位置在输入框内，刷新时机在大模型每次响应过程或响应完成时。
- 2026-06-07：用户校准工具卡片必须在工具开始调用时立即出现，不能等工具响应完成后才显示。
- 2026-06-07：用户新增桌面端链接问题：点击外部链接后不应让桌面端主窗口自身打开该链接。
