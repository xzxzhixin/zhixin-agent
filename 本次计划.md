# 本次计划：LangGraphJS 替换与未完成计划收尾

## 目标

- 将当前自研对话图、任务编排、检查点和工具调用闭环，逐步替换为基于 `@langchain/langgraph` 的执行图。
- 完成 `计划.md` 当前未完成项，不降低既有中心服务事实源、事件流、UI 展示和记忆 Markdown 约束。
- 明确长期记忆实现方式，优先选择适合 TypeScript、中心服务、SQLite 和 Markdown 可迁移事实源的方案。

## 用户最新指令

- 先对齐需求所有细节，再落实 TDD 执行。
- 不使用视觉伴侣。
- 大任务必须持续推进；上下文复杂时创建或更新 `本次计划.md`。
- 主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复。
- 能隔离的执行工作交给子代理；共享文件风险按阶段分批处理。
- 本次测试要求重启保证最新代码生效；重启前检查并关闭运行中的代码；浏览器使用 chrome-devtools 测试。
- 任务完成后，忽略文件不强制加入；发现远端领先先拉取；工作区所有改动自动推送；删除提示词引用的图片。
- 最新校准：中心服务配置不再使用弹窗编辑，进入中心服务页后直接展示编辑面板。
- 最新校准：中心目录允许迁移到其他位置，程序和数据分离，迁移中心目录到其他机器后应能继续使用。
- 最新校准：核心执行图要直接使用 LangGraphJS，再适配项目现有需求和功能，而不是只做外围包装。
- 最新校准：记忆方案需要继续评估好用的三方库，但必须适配中心目录整体迁移。

## 硬性约束

- 开始实现前必须读取 `需求.md`、`架构.md`、`计划.md`、`AGENTS.md`。
- 未经用户明确要求，不修改 `需求.md`。
- 不允许只改代码不改 `计划.md`。
- 中心服务不能独立启动，测试与验收必须通过桌面端或 `dev:desktop-shell` 拉起。
- TypeScript 不通过 `tsc --noEmit` 或 `vue-tsc` 做命令行质量门槛。
- 新增或修改代码必须补中文注释，字段和协议字段必须说明来源、含义、格式、默认值或约束。
- 生产代码变更必须按 TDD：先写失败测试或检查脚本，确认红灯，再写最少实现，最后验证绿灯。
- 默认不提交 Git；本轮用户已要求任务完成后自动推送，因此收尾前需检查远端状态并按实际变更处理提交与推送。

## 当前已知未完成项

- `计划.md` 阶段 8：文件读取、文件写入、文件删除、插件调用、MCP 调用和 skill 使用统一接入工具调用闭环。
- `计划.md` 阶段 8：工具调用事件和 UI 展示补齐审批状态、文件操作、插件/MCP/skill 真实执行结果，以及工具结果影响最终回复的可视化链路。

## 子代理分工

- 子代理 A：只读梳理中心服务自研对话图、执行引擎、模型网关、工具运行时和记忆提交的替换落点。
- 子代理 B：只读梳理 `计划.md` 未完成项、对应源码和验证入口。
- 主线程：查官方资料、对齐设计方案、创建总控计划、后续按用户确认进入 TDD 实施。

## 官方资料初步校准

- `@langchain/langgraph` 当前 npm 查询版本为 `1.3.6`。
- `@langchain/langgraph-checkpoint-sqlite` 当前 npm 查询版本为 `1.0.1`。
- LangGraphJS 官方文档说明：编译图时提供 checkpointer 后，会按 thread 保存每步 graph state checkpoint，用于 human-in-the-loop、会话记忆、time travel 和故障恢复。
- 官方文档说明：SQLite checkpointer 属于独立安装包，适合本地工作流。
- 官方文档说明：跨 thread 的长期记忆不能只靠 checkpointer，需要 Store 或应用自己的长期存储；项目现有 `memory/*.md` 与 SQLite 索引仍应作为可迁移事实源。
- npm 未查询到官方 `@langchain/langmem` 包；当前不把第三方 LangMem 作为 TypeScript 生产依赖前提。
- 记忆三方库初筛方向：优先 local-first、TypeScript SDK、SQLite 或 Markdown 文件事实源、可随中心目录整体迁移；排除强依赖云服务、PostgreSQL/pgvector 或外部常驻服务的方案作为核心事实源。
- Mem0 复核结论：`mem0ai` npm 当前版本 `3.0.6`，许可证 Apache-2.0，仓库目录为 `mem0-ts`；OSS Node 文档推荐 `import { Memory } from "mem0ai/oss"`。
- Mem0 Node OSS 默认说明：使用 OpenAI `gpt-5-mini`、`text-embedding-3-small`、内存向量库和 SQLite history；生产配置可指定 `historyDbPath`。
- Mem0 npm 包内部 `mem0ai/oss` 类型和实现显示：支持 `Memory.add/search/update/delete/history/getAll/reset`，支持 `vectorStore.config.dbPath`、`historyDbPath`、`HistoryStoreConfig`，并包含本地 `MemoryVectorStore`、SQLite history、Qdrant 和 LangChain VectorStore 适配。
- Mem0 适配风险：默认 LLM/Embedding 会直接走 OpenAI 或其他 provider，必须改成通过中心服务供应商网关；默认路径必须显式配置到 `center-data` 下，不能使用包默认目录；不能让 Mem0 云服务或外部向量数据库成为迁移事实源。
- Mem0 建议定位：可以作为“长期记忆抽取、语义索引和检索引擎”重点候选；项目事实源仍保留 Markdown 记忆与 SQLite `memory_index`，Mem0 的 SQLite/向量文件放入中心目录并可重建或迁移。

## 建议方案草案

### 方案 A：渐进式适配 LangGraphJS

- 保留中心服务现有 REST、WebSocket、事件日志、SQLite 表和 Markdown 记忆事实源。
- 新增中心服务内部 `langgraph-runner` 适配层，把当前线性节点映射为 LangGraph `StateGraph` 节点。
- `sessionId` 继续作为 `thread_id`，`turnId` 继续作为本项目事件和 graph run 关联 ID。
- LangGraph checkpointer 用项目中心目录下独立 SQLite 文件或同库隔离表；项目自己的 `events`、`task_steps` 和 `memory_index` 仍为 UI 和迁移事实源。
- 优点：风险低，可用 TDD 分阶段替换，保留当前已验证的前端过程展示。
- 缺点：短期会存在适配层，不能一次删除所有自研文件。

### 方案 B：大幅替换为 LangGraph 原生 Agent

- 使用 LangGraph 预构建 Agent/React 工具循环替换当前 `session-domain` 和 `model-gateway-runtime` 大部分执行逻辑。
- 优点：自研编排代码减少最多。
- 缺点：当前项目有强事件展示、供应商网关、审批、Markdown 记忆和多端同步要求，直接替换会带来较大回归风险。

### 方案 C：只引入 checkpointer，不替换执行图

- 只使用 LangGraph checkpointer 或 Store 思路增强恢复，不改当前执行链路。
- 优点：改动小。
- 缺点：不符合“把我们自己实现的全部替换到 LangGraphJS”的目标。

## 当前推荐

- 推荐方案 A。
- 先把 LangGraphJS 作为中心服务内部执行图内核引入，再逐步把自研节点生成、checkpoint 和工具循环迁移进去。
- 长期记忆保留现有 Markdown + SQLite 索引；LangGraph checkpointer 只负责运行中/短期图状态恢复，避免把可迁移长期记忆锁进框架私有 checkpoint。

## 待用户确认

- 是否同意采用“方案 A：渐进式适配 LangGraphJS”作为实施方向。
- 是否接受长期记忆采用“Markdown 事实源 + SQLite 索引 + LangGraph checkpointer/store 辅助”的组合，而不是引入非官方或不成熟的三方记忆包。
- 是否先按计划未完成项补齐工具闭环和 plugin-compact 验证，再做 LangGraphJS 内核替换；还是先引入 LangGraphJS，再补未完成工具闭环。

## 验收方式

- 静态检查或领域脚本先红灯再绿灯，不使用 `tsc --noEmit` 或 `vue-tsc`。
- 中心服务相关验证通过 `dev:desktop-shell` 拉起，不直接启动 `services/center`。
- `plugin-compact` 通过 chrome-devtools 验证紧凑 WebView。
- 计划完成后同步 `计划.md` 勾选状态，并评估是否需要同步 `架构.md`。
- 收尾检查 Git 状态、远端领先情况、忽略文件、提交和推送。

## 当前状态

- 已读取项目必读文件。
- 已发现当前工作区已有用户改动：`提示词模板.md`，本轮未回滚该文件。
- 用户已确认采纳方案 A：核心执行图直接接入 LangGraphJS，再按中心服务事实源、事件、任务步骤、权限、工具和记忆边界适配。
- 已引入 `@langchain/langgraph`、`@langchain/langgraph-checkpoint-sqlite` 和 `mem0ai`，并建立 `services/center/src/langgraph-runner.ts` 与 `services/center/src/memory-engine.ts`。
- 已把当前会话轮次入口接入 LangGraphJS `StateGraph`，`thread_id` 使用 `sessionId`，本地 checkpoint 放在中心目录 `db/langgraph-checkpoints.sqlite`，每轮结束释放 SQLite 连接。
- 已建立 Mem0 OSS 适配层，默认索引目录固定在中心目录 `memory/mem0`；当前默认不启用外部 LLM/Embedding，写入 `memory.mem0.skipped` 审计，等待后续接中心服务供应商网关或本地 embedding。
- 已把桌面端中心服务页改为进入后直接展示编辑面板，并提供中心目录选择入口；中心目录切换由桌面壳停止、初始化并重启中心服务。
- 已用 Chrome DevTools 验证中心服务页面桌面态无配置弹框、直接显示编辑面板、可选择中心目录。
- 已用 Chrome DevTools 验证 `plugin.html` 紧凑模式有顶部页签、消息列表、输入框、任务/智能体状态和连接信息，且无左侧对话栏和固定右侧状态栏。
- 已同步 `计划.md`：勾选 plugin-compact 验证、中心服务直接编辑面板、LangGraphJS runner、Mem0 OSS 适配层；未勾选仍未完成的文件/插件/MCP/skill 真实工具闭环。
- 已通过 `node scripts/check-all.mjs`，该命令包含前端构建、中心服务构建提示、内置插件构建和桌面壳绿色版资源检查；输出仍有 pnpm `shamefully-flatten` 改名警告和 Vite 大 chunk 警告。
- 本轮追加修复 Electron 启动竞态：用户反馈 `ERR_CONNECTION_REFUSED loading http://127.0.0.1:8866/?port=8866`。根因定位为桌面壳拉起中心服务后立即 `loadURL`，没有等待 `/api/health`，且加载失败会形成未处理 Promise rejection。
- 已按 TDD 新增 `scripts/check-desktop-shell-center-health.mjs`，先确认红灯，再在 `apps/desktop-shell/src/main.ts` 加入中心服务健康检查轮询、超时诊断页和 `loadURL` 错误捕获；已同步 `架构.md` 与 `计划.md`，并把该检查纳入 `scripts/check-all.mjs`。
