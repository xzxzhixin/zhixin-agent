# 本次计划

- 目标：把 Agent 每轮执行从“LangGraph 包裹单个旧闭环”升级为 LangGraphJS 全量承载 agent loop，并把模型网关的内部模型协议改为 OpenAI Chat Completions 协议作为中心规范，其他供应商协议通过适配层转换为 OpenAI 协议接入。
- 用户最新指令：使用 `/using-superpowers`；和用户对齐需求所有细节后再落实 TDD；不使用视觉伴侣；任务明显过大或上下文复杂时先创建或更新 `本次计划.md`；主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复；能隔离的执行工作交给子代理；阶段完成后回看 `本次计划.md`、最新用户要求和文件状态；浏览器使用 chrome-devtools 测试；gitignore 忽略的不要强制加入；发现远端领先先拉取；工作区里面所有改动全部自动推送；不校验 `提示词模板.md` 是否被修改，直接提交；删除提示词里面引用的图片；执行 `dev:desktop-shell` 前必须先关闭之前的进程，保证最新代码生效。
- 硬性约束：中心服务不能独立启动；`dev:frontend` 只负责前端 Vite；`dev:desktop-shell` 只启动桌面壳并由桌面壳拉起中心服务；不使用 `tsc` 或 `vue-tsc` 做命令行质量门槛；代码和文档使用中文注释；多属性对象、数组和参数拆多行；单文件不超过 1500 行；不回滚用户已有改动；不删除应用图标或界面图标资源。
- 已读取技能：`using-superpowers`、`brainstorming`、`writing-plans`、`test-driven-development`、`dispatching-parallel-agents`、`subagent-driven-development`、`requesting-code-review`、`verification-before-completion`、`using-user-environment`。
- 已读取项目事实源：`AGENTS.md`、`需求.md`、`架构.md`、`计划.md`。
- 当前工作区状态：`main` 分支；本地已有 `提示词模板.md` 修改，按用户要求不校验该文件是否被修改且不回滚；`git fetch origin` 因无法连接 GitHub 443 失败，后续提交前必须重试远端检查。
- 子代理分工：LangGraph 现状探索子代理负责只读梳理当前 agent loop；模型协议探索子代理负责只读梳理内部协议和 OpenAI 迁移影响面；提示词图片引用探索子代理已完成，确认 `提示词模板.md` 没有实际图片引用，不删除应用图标和界面 SVG。
- 需求同步口径：用户最新需求涉及核心执行架构和模型协议事实源，必须先同步 `需求.md`、`架构.md`、`计划.md`，再落代码。
- TDD 口径：每个实现阶段先写失败检查或测试脚本，确认红灯，再写最少实现，确认绿灯；不能用直接通过的后补测试冒充 TDD。
- 浏览器验收口径：涉及桌面端或真实对话链路时，执行 `dev:desktop-shell` 前先关闭旧桌面壳和中心服务进程，再用 chrome-devtools 验证最新代码行为。
- 第三方包口径：确需使用三方包时，优先选择 GitHub star 数最多且近期仍维护的包；OpenAI 协议相关首选官方 `openai` npm 包，但本轮优先复用现有 `fetch` 网关，只有确有必要才新增依赖。

## 目标设计草案

- LangGraph 全量接入 agent loop 的目标设计：
  - 每轮对话由 `StateGraph` 拆成明确节点，而不是只有 `completeCreatedTurn` 单节点。
  - 节点至少覆盖：上下文整理与公开思考摘要、模型首轮流式输出、工具调用请求解析、工具计划与审批、工具执行、工具结果回填模型、最终助手消息固化、记忆提交、用量记录、失败或半截工具意图收尾。
  - 每个节点边界写入 `task_steps`、`events.payload.graph` 和必要状态摘要；LangGraph checkpointer 只作为运行态恢复辅助，不替代中心目录 Markdown、JSON、SQLite 和附件事实源。
  - 副作用节点必须携带 `idempotencyKey`，恢复时不能重复执行已经完成的命令、文件写入、插件、MCP 或 skill 调用。
  - 模型工具调用循环要由 LangGraph 条件边控制：有 `tool_calls` 进入工具节点，无工具调用进入最终固化节点；工具结果回填后回到模型节点继续生成最终回复。
- OpenAI 协议统一目标设计：
  - 中心服务内部模型请求体、消息、工具定义、工具调用、工具结果和流式解析的规范统一为 OpenAI Chat Completions 形态。
  - 彻底删除 `packages/model-protocol` 包和工作区引用，不保留自创内部模型协议包。
  - OpenAI 兼容供应商直接按 `/v1/chat/completions` 接入；OpenAI Responses 和 Anthropic Messages 等其他协议必须通过适配层转换为 OpenAI Chat Completions 规范接入。
  - 工具调用只认 OpenAI 标准 `tool_calls` / `tool_call_id` / `role: "tool"` 闭环；历史文本 JSON 工具调用兼容逻辑必须删除，避免非结构化协议继续存在。
  - 内部所有模型请求、工具定义、工具回填、流式解析和用量归一化都围绕 OpenAI 规范命名，不再使用 `ModelRequest`、`ModelMessage`、`ModelStreamEvent` 等旧内部协议名。
- 供应商配置和提示目标设计：
  - 新增供应商只填写 `baseUrl` 和 API Key 时允许保存，保存后可继续拉取模型；配置不完整时不能启用。
  - “不完整无法启用”“保存成功”“模型获取失败”等提示不能被弹框遮挡，所有此类提示统一使用 Element Plus `ElMessage`。
  - 弹框内不再把关键错误或成功提示显示到弹框后面，阻断用户判断。
- 提示词图片处理目标设计：
  - `提示词模板.md` 当前没有实际图片引用，不删除任何图片文件。
  - 若后续发现提示词文本中的图片引用，只删除提示词里的引用，不删除 `图标.png`、`assets/ui-icons/*.svg`、favicon 或应用资源。

## 任务拆分

- [x] 读取 `using-superpowers` 和本轮适用技能。
- [x] 重读 `AGENTS.md`、`需求.md`、`架构.md`、`计划.md`。
- [x] 检查当前 Git 状态和远端配置。
- [x] 并行分派只读子代理梳理 LangGraph、模型协议和提示词图片引用。
- [x] 更新 `本次计划.md` 固化本轮目标、约束、任务拆分和校准记录。
- [x] 等待 LangGraph 和模型协议子代理返回，合并只读结论。
- [x] 和用户确认关键需求边界后进入实现。
- [x] 同步 `需求.md`：新增 OpenAI 协议统一和 LangGraph 全量 agent loop 最新口径。
- [x] 同步 `架构.md`：更新 `packages/model-protocol`、模型网关、协议适配器和 LangGraph 节点边界。
- [x] 同步 `计划.md`：新增本轮实施项，完成后勾选。
- [x] TDD 红灯 1：新增 LangGraph 多节点 agent loop 检查，证明当前单节点实现失败。
- [x] 绿灯 1：把 agent loop 拆入 LangGraph 多节点和条件边。
- [x] TDD 红灯 2：新增 OpenAI 协议统一检查，证明当前内部 `ModelRequest` 核心协议仍存在。
- [x] 绿灯 2：替换内部模型协议为 OpenAI Chat Completions 规范和供应商适配层。
- [x] TDD 红灯 3：新增历史文本 JSON 工具调用禁用检查，证明当前仍兼容旧内部工具调用。
- [x] 绿灯 3：移除或禁用旧文本工具调用协议，失败时写可排查事件。
- [x] TDD 红灯 4：新增提示词图片引用检查，确认没有图片引用或能删除引用。
- [x] 绿灯 4：提示词模板没有实际图片引用；无需删除图片引用。
- [x] TDD 红灯 5：新增供应商保存和启用校验检查，证明当前只填 `baseUrl` 和 API Key 无法保存或提示被弹框遮挡。
- [x] 绿灯 5：允许不完整供应商保存但禁止启用，并把所有提示统一为 `ElMessage`。
- [x] TDD 红灯 6：新增全量项目对话测试检查，使用项目根目录 `项目对话测试` 验证真实对话链路。
- [x] 绿灯 6：修复全量项目对话测试中暴露的问题。
- [x] 执行阶段审查：主线程回看 `本次计划.md`、最新用户要求和文件状态。
- [x] 运行项目允许的回归检查，不使用 `tsc` / `vue-tsc`。
- [x] 若涉及真实对话或桌面链路，关闭旧进程后执行 `dev:desktop-shell`，用 chrome-devtools 验证。
- [x] 最终重试 `git fetch origin`，若远端领先则先拉取并处理冲突。
- [x] 提交全部工作区改动，包括 `提示词模板.md`，不强制添加 gitignore 忽略文件。
- [x] 推送到远端。

## 待确认问题

- [x] `packages/model-protocol` 处理方式：用户确认彻底删除，全量接入 OpenAI 规范。
- [x] 中心协议规范：用户确认内部协议全量使用 OpenAI Chat Completions 规范。
- [x] 历史文本 JSON 工具调用兼容：用户确认删除，全量接入 OpenAI 协议。
- [x] LangGraph 接入范围：用户确认全量接入，并用当前项目目录下 `项目对话测试` 目录作为全量测试。
- [x] 供应商保存口径：用户确认只填 `baseUrl` 和 API Key 时允许保存；不完整配置无法启用。
- [x] 提示口径：用户确认所有提示位置全部使用 `ElMessage`。

## 当前状态

- [x] 已确认本轮任务范围很大，先以 `本次计划.md` 作为恢复事实源。
- [x] 已确认远端检查首次失败，原因是无法连接 GitHub，不把该失败视为远端未领先。
- [x] 已确认提示词模板没有实际图片引用；不删除应用图标和界面图标。
- [x] 已确认 LangGraph 当前只有 `completeCreatedTurn` 一个真实节点，思考、模型流、工具执行、结果回填、消息固化、记忆和用量仍在线性函数中执行。
- [x] 已确认 `memory.write`、`usage.recorded`、部分失败事件等关键事件目前没有 `payload.graph`，恢复和防重复写入存在风险。
- [x] 已确认模型网关当前使用自定义 `ModelRequest` / `ModelMessage` / `ModelStreamEvent` 内部协议，并在主网关内混合处理 OpenAI Chat Completions、OpenAI Responses 和 Anthropic Messages。
- [x] 已确认前端和共享包不直接依赖 `packages/model-protocol`，直接依赖者主要是中心服务和两个内置模型协议插件。
- [x] 已获得用户对关键需求边界的确认，并新增供应商保存、启用和 `ElMessage` 提示口径。
- [x] 已修改业务代码并完成静态回归；已关闭旧中心服务和桌面壳进程后重启 `dev:desktop-shell`，并通过 `项目对话测试` 完成真实项目对话和 chrome-devtools 浏览器验收。

## 校准记录

- 2026-06-08：用户要求不使用视觉伴侣，浏览器测试使用 chrome-devtools。
- 2026-06-08：用户要求所有隔离执行工作交给子代理；主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复。
- 2026-06-08：用户要求发现远端领先先拉取，工作区所有改动自动推送；首次 `git fetch origin` 因网络连接 GitHub 失败，后续提交前必须重试。
- 2026-06-08：提示词图片引用子代理确认 `提示词模板.md` 无实际图片引用，只有删除图片的文字指令。
- 2026-06-08：LangGraph 子代理确认当前只是单节点套壳，不满足“全量接入 agent loop”；模型协议子代理确认当前仍是内部协议，不是 OpenAI 协议事实源。
- 2026-06-08：用户确认彻底删除 `packages/model-protocol`，中心内部全量使用 OpenAI Chat Completions 规范，删除旧文本 JSON 工具调用兼容，LangGraph 全量接入，并用 `项目对话测试` 做全量验收。
- 2026-06-08：用户新增供应商配置口径：只填 `baseUrl` 和 API Key 可以保存，配置不完整不能启用，所有提示统一使用 `ElMessage`，避免提示显示在弹框后面。

- 2026-06-08：主线程完成 LangGraph 多节点 runner、OpenAI Chat Completions 内部协议、供应商保存启用和 ElMessage 提示切片；`node scripts/check-openai-langgraph-provider-regression.mjs` 已通过，后续进入桌面链路和项目对话验收。
- 2026-06-08：项目对话验收红灯发现轮次已完成但默认任务仍为 `running`；已调整 LangGraph 节点顺序为 `message.persist -> memory.commit -> tool.plan -> usage.record`，由 `usage.record` 最终同步轮次和当前任务完成状态。复测 `项目对话测试` 项目会话显示 `turnStatus=completed`、`taskStatus=completed`、全部步骤完成，Chrome DevTools 页面显示任务 `1/1` 和右侧“已完成”。
- 2026-06-08：提交前已重试 `git fetch origin` 成功，`git rev-list --left-right --count HEAD...origin/main` 返回 `0 0`，远端没有领先。
- 2026-06-08：已提交全部非忽略改动，提交为 `e256765 feat: 全量接入 OpenAI 协议和 LangGraph 执行图`；随后仅追加本计划提交状态记录。
- 2026-06-08：已推送到远端 `origin/main`，远端从 `7bdbc2a` 更新到 `1d3958e`。

