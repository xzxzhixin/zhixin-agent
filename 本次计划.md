# 本次计划

- 目标：修复对话停止失效、已回复但仍显示执行中、对话页业务请求全部切到 WebSocket、token 统计按当前对话窗口统计。
- 用户最新指令：使用 `/using-superpowers`；先和用户对齐需求所有细节后落实 TDD；不使用视觉伴侣；主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复；确认过程中持续更新 `本次计划.md`；普通说明使用 `-`，任务项使用 `- [ ]`，完成后改为 `- [x]`；浏览器使用 Chrome DevTools 按人的方式点击、输入等测试；任务完成再做 git 处理；gitignore 忽略的不要强制加入；发现远端领先先拉取；工作区里面所有改动全部自动推送；不校验 `提示词模板.md` 是否被修改，直接提交；任务完成再删除提示词里面引用的图片；执行 `dev:desktop-shell` 前必须先关闭之前的进程；使用用户系统环境里面的 node 启动；检索代码使用 MCP 服务 idea。
- 本次需求：用户点击停止后当前对话轮次必须停止，不能继续生成、继续跑工具或继续显示执行中。
- 本次需求：重新进入会话后状态恢复正常，说明持久化事实可能已经完成，但当前窗口实时状态或前端运行态没有被正确收尾。
- 本次需求：出现“已回复，但还在执行”时，本轮助手消息已经固化，任务、轮次、智能体状态和输入区运行态必须同步收尾。
- 本次需求：对话页业务数据和动作全部切到 WebSocket，不能在对话窗口继续通过 REST 拉取会话详情、历史事件、发送、停止、token 统计等对话业务能力。
- 本次需求：token 统计必须按照当前对话窗口统计，不能按全局、最近会话或错误会话累加；运行中和完成后的上下文 token 显示必须和当前窗口一致。
- 硬性约束：先确认需求细节再实现；实现前必须 TDD 红灯；中心服务不能独立启动；浏览器验收必须通过桌面壳拉起中心服务；执行 `dev:desktop-shell` 前先关闭旧桌面壳和中心服务进程；TS 文件不使用命令行编译器校验；新增或修改代码必须补中文注释；不回滚用户已有改动。
- 已读取技能：`using-superpowers`、`systematic-debugging`、`test-driven-development`、`using-user-environment`、`brainstorming`、`writing-plans`、`dispatching-parallel-agents`、`verification-before-completion`。
- 已读取项目事实源：`需求.md`、`架构.md`、`计划.md`、`AGENTS.md`。
- 当前计划依据：`计划.md` 已存在“长任务自动续跑、WebSocket 对话页、LangChain 与智能体架构重整”阶段，其中 WebSocket-only 已部分完成，但本轮截图反馈说明停止、状态收尾和 token 窗口统计仍有回归。

## 需求细节对齐草案

- 停止范围：点击当前对话窗口的“停止”只取消当前会话当前运行轮次；不影响其他普通会话、项目会话或 IDE 页签。
- 停止后状态：当前轮次标记 `cancelled`，当前任务标记 `cancelled`，相关运行中步骤标记 `cancelled` 或结束态，智能体状态回到 `idle` 或本轮结束态，输入区退出运行态并允许继续发送。
- 停止后输出：已经固化的用户消息、过程事件和已生成助手片段保留；如果助手最终消息尚未固化，不伪造完成回复；如果已经固化助手回复，停止不能再覆盖为失败。
- 已回复但仍执行：当助手最终消息固化、模型流结束且没有待执行工具时，中心服务必须同步完成轮次、任务和智能体状态；前端收到完成事件后必须清理本地运行态。
- 重进会话恢复：重新进入会话能正常，说明前端首屏快照可以修正状态；本轮修复需要让实时事件到达时也立即收尾，不能依赖重进会话。
- WebSocket-only 范围：对话页的首屏快照、历史事件补齐、发送、停止、附件后续状态、tokenizer.count、供应商/模型/智能体对话上下文快照都走 WebSocket；非对话管理页和本机授权健康类接口不纳入本轮。
- token 统计口径：按当前对话窗口的当前会话、当前轮次上下文消息、引用和正式附件计算；切换会话或新建草稿时必须重置请求签名和显示，旧响应不能覆盖新窗口。
- token 统计时机：运行中和完成后刷新；用户单纯输入草稿不触发全局式统计；如果用户发送新轮次，统计应归属新轮次和当前 session。
- 验收方式：先自动化红灯证明现状问题，再实现绿灯；最后通过 Chrome DevTools 真实点击发送、停止、重新进入会话、观察网络请求和 token 显示。

## 任务拆分

- [x] 读取本轮适用技能和项目必读文档。
- [x] 更新 `本次计划.md`，固化目标、用户最新指令、硬性约束、任务拆分、验收方式和当前状态。
- [x] 和用户确认停止范围、停止后状态、WebSocket-only 范围和 token 统计口径。
- [x] 用户确认后同步 `需求.md`、`架构.md` 和 `计划.md` 的本轮新增验收口径。
- [x] 使用 MCP idea 检索停止、WebSocket、tokenizer 和任务状态相关代码，完成根因调查。
- [x] 按独立领域分派只读子代理或并行检索：停止链路、WebSocket REST 残留、token 统计归属。
- [x] TDD 红灯：新增或扩展停止回归检查，证明停止后仍可继续生成或运行态不收尾。
- [x] TDD 红灯：新增或扩展 WebSocket-only 检查，证明对话页仍存在业务 REST 请求。
- [x] TDD 红灯：新增或扩展 token 统计检查，证明统计没有绑定当前对话窗口。
- [x] 绿灯：实现中心服务停止请求、Worker/图执行取消、任务轮次智能体状态收尾和事件广播。
- [x] 绿灯：实现前端停止动作、运行态清理和重进会话前后的状态一致。
- [x] 绿灯：补齐对话页 WebSocket-only 请求和响应，移除对话业务 REST 兜底。
- [x] 绿灯：修复 token 统计请求签名、会话窗口归属和旧响应丢弃。
- [x] 回看 `本次计划.md`，确认没有偏移后再进入验证。
- [x] 运行允许的自动化检查，不使用 `tsc` / `vue-tsc`。
- [x] 关闭旧桌面壳和中心服务进程，通过用户系统环境 Node 启动 `dev:frontend` 和 `dev:desktop-shell`。
- [x] 使用 Chrome DevTools 按人的方式验证发送、停止、重新进入会话、网络请求和 token 统计。
- [x] 同步更新 `计划.md` 任务完成状态，并评估 `需求.md`、`架构.md` 是否仍需补充。
- [x] 检查 `提示词模板.md` 图片引用，任务完成后删除提示词引用的临时图片。
- [x] 远端检查；如远端领先先拉取。
- [ ] 提交并推送全部非忽略改动。

## 待确认问题

- [x] 停止按钮是否只取消当前会话当前轮次，不取消同一会话中本地排队的后续消息？（已确认：采纳建议，只取消当前运行轮次，保留本地排队消息）
- [x] 停止后如果已有部分助手流式内容但尚未固化，是否保留为一条“已停止的部分回复”，还是只保留过程事件不固化助手消息？（采用保守口径：只保留过程事件，不伪造完成回复）
- [x] “对话全部切到 WebSocket”是否只限定对话页业务接口，管理页供应商、MCP、skill、运行环境仍允许 REST？（采用保守口径：限定对话页业务接口）
- [x] token 统计是否只统计当前会话已进入模型上下文的消息，不统计输入框未发送草稿？（沿用既有需求：不统计未发送草稿）

## 当前状态

- [x] 已进入需求对齐阶段，尚未修改业务代码。
- [x] 已确认本轮不使用视觉伴侣。
- [x] 等待用户确认本轮细节后进入 TDD 红灯。
- [x] 正在根因调查阶段，尚未修改业务代码。
- [x] 正在浏览器验收阶段，准备关闭旧进程并通过桌面壳拉起最新代码。
- [x] 已完成浏览器验收；当前仍需检查提示词图片、远端状态、提交和推送。

## 校准记录

- 2026-06-09：用户反馈“无法停止”“对话全部切到 websocket”“重新进到会话就好了”“已回复，但还在执行”“token 统计没有按照对话窗口统计”，并要求先对齐需求后 TDD 执行。
- 2026-06-09：已读取 `需求.md`、`架构.md`、`计划.md`、`AGENTS.md` 和上一轮 `本次计划.md`；确认本轮是在既有 WebSocket-only 和对话执行阶段上修复回归。
- 2026-06-09：用户采纳停止语义建议：停止只取消当前会话当前运行轮次，本地排队消息保留；已同步 `需求.md`、`架构.md` 和 `计划.md`。
- 2026-06-09：已完成只读子代理排查、TDD 红灯、中心服务 `session.turn.cancel`、前端停止动作、WebSocket-only 兜底清理和 token 窗口键校验；相关静态检查已通过，`check-chat-composer-ui-regressions.mjs` 仍有非本轮既有待确认编辑落库缺口。
- 2026-06-09：已按规则关闭旧独立中心服务和 Electron 进程，通过 IDE `dev:desktop-shell` 使用用户环境 Node v20.9.0 拉起中心服务；Chrome DevTools 验证新普通对话发送后按钮切到“停止”、点击后回到“发送”、右侧不再显示执行中、重新进入会话状态保持收尾、对话业务 XHR 仅见本机授权 REST。
- 2026-06-09：提交前复核确认根目录临时截图文件已不存在，并已移除 `提示词模板.md` 中 `img.png`、`img_1.png`、`img_2.png`、`img_3.png`、`img_4.png` 的 Markdown 引用；`git fetch --prune` 后确认 `origin/main` 未领先也未落后。
