# 本次计划

- 目标：判断截图中的 agent loop 是否没有真正完成，并在需求对齐后按 TDD 修复任务状态、轮次状态或工具失败收尾问题。
- 用户最新指令：使用 `/using-superpowers`；先和用户对齐需求所有细节后再落实 TDD；不使用视觉伴侣；主线程负责总控、拆解、分派、审查、合并、计划同步、最终验证和回复；确认过程中持续更新 `本次计划.md`；浏览器使用 Chrome DevTools 按人的方式点击、输入等测试；任务完成再做 git 处理；gitignore 忽略的不要强制加入；发现远端领先先拉取；工作区所有改动全部自动推送；不校验 `提示词模板.md` 是否被修改，直接提交；任务完成再删除提示词里面引用的图片；执行 `dev:desktop-shell` 前必须先关闭之前进程；服务启用使用用户环境的 node 启动。
- 本次需求：基于截图判断 agent loop 是否没有完成，并设计不需要用户不断手动“继续”的工具循环收尾机制；截图显示用户请求“获取 IDEA 打开项目的路径，并输出每个项目下的提示词模板内容”，后续又问“检索 hn-big，都用了什么技术”，过程中多次 MCP 工具调用完成，但最终出现 `MODEL_TOOL_LOOP_LIMIT_EXCEEDED`，agent 停止，用户被迫手动发送“继续啊”让 agent 继续。
- 本次新增需求：长任务拆解出来的小任务必须展示在右侧任务状态区；用户已确认采用“同一任务下连续追加”方案，续跑时只追加或更新当前轮次任务下的子步骤，不新建独立任务块。
- 本次新增需求：长任务中途可能因为调用 superpowers 这类 skill 工作流、审批、澄清或用户主动修改需求而停下来对齐；用户新增或修改任务后，系统必须把新任务与既有拆解合并，避免继续执行已经过期或不匹配的旧计划。
- 本次新增架构需求：插件体系改为全部内联能力，当前阶段不开发插件；插件开发只作为后续目标写入 `计划.md`，删除此前“外置/可安装/动态插件”相关当前需求。
- 本次新增架构需求：中心服务中对外暴露的 API 统一放到专门文件夹；工具代码统一放到工具文件夹内，每个工具单独一个文件，例如创建长期智能体工具一个文件、创建子智能体工具一个文件。
- 本次新增架构需求：智能体代码抽象为基类，并派生出主智能体、长期智能体和子智能体。
- 本次新增业务需求：主智能体可以创建长期智能体和子智能体，因此需要提供对应创建工具。
- 本次新增业务需求：长期智能体可以创建子智能体，并复用同一套创建子智能体工具。
- 本次新增业务约束：子智能体不能继续创建子智能体。
- 本次新增技术需求：LangChain 已支持 OpenAI 和 Anthropic 协议，模型调用直接使用 LangChain 对应能力，不再自研或重复维护这两类协议适配。
- 本次新增前端通信需求：对话页面整个页面全部通过 WebSocket 通信，不允许再使用其他同步请求或 REST 请求拉取对话页面数据，避免页面大量同步接口导致卡顿。
- 本次新增滚动交互需求：对话内容显示区域如果滚动条在最下方，token 级流式输出时继续固定在最下方；用户主动滚动后停止自动贴底。滚动距离超过 `100px` 后右下角显示方向箭头：向上滚动时显示“回到顶部”箭头，向下滚动或离底部超过阈值时显示“回到底部”箭头。
- 硬性约束：先确认需求细节再实现；缺陷修复必须先根因调查和 TDD 红灯；中心服务不能独立启动；浏览器验收必须通过桌面壳拉起中心服务；TS 文件不使用命令行编译器校验；新增或修改代码必须补中文注释；不回滚用户已有改动。
- 已读取技能：`using-superpowers`、`brainstorming`、`systematic-debugging`、`test-driven-development`、`verification-before-completion`、`using-user-environment`、`project-dev-standards`、`dispatching-parallel-agents`、`writing-plans`、`subagent-driven-development`。
- 已读取项目事实源：`需求.md`、`架构.md`、`计划.md`、`AGENTS.md`。
- 当前工作区状态：`main...origin/main`，当前可见改动包含 `提示词模板.md` 和本文件；不会回滚无关改动。
- 初步截图判断：第一组截图不像“正常完整完成用户原始任务”，因为没有输出目标文件内容；第二组截图是工具循环预算耗尽后强制失败。用户消息“继续啊”说明当前系统缺少市面 agent 常见的内部续跑、进度汇总和可恢复任务预算策略。
- 根因调查方向：
  - 中心服务 LangGraph / agent loop：工具失败后是否继续生成澄清回复并把 turn/task 标为 completed。
  - 任务状态语义：需要用户指定项目时是否应写入 `waiting_user`，而不是 `completed`。
  - MCP 工具失败语义：`get_repositories` 失败但模型仍从错误或上下文拿到多个项目路径时，是否应该继续请求用户选择，还是自动选择当前项目。
  - 前端展示：右侧任务状态是否只读 `task.status`，还是把失败过程卡片与最终回复混合后误展示完成。
  - 工具循环上限：当前 `toolRound >= 4` 后直接失败，缺少“任务未完成但可继续”的内部续跑策略；失败后还可能固化模型的无效短回复。
- 市面 Agent 对齐口径：
  - OpenAI Agents SDK 的 agent loop 会在模型输出工具调用后执行工具并继续循环，`maxTurns` 是安全限制，达到时抛 `MaxTurnsExceededError` 或交给错误处理器，不代表用户任务正常完成。
  - OpenAI Agents SDK 支持 `RunState` 和 human-in-the-loop 恢复，说明长任务或中断任务应能从状态继续。
  - LangGraph 的 recursion limit 是图执行保险丝；文档建议可读取 step 计数并在触顶前做 graceful degradation。
  - 本项目应把单批工具轮次上限作为内部预算边界：达到边界时自动续跑、持久化进度、继续同一用户任务；只有总预算、权限、用户选择或无进展触发时才停下来。

## 目标设计草案

- Agent loop 拆成“单批工具循环”和“任务级自动续跑”两层。
- 单批工具循环达到当前 `toolRound` 上限时，不直接把任务标记失败；先生成 `agent.loop.batch_limit_reached` 事件，保存已完成工具结果、待完成目标和下一步计划。
- 中心服务在同一轮次内自动启动下一批 loop，继续沿用同一 `turnId`、`taskId`、上下文和已完成工具结果，用户界面只看到 agent 继续工作。
- 任务级设置总预算，例如最大批次数、最大工具调用次数、最大耗时或无进展次数；达到总预算才进入失败或等待用户。
- 右侧任务状态区按同一 `taskId` 展示长任务分解出的子步骤；自动续跑只追加/更新 `task_steps`，不把同一轮长任务拆成多个互不关联的任务。
- 用户中途对齐或修改需求时，中心服务生成“任务重规划”事件，按稳定子任务 ID 合并：保留已完成且仍相关的步骤，取消过期步骤，新增步骤追加到同一任务下，变更步骤更新目标、原因和依赖关系。superpowers 属于 skill 工作流触发源，不作为独立 agent 类型处理。
- 插件能力从外部安装模型收敛为中心服务内联模块；对用户仍可展示“能力/工具”，但不再保留外置插件安装、同步、扫描和运行边界作为当前阶段需求。插件开发只作为后续目标规划，不在本轮实现。
- 智能体执行层按类层级收敛：`BaseAgent` 承载共享上下文、工具权限、任务执行和事件写入；`MainAgent` 增加创建长期智能体和子智能体能力；`LongTermAgent` 增加创建子智能体能力；`SubAgent` 禁止创建任何智能体。
- 创建智能体表现为工具调用：主智能体工具包含“创建长期智能体”和“创建子智能体”；长期智能体工具只包含“创建子智能体”；子智能体工具集中不注入创建智能体工具。
- 模型运行时优先接入 LangChain 的 OpenAI / Anthropic ChatModel，不再在中心服务内维护重复协议适配代码；现有供应商配置仍作为 LangChain 初始化参数来源。
- 对话页数据流统一走 WebSocket：首屏会话详情、历史消息、事件补齐、发送消息、任务步骤、流式 token、任务重规划、等待用户、错误状态和多端同步都通过 WebSocket 请求、响应、初始快照或增量事件进入页面；对话页不再使用 REST 请求处理这些数据和动作。
- 对话滚动采用“贴底跟随 + 用户接管”模式：仅当用户当前在底部附近时自动跟随 token 流；用户滚动后暂停跟随；超过 `100px` 后按最近滚动方向在右下角绝对定位显示快捷箭头，最近向上滚动显示回顶部，最近向下滚动显示回底部；贴顶或贴底时不显示箭头。
- 如果达到总预算，最终消息必须是结构化进度摘要：已完成、未完成、失败原因、下一步需要用户做什么；不能只显示 `MODEL_TOOL_LOOP_LIMIT_EXCEEDED`。
- 如果缺少权限、审批、项目选择或必要输入，则状态进入 `waiting_user`，而不是失败或完成。
- 如果模型连续调用同类搜索工具但没有新增结果，触发无进展保护，要求模型汇总已有证据或换策略，避免无限搜索。

## 任务拆分

- [x] 读取本轮适用技能和项目必读文档。
- [x] 查看 `img.png` 并记录可见现象。
- [x] 重写 `本次计划.md`，固化本轮问题和约束。
- [x] 读取 agent loop、任务收尾、MCP 工具结果和前端任务状态展示代码。
- [x] 汇总根因证据，并向用户确认推荐修复语义。
- [x] 用户确认后同步 `需求.md`、评估同步 `架构.md` 和 `计划.md`。
- [x] TDD 红灯：新增或扩展回归检查，证明工具失败后需要用户选择时不能把任务显示为已完成，或必须继续完成原始文件读取任务。
- [x] 绿灯：按确认语义修复中心服务和必要前端展示。
- [x] 对齐插件内联化范围，确认需要从 `需求.md` 删除哪些旧插件需求、哪些能力改名为内联能力。
- [x] 对齐中心服务 API 和工具代码目录边界。
- [x] 对齐智能体基类、主智能体、长期智能体、子智能体的类职责和工具注入规则。
- [x] 对齐 LangChain 模型协议接入范围，确认 OpenAI 和 Anthropic 直接使用 LangChain，供应商配置如何映射。
- [x] 对齐对话页 WebSocket-only 通信边界，列出需要迁移的现有 REST 调用。
- [x] 对齐对话消息区滚动跟随、用户接管和方向箭头规则。
- [x] 用户确认后同步 `需求.md`、`架构.md` 和 `计划.md` 中的插件、API、工具和智能体架构口径。
- [x] 回看 `本次计划.md`，确认没有偏移。
- [x] 运行允许的回归检查，不使用 `tsc` / `vue-tsc`。
- [x] 关闭旧进程后使用用户环境启动 `dev:desktop-shell`，用 Chrome DevTools 真实操作验证。
- [ ] 检查 `提示词模板.md` 图片引用，任务完成后删除提示词引用的临时图片。
- [ ] 远端检查；如远端领先先拉取。
- [ ] 提交并推送全部非忽略改动。

## 待确认问题

- [ ] 当工具失败但 agent 给出“请指定项目”的回复时，本轮任务状态应显示为“等待用户”，是否确认？
- [ ] 如果用户请求明确包含当前项目名或当前会话已绑定项目，`get_repositories` 返回多个项目时是否应自动选择当前项目，而不是追问？
- [ ] MCP 工具过程失败但最终给出可操作澄清时，过程卡片保留失败，整体轮次进入等待用户，这个语义是否符合预期？
- [ ] 工具循环达到单轮预算时，是否允许中心服务自动开启“内部续跑”继续执行同一用户任务，而不是要求用户手动输入“继续”？
- [ ] 如果达到总预算仍未完成，是否应固化结构化失败摘要和下一步建议，而不是只暴露 `MODEL_TOOL_LOOP_LIMIT_EXCEEDED` 给用户？
- [x] 是否确认按“直接抄市面 Agent”的两层预算 + 自动续跑 + 可恢复状态设计落地？（用户已确认，并补充右侧小任务展示与中途任务合并）
- [x] “所有插件全部内联，删除之前的需求”是否指删除全部外置插件/插件市场/插件安装/插件 SDK/动态扫描/内置插件包需求，只保留中心服务内联工具能力？（已确认：当前阶段不开发插件，作为后续目标）
- [x] “工具放在一个文件里面”是要求物理上单个文件，还是允许一个 `tools` 文件夹加统一入口文件？（已确认：不是单文件，是工具文件夹，每个工具独立文件）
- [x] “对外暴雷的 api”按上下文理解为“对外暴露的 API”，是否确认？（已确认）
- [x] 对话页是否完全禁止 REST，包括首屏会话详情、历史消息、事件补齐、发送消息和附件相关接口，全部改为 WebSocket 请求/响应？（已确认：全部改 WebSocket）
- [x] 滚动箭头“向上滚动的就出现回到顶部，反之同理”是否按最近滚动方向判断：最近向上滚动显示回顶部，最近向下滚动显示回底部？（已确认：右下角绝对定位，贴顶或贴底隐藏）

## 当前状态

- [x] 正在根因调查，尚未修改业务代码。
- [x] 已确认后端具备 `waiting_user` 轮次和任务状态，前端也能展示“等待用户”。
- [x] 已确认当前 LangGraph 成功路径在 `usage.record` 节点无条件调用 `updateTurnStatus(..., "completed")`。
- [x] 已确认工具失败会作为工具结果回填给模型；如果模型生成“请指定项目”这类澄清回复，当前仍会进入 completed 收尾。
- [x] 新截图显示 `MODEL_TOOL_LOOP_LIMIT_EXCEEDED` 后任务失败；用户发送“继续啊”后 agent 才继续，这属于工具循环预算与内部续跑能力缺失问题。
- [x] 用户确认长任务拆解小任务采用同一任务连续追加方案。
- [x] 用户补充中途对齐、用户修改任务后需要合并既有拆解与新需求；并纠正 superpowers 是 skill。
- [x] 用户新增插件内联化、中心服务代码分类和智能体类层级需求，待确认删除范围和代码目录边界。
- [x] 用户已确认：当前阶段不开发插件，插件作为后续目标；工具放在工具文件夹且每个工具独立文件；“暴雷”按“暴露”理解。
- [x] 用户新增 LangChain 模型协议、对话页 WebSocket-only 和消息区滚动规则需求；已确认对话页全部改 WebSocket，滚动箭头按最近滚动方向且贴顶/贴底隐藏。
- [x] 已同步 `需求.md`、`架构.md` 和 `计划.md` 的本轮确认口径。
- [x] 已新增并跑通本轮 TDD 静态检查 `scripts/check-current-long-task-websocket-agent-regression.mjs`，覆盖长任务自动续跑、WebSocket-only、滚动箭头、LangChain、插件内联、API/工具/智能体目录边界。
- [x] 已实现单批工具循环触顶自动续跑事件、任务级总预算结构化失败、WebSocket 对话快照/补齐/发送请求通道、消息区 100px 方向箭头、智能体类层级和创建工具目录边界。
- [x] 使用用户环境 Node 20 运行 `node scripts/check-all.mjs`，自动化检查、前端构建、中心服务构建占位、桌面绿色版构建和绿色版目录检查均通过。
- [x] 子代理只读审查发现规格缺口：LangChain 目前只是实例化未承载真实调用；对话页仍有 REST 路径；`task.plan.revised` 未接入中途合并；智能体类层级和创建工具未接入真实工具注册；对外 API 仍有历史入口；总预算触顶缺少可展示进度摘要；插件运行时仍残留插件调用工具。
- [x] 修复审查发现的阻塞规格缺口，并补充回归检查覆盖真实接入点。
- [ ] 当前补缺实现风险：子智能体创建工具已进入模型工具链并写事件，但运行期子智能体表仍是任务内临时表，后续需要并入真实任务上下文状态。
- [x] 已新增 `chat.bootstrap.snapshot` 和 `tokenizer.count` WebSocket 请求，对话页供应商、模型列表、智能体列表和上下文 token 统计不再走 REST。
- [x] 使用 Node 20.19.3 PATH 前置运行 `node scripts/check-all.mjs`，全量自动化检查、前端构建、中心服务构建占位、桌面绿色版构建和 IDEA 插件静态检查均通过。
- [x] 已按规则关闭旧桌面壳和中心服务进程，重新通过 `dev:desktop-shell` 拉起中心服务；Chrome DevTools 真实点击新增普通对话、输入并发送“请用一句话回复：WebSocket-only 浏览器验收。”，页面返回回复、任务显示 `1/1` 已完成、控制台无错误，网络只保留本机授权 REST，没有 `/api/provider/*`、`/api/agent/list`、`/api/tokenizer/count` 或 `/api/session/*` 对话页业务 REST。

## 校准记录

- 2026-06-08：用户基于 `img.png` 询问“agent loop 是否没有完成”，并要求先对齐细节后按 TDD 执行。
- 2026-06-08：根因调查显示，截图更像“agent loop 技术上收尾完成，但用户原始任务未完成且需要用户补充选择；任务状态应进入等待用户，而不是已完成”。
- 2026-06-08：用户纠正“继续啊”是用户自己发出的消息，不是 agent 回复；需求校准为直接实现市面 Agent 常见的长任务自动续跑和预算处理方式。
- 2026-06-08：已把市面 Agent 对齐口径写入计划：`maxTurns` / recursion limit 是安全保险，不是用户可见的普通失败；本项目需要自动续跑和可恢复任务状态。
- 2026-06-08：用户确认小任务展示采用方案 1：同一任务下连续追加；并要求处理中途对齐、需求变更后任务拆解合并。
- 2026-06-09：用户纠正 superpowers 是 skill；计划措辞已改为 skill 工作流触发的中断与重规划。
- 2026-06-09：用户新增架构变更：插件全部内联、对外 API 集中、工具集中、智能体抽象为基类并派生主/长期/子智能体，主智能体可创建长期和子智能体，长期智能体可创建子智能体，子智能体不可再创建子智能体。
- 2026-06-09：用户确认插件当前阶段不开发、只作为后续目标；工具不是单文件，而是统一工具文件夹下每个工具一个文件；“对外暴雷 API”修正为“对外暴露 API”。
- 2026-06-09：用户新增要求：OpenAI/Anthropic 协议直接使用 LangChain；对话页面全部通过 WebSocket 通信；消息显示区域滚动支持贴底跟随、用户接管和超过 100px 后显示方向箭头。
- 2026-06-09：用户确认对话页首屏、历史、补齐、发送、附件等全部改为 WebSocket；滚动箭头按最近滚动方向，绝对定位右下角，贴底或贴顶时隐藏。
- 2026-06-09：已将长任务自动续跑、任务重规划、插件内联化、LangChain、WebSocket-only、滚动规则、API/工具/智能体目录边界同步到项目事实源文档。
- 2026-06-09：浏览器验收发现对话页仍有 provider、agent、tokenizer REST 残留；已按 TDD 加严静态检查，新增 WebSocket `chat.bootstrap.snapshot` 和 `tokenizer.count` 后复测通过。
