# 本次计划

- 目标：实现中心服务真实 MCP 调用闭环，同时支持 HTTP MCP 和 stdio MCP 两种配置协议，让模型请求 MCP 工具时可以按中心服务保存的配置连接服务器、持续展示过程并返回结果。
- 用户最新指令：先读取 `提示词模板.md` 严格执行；先读“公共”；测试只做“浏览器测试”；任务完成后走“任务完成执行”；执行 `dev:desktop-shell` 前必须先关闭之前进程；使用给定 IDEA MCP 配置测试。
- MCP 测试配置：

```json
{
  "mcpServers": {
    "idea": {
      "type": "http",
      "url": "http://127.0.0.1:64342/stream"
    }
  }
}
```

- 测试内容：通过 HTTP MCP `idea` 获取当前打开项目路径，并读取项目根目录 `提示词模板.md` 的内容；通过 stdio MCP `chrome-devtools` 打开谷歌首页。
- 交互参考：参考 `https://github.com/zhukunpenglinyutong/idea-claude-code-gui` 的对话体验，消息流需要持续展示调用了什么 MCP、执行了什么工具、读取或写入了什么、输出或失败原因是什么，而不是只在最终回复里一次性描述。
- 硬性约束：不使用视觉伴侣；浏览器测试使用 chrome-devtools；不运行非浏览器测试或构建；中心服务不独立启动；文档、注释和回复使用中文；新增或修改代码补中文注释。
- 需求同步：当前 `需求.md` 已覆盖“MCP 调用全部进入审计记录”和“对话消息列表展示 MCP 调用卡片”，但未明确 stdio MCP 与 CCGUI 式持续过程展示，本轮需要先同步 `需求.md`、`架构.md` 和 `计划.md` 后再落代码。
- 验收方式：先用浏览器复现 MCP 配置或对话过程不满足两个协议和过程卡片展示的红灯，再通过浏览器保存 MCP 配置、发送测试消息，验证对话过程出现真实 MCP 工具卡片并分别返回 IDEA 项目路径、`提示词模板.md` 内容和 chrome-devtools 打开 Google 首页的结果。

## 任务拆分

- [x] 读取 `提示词模板.md`、`需求.md`、`架构.md`、`计划.md` 和 `AGENTS.md`。
- [x] 读取并使用 `using-superpowers`、项目开发规范、MCP 构建、TDD、用户环境和完成前验证相关技能。
- [x] 定位中心服务 MCP 配置、统一工具注册表、模型工具闭环和前端过程卡片渲染。
- [x] 同步 `需求.md`、`架构.md` 和 `计划.md`，写入 HTTP/stdio MCP 双协议与 CCGUI 式持续过程展示验收口径。
- [x] 通过浏览器红灯确认当前应用不满足 stdio MCP、MCP 工具列表展示或连续过程卡片展示要求。
- [x] 实现 MCP 配置解析、HTTP MCP 连接、stdio MCP 进程生命周期、工具发现、工具调用事件和模型工具结果回填。
- [x] 优化对话过程卡片，持续展示 MCP 调用、命令执行、文件读取、文件写入和结果摘要。
- [x] 更新 `计划.md` 对应任务状态。
- [x] 关闭旧 `dev:desktop-shell` 相关进程，并通过 IDEA 或用户可见环境重新拉起最新桌面端。
- [x] 只用 Chrome DevTools 完成浏览器红绿回归验证。
- [ ] 按“任务完成执行”走 git 规则，删除提示词引用图片，检查远端领先并提交推送。

## 当前状态

- [x] 已完成项目必读文档和模板读取。
- [x] 已确认现有代码包含 HTTP MCP 动态工具发现和调用雏形，但前端 MCP 管理页缺少工具列表展示，过程卡片未纳入 `tool.mcp.*` 事件，stdio MCP 尚未纳入可执行配置。
- [x] 已同步 `需求.md`、`架构.md` 和 `计划.md`，写入 HTTP/stdio MCP 双协议与 MCP 独立过程卡片验收口径。
- [x] 已通过 Chrome DevTools 完成浏览器回归：MCP 管理页展示 HTTP `idea` 与 stdio `chrome-devtools` 工具；对话内 `idea` 读取项目路径和 `提示词模板.md`；对话内 `chrome-devtools.new_page` 打开 Google 首页并展示独立 MCP 过程卡片。

## 校准记录

- 2026-06-07：用户补充要求两个 MCP 协议都要支持；`chrome-devtools` 使用 stdio 配置并打开谷歌首页；参考 CCGUI 的对话体验，过程流需要不断展示调用、执行、读写和输出详情。
- 2026-06-07：用户继续校准 MCP 管理页：MCP 列表固定为全局配置，不展示“作用域”列；每行是 `mcpServers` 中的一个服务；新增和编辑都必须按单个服务独立保存，保存一个服务不能覆盖同一全局配置文件中的其他服务。
- 2026-06-07：用户最新校准 MCP 编辑框必须参考完整 JSON 形态，textarea 展示 `{"mcpServers":{"服务 ID":{...}}}`，但仍只允许一个服务并按单服务合并保存。

## 本轮追加任务

- [x] 读取最新用户要求、根提示词和项目必读文件。
- [x] 同步 `需求.md`，明确 MCP 管理页不展示作用域列且单服务独立保存。
- [x] 同步 `架构.md`，明确 `/api/mcp/save` 支持 `serverId + serverConfig` 合并写回。
- [x] 同步 `计划.md`，新增本轮 MCP 管理页单服务编辑保存任务。
- [x] 修改中心服务 MCP 保存接口，单服务保存只更新 `mcp/global.json` 中对应 Server。
- [x] 修改 API client、Pinia 草稿和 MCP 页面弹框，支持 Server ID 与单服务配置 JSON。
- [x] 移除 MCP 页面“作用域”列，保留“查看工具”异步 loading 和二次收起。
- [x] 使用 Chrome DevTools 浏览器测试验证全局列表、单服务编辑和保存后其他服务不丢失。
- [x] 更新 `计划.md` 本轮任务为完成状态，并按任务完成执行处理。

## 本轮二次追加任务

- [x] 同步 `需求.md`、`架构.md` 和 `计划.md`，明确编辑框使用完整 `mcpServers` JSON。
- [x] 修改前端 MCP 草稿默认值，新增配置默认展示完整根结构。
- [x] 修改编辑逻辑，当前 Server 配置包裹为完整 `{"mcpServers":{"服务 ID":{...}}}`。
- [x] 修改保存逻辑，校验 `mcpServers` 只能包含一个服务，并抽取唯一 key 走单服务保存。
- [x] 修改 MCP 页面弹框，移除独立服务 ID 输入，只保留完整 MCP 配置 JSON textarea。
- [x] 使用 Chrome DevTools 浏览器测试验证完整 JSON 编辑、保存和列表保留两个服务。
- [x] 更新 `计划.md` 本轮新增任务为完成状态，并按任务完成执行处理。
