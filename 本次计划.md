# 本次计划

## 主线程摘要

- 状态：完成。
- 是否需要程序员阶段：否。
- 未完成程序员任务编号：无。
- 当前串行程序员任务编号：无。
- 串行程序员任务编号：P01 -> P02 -> P03 -> P04 -> P05 -> P06 -> P07 -> P08 -> P09 -> P10 -> P11。
- 是否可以进入启动阶段：是。
- 是否可以进入测试阶段：否。
- 测试范围：只执行浏览器端验证；P01 至 P11 已全部完成，测试仍必须等待启动子代理产出可用 `项目启动.md`。
- 阻塞归属：无。
- 下一步：主线程进入启动阶段，派发启动子代理通过桌面端拉起或刷新中心服务并写入 `项目启动.md`；启动前置满足后再进入浏览器端测试阶段。

## 本轮范围

- 需求来源：`当前需要实现的需求.md` 中未标记 `-完成` 的一级标题 `# 2026年6月2日13:37:41`。
- 本轮只拆解和计划当前临时需求，不修改 `需求.md`。
- 本轮允许后续程序员修改中心服务、前端、内置模型协议插件、共享协议、测试脚本、构建脚本和工程配置。
- 本轮最终验证端限制为浏览器端；桌面端只由启动子代理用于拉起中心服务，不作为测试子代理的验证端。
- 本轮引用的临时图片为 `img.png`、`img_1.png`、`img_2.png`，全部必测通过并标记需求完成前不得删除。

## 执行任务

- [x] P01 建立代码文件行数治理：检查 `apps/frontend/src/views/Chat/RouterIndex.vue`、`apps/frontend/src/stores/app.ts`、`services/center/src/api-routes.ts` 等超过 1500 行或接近 1500 行的源码，按现有职责边界拆分到同目录专属组件、store 模块或领域模块，并新增脚本防止单个代码文件超过 1500 行。
- [x] P02 修复真实对话链路：在 `services/center` 串联会话发送、启用供应商选择、模型网关调用、流式片段、最终消息固化、任务状态和错误事件，确保对话发送不再只写内部占位回复，并能使用 `AI账号.md` 中账号配置完成真实供应商调用验证。
- [x] P03 补齐模型协议插件脚手架和导入注册：确认 `plugins/builtin-model-openai-compatible` 与 `plugins/builtin-model-anthropic-messages` 有完整清单、导出接口、中心服务注册和供应商页可选项，修复供应商配置没有导入协议插件或无法选择协议插件的问题。
- [x] P04 修复供应商模型与协议能力：补齐模型列表刷新、手填模型、上下文窗口 `K` 单位转 token、推理深度、图片/工具/JSON/流式能力声明和 API Key 脱敏保存，确保前端供应商页和中心服务协议字段一致。
- [x] P05 修复 8866 前端资源刷新策略：调整桌面端拉起的中心服务托管前端资源、开发期资源代理或缓存头，使 `http://127.0.0.1:8866` 在开发期能反映前端最新构建或清晰复用 `5173` HMR，不再出现 5173 已刷新而 8866 长期旧页面的问题。
- [x] P06 修复会话删除功能：实现或修复中心服务会话删除接口、前端删除按钮和列表刷新，项目对话与普通对话删除后不可继续显示或误选中，删除失败必须展示可见错误。
- [x] P07 修复会话标题总结功能：在真实对话完成后生成或更新会话标题摘要，中心服务固化标题并通过列表、详情和 WebSocket 同步到浏览器端，标题总结失败时保留原标题并写入错误事件。
- [x] P08 修复用量统计功能：真实模型调用后写入 `usage_records`，刷新 `usage_daily_stats` 或聚合接口，前端用量统计页展示总量、供应商维度、项目维度和原始记录，不能只展示空图或静态占位。
- [x] P09 修复顶部菜单激活样式：根据 `img.png` 对应问题修复工作台顶部菜单路由激活态，浏览器访问或点击不同顶部菜单后当前菜单必须有明确激活样式，刷新页面后仍正确。
- [x] P10 修复网络代理协议与认证字段：根据需求补齐 HTTP、HTTPS、SOCKS4、SOCKS4a、SOCKS5 协议选项，恢复用户名和密码输入、脱敏展示、无认证代理处理和供应商代理策略关联。
- [x] P11 补齐智能体管理真实功能：实现主智能体查看与不可删除约束、长期智能体创建、修改、停用、删除、记忆影响提示和列表刷新；前端不能继续停留在空页面或不可操作占位状态。

## 验收口径

- `本次计划.md` 中 P01 至 P11 全部由程序员子代理逐项完成并勾选。
- 程序员任务完成后，启动子代理通过桌面端拉起或刷新中心服务，并写入可进入浏览器测试的 `项目启动.md`。
- `本次测试计划.md` 中所有必测浏览器端测试项均为 `[x]`，详情状态均为 `通过`。
- 测试子代理确认全部必测通过后，才允许把 `当前需要实现的需求.md` 对应标题追加 `-完成`，并按规则清理 `img.png`、`img_1.png`、`img_2.png`。

## 风险与阻塞

- 真实对话测试依赖 `AI账号.md` 中账号配置可用；账号、供应商网络或额度不可用时，测试项应阻塞给用户，不能改用假数据通过。
- 本轮涉及中心服务和桌面端资源托管，启动子代理必须通过桌面端拉起中心服务，不能直接运行 `services/center`。
- 当前最终测试范围只允许浏览器端验证；涉及桌面端的行为只作为启动前置状态记录，不由测试子代理做桌面端验证。
