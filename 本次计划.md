# 本次计划

- 目标：修复对话窗口里“思考过程”固定显示“读取当前会话、任务状态、可用供应商和扩展能力后组织回复”的问题，并确保本窗口能按当前会话上下文、任务状态、供应商和扩展能力组织真实回复。
- 用户最新指令：先读取 `提示词模板.md` 并严格执行；测试只做浏览器测试；任务完成后走“任务完成执行”；删除提示词中引用的 `img.png`；按 git 规则处理。
- 硬性约束：不使用视觉伴侣；中心服务不能独立启动；浏览器测试使用 chrome-devtools；文档、注释和回复使用中文；不运行非浏览器测试或构建。
- 需求同步：用户指出“每次思考都是固定提示词，不是模型真实思考”，属于对话过程展示和执行链路需求，需要先同步 `需求.md` 和 `计划.md` 再改代码。
- 验收方式：通过浏览器复现修复前固定思考文本，再通过浏览器验证修复后思考内容不再把该固定提示词重复写入思考正文，且回复能基于当前会话可见上下文回答。

## 任务拆分

- [x] 读取 `提示词模板.md`、`需求.md`、`架构.md`、`计划.md` 和 `AGENTS.md`。
- [x] 同步 `需求.md` 中对“思考过程不得使用固定系统提示词冒充真实思考内容”的产品口径。
- [x] 同步 `计划.md` 中本轮修复任务和状态。
- [x] 定位中心服务思考事件生成、会话上下文读取、供应商与扩展能力读取的数据流。
- [x] 定位前端思考卡片渲染和历史事件恢复的数据流。
- [x] 用浏览器测试复现红灯现象。
- [x] 实施最小修复。
- [x] 更新 `计划.md` 勾选本轮完成项。
- [x] 仅用浏览器测试完成回归验证。
- [x] 按“任务完成执行”走 git 规则并删除 `img.png`。

## 当前状态

- [x] 已完成项目必读文档和模板读取。
- [x] 已完成需求和计划同步。
- [x] 已完成中心服务和前端数据流定位。
- [x] 已完成浏览器红灯复现：旧实现新轮次思考卡片重复展示固定模板文本，且当前窗口用户消息计数回答不正确。
- [x] 已完成浏览器回归：新轮次思考卡片展示真实上下文统计，回复与本轮前同一会话用户消息数一致。
- [x] 已按任务完成执行删除提示词引用图片，并确认远端 main 无领先。
