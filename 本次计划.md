# 本次计划

## 主线程摘要

- 状态：程序员任务已完成，待浏览器端测试。
- 是否需要程序员阶段：是。
- 未完成程序员任务编号：无。
- 程序员连续执行任务编号：P11-02 至 P11-07、P12 均已完成；P01 至 P10、P11-01 保持已完成。
- 程序员是否需要执行启动准备：已执行，`5173` 与宿主拉起的 `8866` 已完成健康确认，详情见 `项目启动.md`。
- 是否可以进入测试阶段：是，可进入浏览器端测试。
- 测试范围：最终只执行浏览器端验证，不执行桌面端测试项，不执行 IDEA 插件端测试项。
- 阻塞归属：无。
- 下一步：测试子代理按本轮测试计划执行浏览器端验证。

## 本轮范围

- 需求来源：`当前需要实现的需求.md` 中未标记 `-完成` 的一级标题 `# 2026年6月4日08:44:47`。
- 续接结论：上一轮 `本次测试计划.md` 与 `测试结果.md` 中浏览器端通过结论只覆盖旧口径；当前标题确认了新的修正需求，因此不能用上一轮通过项跳过本轮实现和验证。
- 旧口径废弃：上一轮“主对话引导”独立入口、右侧“审计摘要”常驻模块、固定 `Node 版本` 工具接口、字符数或简单估算上下文统计、只做部分 SQL 分离的口径均不再作为本轮目标。
- 管理页口径：管理页信息展示继续使用 Element Plus `el-table` 表格，不得按非表格卡片布局改造。
- 正式文档边界：用户明确本轮不要求同步正式需求或正式计划，未授权修改 `需求.md`、`架构.md`、`计划.md`；建议后续同步原因写在本文件，不在本轮直接修改正式文档。
- 代码边界：程序员可修改 `apps/frontend`、`services/center`、`packages/shared`、`packages/api-client`、`packages/model-protocol`、必要内置插件边界、静态回归脚本、pnpm 依赖配置和 lockfile；不得修改 `需求.md`、`架构.md`、`计划.md`，不得读取或更新 `浏览器测试窗口.md`，不得标记临时需求 `-完成`。
- 启动边界：涉及中心服务和浏览器端联调，程序员完成后必须刷新项目约定宿主环境，确认 `5173` 与宿主拉起的 `8866` 健康，并覆盖更新 `项目启动.md`。

## 执行任务

- [x] P01 移除固定 Node 版本工具接口：删除或废弃 `services/center/src/tool-routes.ts` 中 `/api/tool/command/node-version` 专用路由，移除 `packages/api-client/src/index.ts` 中对应专用客户端方法，清理前端“Node 版本”固定按钮或固定接口调用；完成口径为代码中不存在 `node-version` 专用工具 API，浏览器端命令类请求只能走通用 Agent 命令执行链路。
- [x] P02 补齐通用命令执行链路：在中心服务 Agent 执行流程中提供通用命令工具规划、审批状态、事件记录和输出回传能力，支持用户自然语言询问 Node.js、Python 等本机环境时由 Agent 调用通用命令；目标文件包括 `services/center/src/workflow-domain.ts`、工具运行相关模块、事件写入模块和必要共享协议；完成口径为命令过程卡片展示命令摘要、状态、输出摘要、失败原因和 `traceId`，且不依赖单一命令专用 API。
- [x] P03 取消独立主对话引导入口：移除 `apps/frontend/src/views/Chat/RouterIndex.vue` 中主动创建“主对话引导”的入口、默认文案和提交模式，删除或调整相关状态、样式和文案；完成口径为输入区不再显示独立“主对话引导”按钮或默认引导草稿。
- [x] P04 实现排队消息后置引导按钮：基于当前对话内排队消息或待处理轮次，在对应排队对话消息后方显示“引导”按钮，点击后把该排队对话作为本轮引导提交；目标文件包括对话消息渲染、会话状态 store、发送接口和中心服务待确认/排队状态处理；完成口径为只有排队消息后出现引导按钮，非排队消息和空输入区不出现独立引导入口。
- [x] P05 移除右侧审计摘要常驻模块：删除对话页右侧或相关区域的“审计摘要”模块和标题，保留工具、命令、插件、MCP、skill、文件操作和错误排查记录在消息流、任务详情或事件详情中的展示；完成口径为浏览器对话页不再出现“审计摘要”常驻区域，事件仍可追踪。
- [x] P06 修正任务状态实时同步：输入区“任务”和右上角任务区域必须展示同一轮对话由大模型拆解出的任务列表，任务进度、状态、耗时、失败原因和 `traceId` 实时同步；新一轮对话开始时重置为新一轮任务，不沿用上一轮任务；目标文件包括 `apps/frontend/src/views/Chat`、状态 store、`services/center` 任务/步骤事件接口；完成口径为连续发送两轮对话时任务列表随轮次切换重置。
- [x] P07 修正智能体状态树结构：智能体状态弹框使用可折叠树形结构，第一级展示长期智能体，子级展示其创建的子智能体；“测试子智能体”位于“测试长期智能体”折叠节点内并默认展开；目标文件包括 `apps/frontend/src/views/Chat/dialogs/AgentStatusDialog.vue`、相关样式和智能体状态数据构造；完成口径为树节点可折叠、层级正确、默认展开规则明确。
- [x] P08 补齐智能体对话完整输入框能力：智能体详情或对话区域复用外部完整输入框能力，包含文本输入、发送、引用/附件入口可用状态、模型/推理深度/执行模式、上下文统计、任务入口、智能体状态入口和编辑入口；完成口径为智能体对话区域不是简化输入框，且发送后能进入当前会话或目标智能体对话流程。
- [x] P09 建立 tokenizer 协议化能力：在共享协议或模型协议包中定义 tokenizer 请求、响应、适配器接口、模型匹配规则、统计来源、输入范围、窗口上限和错误结构；在中心服务新增 tokenizer 领域模块和至少一个内置自研 tokenizer 适配器，并预留外部 tokenizer 适配器注册边界；完成口径为协议字段均有中文注释，不使用字符串长度或字符数作为最终统计口径。
- [x] P10 接入真实上下文 token 统计：按实际送入模型的上下文包统计当前窗口已用 token，覆盖当前消息、历史消息、系统或开发提示、工具或能力描述、结构化引用、附件摘要和必要运行上下文，并返回 tokenizer 来源、模型标识、输入范围、已用 token 和窗口上限；前端输入区展示真实统计结果；完成口径为删除 `estimateComposerContextUsedTokens` 这类临时估算展示链路，浏览器端可见统计来源和模型窗口上限。
- [x] P11-01 保留 Drizzle 基础接入成果：确认 `services/center` 已引入 `drizzle-orm`，已建立 `services/center/src/data-access/schema.ts` 和 `services/center/src/data-access/database-adapter.ts`，并继续复用中心服务主进程内同一个 `better-sqlite3` 连接；完成口径为后续任务不得重复引入第二套 SQLite 连接，不回退已完成 schema 和适配器基础。
- [x] P11-02 建立 SQL 分布清单和持久层边界检查：梳理 `services/center/src` 中仍直接书写 SQL、表定义、Mapper、Repository 或持久化查询的文件，按数据域写入程序员结果；新增或更新静态回归脚本，允许 SQL 只出现在迁移文件、Drizzle schema、数据库适配器和独立数据访问层目录中；完成口径为脚本能指出路由、领域服务、Worker 编排中残留的直接 SQL，并作为后续迁移收口依据。
- [x] P11-03 迁移会话、项目、消息、轮次、任务和事件持久层：在 `services/center/src/data-access` 下建立对应 Repository/Query 接口和实现，把 `projects`、`sessions`、`messages`、`conversation_turns`、`tasks`、`task_steps`、`events`、`sync_clients` 的查询、写入、状态更新和事件序号递增迁出 `api-routes.ts`、`session-domain.ts`、`workflow-domain.ts` 等业务文件；完成口径为相关路由和服务只调用数据访问层接口，浏览器端会话列表、发送消息、任务状态、事件补齐和删除会话行为保持不退化。
- [x] P11-04 迁移供应商、代理、运行环境、模型用量和通知持久层：建立供应商、代理、运行环境、`usage_records`、`usage_daily_stats`、`notifications` 等数据访问接口，把模型列表刷新、供应商保存、代理策略、用量原始记录、用量聚合和通知查询写入从业务文件迁出；完成口径为浏览器端供应商管理、网络代理、运行环境和用量统计仍可读取真实数据，业务服务不直接拼写 SQL。
- [x] P11-05 迁移智能体、记忆、附件、扩展能力和个人事务持久层：建立 `agents_index`、`agent_runtime_states`、`attachments`、`plugin_installs`、`extension_call_records`、`todos`、`calendar_events`、`knowledge_items` 等数据访问接口，把智能体管理、记忆索引、附件、插件/MCP/skill 调用记录、待办、日程和知识库的数据库访问迁出路由和领域服务；完成口径为浏览器端智能体管理、扩展管理和个人事务相关基础读取不退化，Markdown 记忆追加边界保持不变。
- [x] P11-06 收口事务、Mapper 和执行链路持久化边界：统一数据访问层事务入口、错误到 `traceId` 的映射、业务对象 Mapper 和时间字段格式来源，把 Worker 编排、通用命令工具、tokenizer 统计、会话发送和模型用量写入链路改为通过 Repository/Query 层执行；完成口径为一次对话发送仍能完成消息、轮次、任务、事件、用量和错误事件的一致写入，业务层不直接持有数据库语句。
- [x] P11-07 完成全量 SQL 清理和 Drizzle 边界回归：运行并修正 P11-02 建立的持久层边界检查，确保 `services/center/src` 中除迁移、schema、适配器和数据访问层外不再散落 SQL、表定义或复杂查询；同步更新 `程序员结果.md` 中 P11 拆分任务完成情况、剩余风险和允许的静态检查结果；完成口径为 P11-02 至 P11-07 全部勾选后，原 P11 全量持久层迁移才视为完成。
- [x] P12 补齐回归脚本、结果、宿主刷新和浏览器测试前置确认：在 P11-07 完成后，新增或更新静态回归脚本，覆盖固定命令接口清理、独立主对话引导移除、审计摘要移除、任务重置同步、智能体树、真实 tokenizer、Drizzle 持久层边界、管理页仍使用 `el-table`、禁止 `tsc --noEmit` / `vue-tsc`；程序员完成后更新 `程序员结果.md`，再刷新项目约定宿主环境并更新 `项目启动.md`；完成口径为 P01 至 P10 保持完成，P11-01 至 P11-07 和 P12 均勾选完成，静态检查通过，`项目启动.md` 显示可进入浏览器端测试。

处理原因：P11 已引入 `drizzle-orm`、Drizzle schema 和唯一连接适配器，该基础接入作为 P11-01 保留且不重复开发；现有中心服务仍有大量 SQL 分布在 `api-routes.ts`、`workflow-domain.ts`、`session-domain.ts`、`usage-domain.ts` 等路由和领域文件中，因此剩余工作按数据域和收口检查拆为 P11-02 至 P11-07。P12 保留在 P11 拆分任务之后，专门处理静态回归、程序员结果、宿主刷新、健康检查和浏览器端测试前置确认。

## 验收口径

- P01 至 P10 保持完成，P11-01 至 P11-07 和 P12 均勾选完成，且 `程序员结果.md` 写清修改范围、静态检查、未执行测试项和风险后，才允许进入测试阶段。
- 程序员子代理必须确认 `5173` 前端可访问、中心服务由桌面端或项目约定宿主拉起、`8866` 健康，并覆盖更新 `项目启动.md` 后，才允许测试子代理开始浏览器端验证。
- `本次测试计划.md` 中所有浏览器端必测项均为 `[x]` 且详情状态为“通过”后，测试子代理才允许按工作流判断临时需求是否可标记 `-完成`。
- 本轮最终测试范围只包含浏览器端；桌面端、Electron 壳交互和 IDEA 插件端不执行，不作为本轮通过条件。

## 风险与阻塞

- Drizzle 全量迁移范围大，需保持 SQLite 迁移、中心服务唯一连接、事务和现有接口行为稳定；不得只迁移两个领域后声称完成，必须以 P11-07 的全量 SQL 边界回归作为原 P11 完成口径。
- tokenizer 需求要求真实统计和可审计来源，不能继续用字符数、字符串长度、固定比例或简单估算冒充。
- 通用命令执行必须经过中心服务权限、事件和任务状态链路，不能让浏览器端直接执行命令。
- 排队引导必须限定在当前对话内排队消息后方，避免恢复旧的独立主对话引导入口。
- 管理页表格口径已明确修正，程序员不得把三个管理区域改成卡片或非表格布局。
- 本轮建议后续同步正式需求、架构和计划，原因是 Drizzle、tokenizer 和命令工具链路属于长期产品与架构口径；但当前用户未授权，本轮不修改正式文档。
