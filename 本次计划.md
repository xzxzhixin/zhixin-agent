# 本次计划

## 当前执行状态

- 本轮任务场景：`新窗口任务：只执行浏览器端验证`。
- 本轮只生成或更新过程计划，不同步正式 `计划.md`，不修改业务代码，不运行构建。
- 本轮只创建浏览器测试子代理，不创建桌面测试子代理。
- 现有需求分析已覆盖两段临时需求的边界：`先不做` 的页面拆分与 Web 包瘦身意图，以及开发期桌面端拉起中心服务的启动链路要求。

## 来源与目标

- 需求分析来源：`本次需求分析.md`。
- 最新正式需求来源：`需求.md`。
- 本轮用户明确要求：只执行浏览器端验证，不同步正式计划，不运行构建。

目标：

1. 仅基于浏览器端验证当前前端页面、路由、执行模式说明和可见错误状态。
2. 验证顶部菜单独立 URL、hash 切换、刷新直达和页面非白屏。
3. 验证执行模式下拉面板内可见说明文本。
4. 验证浏览器端不展示桌面专属中心服务管理入口。
5. 为后续需要中心服务的浏览器验证保留桌面端拉起中心服务的启动信息入口，但本轮不创建桌面测试子代理。

## 需求边界

- `本次需求分析.md` 中的临时需求属于当前任务边界的来源说明，不扩展为正式需求变更。
- 最新 `需求.md` 已包含本轮需要验证的产品口径：
  - Web 端不内置中心服务。
  - 顶部菜单页面必须真实独立。
  - 菜单切换必须同步 URL。
  - 执行模式下拉需要解释说明。
  - 开发期桌面端应拉起中心服务。
- 本轮只验证前端呈现和浏览器可见状态，不推进正式文档同步。

## 需要准备的文件范围

### 启动与验证记录

- `项目启动.md`
- `浏览器测试窗口.md`

### 浏览器测试子代理可处理范围

- 仅使用 `chrome-devtools` 验证浏览器端页面、路由、菜单切换、执行模式说明和控制台/网络错误。
- 不修改业务代码。
- 不创建桌面测试子代理。

### 启动子代理可处理范围

- 如后续需要中心服务参与浏览器验证，仅通过桌面端启动链路准备运行环境，并写入 `项目启动.md`。
- 本轮不要求启动子代理必须执行，但若后续浏览器验证需要中心服务，则必须先补齐启动信息，再继续浏览器验证。

## 可并行任务

- [ ] B1 浏览器测试专用页签准备：创建或复用 `浏览器测试窗口.md` 记录的测试页，只保留浏览器测试专用页签。
- [ ] B2 浏览器端页面验证：检查 `/chat`、`/agent-management`、`/providers`、`/proxies`、`/runtimes`、`/usage`、`/plugins`、`/mcp`、`/skills`、`/center` 的路由、非白屏和菜单切换。
- [ ] B3 执行模式说明验证：检查输入区执行模式下拉面板中三种模式的说明是否可见。

## 不可并行任务

- [ ] S1 启动信息准备：若浏览器验证需要中心服务参与，必须先由启动信息记录当前桌面端拉起状态，再进入验证。
- [ ] S2 桌面测试：本场景明确禁止创建桌面测试子代理。
- [ ] S3 业务代码修改：本轮不允许为了验证而改业务代码。

## 验收和测试建议

- 浏览器测试只用 chrome-devtools。
- 优先复用已有测试页签，避免影响用户当前页面。
- 验证时重点观察：
  - 页面是否非白屏。
  - hash URL 是否随顶部菜单切换变化。
  - 刷新后是否仍进入同一页面。
  - 执行模式下拉说明是否直接可见。
  - 控制台是否有阻塞级错误。
  - 管理页是否仍符合独立路由入口边界。
- 如果后续验证需要中心服务参与，只能通过桌面端拉起，不得直接运行 `services/center`。

## 风险与阻塞项

- 当前最大限制是本轮不创建桌面测试子代理，因此凡是必须依赖桌面壳窗口行为的结论都不能在本轮下定论。
- 如果没有可用的 `项目启动.md`，浏览器测试子代理无法确认中心服务是否已由桌面端拉起。
- 本轮不运行构建，因此无法通过新产物再次验证 Web dist 瘦身，只能做浏览器端验证与既有静态边界判断。

## 计划同步说明

- 是否读取了最新 `需求.md`：已读取。
- 是否同步正式 `需求.md`：未同步，本轮用户未要求。
- 是否同步正式 `架构.md`：未同步，本轮只需生成过程计划。
- 是否同步正式 `计划.md`：未同步，用户明确要求不要修改正式计划。
- `计划.md` 冲突旧计划摘要：无，本轮不修改正式计划。

## 结论

- 本次 `本次计划.md` 只作为浏览器验证调度文件，不替代正式 `计划.md`。
- 后续若进入验证执行，主线程只应创建浏览器测试子代理，不创建桌面测试子代理。
