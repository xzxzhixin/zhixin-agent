# 本次计划

## 当前执行状态

- 已完成：页面入口结构已改为“`MainView.vue` 公共工作台壳 + 嵌套路由 `<router-view>` + 各 `views/页面/RouterIndex.vue` 直接承载页面主体”。
- 已完成：`/chat`、`/agent-management`、`/providers`、`/proxies`、`/runtimes`、`/usage`、`/plugins`、`/mcp`、`/skills`、`/center` 保持独立 hash URL 和懒加载入口。
- 已完成：静态回归已覆盖页面入口真实化、管理页能力、反馈回归、代码分包、Web dist 边界和桌面开发启动边界。
- 已完成：浏览器端已验证 `#/chat`、`#/agent-management`、`#/providers`、`#/proxies`、`#/runtimes`、`#/usage`、`#/plugins`、`#/mcp`、`#/skills`、`#/center`、`#/login` 均可直达且非白屏；菜单切换会同步 hash URL；执行模式下拉可见说明。
- 未完成：依赖中心服务真实接口的浏览器联调未通过；桌面壳已尝试拉起中心服务，但 `better-sqlite3` 原生绑定需要本机 Visual Studio C++ Build Tools 才能完成生成。
- 当前阻塞：`node-gyp` 报错找不到 Visual Studio 安装，缺少 “Desktop development with C++” 工作负载；在该环境补齐前，`8866` 无法监听。

## 来源与目标

- 需求分析来源：`本次需求分析.md`。
- 本轮需求标题：`当前修改的需求-2026年6月1日11:30:04`。
- 本轮执行边界：只生成本次计划，不修改业务代码。
- 本轮验证边界：架构师任务不启动、不测试、不构建；后续由主线程按“全量测试”或“只用浏览器测试”的实际调度选择启动、浏览器测试、桌面测试和检验子代理。

目标：

1. 保证 Web 端构建产物只包含浏览器资源，不携带中心服务入口、SQLite、本地数据库驱动、`services/center` 源码相对引用或 Electron 桌面壳专属文件。
2. 保证中心服务只由桌面端开发启动链路或桌面端打包产物动态装配和拉起，测试时禁止直接运行 `services/center`。
3. 完成顶部菜单页面真实拆分：独立 URL、独立 `RouterIndex.vue`、独立页面组件、明确数据加载边界，移除 `<MainView initial-page="..."/>` 或同类伪拆分。
4. 让菜单切换同步更新 hash URL，支持刷新、复制链接和直接进入对应页面。
5. 在执行模式下拉面板内展示“建议模式 / 自动编辑 / 全自动”的审批语义说明。
6. 补充静态回归检查和浏览器验证，覆盖包体边界、路由拆分、菜单 URL、执行模式说明和桌面端开发启动链路。

## 架构决策

### 1. 页面拆分层级

- `apps/frontend/src/views/MainView.vue` 只保留公共工作台壳、顶部导航、客户端菜单过滤、连接状态、公共插槽出口和对话页承载边界。
- `Chat/RouterIndex.vue` 可以继续挂载公共工作台壳，但写法必须清楚表达“公共壳 + 对话页主体”的关系，避免几行隐式转发导致阅读困难。
- 管理类顶部菜单页必须使用各自目录下的 `RouterIndex.vue` 作为路由入口，再加载本页面的真实业务组件。
- 允许公共页面头部、顶部菜单、二级菜单抽成公共组件；禁止把智能体管理、供应商、代理、运行环境、用量统计、插件、MCP、skill、中心服务等业务模板长期堆回 `MainView.vue` 或某个 `WorkspacePage.vue`。

### 2. AgentManagement 页面边界

- `apps/frontend/src/views/AgentManagement/RouterIndex.vue` 负责页面入口、公共壳挂载和本页面子路由出口。
- 智能体列表、主智能体说明、长期智能体管理入口、删除影响提示等业务模板放入 AgentManagement 页面自身组件或子页面组件。
- 如果需要二级菜单，使用 AgentManagement 内部子路由和 `router-view` 承载；不要通过字符串状态切换多个业务页。

### 3. 路由与菜单边界

- 顶部菜单点击必须调用 Vue Router 导航到固定 hash 路由，例如 `#/chat`、`#/agent-management`、`#/providers`、`#/usage`。
- 桌面端、Web 本机、Web 远程和插件端的菜单可见性由客户端能力或入口模式过滤；过滤只影响菜单展示，不影响路由组件的职责拆分。
- 浏览器直接访问各管理页 URL 时必须能进入对应页面，不依赖先进入 `/chat` 后设置本地状态。

### 4. 数据加载边界

- 页面组件可以复用 Pinia store 和 API client，但加载动作由页面自身在挂载时触发或由页面专属组合函数触发。
- Store 只保存当前事实数据、加载状态和错误信息，不承担“根据当前菜单状态渲染多个页面模板”的职责。
- 不允许通过多个候选字段兜底猜测协议；没有明确服务端字段时，页面展示可见错误或空状态。

### 5. 桌面开发启动边界

- `dev:desktop-shell` 对应的 `scripts/dev-desktop-shell.mjs` 必须通过桌面壳生命周期拉起中心服务，或调用桌面壳开发期启动适配层完成同等链路。
- 测试和启动记录必须说明中心服务是否由桌面端拉起、端口、实际访问 URL 和停止方式。
- 禁止为了浏览器测试直接运行 `services/center`。

### 6. Web 包瘦身验收方式

- 静态检查优先扫描 Web dist、前端源码依赖和桌面打包装配脚本边界。
- 浏览器测试只验证运行页面、路由和交互，不以浏览器端直接启动中心服务作为前置条件。
- 如果需要中心服务接口参与浏览器测试，由启动子代理先启动桌面端，再访问桌面端拉起的中心服务地址。

### 7. 执行模式控件边界

- 执行模式下拉说明应抽为输入区可复用控件或可复用选项渲染结构，供 `workspace`、`plugin-compact` 和后续复用输入区使用。
- 说明文案必须覆盖：
  - 建议模式：每一步副作用操作都需要用户确认。
  - 自动编辑：低风险读取或编辑流程可自动执行，高风险操作仍需确认。
  - 全自动：在权限和沙箱范围内自动执行。
- 切换只影响后续操作，不回改历史任务、消息或用量。

## 需要修改的文件范围

### 程序员子代理可处理范围

- `apps/frontend/src/router.ts`
- `apps/frontend/src/views/MainView.vue`
- `apps/frontend/src/views/Chat/RouterIndex.vue`
- `apps/frontend/src/views/AgentManagement/**`
- `apps/frontend/src/views/Providers/**`
- `apps/frontend/src/views/Proxies/**`
- `apps/frontend/src/views/Runtimes/**`
- `apps/frontend/src/views/Usage/**`
- `apps/frontend/src/views/Plugins/**`
- `apps/frontend/src/views/Mcp/**`
- `apps/frontend/src/views/Skills/**`
- `apps/frontend/src/views/Center/**`
- `apps/frontend/src/components/**`
- `apps/frontend/src/stores/**`
- `apps/frontend/src/api/**`
- `apps/frontend/src/styles.css`，仅限全局主题变量、第三方基础覆盖、统一滚动条和跨入口根布局。
- `scripts/dev-desktop-shell.mjs`
- `scripts/check-frontend-management-pages.mjs`
- `scripts/check-frontend-workspace-ui.mjs`
- `scripts/check-frontend-feedback-regressions.mjs`
- `scripts/check-center-local-dev-cors.ts`
- 可新增脚本：`scripts/check-web-dist-boundary.mjs`、`scripts/check-desktop-dev-startup.mjs` 或同职责脚本。
- 根 `package.json` 仅限补充非 TypeScript 编译器类静态检查脚本引用；不得新增 `tsc --noEmit`、`vue-tsc` 或等价命令。

### 启动子代理可处理范围

- `项目启动.md`
- 启动命令执行和运行状态记录。
- 不修改业务代码。

### 浏览器测试子代理可处理范围

- 使用 chrome-devtools 验证浏览器端页面、路由、交互、控制台和网络错误。
- 不修改业务代码。
- 不使用 windows-mcp。

### 检验子代理可处理范围

- `提示词模板.md`，仅在测试和检验均通过后，把对应 `当前修改的需求-2026年6月1日11:30:04` 标题追加 `-完成`。
- `计划.md`，仅在确认本轮确实完成了既有计划任务时同步勾选或补充说明。
- 不修改业务代码。

## 可并行任务清单

- [ ] P1 Web 包边界检查与桌面装配边界：检查或调整前端构建产物、桌面打包装配脚本和静态回归脚本，确认 Web dist 不含中心服务、SQLite、本地数据库驱动或 Electron 专属文件。建议文件范围：`apps/frontend/vite.config.ts`、`apps/desktop-shell/scripts/build-zip.mjs`、`scripts/check-web-dist-boundary.mjs`、`scripts/check-desktop-portable.mjs`、根 `package.json` 中相关脚本。
- [ ] P2 页面路由与菜单 URL 同步：检查或调整 Vue Router、顶部菜单点击和 hash URL，确认所有顶部菜单能通过独立 URL 进入并刷新不白屏。建议文件范围：`apps/frontend/src/router.ts`、`apps/frontend/src/views/MainView.vue`、各 `views/*/RouterIndex.vue`。
- [ ] P3 管理页全量拆分：拆分 `AgentManagement`、`Providers`、`Proxies`、`Runtimes`、`Usage`、`Plugins`、`Mcp`、`Skills`、`Center` 的入口、页面组件和数据加载边界，移除 `initial-page` 或同类伪拆分。建议文件范围：上述页面目录、`apps/frontend/src/stores/app.ts`、`packages/api-client/src/index.ts`。
- [ ] P4 Chat 入口可读性修复：整理 `Chat/RouterIndex.vue` 与公共工作台壳关系，只处理入口可读性和挂载边界，不顺手重构聊天业务逻辑。建议文件范围：`apps/frontend/src/views/Chat/RouterIndex.vue`、必要时 `apps/frontend/src/views/MainView.vue`。
- [ ] P5 执行模式下拉说明：更新输入区执行模式控件，让下拉面板内直接展示三种模式解释，并覆盖 workspace 与 plugin-compact 复用入口。建议文件范围：`apps/frontend/src/views/MainView.vue`、可能抽取到 `apps/frontend/src/components` 的复用控件。
- [ ] P6 静态回归检查：扩展或新增脚本覆盖 Web 包边界、页面拆分、路由懒加载、菜单 URL 同步、执行模式说明和 `dev:desktop-shell` 启动链路。建议文件范围：`scripts/check-frontend-management-pages.mjs`、`scripts/check-frontend-workspace-ui.mjs`、`scripts/check-frontend-feedback-regressions.mjs`、`scripts/check-desktop-dev-startup.mjs`。

## 不可并行任务清单

- [ ] S1 架构计划确认：先以本文件为唯一实施边界，程序员子代理不得自行扩大范围。
- [ ] S2 程序员实现收敛：P1 至 P6 可分配给程序员子代理并行处理，但涉及同一文件时必须由主线程协调合并，避免多个子代理同时改同一页面壳或同一检查脚本。
- [ ] S3 启动准备：程序员实现完成后，再由启动子代理运行 `dev:desktop-shell` 或对应 IDEA Run Configuration，创建或更新 `项目启动.md`。
- [ ] S4 浏览器测试：启动子代理确认中心服务由桌面端拉起后，再由浏览器测试子代理使用 chrome-devtools 验证浏览器端。
- [ ] S5 桌面测试：仅当主线程选择“全量测试”时执行；由桌面测试子代理验证桌面壳窗口、菜单和中心服务拉起证据。
- [ ] S6 最终检验：浏览器测试和必要桌面测试通过后，检验子代理复核 diff、计划同步建议、需求标题完成状态和未测试/未构建项。

## 启动与浏览器测试边界

启动子代理需要在 `项目启动.md` 写明：

- 启动方式：优先 IDEA Run Configuration 或 IDEA Terminal。
- 使用的命令：例如 `pnpm dev:desktop-shell`，以实际仓库脚本为准。
- 实际访问 URL：桌面端拉起中心服务后提供的 `http://127.0.0.1:端口` 和具体 hash 路由。
- 中心服务是否由桌面端拉起：必须记录证据，不能写成推测。
- 端口：默认应为 `8866`，如被占用则记录实际端口和原因。
- 关键日志位置：桌面壳日志、中心服务日志或终端输出位置。
- 测试前置状态：前端页面可访问、中心服务健康检查可用或明确的错误状态。
- 停止方式：如何停止桌面壳及其管理的中心服务。
- 当前阻塞项：Node.js、pnpm、端口、桌面壳启动或中心服务锁等问题。

浏览器测试子代理只验证：

- `/chat` 页面非白屏，顶部导航、左侧对话树、消息区、输入区和右侧状态栏可见。
- `/agent-management`、`/providers`、`/proxies`、`/runtimes`、`/usage`、`/plugins`、`/mcp`、`/skills` 可通过独立 hash URL 进入且非白屏。
- Web 浏览器端不展示桌面专属“中心服务”管理入口；桌面壳模式才展示。
- 顶部菜单点击后 hash URL 同步变化，刷新后仍进入同一页面。
- 执行模式下拉面板内可见“建议模式 / 自动编辑 / 全自动”的解释说明。
- 控制台无阻塞级错误，关键网络请求失败时页面有可见错误状态。
- Network 面板能观察到管理页按路由懒加载，不把所有页面一次性塞入首屏主入口。

桌面测试子代理仅在主线程选择“全量测试”时验证：

- 桌面客户端窗口可打开，顶部菜单和当前页面内容可见。
- 中心服务由桌面端拉起，不是直接运行 `services/center`。
- 桌面端开发启动链路使用当前代码而非旧目录或旧中心服务入口。
- 桌面端关键日志无阻塞级错误。

## 验收标准

- Web dist 中不存在中心服务入口、SQLite、本地数据库驱动、`services/center` 相对源码引用或 Electron 桌面壳专属文件。
- 桌面端开发启动链路能拉起中心服务，`项目启动.md` 有明确证据。
- 未直接运行 `services/center` 作为测试启动方式。
- 所有顶部菜单页面都有独立 URL、独立入口和清晰页面组件边界。
- 代码中不再出现 `<MainView initial-page="..."/>` 或同类初始参数伪拆分。
- `AgentManagement/WorkspacePage.vue` 不再作为智能体管理页全部业务模板和状态的集中堆放处；如保留，职责必须收敛并有明确说明。
- `Chat/RouterIndex.vue` 可读性改善，能清楚看出公共壳和页面主体关系。
- 菜单切换会更新 hash URL，直接打开 URL 和刷新页面不白屏。
- 执行模式下拉面板内展示三种模式解释。
- 静态回归检查覆盖本轮核心边界并通过。
- 浏览器端 chrome-devtools 验证通过；如果主线程选择全量测试，还需要桌面端验证通过。

## 测试建议

- 静态检查：运行本轮新增或扩展的回归脚本，避免使用 `tsc --noEmit`、`vue-tsc` 或等价 TS 编译器检查命令。
- 构建检查：如需要扫描 Web dist，应使用项目既有前端构建命令生成产物后扫描；最终报告必须说明是否构建。
- 启动检查：只通过桌面端启动链路拉起中心服务，禁止直接启动 `services/center`。
- 浏览器检查：使用 chrome-devtools 打开启动子代理记录的 URL，验证页面、路由、下拉面板、控制台和网络请求。
- 插件端：本轮只允许代码层面覆盖或静态自查，不做插件 UI 测试，不构建 IDEA 插件。

## 与 `计划.md` 既有任务关系

- 涉及 `计划.md` 阶段 4：统一前端应用。
  - 关联任务：使用 chrome-devtools 或 windows-mcp 验证 `plugin-compact` 紧凑 WebView。
  - 本轮只做浏览器端验证，不创建桌面测试子代理；如果未覆盖 plugin-compact 紧凑 WebView，不应勾选该既有待办。
- 涉及 `计划.md` 阶段 15：构建、迁移与质量门槛。
  - 关联任务：补齐 Web 包瘦身、页面全量拆分和执行模式说明回归。
  - 当前 `计划.md` 中该任务已标为完成；本轮如果发现实现或验证不足，应在 `计划.md` 保留原状态并补充新的修正任务说明，不能无依据回滚用户既有勾选。
- 本轮计划文件本身不表示已完成 `计划.md` 中任何任务；只有程序员实现、启动、浏览器测试和检验通过后，才允许同步 `计划.md` 状态。

## 是否建议同步正式文档

- 是否建议同步 `需求.md`：暂不建议。
  - 原因：用户本轮要求生成或更新 `本次计划.md`，不要修改业务代码；未明确要求同步正式需求。
  - 当前正式需求中已包含 Web 端不内置中心服务、顶部菜单真实独立页面、执行模式说明、URL 路由同步和桌面端管理中心服务等口径。
- 是否建议同步 `架构.md`：建议在实现前由主线程确认是否追加更细的页面拆分层级。
  - 原因：`本次需求分析.md` 提出公共头部、二级菜单和 `router-view` 的组件层级决策；如果实现后形成稳定边界，应同步进 `架构.md` 的前端入口、布局与客户端能力章节。
- 是否建议同步 `计划.md`：本轮生成计划时暂不同步。
  - 原因：本轮未完成实现、启动、浏览器测试或检验；按规则不能只因生成本次计划就勾选正式计划任务。

## 可分配给子代理的任务

- 程序员子代理：
  - P1 Web 包边界检查与桌面装配边界。
  - P2 页面路由与菜单 URL 同步。
  - P3 管理页全量拆分。
  - P4 Chat 入口可读性修复。
  - P5 执行模式下拉说明。
  - P6 静态回归检查。
- 启动子代理：
  - S3 启动准备，创建或更新 `项目启动.md`。
- 浏览器测试子代理：
  - S4 浏览器测试，只使用 chrome-devtools。
- 桌面测试子代理：
  - S5 桌面测试；仅全量测试场景创建。
- 检验子代理：
  - S6 最终检验；测试和检验通过后再判断是否建议更新 `计划.md`，以及是否把需求标题追加 `-完成`。

## 风险与阻塞项

- 当前计划阶段无阻塞项。
- 后续实现风险：
  - 多个页面拆分任务可能同时触碰 `MainView.vue`、router 或共享 store，需要主线程协调文件所有权。
  - 如果 `dev:desktop-shell` 仍无法拉起中心服务，浏览器测试只能验证前端壳和可见错误状态，不能完成中心服务联动验收。
  - 如果 Web dist 扫描依赖构建产物，而本轮未运行构建，检验阶段必须明确“未构建”并保留风险。
  - 如果浏览器端菜单过滤依赖桌面壳注入能力，需分别记录普通浏览器和桌面壳模式的可见差异，不能混用结论。

## 架构师结论

- `本次计划.md` 已按 `本次需求分析.md` 更新，作为后续程序员、启动、浏览器测试、桌面测试和检验子代理的实施边界。
- 本次架构师任务未修改业务代码。
- 本次架构师任务未同步 `需求.md`：用户未明确要求同步正式需求，且当前正式需求已有对应产品口径。
- 本次架构师任务未同步 `架构.md`：建议实现稳定后，把“公共头部 + 二级菜单 + 页面 `router-view`”的最终组件层级补入 `架构.md` 前端章节。
- 本次架构师任务未同步 `计划.md`：当前只完成过程计划，未完成实现、启动、测试和检验，不能勾选正式计划任务。
